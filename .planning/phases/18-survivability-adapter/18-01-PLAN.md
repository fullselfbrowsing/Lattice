# Plan 18-01: SurvivabilityAdapter contract + ref impl + tests

**Status:** Complete (retro). **Wave:** 1. **REQ-IDs:** SURV-01, SURV-02, SURV-03, SURV-04.
**Originating commit:** `a4609bc` feat(runtime): add MV3-survivability adapter contract + noop reference impl.

## Goal

Ship `packages/lattice/src/runtime/survivability.ts` exporting the `SurvivabilityAdapter<TState>` interface, `SerializedSnapshot` opaque wrapper, `ResumePolicy` taxonomy, companion types (`EvictionHook<TState>`, `UnsubscribeFn`), and `createNoopSurvivabilityAdapter()` reference implementation. Cover with 17 vitest cases including a DSSE+JCS round-trip with a real ephemeral Ed25519 keypair.

## Tasks

1. New `packages/lattice/src/runtime/survivability.ts` (+244 lines).
   - `SurvivabilityAdapter<TState>` interface with `serialize`, `deserialize`, `onEviction`, `resume`.
   - `SerializedSnapshot` shape with `kind`, `version`, `payload`, `capturedAt`.
   - `ResumePolicy` literal-union with 4 variants.
   - `EvictionHook<TState>` + `UnsubscribeFn` companion types.
   - `createNoopSurvivabilityAdapter()` reference impl.
2. New `packages/lattice/src/runtime/survivability.test.ts` (+265 lines, 17 cases).

## Acceptance

- 17 vitest cases pass including Test 12 (real Ed25519 + ReceiptEnvelope round-trip).
- SURV-01..04 closed.

## Outcome

Cherry-pick `a4609bc` lands on phase branch.
