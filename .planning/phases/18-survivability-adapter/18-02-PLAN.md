# Plan 18-02: Public surface re-exports + audit doc Phase 5 close

**Status:** Complete (retro). **Wave:** 2 (closure). **REQ-IDs:** INDEX-05.
**Originating commits:** `109d6ae` feat(api): re-export survivability adapter contract; `e95067b` docs(fsb-integration): close Phase 5 audit rows.

## Goal

Re-export `createNoopSurvivabilityAdapter` value + 5 type-only re-exports (`SurvivabilityAdapter`, `SerializedSnapshot`, `EvictionHook`, `ResumePolicy`, `UnsubscribeFn`) from `packages/lattice/src/index.ts`. Close the 2 MV3-survivability audit-doc rows.

## Tasks

1. `packages/lattice/src/index.ts` — +9 lines (1 value + 5 type re-exports).
2. `docs/fsb-integration-gaps.md` — flip the 2 MV3-survivability Blocker rows to Covered.

## Acceptance

- `import { createNoopSurvivabilityAdapter, type SurvivabilityAdapter, type SerializedSnapshot, type EvictionHook, type ResumePolicy, type UnsubscribeFn } from "lattice"` resolves.
- INDEX-05 closed.

## Outcome

Cherry-picks `109d6ae` and `e95067b` land on phase branch.
