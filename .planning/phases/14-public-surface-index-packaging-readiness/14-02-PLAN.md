# Plan 14-02: Resolve `pnpm-workspace` catalog: specifiers in `packages/lattice/package.json`

**Status:** Complete (retro from FSB v0.10.0-attempt-2 Phase 1).
**Wave:** 1
**REQ-IDs:** PKG-01
**Originating commit:** `22bf986` chore(packaging): resolve pnpm catalog: literals for npm consumers.

## Goal

Replace `"catalog:"` dependency specifiers in `packages/lattice/package.json` with concrete semver strings so npm 11 can install the package via a `file:` dependency. npm 11 rejects `catalog:` at parse time; pnpm `catalog:` is a pnpm-only feature. FSB consumes Lattice via `file:./lattice/packages/lattice` in its `package.json`.

## Tasks

1. Resolve each `catalog:` in `packages/lattice/package.json` to the literal version recorded in `pnpm-workspace.yaml` at the originating commit time:
   - `@standard-schema/spec`: `catalog:` → `1.1.0`
   - `canonicalize`: `catalog:` → `3.0.0`
   - `mime`: `catalog:` → `4.1.0`
   - `@noble/ed25519`: `catalog:` → `3.1.0`
   - `@types/node`: `catalog:` → `24.12.2`
   - `zod`: `catalog:` → `4.3.6`
2. Regenerate `pnpm-lock.yaml` (lockfile update removes the catalog indirection).

## Acceptance

- `packages/lattice/package.json` contains zero `catalog:` strings.
- `pnpm-lock.yaml` resolves without referencing the `lattice` package's `catalog:` indirection.
- pnpm install at the workspace root succeeds (`pnpm-workspace.yaml` is unchanged; other workspace packages may still use `catalog:`).
- `npm install file:./packages/lattice` from an external Node project (with a Node 24 runtime) succeeds.

## Verification

- pnpm install succeeds locally at the originating commit time (FSB-side v0.10.0-attempt-2 Plan 01-02 SUMMARY).
- npm-side smoke deferred — exercised end-to-end by FSB's own `package.json` consumption via `file:` dep (FSB v0.10.0-attempt-2 Phase 1 audit row).

## Outcome

Land commit `48f444d` on this phase branch (cherry-pick of `22bf986`). 2 files changed: `packages/lattice/package.json` (+12 -12), `pnpm-lock.yaml` (+27 -...).
