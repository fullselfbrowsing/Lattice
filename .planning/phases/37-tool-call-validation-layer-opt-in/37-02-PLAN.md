---
phase: 37
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - packages/lattice/src/providers/adapters.ts
  - packages/lattice/src/providers/openrouter.ts
  - packages/lattice/src/providers/xai.ts
  - packages/lattice/src/providers/lm-studio.ts
  - packages/lattice/src/agent/runtime.ts
  - packages/lattice/src/agent/runtime.test.ts
  - packages/lattice/src/providers/adapters.test.ts
  - packages/lattice/src/providers/openrouter.test.ts
  - packages/lattice/src/providers/xai.test.ts
  - packages/lattice/src/providers/lm-studio.test.ts
autonomous: true
requirements:
  - VALID-01
  - VALID-02
  - VALID-03
tags:
  - provider-adapters
  - openai-compatible
  - agent-runtime
must_haves:
  truths:
    - "OpenAICompatibleProviderOptions accepts validateToolCalls?: ValidateToolCallsOption"
    - "OpenAI-compatible execute preserves rawOutputs and rawResponse while populating toolCalls"
    - "OpenAI, OpenRouter, xAI, and LM Studio inherit validation through the shared OpenAI-compatible path"
    - "Wrapper providers do not validate twice"
    - "runAgent prefers response.toolCalls when present and keeps parser fallback when absent"
    - "Invalid calls are never executed when validation is enabled"
  artifacts:
    - path: "packages/lattice/src/providers/adapters.ts"
      provides: "Shared OpenAI-compatible validation wiring"
    - path: "packages/lattice/src/agent/runtime.ts"
      provides: "Agent runtime consumption of validated tool calls"
    - path: "packages/lattice/src/providers/adapters.test.ts"
      provides: "OpenAI-compatible family validation behavior"
    - path: "packages/lattice/src/agent/runtime.test.ts"
      provides: "Agent loop validated-call preference and invalid-call non-execution"
  key_links:
    - from: "packages/lattice/src/providers/adapters.ts"
      to: "packages/lattice/src/tools/tool-call-validation.ts"
      via: "imports ValidateToolCallsOption and validateToolCallRequests"
      pattern: "validateToolCallRequests"
    - from: "packages/lattice/src/agent/runtime.ts"
      to: "packages/lattice/src/providers/provider.ts"
      via: "prefers ProviderRunResponse.toolCalls when present"
      pattern: "response.toolCalls"
---

<objective>
Wire `validateToolCalls` through the OpenAI-compatible adapter family and teach
the agent runtime to consume validated normalized calls when adapters provide
them.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-CONTEXT.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-RESEARCH.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-PATTERNS.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-01-PLAN.md
@packages/lattice/src/tools/tool-call-validation.ts
@packages/lattice/src/providers/adapters.ts
@packages/lattice/src/agent/runtime.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Wrapper providers invoke validation twice | high | Apply validation only in `createOpenAICompatibleProvider`; wrapper tests assert one callback event |
| Agent loop ignores validated adapter results | high | Runtime test proves `response.toolCalls` is preferred over parser fallback |
| Invalid calls execute in drop/callback mode | high | Runtime test uses a tool spy and asserts malformed call execute count remains 0 |
| Raw provider output is mutated during validation | medium | Adapter tests assert rawOutputs text and rawResponse body are preserved |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add validateToolCalls to OpenAI-compatible execution</name>
  <files>packages/lattice/src/providers/adapters.ts</files>
  <read_first>
    - packages/lattice/src/providers/adapters.ts
    - packages/lattice/src/tools/tool-call-validation.ts
    - packages/lattice/src/agent/format-tools.ts
    - packages/lattice/src/providers/provider.ts
  </read_first>
  <action>
    1. Import `type ValidateToolCallsOption` and `validateToolCallRequests` from `../tools/tool-call-validation.js`.
    2. Import `parseToolUseEnvelope` from `../agent/format-tools.js`.
    3. Add `validateToolCalls?: ValidateToolCallsOption;` to `OpenAICompatibleProviderOptions`.
    4. After extracting `text`, call `parseToolUseEnvelope(text)`.
    5. If parsed requests are non-null, call `validateToolCallRequests(parsed, options.validateToolCalls)`.
    6. Return `toolCalls` only when the helper returns a defined list.
    7. Preserve `rawOutputs: sanitizedOutputs`, `normalizedUsage`, `usage`, and `rawResponse: body`.
    8. Absent `validateToolCalls` must preserve current behavior and must not add `toolCalls`.
  </action>
  <verify>
    <automated>rg -n 'validateToolCalls|ValidateToolCallsOption|validateToolCallRequests|parseToolUseEnvelope|toolCalls' packages/lattice/src/providers/adapters.ts</automated>
  </verify>
  <acceptance_criteria>
    - `OpenAICompatibleProviderOptions` contains `validateToolCalls?: ValidateToolCallsOption`.
    - `createOpenAICompatibleProvider.execute` calls `parseToolUseEnvelope`.
    - `createOpenAICompatibleProvider.execute` calls `validateToolCallRequests`.
    - `rawOutputs` and `rawResponse` return as before.
    - `toolCalls` is optional and absent when validation option is absent.
  </acceptance_criteria>
  <done>OpenAI-compatible adapter produces validated normalized tool calls when opted in.</done>
</task>

<task type="auto">
  <name>Task 2: Verify wrapper providers inherit validation once</name>
  <files>
    packages/lattice/src/providers/openrouter.ts
    packages/lattice/src/providers/xai.ts
    packages/lattice/src/providers/lm-studio.ts
  </files>
  <read_first>
    - packages/lattice/src/providers/openrouter.ts
    - packages/lattice/src/providers/xai.ts
    - packages/lattice/src/providers/lm-studio.ts
    - packages/lattice/src/providers/adapters.ts
  </read_first>
  <action>
    Inspect wrapper factories and make only minimal edits:

    1. Ensure wrapper option types inherit `validateToolCalls` through `OpenAICompatibleProviderOptions`.
    2. Ensure wrapper factory calls pass `...options` to `createOpenAICompatibleProvider`.
    3. Do not add wrapper-local calls to `validateToolCallRequests`.
    4. Preserve xAI's existing usage augmentation wrapper and do not mutate `response.toolCalls`.
  </action>
  <verify>
    <automated>rg -n 'validateToolCalls|validateToolCallRequests|createOpenAICompatibleProvider' packages/lattice/src/providers/openrouter.ts packages/lattice/src/providers/xai.ts packages/lattice/src/providers/lm-studio.ts</automated>
  </verify>
  <acceptance_criteria>
    - Wrapper provider files do not contain wrapper-local `validateToolCallRequests` calls.
    - Wrapper option types accept `validateToolCalls`.
    - xAI preserves its reasoning token normalization behavior.
  </acceptance_criteria>
  <done>OpenAI-compatible wrapper providers inherit validation exactly once.</done>
</task>

<task type="auto">
  <name>Task 3: Make agent runtime prefer validated calls</name>
  <files>packages/lattice/src/agent/runtime.ts</files>
  <read_first>
    - packages/lattice/src/agent/runtime.ts
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/tools/tool-call-validation.ts
  </read_first>
  <action>
    Update the agent loop after provider response text extraction:

    1. Compute tool requests as:
       - if `response.toolCalls` is defined, map each validated call to `{ id, name, args }`
       - otherwise use the existing `handle.parseToolUse(responseText)` fallback
    2. Preserve existing final-answer behavior when the result is null or an empty array.
    3. Preserve existing tool dispatch and transcript behavior for valid calls.
    4. Ensure invalid calls that were dropped by validation are not executed.
    5. Do not require all adapters to implement validation; consumer adapters still work through parser fallback.
  </action>
  <verify>
    <automated>rg -n 'response\\.toolCalls|parseToolUse|toolUseRequests' packages/lattice/src/agent/runtime.ts</automated>
  </verify>
  <acceptance_criteria>
    - Runtime references `response.toolCalls`.
    - Runtime still references parser fallback for adapters without `toolCalls`.
    - Existing no-tool final answer behavior is preserved.
  </acceptance_criteria>
  <done>Agent runtime composes with adapter-side validation.</done>
</task>

<task type="auto">
  <name>Task 4: Add OpenAI-compatible family and runtime tests</name>
  <files>
    packages/lattice/src/providers/adapters.test.ts
    packages/lattice/src/providers/openrouter.test.ts
    packages/lattice/src/providers/xai.test.ts
    packages/lattice/src/providers/lm-studio.test.ts
    packages/lattice/src/agent/runtime.test.ts
  </files>
  <read_first>
    - packages/lattice/src/providers/adapters.test.ts
    - packages/lattice/src/providers/openrouter.test.ts
    - packages/lattice/src/providers/xai.test.ts
    - packages/lattice/src/providers/lm-studio.test.ts
    - packages/lattice/src/agent/runtime.test.ts
    - packages/lattice/src/tools/tool-call-validation.test.ts
  </read_first>
  <action>
    Add focused tests:

    1. OpenAI-compatible adapter with `validateToolCalls` returns `response.toolCalls[0]` for `{"tool_calls":[{"id":"c1","name":"search","args":{"query":"ok"}}]}`.
    2. OpenAI-compatible adapter without `validateToolCalls` preserves current behavior and has no `toolCalls` property.
    3. OpenAI-compatible adapter throw mode rejects the roadmap typo `{ name: "search_database", args: { quer: "..." } }`.
    4. OpenAI provider inherits validation behavior.
    5. OpenRouter, xAI, and LM Studio accept `validateToolCalls` through wrapper options.
    6. xAI wrapper callback test asserts callback fires exactly once for one invalid call.
    7. Runtime test proves `response.toolCalls` is preferred by using provider raw text that would parse differently if fallback parser were used.
    8. Runtime test proves a dropped invalid call is not executed.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test adapters openrouter xai lm-studio agent/runtime</automated>
  </verify>
  <acceptance_criteria>
    - Tests contain `validateToolCalls`.
    - Tests contain `search_database`.
    - xAI test asserts one callback invocation.
    - Runtime test fails if parser fallback ignores `response.toolCalls`.
    - Verification command exits 0.
  </acceptance_criteria>
  <done>OpenAI-compatible family and agent runtime validation behavior is covered.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
pnpm --filter @full-self-browsing/lattice test adapters openrouter xai lm-studio agent/runtime
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>
