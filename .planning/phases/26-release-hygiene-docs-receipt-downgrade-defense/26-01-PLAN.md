---
phase: 26-release-hygiene-docs-receipt-downgrade-defense
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - CONTRIBUTING.md
autonomous: true
requirements: [DOC-02, DOC-04]
must_haves:
  truths:
    - "Repo root has a SECURITY.md naming lakshmantvnm@gmail.com as the private disclosure address"
    - "SECURITY.md documents the three v1.3 threat categories (Ed25519 entropy, KeySet rotation cadence, receipt-downgrade defense)"
    - "SECURITY.md cites the Radicle 2026-03 receipt-downgrade precedent and cross-references packages/lattice/src/receipts/verify.ts and the schema-version-too-low error kind"
    - "Repo root has a CONTRIBUTING.md (under 200 lines) linking to AGENTS.md at the top and listing the canonical pre-PR check commands"
    - "Neither file contains emojis or uses em-dashes / hyphens to connect sentences"
  artifacts:
    - path: "SECURITY.md"
      provides: "OSS security policy + threat catalog + disclosure contact"
      contains: "lakshmantvnm@gmail.com"
    - path: "CONTRIBUTING.md"
      provides: "Contributor onboarding + commands + PR conventions"
      contains: "AGENTS.md"
  key_links:
    - from: "SECURITY.md"
      to: "packages/lattice/src/receipts/verify.ts"
      via: "explicit file-path reference"
      pattern: "packages/lattice/src/receipts/verify.ts"
    - from: "SECURITY.md"
      to: "VerifyErrorKind schema-version-too-low literal"
      via: "named cross-reference"
      pattern: "schema-version-too-low"
    - from: "CONTRIBUTING.md"
      to: "AGENTS.md"
      via: "top-of-file link"
      pattern: "AGENTS.md"
---

<objective>
Author the two repo-root markdown surfaces npm reviewers and external contributors will look for on first contact with `@fullselfbrowsing/lattice`: `SECURITY.md` (CVE disclosure policy plus the v1.3 threat catalog wired to the CRYPTO-01 defense landing in Plan 26-04) and `CONTRIBUTING.md` (concise contributor onboarding that delegates deep context to the existing `AGENTS.md`).

Purpose: Cover DOC-02 and DOC-04. SECURITY.md is the writeup half of the security-writeup-coupled-to-code pairing for Phase 26; CRYPTO-01 (Plan 26-04) is the code half. CONTRIBUTING.md is the standard OSS surface npm and GitHub both surface prominently.

Output: `SECURITY.md` and `CONTRIBUTING.md` at the repository root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-release-hygiene-docs-receipt-downgrade-defense/26-CONTEXT.md
@AGENTS.md
@packages/lattice/src/receipts/verify.ts
@packages/lattice/src/receipts/sign.ts
@packages/lattice/src/receipts/types.ts

<interfaces>
The defense Plan 26-04 will land in verify.ts (cross-referenced from SECURITY.md):

```ts
// packages/lattice/src/receipts/types.ts (after Plan 26-04)
export type VerifyErrorKind =
  | "key-not-found"
  | "key-revoked"
  | "canonicalization-mismatch"
  | "signature-invalid"
  | "envelope-malformed"
  | "version-mismatch"
  | "schema-version-too-low"; // CRYPTO-01

// packages/lattice/src/receipts/verify.ts (after Plan 26-04, inserted before the signature-validity check)
if (body.version === undefined || body.version === "lattice-receipt/v1") {
  return fail(
    "schema-version-too-low",
    "Receipt body.version must be 'lattice-receipt/v1.1' ...",
  );
}
```

Existing KeySet rotation surface (referenced by the SECURITY.md "Signing key rotation" section):

```ts
// packages/lattice/src/receipts/types.ts
export type KeyState = "active" | "retired" | "revoked";

export interface KeyEntry {
  readonly kid: string;
  readonly publicKeyJwk: JsonWebKey;
  readonly state: KeyState;
}

export interface KeySet {
  lookup(kid: string): KeyEntry | undefined;
}
```

Existing Ed25519 generation surface (referenced by the SECURITY.md "Ed25519 entropy" section):

```ts
// packages/lattice/src/receipts/sign.ts
export async function generateEd25519KeyPairJwk(): Promise<GeneratedEd25519KeyPair>;
// internally calls: crypto.subtle.generateKey("Ed25519", true, ["sign", "verify"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SECURITY.md at repo root (DOC-02)</name>
  <files>SECURITY.md</files>
  <action>
Create `SECURITY.md` at the repository root. Target length: 120 to 200 lines of markdown. Use the standard OSS security-policy outline. Do NOT use emojis anywhere in the file (user global rule). Do NOT use em-dashes or hyphens to connect independent sentences (user feedback memory `feedback_no_hyphens_between_sentences`); use commas, periods, or rewrite.

Required structure and content:

1. `# Security Policy` H1.

2. `## Reporting a Vulnerability` section. State exactly: private disclosure goes to `lakshmantvnm@gmail.com` (D-01 verbatim). State a 90-day coordination window before any public CVE disclosure. State that the maintainer will acknowledge receipt within 5 business days and provide a remediation ETA within 14 business days.

3. `## Scope` section. List in scope: `@fullselfbrowsing/lattice` (runtime SDK) and `@fullselfbrowsing/lattice-cli` (CLI). List out of scope: the canary consumer repo (`fullselfbrowsing/lattice-canary`) lives under its own security policy; bugs in user-supplied provider adapters or in third-party transitive dependencies should be reported to those projects directly. Note that release supply-chain trust is enforced via npm OIDC Trusted Publisher with provenance attestations (no long-lived `NPM_TOKEN`); compromise of the publishing pipeline itself is in scope.

4. `## Threat Model` section. Three subsections in this order (D-02):

   a. `### Ed25519 Signing Key Entropy`. State that capability receipts are signed with Ed25519 keys generated via `crypto.subtle.generateKey("Ed25519", ...)` in `packages/lattice/src/receipts/sign.ts` (`generateEd25519KeyPairJwk`). State the assumption: the host runtime supplies a cryptographically-secure pseudo-random number generator via WebCrypto. State that Lattice does not fall back to weaker PRNGs; on a runtime that lacks `crypto.subtle.generateKey` with the Ed25519 algorithm, key generation throws and the runtime declines to mint receipts. State that users plugging custom signers (KMS adapters, OS keyring) are responsible for the entropy posture of their key-material source.

   b. `### Signing Key Rotation`. State that the rotation surface is `KeyEntry.state` (file path: `packages/lattice/src/receipts/types.ts`) with the three states `active`, `retired`, `revoked`. Describe the lifecycle: new keys join the KeySet as `active`; superseded keys transition to `retired` and continue to verify historical receipts; compromised keys transition to `revoked` and all receipts under that `kid` are rejected by `verifyReceipt` with the `key-revoked` error kind. Recommended rotation cadence: annually, OR within 30 days of any suspected key compromise, whichever is sooner. Reference `createMemoryKeySet` (file path: `packages/lattice/src/receipts/keyset.ts`) as the in-process reference implementation; users wiring KMS-backed key sets implement the same `KeySet` interface.

   c. `### Receipt Downgrade Defense (CRYPTO-01)`. State that capability receipts carry an explicit `body.version` discriminator with the two accepted literals `"lattice-receipt/v1"` and `"lattice-receipt/v1.1"`. State the attack: an attacker holding a valid signing key could mint a v1-shaped body and submit it to a v1.1 verifier; v1 receipts predate the step-marker integrity surface added in v1.2, so a successful downgrade would let an attacker bypass the v1.1 step-chain commitments. Cite the precedent verbatim: "Radicle disclosed the analogous attack in March 2026; their receipt protocol's `schemaVersion` field was optional, and an attacker could omit it to suppress downstream invariant checks." State the defense: `verifyReceipt` short-circuits with the `schema-version-too-low` error kind whenever `body.version` is absent or equals `"lattice-receipt/v1"`. Reference the file path `packages/lattice/src/receipts/verify.ts` and the function `verifyReceipt`. Reference the error kind by its literal string `schema-version-too-low` so the writeup and the defense are auditably linked (D-03).

5. `## Supply Chain` section. Brief. Three bullets: (a) all third-party GitHub Actions used in `.github/workflows/` are pinned by 40-character commit SHA (TanStack 2026 OIDC compromise mitigation), (b) the publish pipeline uses npm OIDC Trusted Publisher with provenance attestations rather than a long-lived `NPM_TOKEN`, (c) the `npm-publish` GitHub Environment requires manual reviewer approval for the first three publishes.

6. `## Provenance Verification` section. One paragraph plus one fenced bash block showing `npm view @fullselfbrowsing/lattice --json | jq .dist`. State that the provenance attestation appears once the first OIDC-signed publish lands (Phase 28). Reference `README.md` for the user-facing copy-pastable example.

7. `## License` footer line. State that this policy is licensed MIT alongside the rest of the repository.

Constraints:
- NO emojis (user global rule). Verify with `grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' SECURITY.md` returning empty.
- NO em-dash characters (U+2014 or U+2013) connecting independent sentences. Plain ASCII hyphen in compound words is fine (e.g., "receipt-downgrade").
- Cross-references to file paths use backtick-quoted forward-slash paths.
- Email address is `lakshmantvnm@gmail.com`, exact spelling.
- The literal string `schema-version-too-low` MUST appear in the threat model section as the error kind name.
- The literal string `Radicle` MUST appear in the receipt-downgrade subsection.
- Reference both `packages/lattice/src/receipts/verify.ts` and `packages/lattice/src/receipts/types.ts` in backticks.
  </action>
  <verify>
    <automated>test -f SECURITY.md &amp;&amp; grep -q "lakshmantvnm@gmail.com" SECURITY.md &amp;&amp; grep -q "Radicle" SECURITY.md &amp;&amp; grep -q "schema-version-too-low" SECURITY.md &amp;&amp; grep -q "packages/lattice/src/receipts/verify.ts" SECURITY.md &amp;&amp; ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' SECURITY.md</automated>
  </verify>
  <done>
SECURITY.md exists at repo root, contains the disclosure email, cites Radicle, names the schema-version-too-low error kind, cross-references verify.ts, and is emoji-free.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author CONTRIBUTING.md at repo root (DOC-04)</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create `CONTRIBUTING.md` at the repository root. Hard cap: 200 lines (D-04). Concise OSS template. Do NOT use emojis. Do NOT use em-dashes or hyphens between sentences.

Required structure and content:

1. `# Contributing to Lattice` H1.

2. First non-header paragraph: link to `AGENTS.md` (D-05). Exact wording suggestion: "For deep architectural context, decision log, and the full internal contributor guide, read [AGENTS.md](./AGENTS.md) first. This file is the lightweight summary." The link MUST appear at the top so deep-dive contributors find it without scrolling.

3. `## Development Setup` section. Cover:
   - Node 24 or newer is required (the receipt signing path uses `crypto.subtle.generateKey("Ed25519", ...)` which landed stable in Node 24).
   - `pnpm` is the workspace package manager. Install via `corepack enable && corepack prepare pnpm@latest --activate` or follow `https://pnpm.io/installation`.
   - Initial install: `pnpm install --frozen-lockfile`.
   - Workspace layout: `packages/lattice` (runtime SDK) and `packages/lattice-cli` (CLI). Examples under `examples/`.

4. `## Pre-PR Checks` section. List the canonical commands as a single fenced block (these are the same gates `ci.yml` runs in Phase 25):
   ```bash
   pnpm -r typecheck
   pnpm -r test
   pnpm -r test:types
   pnpm -r lint:packages
   ```
   State that `lint:packages` wraps `publint` and `arethetypeswrong/cli`. State that all four commands must pass green locally before opening a PR.

5. `## Commit Conventions` section. State that Lattice uses Conventional Commits. List the accepted type prefixes: `feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`, `ci:`, `build:`. State that changesets (`pnpm changeset`) drive npm version bumps; every PR that changes published code should include a changeset entry.

6. `## Pull Request Flow` section. Brief. Cover:
   - Fork or branch off `main`.
   - Run the pre-PR checks locally.
   - Open a PR; CI runs the same gates plus SHA-pinned third-party action checks.
   - All commits must be signed off (DCO style is fine; full GPG signing not required).
   - For non-trivial changes, link to or open a planning thread under `.planning/` first.

7. `## Code of Conduct` section. One paragraph stating that contributors are expected to act with good faith and respect; harassment and discrimination are not tolerated; concerns can be raised privately to `lakshmantvnm@gmail.com`.

8. `## License` footer. State that contributions are licensed MIT under the project `LICENSE`.

Constraints:
- Hard line count cap: 200 lines (verifier: `wc -l < CONTRIBUTING.md`). Aim for ~120 to 180 lines.
- NO emojis (user global rule).
- NO em-dashes or hyphens connecting sentences.
- The link `[AGENTS.md](./AGENTS.md)` MUST appear at or near the top (first 10 non-blank lines).
- The four `pnpm -r ...` commands MUST appear verbatim.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md &amp;&amp; [ "$(wc -l &lt; CONTRIBUTING.md)" -le 200 ] &amp;&amp; grep -q "AGENTS.md" CONTRIBUTING.md &amp;&amp; grep -q "pnpm -r test" CONTRIBUTING.md &amp;&amp; grep -q "pnpm -r test:types" CONTRIBUTING.md &amp;&amp; grep -q "pnpm -r lint:packages" CONTRIBUTING.md &amp;&amp; ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' CONTRIBUTING.md</automated>
  </verify>
  <done>
CONTRIBUTING.md exists at repo root, is at most 200 lines, links AGENTS.md near the top, lists the three canonical pnpm gates, and is emoji-free.
  </done>
</task>

</tasks>

<verification>
- `test -f SECURITY.md` and `test -f CONTRIBUTING.md` both succeed.
- SECURITY.md contains: `lakshmantvnm@gmail.com`, `Radicle`, `schema-version-too-low`, `packages/lattice/src/receipts/verify.ts`, `packages/lattice/src/receipts/types.ts`.
- CONTRIBUTING.md contains: `[AGENTS.md](./AGENTS.md)`, `pnpm -r typecheck`, `pnpm -r test`, `pnpm -r test:types`, `pnpm -r lint:packages`; line count ≤ 200.
- Neither file contains emojis (Unicode pictographic blocks U+1F300 to U+1FAFF or U+2600 to U+27BF).
- Neither file uses em-dashes (U+2014) or en-dashes (U+2013) to connect sentences.
- `pnpm -r lint:packages` continues to pass (markdown files are not in tarballs by default; verification is sanity rather than regression).
</verification>

<success_criteria>
SECURITY.md at repo root names `lakshmantvnm@gmail.com` as the disclosure address, documents Ed25519 entropy, KeySet rotation, and the receipt-downgrade defense citing Radicle 2026-03, and cross-references `packages/lattice/src/receipts/verify.ts` plus the `schema-version-too-low` error kind. CONTRIBUTING.md at repo root is under 200 lines, links AGENTS.md from the top, and lists `pnpm -r test`, `pnpm -r test:types`, `pnpm -r lint:packages` as the canonical pre-PR checks. Both files are emoji-free and free of em-dashes between sentences.
</success_criteria>

<output>
After completion, create `.planning/phases/26-release-hygiene-docs-receipt-downgrade-defense/26-01-SUMMARY.md`
</output>
