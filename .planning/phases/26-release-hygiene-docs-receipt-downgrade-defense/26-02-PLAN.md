---
phase: 26-release-hygiene-docs-receipt-downgrade-defense
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/CHANGELOG.md
  - packages/lattice-cli/CHANGELOG.md
  - .changeset/v1.3.0-initial.md
autonomous: true
requirements: [DOC-03, DOC-05]
must_haves:
  truths:
    - "packages/lattice/CHANGELOG.md exists with four version sections (Unreleased, 1.2.0, 1.1.0, 1.0.0) in keepachangelog format"
    - "packages/lattice-cli/CHANGELOG.md exists with four version sections (Unreleased, 1.2.0, 1.1.0, 1.0.0) in keepachangelog format"
    - "Both CHANGELOGs use the scoped package name (@fullselfbrowsing/lattice or @fullselfbrowsing/lattice-cli) in any name reference"
    - ".changeset/v1.3.0-initial.md body is enriched into the actual v1.3.0 release notes covering scope rename, license/metadata, OIDC publish posture, receipt-downgrade defense, CI workflow"
    - "No emojis appear in any of the three files"
  artifacts:
    - path: "packages/lattice/CHANGELOG.md"
      provides: "retroactive v1.0/v1.1/v1.2 history + Unreleased pointer for @fullselfbrowsing/lattice"
      contains: "## [1.2.0]"
      min_lines: 60
    - path: "packages/lattice-cli/CHANGELOG.md"
      provides: "retroactive v1.0/v1.1/v1.2 history + Unreleased pointer for @fullselfbrowsing/lattice-cli"
      contains: "## [1.2.0]"
      min_lines: 60
    - path: ".changeset/v1.3.0-initial.md"
      provides: "v1.3.0 release notes covering scope rename, license/metadata, OIDC, CRYPTO-01, CI"
      min_lines: 15
  key_links:
    - from: ".changeset/v1.3.0-initial.md"
      to: "Phase 26 deliverables"
      via: "release-notes body content"
      pattern: "schema-version-too-low|receipt-downgrade|CRYPTO-01"
    - from: "packages/lattice/CHANGELOG.md [Unreleased]"
      to: ".changeset/v1.3.0-initial.md"
      via: "pointer to upcoming v1.3.0 release"
      pattern: "1.3.0"
---

<objective>
Create per-package `CHANGELOG.md` files (`packages/lattice/CHANGELOG.md` and `packages/lattice-cli/CHANGELOG.md`) retroactively seeded with v1.0, v1.1, and v1.2 history pulled from `.planning/MILESTONES.md`, and enrich the Phase 24 placeholder `.changeset/v1.3.0-initial.md` into the actual v1.3.0 release notes covering all Phase 24 to Phase 26 deliverables.

Purpose: Cover DOC-03 (per-package CHANGELOG retroactively seeded under the new scoped names) and DOC-05 (the changeset half — the README badges + install + provenance half lives in Plan 26-03). The per-package CHANGELOG.md files are the historical record npm reviewers will see on `npmjs.com/package/@fullselfbrowsing/lattice` once Phase 28 publishes. The enriched changeset becomes the auto-attached release notes for the first v1.3.0 release.

Output: Two new `CHANGELOG.md` files and one rewritten `.changeset/v1.3.0-initial.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/MILESTONES.md
@.planning/phases/26-release-hygiene-docs-receipt-downgrade-defense/26-CONTEXT.md
@.changeset/v1.3.0-initial.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/lattice/CHANGELOG.md (DOC-03 part 1)</name>
  <files>packages/lattice/CHANGELOG.md</files>
  <action>
Create `packages/lattice/CHANGELOG.md`. Use the keepachangelog.com 1.1 format (D-07, D-09). Do NOT use emojis. Do NOT use em-dashes or hyphens between sentences.

Required header block (exact form):

```
# Changelog

All notable changes to `@fullselfbrowsing/lattice` are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
```

Then four version sections in descending order (D-08). Use the exact dates from `.planning/MILESTONES.md`. Use the bracket-version + ISO-date convention.

1. `## [Unreleased]`
   - One subsection `### Added` with one bullet: "v1.3.0 prepares the first public npm release under the `@fullselfbrowsing` scope. See `.changeset/v1.3.0-initial.md` for the full release notes."

2. `## [1.2.0] - 2026-05-31` — title hint: "FSB Integration + Agent Capability"
   Pull bullets from `.planning/MILESTONES.md` v1.2 "Key accomplishments". Use keepachangelog categories. Suggested distribution (4 to 6 bullets per applicable category):
   - `### Added`:
     - `ai.runAgent(intent)` runtime entrypoint with uniform prompt-reencoded tool-use across all seven provider adapters (Anthropic Messages, Gemini, xAI, OpenRouter, LM Studio, OpenAI, OpenAI-compatible).
     - `AgentHost` interface with three optional seams (scheduler, transport, storage) and `createNoopAgentHost` reference implementation.
     - Five agent infrastructure primitives: cost tracker, transcript store, goal-progress tracker, action-history dedup, permission context.
     - `SurvivabilityAdapter<TState>` contract with `SerializedSnapshot` JSON round-trip and DSSE + JCS canonicalization under real Ed25519.
     - `createCheckpointHook` factory minting exactly one v1.1 receipt per invocation.
     - `examples/agent-loop` showcase with `evalAgentRun` regression-gate kernel.
   - `### Changed`:
     - `CapabilityReceiptBody.version` widens to `"lattice-receipt/v1" | "lattice-receipt/v1.1"` with auto-bump when any step-marker field is set.
     - `createHookPipeline` ships SAFETY, OBSERVABILITY, EXTENSION priority bands with per-handler `budgetMs` race-with-log, frozen contexts, irreversible freeze, and matcher regex filter.
     - `RunEventKind` gains `step.transition`, `recovery.start`, `recovery.complete`, `recovery.failed` additive literals.
     - Delegation policy in `AGENTS.md` flipped from "multi-agent crews: Out of Scope" to "agent execution: First-class, runtime-agnostic" for the single-agent surface.
   - `### Security`:
     - Multi-agent crews remain out of scope; SAFETY-band hooks can deny or abort an iteration before provider invocation.
     - V1.2-LIMITATION-1 documented: native tool-use deferred to v1.4 (prompt-reencoded protocol preserves INV-03 parity in the meantime).

3. `## [1.1.0] - 2026-05-12` — title hint: "Capability Receipts"
   - `### Added`:
     - WebCrypto Ed25519 signer with in-memory factory plus DSSE v1.0 envelope encoder/decoder plus `@noble/ed25519@3.1.0` parity oracle (dev-only).
     - `CapabilityReceipt` schema with RFC 8785 JCS canonicalization via `canonicalize@3.0.0`, `kid` plus `KeySet` rotation lifecycle (active / retired / revoked).
     - Pure `verifyReceipt` returning typed `VerifyResult` with six error kinds.
     - Receipts inside the replay envelope via `materializeReplayEnvelope`.
     - `lattice` CLI shipped from `packages/lattice-cli` with `repro`, `verify`, `eval` subcommands and disk-backed judge cache.
     - Tripwire invariants with terminal semantics (`mustCite`, `fieldFromTable`, `noPII`, `matches`) and pure `evaluateTripwires` kernel.
     - Sidecar primitives (`loadSidecar`, `applySidecar`, `walkReceiptsWithSidecars`) closing the replay round-trip.
   - `### Changed`:
     - `usage.costUsd` serialized as I-JSON string inside receipts (NOT number).
     - Redact-then-sign ordering enforced structurally with `redactions[]` manifest and signed `redactionPolicyId`.

4. `## [1.0.0] - 2026-04-22` — title hint: "Foundation"
   - `### Added`:
     - ESM-first pnpm workspace package with strict TypeScript, tsdown build output, Vitest smoke coverage, package declaration checks.
     - Provider-neutral runtime / config / policy / artifact / session contracts.
     - Typed `ai.run({ task, artifacts, outputs, policy, session })` with Standard Schema / Zod output inference.
     - Provider-neutral artifact lifecycle (text, JSON, file, image, audio, document, URL, tool-result, derived) with payload-free refs, fingerprints, lineage.
     - Memory and local filesystem development artifact stores.
     - `ai.plan(...)` deterministic dry-run with capability catalog routing, fallback chains, fake providers.
     - Memory sessions, context packs, progressive overrides, narrow OpenAI / OpenAI-compatible / AI SDK adapter factories.
     - Standard Schema local tools, MCP-like tool imports, replay envelopes, offline replay, default redaction.
     - `examples/work-inbox` multimodal showcase using the public package entrypoint.

Constraints:
- The header line MUST name the scoped package `@fullselfbrowsing/lattice` (D-07). Do NOT write bare `lattice`.
- Each version section MUST use `## [VERSION] - DATE` form (square brackets, hyphen, ISO date).
- Pull bullet wording from `.planning/MILESTONES.md` "Key accomplishments" sections (Claude's discretion on exact phrasing, see CONTEXT.md). Tighten where the source is verbose.
- NO emojis (user global rule).
- NO em-dashes connecting sentences. Use commas or periods.
- File length: 80 to 200 lines is the target band.
  </action>
  <verify>
    <automated>test -f packages/lattice/CHANGELOG.md &amp;&amp; grep -q "@fullselfbrowsing/lattice" packages/lattice/CHANGELOG.md &amp;&amp; grep -q "## \[Unreleased\]" packages/lattice/CHANGELOG.md &amp;&amp; grep -q "## \[1.2.0\] - 2026-05-31" packages/lattice/CHANGELOG.md &amp;&amp; grep -q "## \[1.1.0\] - 2026-05-12" packages/lattice/CHANGELOG.md &amp;&amp; grep -q "## \[1.0.0\] - 2026-04-22" packages/lattice/CHANGELOG.md &amp;&amp; ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' packages/lattice/CHANGELOG.md</automated>
  </verify>
  <done>
packages/lattice/CHANGELOG.md exists, uses the scoped name, has the four version sections in keepachangelog format, and is emoji-free.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create packages/lattice-cli/CHANGELOG.md (DOC-03 part 2)</name>
  <files>packages/lattice-cli/CHANGELOG.md</files>
  <action>
Create `packages/lattice-cli/CHANGELOG.md`. Same keepachangelog format as Task 1, but the package is `@fullselfbrowsing/lattice-cli` and the historical entries focus on the CLI surface (the runtime SDK history appears in the sibling `packages/lattice/CHANGELOG.md`).

Required header block (substitute the CLI package name):

```
# Changelog

All notable changes to `@fullselfbrowsing/lattice-cli` are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
```

Four version sections in descending order:

1. `## [Unreleased]`
   - `### Added`: "v1.3.0 prepares the first public npm release under the `@fullselfbrowsing` scope. See `.changeset/v1.3.0-initial.md` for the full release notes."

2. `## [1.2.0] - 2026-05-31` — title hint: "Tracks lattice runtime v1.2"
   - `### Added`:
     - CLI tracks the v1.2 runtime; no new subcommands shipped in v1.2.
     - Test posture: 144 of 144 lattice-cli tests passing alongside the 589 runtime tests.
   - `### Changed`:
     - Bumped the workspace runtime peer to v1.2 to consume the receipt v1.1 schema extension and the step-transition tracing literal.

3. `## [1.1.0] - 2026-05-12` — title hint: "lattice CLI"
   - `### Added`:
     - New `packages/lattice-cli` workspace package with `lattice` bin via tsdown shebang detection.
     - `citty@0.2.2` lazy subcommand loading.
     - `lattice repro <id-or-path>` subcommand running load -> verify -> materialize -> replayOffline -> diff outputHash, exit codes 0 / 1 / 2.
     - `lattice verify <path>` subcommand with single-line OK / FAIL output for signature plus structural verification.
     - `lattice eval` subcommand walking `.lattice/receipts/`, replaying each via `replayOffline`, gating layered determinism (Exact -> Semantic-cheap no-op -> Semantic-expensive judge with N=3 median).
     - `--init-baseline` flag writing a fresh baseline JSON.
     - Disk-backed judge cache keyed by `hash(fixtureId, model_fingerprint, judge_prompt, output_canonicalized)`.
     - Filesystem artifact loader reading `.lattice/fixtures/<sha256>.bin`.
   - `### Security`:
     - Redacted-by-default; no `--unsafe-unredacted` flag in v1.1.
     - Depcheck gate prevents CLI dependencies from leaking into the runtime SDK.

4. `## [1.0.0] - 2026-04-22` — title hint: "Pre-CLI placeholder"
   - `### Added`:
     - Package not published in v1.0. The CLI surface landed in v1.1; this entry exists for completeness so the version history aligns with the runtime SDK.

Constraints same as Task 1: scoped name `@fullselfbrowsing/lattice-cli`, keepachangelog format, no emojis, no em-dashes between sentences. Target length 60 to 150 lines.
  </action>
  <verify>
    <automated>test -f packages/lattice-cli/CHANGELOG.md &amp;&amp; grep -q "@fullselfbrowsing/lattice-cli" packages/lattice-cli/CHANGELOG.md &amp;&amp; grep -q "## \[Unreleased\]" packages/lattice-cli/CHANGELOG.md &amp;&amp; grep -q "## \[1.2.0\] - 2026-05-31" packages/lattice-cli/CHANGELOG.md &amp;&amp; grep -q "## \[1.1.0\] - 2026-05-12" packages/lattice-cli/CHANGELOG.md &amp;&amp; grep -q "## \[1.0.0\] - 2026-04-22" packages/lattice-cli/CHANGELOG.md &amp;&amp; ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' packages/lattice-cli/CHANGELOG.md</automated>
  </verify>
  <done>
packages/lattice-cli/CHANGELOG.md exists with the scoped CLI name, four version sections, and is emoji-free.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enrich .changeset/v1.3.0-initial.md with v1.3.0 release notes (DOC-05 changeset half)</name>
  <files>.changeset/v1.3.0-initial.md</files>
  <action>
Rewrite `.changeset/v1.3.0-initial.md`. The Phase 24 placeholder reads:

```
---
"@fullselfbrowsing/lattice": minor
"@fullselfbrowsing/lattice-cli": minor
---

Initial public release under the @fullselfbrowsing scope.
```

Keep the frontmatter EXACTLY as is (both packages stay `minor` bumps per D-10). Replace the body (everything after the closing `---`) with a multi-paragraph release-notes body covering the five themes from D-10. Target body length: 25 to 60 lines.

Required body sections (Markdown subheadings, no H1 since the frontmatter title takes that role):

1. Opening paragraph: state that v1.3.0 is the first public npm release. Both `@fullselfbrowsing/lattice` and `@fullselfbrowsing/lattice-cli` are now installable from the public npm registry under the `@fullselfbrowsing` scope.

2. `### Package Rename` — explain the Phase 24 outcome:
   - `lattice` is now `@fullselfbrowsing/lattice`.
   - `lattice-cli` is now `@fullselfbrowsing/lattice-cli`.
   - The CLI bin name remains `lattice`. After `pnpm add -g @fullselfbrowsing/lattice-cli`, the user-facing command is still `lattice --version`.
   - The workspace dependency from CLI to runtime flips from `workspace:*` to `workspace:^` to prevent CLI users from being pinned to a single patch of the core (RENAME-03).

3. `### License and Metadata` — `license: "MIT"`, `repository`, `bugs`, `homepage`, and `publishConfig.access: "public"` now ship on both publishable packages. Root `package.json#private: true` preserved so `.planning/` and internal scripts cannot leak (PKG-01 through PKG-04).

4. `### CI Workflow` — `.github/workflows/ci.yml` runs install plus typecheck plus test plus `pnpm -r lint:packages` (publint and arethetypeswrong) on every PR and push to main, with every third-party action pinned by 40-character commit SHA (CI-01, CI-02; TanStack 2026 OIDC compromise mitigation).

5. `### OIDC Trusted Publisher (Future)` — note that v1.3.0 is published via npm OIDC Trusted Publisher with provenance attestations, not a long-lived `NPM_TOKEN`. The release workflow lands in Phase 28; the trust tuple `(fullselfbrowsing/Lattice, release.yml, npm-publish)` is bound on npmjs.com in Phase 27 before the first publish.

6. `### Receipt Downgrade Defense (CRYPTO-01)` — describe the security-relevant behavior change. `verifyReceipt` now rejects receipts whose `body.version` is absent or equals `"lattice-receipt/v1"`. The new `VerifyResult` error kind is `schema-version-too-low`. Cite the Radicle 2026-03 precedent: their receipt protocol's `schemaVersion` field was optional and an attacker could omit it to suppress downstream invariant checks. State that this is a breaking semantic change acceptable in 1.3.0 because there are no public consumers yet; SECURITY.md documents the full threat model.

7. Final paragraph: "See per-package `CHANGELOG.md` for the historical record."

Constraints:
- Frontmatter MUST stay exactly two lines plus delimiters with the two scoped package names set to `minor`. Do not reorder or change the bump levels.
- NO emojis.
- NO em-dashes between sentences.
- The literal strings `schema-version-too-low`, `CRYPTO-01`, `Radicle`, `@fullselfbrowsing/lattice`, `@fullselfbrowsing/lattice-cli`, and `release.yml` MUST appear in the body.
- Body MUST be substantially longer than the Phase 24 placeholder one-liner (min 15 non-frontmatter lines).
  </action>
  <verify>
    <automated>test -f .changeset/v1.3.0-initial.md &amp;&amp; grep -q '"@fullselfbrowsing/lattice": minor' .changeset/v1.3.0-initial.md &amp;&amp; grep -q '"@fullselfbrowsing/lattice-cli": minor' .changeset/v1.3.0-initial.md &amp;&amp; grep -q "schema-version-too-low" .changeset/v1.3.0-initial.md &amp;&amp; grep -q "Radicle" .changeset/v1.3.0-initial.md &amp;&amp; grep -q "CRYPTO-01" .changeset/v1.3.0-initial.md &amp;&amp; [ "$(wc -l &lt; .changeset/v1.3.0-initial.md)" -ge 20 ] &amp;&amp; ! grep -P '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]' .changeset/v1.3.0-initial.md</automated>
  </verify>
  <done>
.changeset/v1.3.0-initial.md keeps the two-package minor-bump frontmatter, body covers scope rename, license/metadata, CI, OIDC, CRYPTO-01 with Radicle precedent, and is emoji-free with at least 20 lines.
  </done>
</task>

</tasks>

<verification>
- Both `packages/lattice/CHANGELOG.md` and `packages/lattice-cli/CHANGELOG.md` exist with the four required version sections in descending order.
- Both CHANGELOGs use the scoped package name in the header sentence.
- `.changeset/v1.3.0-initial.md` body is substantially expanded (≥ 20 lines total file length including frontmatter).
- The Phase 25 `scripts/verify-rename.mjs` allowlist permits these CHANGELOG.md files (they cite the scoped names, not unscoped `lattice` imports). `pnpm -r lint:packages` continues to pass.
- No emojis in any of the three files.
- No em-dashes connecting sentences in any of the three files.
</verification>

<success_criteria>
Two per-package CHANGELOG.md files exist with four version sections each (Unreleased, 1.2.0 dated 2026-05-31, 1.1.0 dated 2026-05-12, 1.0.0 dated 2026-04-22) seeded from MILESTONES.md under the scoped names. `.changeset/v1.3.0-initial.md` body covers scope rename, license/metadata, CI workflow, OIDC posture, and the CRYPTO-01 receipt-downgrade defense citing Radicle 2026-03. All three files are emoji-free.
</success_criteria>

<output>
After completion, create `.planning/phases/26-release-hygiene-docs-receipt-downgrade-defense/26-02-SUMMARY.md`
</output>
