# Plan 17-01: Anthropic provider adapter

**Status:** Complete (retro). **Wave:** 1. **REQ-IDs:** PROV-01.
**Originating commit:** `cf31d82` feat(providers): add Anthropic provider adapter.

## Goal

Ship `packages/lattice/src/providers/anthropic.ts` as a full custom adapter for Anthropic's `/v1/messages` API, mirroring FSB universal-provider.js:280-297 + 566-573 production shape.

## Tasks

1. New `packages/lattice/src/providers/anthropic.ts` (+161 lines).
   - Endpoint `/v1/messages`.
   - Top-level `system` field; `content[0].text` response parsing.
   - Usage: `input_tokens` + `output_tokens` from `usage` block.
   - Headers: `x-api-key` + `anthropic-version: 2023-06-01`.
   - Single-shot Promise.
2. New `packages/lattice/src/providers/anthropic.test.ts` (+218 lines, 9 cases).

## Acceptance

9 vitest cases pass. PROV-01 closed.

## Outcome

Cherry-pick `cf31d82` lands on phase branch.
