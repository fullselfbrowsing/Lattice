# Plan 17-06: Public surface re-exports + INV-03 parity smoke + audit doc close

**Status:** Complete (retro). **Wave:** 2 (closure). **REQ-IDs:** INDEX-04, PARITY-01.
**Originating commits:** `e5659a8` feat(api): re-export 5 new provider factories; `f9c7ef4` test(providers): add INV-03 parity smoke covering 7 logical providers; `f1c943b` docs(fsb-integration): close Phase 4 audit rows.

## Goal

Re-export 5 new provider factories from `packages/lattice/src/index.ts`. Ship INV-03 parity smoke that proves every adapter conforms to the same `ProviderAdapter` contract. Close 5 Provider audit-doc rows.

## Tasks

1. `packages/lattice/src/index.ts` — +10 lines (5 factory re-exports + 5 option-type aliases).
2. New `packages/lattice/src/providers/parity.test.ts` (+268 lines, 7 cases). Iterates all 7 logical provider factories (OpenAI, OpenAI-compat, Anthropic, Gemini, xAI, OpenRouter, LM Studio) under fake fetch. Asserts: ProviderAdapter shape; `rawOutputs` populated; normalized `Usage` shape; provider-name in error on non-OK; AbortSignal propagation; `rawResponse` preserved; distinct request ids.
3. `docs/fsb-integration-gaps.md` — flip 5 Providers rows to Covered.

## Acceptance

INDEX-04 + PARITY-01 closed.

## Outcome

Cherry-picks `e5659a8`, `f9c7ef4`, `f1c943b` land on phase branch.
