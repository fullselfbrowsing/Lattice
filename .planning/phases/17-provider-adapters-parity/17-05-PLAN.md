# Plan 17-05: LM Studio provider adapter

**Status:** Complete (retro). **Wave:** 1. **REQ-IDs:** PROV-05.
**Originating commit:** `40457ff` feat(providers): add LM Studio provider adapter.

## Goal

Ship `packages/lattice/src/providers/lm-studio.ts` as a thin wrapper around `createOpenAICompatibleProvider` pinned to `http://localhost:1234/v1`. `apiKey` optional (no-auth by convention). Latency-tail diagnostics module deferred.

## Tasks

1. New `packages/lattice/src/providers/lm-studio.ts` (+47 lines).
2. New `packages/lattice/src/providers/lm-studio.test.ts` (+181 lines, 8 cases).

## Acceptance

8 vitest cases pass. PROV-05 closed.

## Outcome

Cherry-pick `40457ff` lands on phase branch.
