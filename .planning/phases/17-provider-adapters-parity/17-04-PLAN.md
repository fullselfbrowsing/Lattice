# Plan 17-04: OpenRouter provider adapter

**Status:** Complete (retro). **Wave:** 1. **REQ-IDs:** PROV-04.
**Originating commit:** `1cfc13c` feat(providers): add OpenRouter provider adapter.

## Goal

Ship `packages/lattice/src/providers/openrouter.ts` as a thin wrapper around `createOpenAICompatibleProvider` pinned to `https://openrouter.ai/api/v1`. First-class named adapter for ceremony parity. Multi-model routing / fallback array deferred.

## Tasks

1. New `packages/lattice/src/providers/openrouter.ts` (+41 lines).
2. New `packages/lattice/src/providers/openrouter.test.ts` (+155 lines, 7 cases).

## Acceptance

7 vitest cases pass. PROV-04 closed.

## Outcome

Cherry-pick `1cfc13c` lands on phase branch.
