# Plan 17-02: Gemini provider adapter

**Status:** Complete (retro). **Wave:** 1. **REQ-IDs:** PROV-02.
**Originating commit:** `7a32b00` feat(providers): add Gemini provider adapter.

## Goal

Ship `packages/lattice/src/providers/gemini.ts` as a full custom adapter for Google's `/v1beta/models/{model}:generateContent` API.

## Tasks

1. New `packages/lattice/src/providers/gemini.ts` (+176 lines).
   - Request shape `contents[].parts[].text`; response `candidates[0].content.parts[0].text`.
   - Usage: `usageMetadata.promptTokenCount` + `candidatesTokenCount`.
   - 4 `HARM_CATEGORY` safety settings at `BLOCK_NONE`.
   - `?key=` query-string auth.
   - Role mapping `user` / `model` (NOT `assistant`).
2. New `packages/lattice/src/providers/gemini.test.ts` (+251 lines, 10 cases).

## Acceptance

10 vitest cases pass. PROV-02 closed.

## Outcome

Cherry-pick `7a32b00` lands on phase branch.
