---
phase: 48-eval-diagnostics-cli-expansion
plan: 03
type: execute
wave: 3
depends_on: [48-01]
files_modified:
  - packages/lattice-cli/src/diagnostics/lm-studio.ts
  - packages/lattice-cli/src/commands/diagnostics.ts
  - packages/lattice-cli/src/cli.ts
  - packages/lattice-cli/test/diagnostics.test.ts
  - packages/lattice-cli/test/cli.test.ts
autonomous: true
requirements: [EVAL-04]
tags:
  - cli
  - diagnostics
  - lm-studio
must_haves:
  truths:
    - "lattice diagnostics lm-studio reads local RunEvent JSON"
    - "diagnostics summarize LM Studio provider latency tails without network calls"
    - "malformed event files exit 2 and non-LM Studio events are ignored"
  artifacts:
    - path: "packages/lattice-cli/src/diagnostics/lm-studio.ts"
      provides: "LM Studio latency-tail summarizer"
    - path: "packages/lattice-cli/src/commands/diagnostics.ts"
      provides: "diagnostics command group with lm-studio subcommand"
---

<objective>
Add local LM Studio latency-tail diagnostics over persisted Lattice run events.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-eval-diagnostics-cli-expansion/48-CONTEXT.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-RESEARCH.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-PATTERNS.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement event loader and latency summarizer</name>
  <files>packages/lattice-cli/src/diagnostics/lm-studio.ts</files>
  <action>
    Validate JSON input as either `RunEvent[]` or `{ events: RunEvent[] }`, filter LM Studio `provider.attempt` events, pair start/terminal events by run/provider/model FIFO, support explicit duration metadata where present, and compute count/min/p50/p95/p99/max plus slowest attempts.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- diagnostics</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add diagnostics command group</name>
  <files>packages/lattice-cli/src/commands/diagnostics.ts packages/lattice-cli/src/cli.ts</files>
  <action>
    Register `lattice diagnostics lm-studio --events <path>`. Emit one `lattice-diagnostics/lm-studio-latency/v1` JSON line on successful reports and map malformed input to exit 2.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- diagnostics cli</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Cover latency tails and malformed input</name>
  <files>packages/lattice-cli/test/diagnostics.test.ts packages/lattice-cli/test/cli.test.ts</files>
  <action>
    Add tests for successful and failed attempts, p95/p99 tail calculation, ignored non-LM Studio providers, empty reports, malformed event JSON, and help registration for `diagnostics`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- diagnostics cli</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice-cli test -- diagnostics cli
pnpm --filter @full-self-browsing/lattice-cli typecheck
```
</verification>

<success_criteria>
- Diagnostics reports are generated entirely from local event files.
- Tail statistics are deterministic and covered by tests.
- Empty or non-LM Studio event files produce a valid zero-count report, not a failure.
</success_criteria>
