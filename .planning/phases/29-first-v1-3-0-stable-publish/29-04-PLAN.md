---
phase: 29-first-v1-3-0-stable-publish
plan: 04
type: execute
wave: 4
depends_on:
  - 29-03
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/29-first-v1-3-0-stable-publish/29-04-SUMMARY.md
autonomous: false
requirements:
  - PUB-02
  - PUB-03
  - PUB-04
tags:
  - postflight
  - provenance
  - release-proof
must_haves:
  truths:
    - "Both npm packages resolve at 1.3.0 and latest dist-tags point at 1.3.0"
    - "Both npm package versions expose signatures and attestations/provenance proof"
    - "GitHub Release v1.3.0 exists, is not prerelease, and includes changelog-derived release notes plus npm links"
    - "Registry tarball inspection confirms scoped package names, exports/types, and CLI bin shape"
    - "PUB-02, PUB-03, and PUB-04 are marked complete only after proof is captured"
  artifacts:
    - path: ".planning/phases/29-first-v1-3-0-stable-publish/29-04-SUMMARY.md"
      provides: "release proof dossier"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PUB-02..04 completion state"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 29 complete status"
    - path: ".planning/STATE.md"
      provides: "next focus Phase 30"
  key_links:
    - from: "npm package provenance"
      to: "GitHub Actions run"
      via: "npm provenance source metadata and run URL"
      pattern: "v1.3.0"
---

<objective>
Prove and record the stable release. Query npm and GitHub for machine-readable evidence, verify registry tarballs and consumer provenance signatures, then mark PUB-02, PUB-03, and PUB-04 complete in planning state only after the proof exists.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-first-v1-3-0-stable-publish/29-CONTEXT.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-RESEARCH.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-VALIDATION.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-03-SUMMARY.md
@scripts/check-tarball-leak.mjs
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-29-07: provenance/signature verification is skipped or inferred from workflow success | high | Query npm JSON, install in temp consumer, run `npm audit signatures`, and record UI/source metadata where needed |
| T-29-13: dist-tag remains on rc.0 after stable publish | high | Query both package dist-tags and require `latest: 1.3.0` |
| T-29-14: GitHub Release exists but is prerelease or body lacks changelog/npm links | medium | Query release JSON and grep body for changelog headings plus npm package URLs |
| T-29-15: planning docs mark complete before proof exists | high | Update REQUIREMENTS/ROADMAP/STATE only after all postflight commands pass |
| T-29-16: registry tarball differs from local expected package shape | high | `npm pack <pkg>@1.3.0` both packages and inspect `package/package.json` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Capture npm and GitHub release proof</name>
  <files>.planning/phases/29-first-v1-3-0-stable-publish/29-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/29-first-v1-3-0-stable-publish/29-03-SUMMARY.md
  </read_first>
  <action>
    Run and save the essential outputs in the summary:
    ```bash
    npm view @full-self-browsing/lattice@1.3.0 --json
    npm view @full-self-browsing/lattice-cli@1.3.0 --json
    npm view @full-self-browsing/lattice dist-tags --json
    npm view @full-self-browsing/lattice-cli dist-tags --json
    gh release view v1.3.0 --repo fullselfbrowsing/Lattice --json tagName,name,url,body,isLatest,isPrerelease
    gh run view <publish-run-id> --repo fullselfbrowsing/Lattice --json databaseId,displayTitle,event,headSha,status,conclusion,url
    ```
    The npm JSON for each package must show `version: "1.3.0"`, `dist.signatures` present, and either `dist.attestations` present or npm provenance UI proof recorded. Both dist-tags outputs must show `"latest": "1.3.0"`. The GitHub Release JSON must show `tagName: "v1.3.0"` and `isPrerelease: false`.
  </action>
  <verify>
    <automated>bash -c 'npm view @full-self-browsing/lattice@1.3.0 --json && npm view @full-self-browsing/lattice-cli@1.3.0 --json && npm view @full-self-browsing/lattice dist-tags --json && npm view @full-self-browsing/lattice-cli dist-tags --json && gh release view v1.3.0 --repo fullselfbrowsing/Lattice --json tagName,name,url,body,isLatest,isPrerelease'</automated>
  </verify>
  <acceptance_criteria>
    - Summary records npm JSON proof for both packages.
    - Summary records both package shasums.
    - Summary records both packages have registry signatures.
    - Summary records both packages have attestations/provenance proof.
    - Summary records both `latest` dist-tags are `1.3.0`.
    - Summary records GitHub Release `v1.3.0` URL.
    - GitHub Release is not prerelease.
  </acceptance_criteria>
  <done>External release proof dossier exists.</done>
</task>

<task type="auto">
  <name>Task 2: Verify registry tarballs and consumer provenance audit</name>
  <files>.planning/phases/29-first-v1-3-0-stable-publish/29-04-SUMMARY.md</files>
  <read_first>
    - scripts/check-tarball-leak.mjs
    - packages/lattice/package.json
    - packages/lattice-cli/package.json
  </read_first>
  <action>
    Create a temp directory and inspect registry tarballs:
    ```bash
    TMP=$(mktemp -d)
    npm pack @full-self-browsing/lattice@1.3.0 --pack-destination "$TMP"
    npm pack @full-self-browsing/lattice-cli@1.3.0 --pack-destination "$TMP"
    tar -xOf "$TMP"/*lattice-1.3.0.tgz package/package.json
    tar -xOf "$TMP"/*lattice-cli-1.3.0.tgz package/package.json
    ```
    Confirm runtime manifest has `name: "@full-self-browsing/lattice"`, `version: "1.3.0"`, `exports["."].types: "./dist/index.d.ts"`, and `files: ["dist"]`. Confirm CLI manifest has `name: "@full-self-browsing/lattice-cli"`, `version: "1.3.0"`, `bin.lattice: "./dist/cli.js"`, dependency `@full-self-browsing/lattice` with a caret-compatible `^1.3.0` or workspace-rewritten equivalent, and `files: ["dist"]`.

    Then create a temp npm consumer:
    ```bash
    CONSUMER=$(mktemp -d)
    cd "$CONSUMER"
    npm init -y
    npm install @full-self-browsing/lattice@1.3.0 @full-self-browsing/lattice-cli@1.3.0
    npm audit signatures
    ```
    Record command outputs in the summary. Remove temp directories after recording.
  </action>
  <verify>
    <manual>Executor records tarball manifest excerpts and `npm audit signatures` output in the summary.</manual>
  </verify>
  <acceptance_criteria>
    - Runtime registry tarball manifest name is `@full-self-browsing/lattice`.
    - Runtime registry tarball manifest version is `1.3.0`.
    - CLI registry tarball manifest name is `@full-self-browsing/lattice-cli`.
    - CLI registry tarball manifest version is `1.3.0`.
    - CLI registry tarball manifest contains `bin.lattice`.
    - Temp consumer `npm install` exits 0.
    - Temp consumer `npm audit signatures` exits 0.
  </acceptance_criteria>
  <done>Published artifacts install and verify like a real consumer would use them.</done>
</task>

<task type="auto">
  <name>Task 3: Mark PUB-02..04 complete and advance state to Phase 30</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/29-first-v1-3-0-stable-publish/29-04-SUMMARY.md
  </read_first>
  <action>
    Only after Tasks 1 and 2 pass, update planning docs:
    1. In `.planning/REQUIREMENTS.md`, change `PUB-02`, `PUB-03`, and `PUB-04` checkboxes from `[ ]` to `[x]`; change traceability rows for those IDs from `pending` to `complete`; update the authored-complete count by +3 if present; add footer line:
       `*Phase 29 REQ-IDs (PUB-02..04) completed: 2026-06-11 - stable v1.3.0 published with npm provenance and GitHub Release proof*`
    2. In `.planning/ROADMAP.md`, check Phase 29 and add `(completed 2026-06-11)`; change Phase 29 progress table row from `0/0 | Not started | -` to `4/4 | Complete | 2026-06-11` if that progress table exists.
    3. In `.planning/STATE.md`, move current focus to Phase 30 Canary Bootstrap + Layer 1 Fake-Provider Suite; update blockers/concerns so stable `1.3.0` is no longer listed as unpublished; preserve existing notes that canary repo work remains separate.
    Use GSD CLI helpers where available; if a helper cannot express a specific table edit, patch only the relevant lines and record why in the summary.
  </action>
  <verify>
    <automated>bash -c 'rg "PUB-02.*complete|PUB-03.*complete|PUB-04.*complete" .planning/REQUIREMENTS.md && rg "\\[x\\].*Phase 29: First v1\\.3\\.0 Stable Publish|29\\. First v1\\.3\\.0 Stable Publish \\| 4/4 \\| Complete" .planning/ROADMAP.md && rg "Phase 30|Canary Bootstrap" .planning/STATE.md'</automated>
  </verify>
  <acceptance_criteria>
    - `PUB-02`, `PUB-03`, and `PUB-04` are checked in `.planning/REQUIREMENTS.md`.
    - Traceability rows for `PUB-02`, `PUB-03`, and `PUB-04` are complete.
    - `.planning/ROADMAP.md` marks Phase 29 complete with date `2026-06-11`.
    - `.planning/STATE.md` points to Phase 30 as next focus.
    - Summary links to the npm and GitHub Release proof used before marking complete.
  </acceptance_criteria>
  <done>Phase 29 is closed and the milestone advances to canary validation.</done>
</task>

</tasks>

<verification>
Plan 04 passes when npm, GitHub Release, registry tarball, and consumer `npm audit signatures` proof are recorded and planning state marks PUB-02..04 complete.
</verification>

<success_criteria>
- Both stable packages are visible on npm with provenance/signature proof.
- GitHub Release `v1.3.0` exists with changelog-derived notes and npm links.
- Registry tarballs install and verify in a temp consumer.
- Planning state advances to Phase 30.
</success_criteria>

<output>
Create `.planning/phases/29-first-v1-3-0-stable-publish/29-04-SUMMARY.md` when done.
</output>
