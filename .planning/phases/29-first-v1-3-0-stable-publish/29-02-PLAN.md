---
phase: 29-first-v1-3-0-stable-publish
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - "external:github-actions-settings"
  - "external:changesets-version-pr"
  - packages/lattice/package.json
  - packages/lattice-cli/package.json
  - packages/lattice/CHANGELOG.md
  - packages/lattice-cli/CHANGELOG.md
autonomous: false
requirements:
  - PUB-02
  - PUB-03
  - PUB-04
tags:
  - release-preflight
  - changesets
  - manual-gate
must_haves:
  truths:
    - "GitHub Actions repository setting allows the version-pr job to create/update the Version Packages PR, or the workflow stops with a user handoff"
    - "Full local release gates pass before the Version Packages PR is merged"
    - "npm registry pre-state confirms neither package has 1.3.0 before stable publish"
    - "Changesets version math resolves both fixed packages to 1.3.0"
  artifacts:
    - path: "GitHub Actions repository settings"
      provides: "can_approve_pull_request_reviews true or observed version-pr success"
    - path: "Version Packages PR"
      provides: "manifest/changelog bump to 1.3.0"
  key_links:
    - from: "Version Packages PR"
      to: ".changeset/v1.3.0-*.md"
      via: "pnpm changeset version"
      pattern: "1.3.0"
---

<objective>
Run the stable-release preflight and drive the Changesets Version Packages PR up to the point where it is ready to merge. This plan stops before the tag-triggered publish side effect unless all local gates, registry pre-state checks, and GitHub Actions PR-creation prerequisites are satisfied.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-first-v1-3-0-stable-publish/29-CONTEXT.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-RESEARCH.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-VALIDATION.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-01-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@.changeset/config.json
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-29-04: stable tag or release points at the wrong commit | high | Only merge the generated Version Packages PR after gates pass; record PR number and merge commit |
| T-29-05: registry drift changes model capability snapshot at stable cut | medium | Run `node scripts/refresh-model-registry.mjs --check` before merge |
| T-29-09: GitHub Actions cannot create the Version Packages PR and release automation silently stalls | medium | Check `gh api repos/fullselfbrowsing/Lattice/actions/permissions/workflow` or observe successful `version-pr` run before proceeding |
| T-29-10: npm stable slot already exists | high | Query both package version lists and stop if `1.3.0` appears |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run local release preflight gates</name>
  <files>(verification only)</files>
  <read_first>
    - package.json
    - pnpm-lock.yaml
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - scripts/check-tarball-leak.mjs
    - scripts/verify-rename.mjs
    - scripts/check-workflow-safety.mjs
    - scripts/refresh-model-registry.mjs
  </read_first>
  <action>
    Run the full Phase 29 preflight command sequence from `29-VALIDATION.md`:
    ```bash
    pnpm install --frozen-lockfile
    pnpm build
    pnpm typecheck
    pnpm test
    pnpm test:types
    pnpm -r lint:packages
    node scripts/check-tarball-leak.mjs
    node scripts/verify-rename.mjs
    node scripts/check-workflow-safety.mjs
    node scripts/refresh-model-registry.mjs --check
    pnpm changeset status --verbose
    ```
    Confirm the Changesets status output includes both `@full-self-browsing/lattice 1.3.0` and `@full-self-browsing/lattice-cli 1.3.0`. If any command fails, stop and fix before continuing to Task 2.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm build && pnpm typecheck && pnpm test && pnpm test:types && pnpm -r lint:packages && node scripts/check-tarball-leak.mjs && node scripts/verify-rename.mjs && node scripts/check-workflow-safety.mjs && node scripts/refresh-model-registry.mjs --check && pnpm changeset status --verbose</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the preflight sequence exits 0.
    - Changesets status output contains `@full-self-browsing/lattice 1.3.0`.
    - Changesets status output contains `@full-self-browsing/lattice-cli 1.3.0`.
  </acceptance_criteria>
  <done>Local release preflight is green and version math is stable.</done>
</task>

<task type="manual">
  <name>Task 2: Confirm GitHub Actions can create the Version Packages PR</name>
  <files>external:github-actions-settings</files>
  <read_first>
    - .planning/phases/28-release-workflow-rc0-oidc-smoke/28-SUMMARY.md
    - .github/workflows/release.yml
  </read_first>
  <action>
    Check the repository workflow permission:
    ```bash
    gh api repos/fullselfbrowsing/Lattice/actions/permissions/workflow
    ```
    The JSON must show `can_approve_pull_request_reviews: true`, OR the executor must observe a successful `version-pr` run that creates/updates the Version Packages PR. If it is false, the user must open GitHub repository Settings -> Actions -> General -> Workflow permissions and enable "Allow GitHub Actions to create and approve pull requests", then rerun/trigger the `version-pr` job.
  </action>
  <verify>
    <manual>Record either the `gh api` JSON showing `can_approve_pull_request_reviews: true` or the successful `version-pr` workflow run URL that created/updated the Version Packages PR.</manual>
  </verify>
  <acceptance_criteria>
    - Summary records `can_approve_pull_request_reviews: true`, or records a successful `version-pr` run URL.
    - If the setting was false, summary records the user handoff and completion.
  </acceptance_criteria>
  <done>Version PR creation is unblocked.</done>
</task>

<task type="auto">
  <name>Task 3: Verify registry pre-state and Version Packages PR contents</name>
  <files>external:changesets-version-pr</files>
  <read_first>
    - .changeset/config.json
    - .changeset/v1.3.0-adapter-quirks-negotiation.md
    - .changeset/v1.3.0-agent-crew.md
    - .changeset/v1.3.0-capability-registry.md
    - .changeset/v1.3.0-output-sanitizers.md
    - .changeset/v1.3.0-prompt-scaffolds.md
    - .changeset/v1.3.0-receipt-v12-model-class.md
    - .changeset/v1.3.0-tool-call-validation.md
  </read_first>
  <action>
    Query npm pre-state:
    ```bash
    npm view @full-self-browsing/lattice versions dist-tags --json
    npm view @full-self-browsing/lattice-cli versions dist-tags --json
    ```
    Stop if either version list contains `1.3.0`. Then ensure push-to-main has triggered/updated the Version Packages PR. Use `gh pr list --repo fullselfbrowsing/Lattice --search "chore(release): Version Packages" --state open --json number,title,url,headRefName,statusCheckRollup` and inspect the PR diff. The PR diff must set both package manifests to `1.3.0`, add `## [1.3.0]` changelog sections, and consume/delete the pending `.changeset/v1.3.0-*.md` files.
  </action>
  <verify>
    <automated>bash -c 'npm view @full-self-browsing/lattice versions dist-tags --json && npm view @full-self-browsing/lattice-cli versions dist-tags --json && gh pr list --repo fullselfbrowsing/Lattice --search "chore(release): Version Packages" --state open --json number,title,url,headRefName,statusCheckRollup'</automated>
  </verify>
  <acceptance_criteria>
    - npm pre-state output for both packages does not include version `1.3.0`.
    - A Version Packages PR URL is recorded in the summary.
    - PR diff shows `packages/lattice/package.json` version `1.3.0`.
    - PR diff shows `packages/lattice-cli/package.json` version `1.3.0`.
    - PR diff shows `## [1.3.0]` in both package changelogs.
    - PR checks are green before Plan 03 merges.
  </acceptance_criteria>
  <done>Stable slot is open and the Version Packages PR is ready for review.</done>
</task>

</tasks>

<verification>
This plan passes when local gates are green, the Version Packages PR exists with both package versions set to `1.3.0`, and npm pre-state proves stable has not already been published.
</verification>

<success_criteria>
- Full local release gate passed.
- GitHub Actions PR creation prerequisite handled.
- Version Packages PR is open/updated and ready to merge.
- npm stable slot remains open for both packages.
</success_criteria>

<output>
Create `.planning/phases/29-first-v1-3-0-stable-publish/29-02-SUMMARY.md` when done.
</output>
