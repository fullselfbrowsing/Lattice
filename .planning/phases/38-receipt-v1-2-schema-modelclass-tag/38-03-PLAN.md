---
phase: 38
plan: 03
type: execute
wave: 3
depends_on:
  - 38-01
  - 38-02
files_modified:
  - packages/lattice/src/index.ts
  - packages/lattice/src/runtime/public-types.ts
  - packages/lattice/test/public-surface.test.ts
  - packages/lattice/test-d/receipt-v12.test-d.ts
  - .changeset/v1.3.0-receipt-v12-model-class.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - RECEIPT12-01
  - RECEIPT12-04
tags:
  - public-api
  - type-surface
  - release-notes
must_haves:
  truths:
    - "TrainingClass is usable with CapabilityReceiptBody modelClass from public imports"
    - "runtime/public-types.ts re-exports TrainingClass if needed for root type coherence"
    - "Public surface tests cover receipt modelClass type reachability"
    - "tsd package tests reject invalid modelClass literals"
    - "Changeset documents v1.2 receipt minting and v1.1 verification compatibility"
    - "Final targeted suite, build, typecheck, and tsd pass before requirements are marked complete"
  artifacts:
    - path: "packages/lattice/test-d/receipt-v12.test-d.ts"
      provides: "Package-level type tests for CapabilityReceiptBody modelClass"
    - path: ".changeset/v1.3.0-receipt-v12-model-class.md"
      provides: "Release notes for receipt v1.2 schema bump"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RECEIPT12 completion status after final verification"
  key_links:
    - from: "packages/lattice/src/runtime/public-types.ts"
      to: "packages/lattice/src/capabilities/profile.ts"
      via: "exports TrainingClass type"
      pattern: "TrainingClass"
    - from: "packages/lattice/test-d/receipt-v12.test-d.ts"
      to: "packages/lattice/src/receipts/types.ts"
      via: "tests CapabilityReceiptBody modelClass"
      pattern: "CapabilityReceiptBody"
---

<objective>
Close Phase 38 by making the v1.2 receipt modelClass type coherent through the
public package surface, documenting the release-visible schema bump, running the
full targeted verification gates, and marking `RECEIPT12-*` complete only after
those gates pass.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-CONTEXT.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-RESEARCH.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-PATTERNS.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-01-PLAN.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-02-PLAN.md
@packages/lattice/src/index.ts
@packages/lattice/src/runtime/public-types.ts
@packages/lattice/test/public-surface.test.ts
@packages/lattice/test-d/capabilities.test-d.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Runtime type barrel omits TrainingClass, making CapabilityReceiptBody awkward to use publicly | medium | Re-export `TrainingClass` and add public-surface + tsd tests |
| Release notes overstate adapter behavior | medium | Changeset must say runtime terminal receipts derive modelClass; provider response APIs are unchanged |
| Requirements are marked complete before gates pass | high | Final task runs tests/build/typecheck/tsd before updating REQUIREMENTS/ROADMAP/STATE |
| Public type test misses invalid modelClass literals | medium | tsd test uses `expectError` for invalid string values |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make public type surface coherent for receipt modelClass</name>
  <files>
    packages/lattice/src/index.ts
    packages/lattice/src/runtime/public-types.ts
    packages/lattice/test/public-surface.test.ts
    packages/lattice/test-d/receipt-v12.test-d.ts
  </files>
  <read_first>
    - packages/lattice/src/index.ts
    - packages/lattice/src/runtime/public-types.ts
    - packages/lattice/test/public-surface.test.ts
    - packages/lattice/test-d/capabilities.test-d.ts
    - packages/lattice/src/receipts/types.ts
  </read_first>
  <action>
    Ensure public type reachability:

    1. If `packages/lattice/src/runtime/public-types.ts` does not export
       `TrainingClass`, add:
       ```ts
       export type { TrainingClass } from "../capabilities/profile.js";
       ```
       or include it in an existing capability type export block.
    2. Confirm root `packages/lattice/src/index.ts` exports `TrainingClass`
       from `./capabilities/index.js`; keep or adjust the existing Phase 33
       capability export block rather than creating a duplicate export.
    3. In `packages/lattice/test/public-surface.test.ts`, extend the receipt
       type-only public surface test so it references:
       ```ts
       const _class: TrainingClass | undefined = undefined;
       const _bodyModelClass: CapabilityReceiptBody["modelClass"] | undefined = _class;
       ```
    4. Create `packages/lattice/test-d/receipt-v12.test-d.ts` importing from
       `@full-self-browsing/lattice` and asserting:
       - `expectAssignable<CapabilityReceiptBody["modelClass"]>("frontier_rlhf")`
       - `expectAssignable<CapabilityReceiptBody["modelClass"]>(undefined)`
       - `expectError<CapabilityReceiptBody["modelClass"]>("not-a-real-class")`
       - `expectType<TrainingClass | undefined>(body.modelClass)`
    5. Do not add a runtime value export for `TrainingClass`; it is a type.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test public-surface && pnpm --filter @full-self-browsing/lattice exec tsd</automated>
  </verify>
  <acceptance_criteria>
    - `runtime/public-types.ts` exports `TrainingClass`.
    - Root index exports `TrainingClass` exactly once.
    - Public surface test references `CapabilityReceiptBody["modelClass"]`.
    - `receipt-v12.test-d.ts` exists.
    - tsd rejects `not-a-real-class` for receipt modelClass.
    - Public surface and tsd commands exit 0.
  </acceptance_criteria>
  <done>Consumers can type receipt modelClass from the package public surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add release notes for receipt v1.2</name>
  <files>.changeset/v1.3.0-receipt-v12-model-class.md</files>
  <read_first>
    - .changeset/v1.3.0-capability-registry.md
    - .changeset/v1.3.0-tool-call-validation.md
    - .planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-CONTEXT.md
  </read_first>
  <action>
    Create `.changeset/v1.3.0-receipt-v12-model-class.md` using the adjacent
    v1.3 changeset style:

    1. Frontmatter:
       ```md
       ---
       "@full-self-browsing/lattice": minor
       ---
       ```
    2. Mention that newly minted receipts now use
       `lattice-receipt/v1.2`.
    3. Mention optional `modelClass` on receipt bodies.
    4. Mention runtime terminal receipts derive `modelClass` from the Phase 33
       strict registry when the selected model is known.
    5. Mention `verifyReceipt` continues to accept signed v1.1 receipts and
       reject v1 downgrade attempts.
    6. Do not claim provider adapters or `ProviderRunResponse` gained a
       `modelClass` field.
  </action>
  <verify>
    <automated>test -f .changeset/v1.3.0-receipt-v12-model-class.md && rg -n 'lattice-receipt/v1\\.2|modelClass|v1\\.1|downgrade|ProviderRunResponse' .changeset/v1.3.0-receipt-v12-model-class.md</automated>
  </verify>
  <acceptance_criteria>
    - Changeset file exists.
    - Changeset frontmatter targets `@full-self-browsing/lattice`.
    - Changeset mentions `lattice-receipt/v1.2`.
    - Changeset mentions `modelClass`.
    - Changeset mentions v1.1 verification compatibility.
    - Changeset does not say `ProviderRunResponse` gained modelClass.
  </acceptance_criteria>
  <done>Release notes document the schema bump accurately.</done>
</task>

<task type="auto">
  <name>Task 3: Run final gates and mark Phase 38 complete in planning docs</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-01-PLAN.md
    - .planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-02-PLAN.md
  </read_first>
  <action>
    1. Run final verification:
       ```bash
       pnpm --filter @full-self-browsing/lattice test receipts/receipt receipts/verify receipts/canonical contract/checkpoint runtime/create-ai agent/integration runtime/survivability public-surface
       pnpm --filter @full-self-browsing/lattice build
       pnpm --filter @full-self-browsing/lattice typecheck
       pnpm --filter @full-self-browsing/lattice exec tsd
       ```
    2. Only if all commands exit 0, update `.planning/REQUIREMENTS.md`:
       - Change `RECEIPT12-01` through `RECEIPT12-04` from `[ ]` to `[x]`.
       - Change traceability rows for `RECEIPT12-01` through `RECEIPT12-04`
         from `pending` to `complete`.
       - Update total complete count from `49 / 79` to `53 / 79`.
       - Add footer line:
         `*Phase 38 REQ-IDs (RECEIPT12-01..04) completed: 2026-06-09 — receipt v1.2 schema + modelClass tag executed and verified*`
    3. Update `.planning/ROADMAP.md`:
       - Top coverage complete count from `49 / 79` to `53 / 79`.
       - Progress row for Phase 38 to `4/4 | Complete | 2026-06-09`.
       - If the phase checklist entry is still unchecked, change Phase 38 to
         checked and add `(completed 2026-06-09)`.
    4. Update `.planning/STATE.md`:
       - Move focus to Phase 39 discussion/planning.
       - Keep completed phases at 11/16.
       - Keep completed plans at 30 after this plan is complete.
       - Pending todos should say Phase 39 must author `DELEG`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test receipts/receipt receipts/verify receipts/canonical contract/checkpoint runtime/create-ai agent/integration runtime/survivability public-surface && pnpm --filter @full-self-browsing/lattice build && pnpm --filter @full-self-browsing/lattice typecheck && pnpm --filter @full-self-browsing/lattice exec tsd && rg -n 'RECEIPT12-01.*complete|38\\. Receipt v1\\.2 Schema \\+ modelClass Tag \\| 4/4 \\| Complete' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - All final verification commands exit 0.
    - `RECEIPT12-01` through `RECEIPT12-04` are checked in requirements.
    - Traceability rows for `RECEIPT12-*` are complete.
    - Roadmap Phase 38 progress row is `4/4 | Complete | 2026-06-09`.
    - STATE points at Phase 39 as the next work item.
  </acceptance_criteria>
  <done>Phase 38 is verified and planning state advances to Phase 39.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
pnpm --filter @full-self-browsing/lattice test receipts/receipt receipts/verify receipts/canonical contract/checkpoint runtime/create-ai agent/integration runtime/survivability public-surface
pnpm --filter @full-self-browsing/lattice build
pnpm --filter @full-self-browsing/lattice typecheck
pnpm --filter @full-self-browsing/lattice exec tsd
```

After successful execution, mark `RECEIPT12-01` through `RECEIPT12-04`
complete in `.planning/REQUIREMENTS.md`, update Phase 38 progress in
`.planning/ROADMAP.md`, and record Phase 39 as the next action in
`.planning/STATE.md`.
</verification>
