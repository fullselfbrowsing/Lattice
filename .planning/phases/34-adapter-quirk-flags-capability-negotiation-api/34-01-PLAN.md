---
phase: 34
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - packages/lattice/src/providers/provider.ts
  - packages/lattice/src/providers/quirks.ts
  - packages/lattice/src/capabilities/sanitizer-recommendations.ts
  - packages/lattice/src/capabilities/negotiate.ts
  - packages/lattice/src/capabilities/index.ts
  - packages/lattice/src/tracing/tracing.ts
  - packages/lattice/src/index.ts
  - packages/lattice/test-d/quirks-negotiation.test-d.ts
  - packages/lattice/test/capabilities-sanitizer-recommendations.test.ts
  - packages/lattice/test/capabilities-negotiate-helper.test.ts
autonomous: true
requirements:
  - QUIRK-01
  - QUIRK-03
  - NEG-01
must_haves:
  truths:
    - "REQUIREMENTS.md authors QUIRK-01, QUIRK-02, QUIRK-03, NEG-01, NEG-02 with bullet-level acceptance language matching v1.3 REQ-ID conventions"
    - "AdapterQuirks base interface exists with 5 universal booleans (supportsToolChoice, parallelToolCalls, structuredOutputs, responseFormatHonored, streamingDiverges)"
    - "7 per-adapter narrowed quirks interfaces exist (AnthropicQuirks, OpenAIQuirks, OpenAICompatQuirks, GeminiQuirks, XaiQuirks, OpenRouterQuirks, LmStudioQuirks) each extending AdapterQuirks"
    - "NegotiatedCapabilities interface exists with the 8 fields from the success criteria (modelId, contextWindow, supports.{nativeToolCalling,structuredOutputs,parallelToolCalls,extendedThinking,streaming}, knownFailureModes, recommendedSanitizers, source)"
    - "NegotiationAuthError class exists extending Error with adapter, modelId, httpStatus, kind discriminant"
    - "SanitizerKey closed string-literal union exists with the 3 D-13 values"
    - "SANITIZER_BY_FAILURE_MODE record exists, exhaustive over KnownFailureMode, with D-14 mapping"
    - "getRecommendedSanitizers helper exists; filters nulls; returns dedup'd readonly array"
    - "Top-level negotiateCapabilities(adapter, modelId) helper exists; delegates to adapter.negotiateCapabilities when present, else synthesizes from Phase 33 registry with source 'registry'"
    - "RunEventKind union includes new 'capabilities.negotiation.fallback' literal"
    - "ProviderAdapter interface gains optional quirks?: AdapterQuirks and optional negotiateCapabilities?(modelId): Promise<NegotiatedCapabilities>"
    - "All new public types and the helper re-exported from packages/lattice/src/index.ts"
  artifacts:
    - path: "packages/lattice/src/providers/quirks.ts"
      provides: "AdapterQuirks base + 7 narrowed sub-interfaces"
      contains: "interface AdapterQuirks, interface AnthropicQuirks, interface OpenAIQuirks, interface OpenAICompatQuirks, interface GeminiQuirks, interface XaiQuirks, interface OpenRouterQuirks, interface LmStudioQuirks"
    - path: "packages/lattice/src/capabilities/negotiate.ts"
      provides: "NegotiatedCapabilities + NegotiationAuthError + top-level negotiateCapabilities helper"
      contains: "interface NegotiatedCapabilities, class NegotiationAuthError, function negotiateCapabilities"
    - path: "packages/lattice/src/capabilities/sanitizer-recommendations.ts"
      provides: "SanitizerKey + SANITIZER_BY_FAILURE_MODE + getRecommendedSanitizers"
      contains: "type SanitizerKey, const SANITIZER_BY_FAILURE_MODE, function getRecommendedSanitizers"
    - path: "packages/lattice/test-d/quirks-negotiation.test-d.ts"
      provides: "tsd type-level assertions for AdapterQuirks discriminant narrowing, SanitizerKey exhaustive switch, NegotiatedCapabilities shape"
      min_lines: 60
    - path: "packages/lattice/test/capabilities-sanitizer-recommendations.test.ts"
      provides: "vitest unit tests covering SANITIZER_BY_FAILURE_MODE table + getRecommendedSanitizers dedupe + null filter behavior"
      min_lines: 40
    - path: "packages/lattice/test/capabilities-negotiate-helper.test.ts"
      provides: "vitest unit tests covering the top-level helper: delegates when adapter has negotiateCapabilities; falls back to registry source='registry' when absent"
      min_lines: 50
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authored REQ-IDs QUIRK-01, QUIRK-02, QUIRK-03, NEG-01, NEG-02 with traceability rows"
      contains: "QUIRK-01, QUIRK-02, QUIRK-03, NEG-01, NEG-02"
  key_links:
    - from: "packages/lattice/src/capabilities/sanitizer-recommendations.ts"
      to: "packages/lattice/src/capabilities/profile.ts"
      via: "type-only import of KnownFailureMode for Record exhaustiveness"
      pattern: "import type \\{ KnownFailureMode \\}"
    - from: "packages/lattice/src/capabilities/negotiate.ts"
      to: "packages/lattice/src/capabilities/lookup.ts"
      via: "getCapabilityProfile + findCapabilityProfile import for registry fallback"
      pattern: "import \\{ (getCapabilityProfile|findCapabilityProfile)"
    - from: "packages/lattice/src/capabilities/negotiate.ts"
      to: "packages/lattice/src/capabilities/sanitizer-recommendations.ts"
      via: "getRecommendedSanitizers import for recommendedSanitizers derivation"
      pattern: "import \\{ getRecommendedSanitizers"
    - from: "packages/lattice/src/providers/provider.ts"
      to: "packages/lattice/src/providers/quirks.ts and packages/lattice/src/capabilities/negotiate.ts"
      via: "type-only imports of AdapterQuirks and NegotiatedCapabilities for optional ProviderAdapter fields"
      pattern: "import type \\{ AdapterQuirks|import type \\{ NegotiatedCapabilities"
    - from: "packages/lattice/src/index.ts"
      to: "providers/quirks.js + capabilities/negotiate.js + capabilities/sanitizer-recommendations.js"
      via: "PKG-01/INDEX-01 re-export discipline for all new public types"
      pattern: "from \"\\./providers/quirks\\.js\"|from \"\\./capabilities/sanitizer-recommendations\\.js\"|from \"\\./capabilities/negotiate\\.js\""
---

<objective>
Lay the typed foundation for Phase 34: author the 5 QUIRK-/NEG- REQ-IDs in REQUIREMENTS.md, ship the AdapterQuirks base + 7 narrowed sub-interfaces, ship the NegotiatedCapabilities + NegotiationAuthError + top-level negotiateCapabilities helper, ship the SanitizerKey + SANITIZER_BY_FAILURE_MODE + getRecommendedSanitizers module, and add `capabilities.negotiation.fallback` to RunEventKind. NO adapter-specific code in this plan -- this is the contract surface that Plans 02..05 implement against.

Purpose: Plans 02-05 are parallel adapter implementations. They MUST share a single canonical type set. Shipping the types in Wave 0 prevents downstream contract drift and lets all three Wave 1 plans run in parallel without waiting on each other for type definitions. This also gives the consumer-adapter fallback path (D-04) and the SanitizerKey dispatch (D-13/D-14) a unit-tested home BEFORE any adapter is wired.

Output: Greenfield `quirks.ts`, `sanitizer-recommendations.ts`, `negotiate.ts`; modified `provider.ts`, `tracing.ts`, `index.ts`, `capabilities/index.ts`; new test-d file + 2 vitest unit-test files; REQUIREMENTS.md rows for QUIRK-01..03 + NEG-01..02 with the planner-supplied phrasing baseline from this PLAN's planning_context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md
@.planning/phases/33-model-capability-registry-200-via-openrouter-feed/33-SUMMARY.md

@packages/lattice/src/capabilities/profile.ts
@packages/lattice/src/capabilities/lookup.ts
@packages/lattice/src/capabilities/index.ts
@packages/lattice/src/providers/provider.ts
@packages/lattice/src/tracing/tracing.ts
@packages/lattice/src/index.ts
@packages/lattice/src/agent/types.ts

<interfaces>
<!-- Key contracts from Phase 33 that this plan consumes. -->
<!-- Executor MUST use these names verbatim -- no re-derivation. -->

From packages/lattice/src/capabilities/profile.ts (Phase 33):
```typescript
export type CapabilityAdapter =
  | "openrouter" | "anthropic" | "openai" | "openai-compat" | "xai" | "gemini" | "lm-studio";

export type KnownFailureMode =
  | "internal_envelope_leak"
  | "reasoning_tag_leak"
  | "system_prompt_echo"
  | "template_artifact_leak"
  | "hallucinated_tool_name"
  | "malformed_tool_arguments"
  | "premature_termination";

export interface ModelCapabilityProfile {
  readonly id: string;
  readonly adapter: CapabilityAdapter;
  readonly originFamily: string;
  readonly trainingClass: TrainingClass;
  readonly reasoningSurface: ReasoningSurface;
  readonly toolCallSurface: ToolCallSurface;
  readonly contextWindow: number;
  readonly knownFailureModes: readonly KnownFailureMode[];
  readonly recommendedPromptStrategy: RecommendedPromptStrategy;
}
```

From packages/lattice/src/capabilities/lookup.ts (Phase 33):
```typescript
export function getCapabilityProfile(canonicalKey: string): ModelCapabilityProfile | undefined;
export function findCapabilityProfile(id: string): ModelCapabilityProfile[];
export function stripOpenRouterVariant(id: string): string;
```

From packages/lattice/src/agent/types.ts:172 (precedent for class extends Error):
```typescript
export class AgentDeniedError extends Error {
  readonly kind = "agent-denied" as const;
  // ... mirror this shape for NegotiationAuthError
}
```

From packages/lattice/src/providers/provider.ts:118-123 (existing ProviderAdapter -- 4 fields):
```typescript
export interface ProviderAdapter {
  readonly id: string;
  readonly kind: "provider-adapter";
  readonly capabilities?: readonly ModelCapability[];
  readonly execute?: (request: ProviderRunRequest) => Promise<ProviderRunResponse>;
}
```

From packages/lattice/src/tracing/tracing.ts:11-33 (existing RunEventKind):
```typescript
export type RunEventKind =
  | "run.start" | ... | "step.transition"
  | "recovery.start" | "recovery.complete" | "recovery.failed";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author REQ-IDs + SanitizerKey module + tsd type tests</name>
  <files>
    .planning/REQUIREMENTS.md,
    packages/lattice/src/capabilities/sanitizer-recommendations.ts,
    packages/lattice/src/capabilities/index.ts,
    packages/lattice/src/index.ts,
    packages/lattice/test/capabilities-sanitizer-recommendations.test.ts,
    packages/lattice/test-d/quirks-negotiation.test-d.ts
  </files>
  <read_first>
    .planning/REQUIREMENTS.md (lines 107-238, the CAPS-01..05 block + Traceability table -- match this exact style for the new rows),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (full file -- D-13, D-14, D-15, D-16 lock the sanitizer-recommendations shape),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 477-525 -- Pattern 5 has the verbatim implementation),
    packages/lattice/src/capabilities/profile.ts (KnownFailureMode shape + ALL_KNOWN_FAILURE_MODES const),
    packages/lattice/src/capabilities/index.ts (existing barrel pattern -- mirror for new exports),
    packages/lattice/test-d/capabilities.test-d.ts (Phase 33 tsd pattern -- mirror exhaustive switch idiom),
    packages/lattice/src/index.ts (re-export pattern lines 30-50 and 234-245)
  </read_first>
  <behavior>
    - Test 1 (vitest, capabilities-sanitizer-recommendations.test.ts): SANITIZER_BY_FAILURE_MODE has a key for every value in ALL_KNOWN_FAILURE_MODES (assert via `for (const mode of ALL_KNOWN_FAILURE_MODES) { expect(SANITIZER_BY_FAILURE_MODE).toHaveProperty(mode); }`)
    - Test 2: getRecommendedSanitizers(["internal_envelope_leak", "reasoning_tag_leak"]) returns ["unwrapInternalEnvelope", "stripReasoningTags"] (order: insertion order via Set)
    - Test 3: getRecommendedSanitizers(["system_prompt_echo", "hallucinated_tool_name", "premature_termination"]) returns [] (all map to null)
    - Test 4: getRecommendedSanitizers(["internal_envelope_leak", "internal_envelope_leak"]) returns ["unwrapInternalEnvelope"] (dedup via Set)
    - Test 5: getRecommendedSanitizers([]) returns []
    - Test 6: SANITIZER_BY_FAILURE_MODE["internal_envelope_leak"] === "unwrapInternalEnvelope", ["reasoning_tag_leak"] === "stripReasoningTags", ["template_artifact_leak"] === "stripChatTemplateArtifacts" (verbatim D-14 mapping)
    - tsd test (quirks-negotiation.test-d.ts): exhaustive switch over SanitizerKey using the `_exhaustive: never` pattern (mirror capabilities.test-d.ts lines covering KnownFailureMode); assert `SanitizerKey extends string`; assert SANITIZER_BY_FAILURE_MODE inferred type is `Record<KnownFailureMode, SanitizerKey | null>`
  </behavior>
  <action>
    1. **Author REQ-IDs in .planning/REQUIREMENTS.md**: Insert a new "### Adapter Quirk Flags + Capability Negotiation API (`QUIRK-*` / `NEG-*`)" subsection AFTER the existing "### Model Capability Registry (`CAPS-*`)" subsection (currently lines 107-113). Use the phrasing baseline from this PLAN's planning_context but rewrite to match the exact bullet-checkbox style used in CAPS-01..05. The 5 rows are:
       - **QUIRK-01**: `AdapterQuirks` base interface in `packages/lattice/src/providers/quirks.ts` exposing the 5 universal booleans `supportsToolChoice`, `parallelToolCalls`, `structuredOutputs`, `responseFormatHonored`, `streamingDiverges`; 7 per-adapter narrowed sub-interfaces (`AnthropicQuirks`, `OpenAIQuirks`, `OpenAICompatQuirks`, `GeminiQuirks`, `XaiQuirks`, `OpenRouterQuirks`, `LmStudioQuirks`) each extending `AdapterQuirks` with provider-specific flags. `quirks?: AdapterQuirks` added as OPTIONAL field to `ProviderAdapter` (D-01 non-breaking for v1.2 consumer adapters). All types re-exported from `packages/lattice/src/index.ts`.
       - **QUIRK-02**: Each of the 7 first-party adapter factories (`createOpenAIProvider`, `createOpenAICompatibleProvider`, `createAnthropicProvider`, `createGeminiProvider`, `createXaiProvider`, `createOpenRouterProvider`, `createLmStudioProvider`) returns an adapter object whose `quirks` block is populated with values matching real provider behavior per RESEARCH §Q6 / per-adapter quirks vocabulary; per-adapter quirk-fixture vitest tests assert each value.
       - **QUIRK-03**: Per-adapter `quirks` narrowing accessible via discriminant check on `adapter.id`; tsd type-level test (`test-d/quirks-negotiation.test-d.ts`) asserts `if (adapter.id === "anthropic")` narrows `adapter.quirks` to `AnthropicQuirks` and exposes `promptCachingSupported` / `extendedThinkingSupported` / `toolUseInputSchemaStrict`.
       - **NEG-01**: `negotiateCapabilities?(modelId: string): Promise<NegotiatedCapabilities>` OPTIONAL method on `ProviderAdapter` (D-02). Top-level helper `negotiateCapabilities(adapter, modelId)` in `packages/lattice/src/capabilities/negotiate.ts` delegates to `adapter.negotiateCapabilities` when present; otherwise synthesizes from Phase 33 registry via `getCapabilityProfile(`${adapter.id}:${modelId}`)` with `source: "registry"` (D-04). All 7 first-party adapters with `/models` endpoints (Anthropic, OpenAI, Gemini, OpenRouter) implement; 3 without (xAI sparse, OpenAI-compat, LM Studio) fall back per D-04. Per-instance TTL cache (`modelsCacheTtlMs` factory option, default 300000ms, 0 disables, Infinity = process-lifetime per D-08); single-flight inflight-coalescing via `Map<modelId, Promise<T>>` with `.finally` cleanup (Pitfall 4 mandatory).
       - **NEG-02**: Fetch-failure policy: transient errors (5xx, network, timeout) fall back to Phase 33 registry with `source: "registry-fallback"` (D-09); auth errors (401/403) throw `NegotiationAuthError extends Error` carrying `adapter: CapabilityAdapter`, `modelId: string`, `httpStatus: 401 | 403` (D-10). Retry policy: 2 retries with exponential backoff [0ms, 200ms, 1000ms] = 3 total attempts before fallback (D-11); `modelsRetryCount` factory option (default 2, 0 disables). New `RunEventKind` literal `"capabilities.negotiation.fallback"` (D-12) added to `packages/lattice/src/tracing/tracing.ts`. Anchor case study (`session_1780792387779`): `negotiateCapabilities('openrouter:openai/gpt-oss-120b:free')` returns `recommendedSanitizers.includes("unwrapInternalEnvelope")` AND `knownFailureModes.includes("internal_envelope_leak")`.
    2. **Update Traceability table at end of REQUIREMENTS.md**: Add 5 rows for QUIRK-01..03 + NEG-01..02 mapped to Phase 34 with the plan number assignments per the gsd-plan-phase split (QUIRK-01 -> 34-01, QUIRK-02 -> 34-02/03/04/05, QUIRK-03 -> 34-01, NEG-01 -> 34-01, NEG-02 -> 34-02/03/04). Update the "Total Requirements" summary count from 59 to 64 (or commensurate) and update the Coverage table.
    3. **Create packages/lattice/src/capabilities/sanitizer-recommendations.ts** per RESEARCH §Pattern 5 verbatim shape: import type `KnownFailureMode` from `./profile.js`; export `type SanitizerKey = "stripReasoningTags" | "stripChatTemplateArtifacts" | "unwrapInternalEnvelope"`; export `const SANITIZER_BY_FAILURE_MODE: Record<KnownFailureMode, SanitizerKey | null> = { internal_envelope_leak: "unwrapInternalEnvelope", reasoning_tag_leak: "stripReasoningTags", template_artifact_leak: "stripChatTemplateArtifacts", system_prompt_echo: null, hallucinated_tool_name: null, malformed_tool_arguments: null, premature_termination: null } as const`; export `function getRecommendedSanitizers(modes: readonly KnownFailureMode[]): readonly SanitizerKey[]` using the `new Set<SanitizerKey>()` + filter-null pattern. Include JSDoc on each export citing D-13, D-14, D-15, D-16. Zero runtime deps. Order entries to match RESEARCH §Pattern 5 exactly.
    4. **Update packages/lattice/src/capabilities/index.ts barrel**: Add `export type { SanitizerKey } from "./sanitizer-recommendations.js";` and `export { SANITIZER_BY_FAILURE_MODE, getRecommendedSanitizers } from "./sanitizer-recommendations.js";` -- mirror the existing pattern (no default exports, named only).
    5. **Update packages/lattice/src/index.ts**: Re-export `SanitizerKey`, `SANITIZER_BY_FAILURE_MODE`, `getRecommendedSanitizers` from `./capabilities/index.js` per PKG-01/INDEX-01. Find the existing `from "./capabilities/index.js"` re-export block (around lines 234-245) and extend it.
    6. **Create packages/lattice/test/capabilities-sanitizer-recommendations.test.ts**: Implement Test 1-6 above. Import `ALL_KNOWN_FAILURE_MODES` from `../src/capabilities/profile.js` for Test 1. Use vitest `describe`/`it`/`expect`. Match the existing test layout from `capabilities-registry-integration.test.ts` for vitest style.
    7. **Create packages/lattice/test-d/quirks-negotiation.test-d.ts**: SHELL ONLY for this task -- add the SanitizerKey exhaustive switch + `Record<KnownFailureMode, SanitizerKey | null>` inference assertions. Defer the AdapterQuirks discriminant narrowing assertions to Task 2 (which adds the quirks types).
    8. Run `pnpm --filter @full-self-browsing/lattice typecheck` -- must pass clean.
    9. Run `pnpm --filter @full-self-browsing/lattice test capabilities-sanitizer-recommendations.test.ts` -- all 6 vitest tests pass.
    10. Run `pnpm --filter @full-self-browsing/lattice test:types` -- tsd type tests pass.
    11. Verify zero new runtime dependencies introduced: `node -e "console.log(require('./packages/lattice/package.json').dependencies)"` matches pre-change snapshot (the 3 prod deps: `@standard-schema/spec`, `canonicalize`, `mime`).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test capabilities-sanitizer-recommendations.test.ts &amp;&amp; pnpm --filter @full-self-browsing/lattice test:types</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains 5 new REQ-ID bullet entries (QUIRK-01, QUIRK-02, QUIRK-03, NEG-01, NEG-02) styled per CAPS-01..05 precedent, AND 5 new rows in the Traceability table, AND updated Coverage table count.
    - packages/lattice/src/capabilities/sanitizer-recommendations.ts exports `type SanitizerKey`, `const SANITIZER_BY_FAILURE_MODE`, `function getRecommendedSanitizers` with the EXACT D-14 mapping (verifiable via `grep -E '"internal_envelope_leak": "unwrapInternalEnvelope"' packages/lattice/src/capabilities/sanitizer-recommendations.ts`).
    - packages/lattice/src/capabilities/index.ts exports the 3 new symbols.
    - packages/lattice/src/index.ts re-exports the 3 new symbols (verifiable via `grep -E 'SanitizerKey|SANITIZER_BY_FAILURE_MODE|getRecommendedSanitizers' packages/lattice/src/index.ts | wc -l` >= 3).
    - All 6 vitest tests in capabilities-sanitizer-recommendations.test.ts pass.
    - `pnpm --filter @full-self-browsing/lattice typecheck` exits 0.
    - `pnpm --filter @full-self-browsing/lattice test:types` exits 0; the SanitizerKey exhaustive switch is present in test-d/quirks-negotiation.test-d.ts.
    - No new runtime deps added to packages/lattice/package.json (production `dependencies` block unchanged byte-for-byte except for whitespace).
  </acceptance_criteria>
  <done>SANITIZER_BY_FAILURE_MODE table exhaustive over KnownFailureMode, getRecommendedSanitizers helper filters nulls + dedups, SanitizerKey re-exported from main index, REQ-IDs authored. Plans 02-05 can now consume SanitizerKey + getRecommendedSanitizers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdapterQuirks types + ProviderAdapter optional field wiring + tsd narrowing</name>
  <files>
    packages/lattice/src/providers/quirks.ts,
    packages/lattice/src/providers/provider.ts,
    packages/lattice/src/index.ts,
    packages/lattice/test-d/quirks-negotiation.test-d.ts
  </files>
  <read_first>
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (D-01, D-03 -- locked quirks shape decisions),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 268-339 -- Pattern 2 has the verbatim 8-interface implementation; lines 285-339 are the literal source),
    packages/lattice/src/providers/provider.ts (the existing ProviderAdapter interface at lines 118-123 -- this is what we extend),
    packages/lattice/src/index.ts (re-export pattern for type-only exports lines 30-50),
    packages/lattice/test-d/capabilities.test-d.ts (Phase 33 tsd pattern for discriminant narrowing)
  </read_first>
  <behavior>
    - Test 1 (tsd, quirks-negotiation.test-d.ts): given a value of type `AdapterQuirks`, the 5 universal booleans are present and typed `boolean`
    - Test 2: `AnthropicQuirks extends AdapterQuirks` -- verifiable via `expectAssignable<AdapterQuirks>(({} as AnthropicQuirks))` (mirror capabilities.test-d.ts assertion form)
    - Test 3: discriminant narrowing -- given `adapter: { id: string; quirks: AdapterQuirks; }`, after `if (adapter.id === "anthropic")` the `adapter.quirks` is NOT auto-narrowed to AnthropicQuirks (TypeScript can't infer that without explicit user assertion); document this with a JSDoc explaining the consumer MUST cast via `adapter.quirks as AnthropicQuirks` after the discriminant check OR use a typed factory return as documented in Plan 02
    - Test 4: a literal `{ supportsToolChoice: true, parallelToolCalls: true, structuredOutputs: true, responseFormatHonored: true, streamingDiverges: false, promptCachingSupported: true, extendedThinkingSupported: true, toolUseInputSchemaStrict: true } satisfies AnthropicQuirks` typechecks clean
    - Test 5: ProviderAdapter remains backward-compatible -- a literal `{ id: "consumer", kind: "provider-adapter" }` satisfies `ProviderAdapter` (quirks + negotiateCapabilities are OPTIONAL per D-01/D-02)
  </behavior>
  <action>
    1. **Create packages/lattice/src/providers/quirks.ts** per RESEARCH §Pattern 2 verbatim:
       - JSDoc header comment citing D-03 and explaining the discriminant-narrowing contract (consumer MUST narrow via factory return type OR cast after `adapter.id === "anthropic"` check)
       - Export `interface AdapterQuirks` with the 5 readonly booleans (supportsToolChoice, parallelToolCalls, structuredOutputs, responseFormatHonored, streamingDiverges)
       - Export `interface AnthropicQuirks extends AdapterQuirks` with `promptCachingSupported`, `extendedThinkingSupported`, `toolUseInputSchemaStrict` (RESEARCH §Q6 vocabulary; cite Anthropic docs in JSDoc)
       - Export `interface OpenAIQuirks extends AdapterQuirks` with `strictModeSupported`, `structuredOutputsTier2`
       - Export `interface OpenAICompatQuirks extends AdapterQuirks` (no new fields -- documented as conservative per RESEARCH §Pattern 2)
       - Export `interface GeminiQuirks extends AdapterQuirks` with `responseSchemaSupported`, `safetySettingsConfigurable`, `systemInstructionSupported`
       - Export `interface XaiQuirks extends AdapterQuirks` with `reasoningTokensReported`, `logprobsSupported`
       - Export `interface OpenRouterQuirks extends AdapterQuirks` with `providerRoutingArraySupported`, `floorPricingHints`, `allowFallbacks`
       - Export `interface LmStudioQuirks extends AdapterQuirks` with `customChatTemplateRiskFlag`, `noAuthRequired`
       - Inline JSDoc on each narrowed field per the RESEARCH §Q6 vocabulary with CITED markers (do NOT use emoji)
       - All interfaces use `readonly` modifier on every field
    2. **Modify packages/lattice/src/providers/provider.ts** (lines 118-123): Add `import type { AdapterQuirks } from "./quirks.js";` near top; extend `interface ProviderAdapter` to add OPTIONAL fields:
       ```typescript
       readonly quirks?: AdapterQuirks;
       readonly negotiateCapabilities?: (modelId: string) => Promise<NegotiatedCapabilities>;
       ```
       Add `import type { NegotiatedCapabilities } from "../capabilities/negotiate.js";` -- NOTE: this forward-reference is intentional; Task 3 creates `negotiate.ts`. Verify the typecheck still works after Task 3 lands (Task 2 in isolation will tsc-error on the missing module; that's expected -- this task's success is gated AFTER Task 3 lands per the dependency order). To resolve circularity at this step: add a temporary type-only stub at the bottom of provider.ts `// TEMPORARY: Task 3 in 34-01 ships the real type. Inline minimal stub to keep typecheck green this commit.` of `export interface NegotiatedCapabilities { readonly modelId: string; readonly contextWindow: number; readonly supports: { readonly nativeToolCalling: boolean; readonly structuredOutputs: boolean; readonly parallelToolCalls: boolean; readonly extendedThinking: boolean; readonly streaming: boolean; }; readonly knownFailureModes: readonly string[]; readonly recommendedSanitizers: readonly string[]; readonly source: "live" | "registry-fallback" | "registry"; }` then DELETE this stub during Task 3 when the real type lands. RECONSIDER: simpler approach is to make Task 2 + Task 3 a single atomic commit where both files land together. Plan executor decision: commit Task 2 + Task 3 together as one logical change since they cross-reference.
    3. **Modify packages/lattice/src/index.ts**: Re-export all 8 quirks interfaces (`AdapterQuirks`, `AnthropicQuirks`, `OpenAIQuirks`, `OpenAICompatQuirks`, `GeminiQuirks`, `XaiQuirks`, `OpenRouterQuirks`, `LmStudioQuirks`) via `export type { ... } from "./providers/quirks.js";`. Insert near the existing `from "./providers/..."` re-export block (lines 30-50). Type-only export (no runtime impact -- these are interfaces).
    4. **Extend packages/lattice/test-d/quirks-negotiation.test-d.ts**: Add Test 1-5 from Behavior. Use `expectAssignable`, `expectType`, `expectError` from `tsd` matching the existing test-d patterns. Include a TODO comment in the discriminant narrowing test noting that downstream Plan 02+ tests will verify the FACTORY-LEVEL narrowing (factory return types override the base ProviderAdapter for first-party adapters per RESEARCH §Pattern 1).
    5. Run `pnpm --filter @full-self-browsing/lattice typecheck` -- expect ONE expected error on the forward-reference `NegotiatedCapabilities`; resolve by completing Task 3 (negotiate.ts module) before re-running.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test:types</automated>
  </verify>
  <acceptance_criteria>
    - packages/lattice/src/providers/quirks.ts exports 8 interfaces: 1 base + 7 narrowed (verifiable via `grep -E '^export interface (AdapterQuirks|AnthropicQuirks|OpenAIQuirks|OpenAICompatQuirks|GeminiQuirks|XaiQuirks|OpenRouterQuirks|LmStudioQuirks)' packages/lattice/src/providers/quirks.ts | wc -l` == 8).
    - Every base + narrowed interface uses `readonly` on every field.
    - AnthropicQuirks JSDoc cites "Anthropic prompt caching docs" or similar source; OpenAIQuirks JSDoc cites "OpenAI structured outputs" or similar; GeminiQuirks JSDoc cites "Gemini API" or similar.
    - packages/lattice/src/providers/provider.ts has `quirks?: AdapterQuirks` and `negotiateCapabilities?: (modelId: string) => Promise<NegotiatedCapabilities>` as OPTIONAL fields on ProviderAdapter.
    - Existing 4-field consumer adapter literal `{ id: "x", kind: "provider-adapter" } satisfies ProviderAdapter` still typechecks (D-01/D-02 non-breaking).
    - packages/lattice/src/index.ts re-exports all 8 quirks interfaces (verifiable via grep).
    - `pnpm --filter @full-self-browsing/lattice typecheck` PASSES (after Task 3 lands; this task may stage a temporary stub or commit atomically with Task 3 -- executor's call).
    - `pnpm --filter @full-self-browsing/lattice test:types` passes; the Test 1-5 type-level assertions are present in quirks-negotiation.test-d.ts.
  </acceptance_criteria>
  <done>AdapterQuirks base + 7 narrowed interfaces shipped; ProviderAdapter has optional quirks + negotiateCapabilities fields; all types re-exported from src/index.ts; tsd narrowing tests assert the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NegotiatedCapabilities + NegotiationAuthError + top-level helper + RunEventKind addition</name>
  <files>
    packages/lattice/src/capabilities/negotiate.ts,
    packages/lattice/src/capabilities/index.ts,
    packages/lattice/src/tracing/tracing.ts,
    packages/lattice/src/index.ts,
    packages/lattice/test/capabilities-negotiate-helper.test.ts,
    packages/lattice/test-d/quirks-negotiation.test-d.ts
  </files>
  <read_first>
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (D-02, D-04, D-09, D-10, D-12 -- top-level helper + auth error + source enum + event kind),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 343-411 -- Pattern 3 NegotiatedCapabilities + NegotiationAuthError; lines 829-855 -- Q9 RunEventKind addition; lines 612-617 -- Pitfall 5 helper-vs-adapter drift),
    packages/lattice/src/agent/types.ts (lines 172-185 -- AgentDeniedError precedent for class extends Error),
    packages/lattice/src/tracing/tracing.ts (lines 11-33 -- existing RunEventKind union; the new literal lands as the last entry),
    packages/lattice/src/capabilities/lookup.ts (the getCapabilityProfile + findCapabilityProfile signatures used in the registry-fallback path),
    packages/lattice/src/capabilities/profile.ts (CapabilityAdapter + KnownFailureMode + ModelCapabilityProfile shapes used for fallback synthesis)
  </read_first>
  <behavior>
    - Test 1 (vitest, capabilities-negotiate-helper.test.ts): when adapter has `negotiateCapabilities`, the helper delegates verbatim -- given a mock adapter with `negotiateCapabilities: vi.fn().mockResolvedValue({modelId: "test", source: "live", ...})`, the helper returns the exact mock result and the mock fn was called with `"test"`
    - Test 2: when adapter has NO `negotiateCapabilities`, the helper looks up `${adapter.id}:${modelId}` via getCapabilityProfile and synthesizes NegotiatedCapabilities with `source: "registry"`. Inject a known profile (use existing static profile `anthropic:claude-opus-4` from Phase 33) and verify `result.source === "registry"`, `result.modelId === "anthropic:claude-opus-4"` (or `"claude-opus-4"` depending on canonical-key handling -- planner clarification: helper passes modelId verbatim; canonical key construction happens inside helper), `result.knownFailureModes` matches profile.knownFailureModes
    - Test 3: when adapter has no `negotiateCapabilities` AND the registry has no profile for that key, the helper returns a stub NegotiatedCapabilities with `source: "registry"`, `contextWindow: 0`, all supports.* false, knownFailureModes empty, recommendedSanitizers empty. Planner clarification: alternative is to throw -- prefer the empty-stub approach for graceful degradation. Document this in JSDoc.
    - Test 4: NegotiationAuthError throws with `httpStatus`, `adapter`, `modelId` populated; `error instanceof NegotiationAuthError` returns true; `error instanceof Error` returns true; `error.kind === "negotiation-auth-failed"`
    - Test 5 (tsd): `NegotiatedCapabilities.source` is `"live" | "registry-fallback" | "registry"` -- exhaustive switch using `_exhaustive: never`
    - Test 6: RunEventKind union includes `"capabilities.negotiation.fallback"` -- assert via `expectAssignable<RunEventKind>("capabilities.negotiation.fallback" as const)`
  </behavior>
  <action>
    1. **Create packages/lattice/src/capabilities/negotiate.ts**:
       - JSDoc header citing D-02, D-04, D-09, D-10, D-12 + Pitfall 5
       - Import types: `import type { CapabilityAdapter, KnownFailureMode, ModelCapabilityProfile } from "./profile.js";` + `import type { SanitizerKey } from "./sanitizer-recommendations.js";` + `import { getCapabilityProfile, findCapabilityProfile } from "./lookup.js";` + `import { getRecommendedSanitizers } from "./sanitizer-recommendations.js";` + `import type { ProviderAdapter } from "../providers/provider.js";`
       - Export `interface NegotiatedCapabilities` with the 8 fields from CONTEXT §domain: `modelId: string`, `contextWindow: number`, `supports: { nativeToolCalling, structuredOutputs, parallelToolCalls, extendedThinking, streaming }: all readonly boolean`, `knownFailureModes: readonly KnownFailureMode[]`, `recommendedSanitizers: readonly SanitizerKey[]`, `source: "live" | "registry-fallback" | "registry"`. All readonly.
       - Export `class NegotiationAuthError extends Error` mirroring `AgentDeniedError` shape exactly: `readonly kind = "negotiation-auth-failed" as const;`, `readonly adapter: CapabilityAdapter;`, `readonly modelId: string;`, `readonly httpStatus: 401 | 403;`, constructor `(adapter, modelId, httpStatus, message)` calls `super(message)` + sets `this.name = "NegotiationAuthError"` + assigns the readonly fields.
       - Export `async function negotiateCapabilities(adapter: ProviderAdapter, modelId: string): Promise<NegotiatedCapabilities>`: if `adapter.negotiateCapabilities` is defined, `return adapter.negotiateCapabilities(modelId);` (verbatim delegation per Pitfall 5 -- no logic in the helper for the live path); otherwise synthesize from registry via the helper `synthesizeFromRegistry(adapter.id as CapabilityAdapter, modelId, "registry")` (internal, file-private)
       - Export internal-but-importable `function synthesizeFromRegistry(adapter: CapabilityAdapter, modelId: string, source: "registry" | "registry-fallback"): NegotiatedCapabilities`: builds canonical key `${adapter}:${modelId}`; calls `getCapabilityProfile(canonicalKey)`; if profile found, maps `profile.toolCallSurface` to `supports.nativeToolCalling` (true if `native_*` per the closed union from Phase 33; document the mapping in JSDoc -- planner's interpretation: any `toolCallSurface` starting with `native_` returns true), maps `profile.reasoningSurface` to `supports.extendedThinking` (true if not `none` per Phase 33's enum), populates `supports.structuredOutputs` from profile (planner: derive heuristically -- if `trainingClass === "frontier_rlhf"`, true; otherwise mirror the Phase 33 static profile's stored knowledge), `supports.parallelToolCalls` (heuristic per RESEARCH §Q6 OpenAI quirks), `supports.streaming` (default true for all adapters; LM Studio may set false). On not-found, return the empty-stub shape per Test 3 behavior. Recommendation: KEEP this synthesis logic minimal and documented; per-adapter negotiate() implementations (Plans 02-05) own the richer derivation. The helper is a SAFETY NET. Document this clearly.
       - Export the internal helper for adapter use AS A NAMED EXPORT so Plans 02-05 can reuse the fallback synthesis when /models fails (D-09). Rename if needed: `export function synthesizeNegotiatedCapabilitiesFromRegistry(adapter, modelId, source)`.
       - The mapping from `ModelCapabilityProfile.toolCallSurface` to `supports.nativeToolCalling`: planner advisory in RESEARCH §<specifics> "anti-shape" warns that this boolean loses the strict-vs-lenient distinction. Document that the boolean is the SIMPLIFIED view; consumers needing the enum read via `getCapabilityProfile()` directly.
    2. **Update packages/lattice/src/capabilities/index.ts barrel**: Add `export type { NegotiatedCapabilities } from "./negotiate.js";` and `export { NegotiationAuthError, negotiateCapabilities, synthesizeNegotiatedCapabilitiesFromRegistry } from "./negotiate.js";`.
    3. **Update packages/lattice/src/tracing/tracing.ts** (after line 33): Add the new RunEventKind literal as documented in RESEARCH §Q9 exactly:
       ```typescript
         // Phase 34 (v1.3): capability-negotiation fallback marker. Fires when
         // adapter.negotiateCapabilities() falls back from /models to the static
         // Phase 33 registry due to transient (5xx, network, timeout) failure.
         // Auth errors (401, 403) do NOT fire this event -- they throw
         // NegotiationAuthError instead.
         | "capabilities.negotiation.fallback";
       ```
       Place it as the LAST union member. NO other changes to tracing.ts.
    4. **Update packages/lattice/src/index.ts**: Re-export `NegotiatedCapabilities`, `NegotiationAuthError`, `negotiateCapabilities`, `synthesizeNegotiatedCapabilitiesFromRegistry` from `./capabilities/index.js`. RunEventKind is already re-exported; no change needed there.
    5. **DELETE the temporary stub from provider.ts** added in Task 2 (the inline `export interface NegotiatedCapabilities`). The forward reference via `import type { NegotiatedCapabilities } from "../capabilities/negotiate.js";` now resolves to the real type.
    6. **Create packages/lattice/test/capabilities-negotiate-helper.test.ts**: Implement Test 1-4 from Behavior using vitest. Import `vi` for the spy in Test 1; import the Phase 33 static profile reference for Test 2 (the registry is pre-populated -- use `anthropic:claude-opus-4` per Phase 33 SUMMARY). For Test 3, use a non-existent canonical key like `"openai:nonexistent-model-id-test-12345"` to force the not-found branch.
    7. **Extend packages/lattice/test-d/quirks-negotiation.test-d.ts**: Add Test 5 (NegotiatedCapabilities.source exhaustive switch) and Test 6 (RunEventKind includes the new literal).
    8. Run typecheck + tests + type tests.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test capabilities-negotiate-helper &amp;&amp; pnpm --filter @full-self-browsing/lattice test:types</automated>
  </verify>
  <acceptance_criteria>
    - packages/lattice/src/capabilities/negotiate.ts exports `interface NegotiatedCapabilities`, `class NegotiationAuthError extends Error`, `async function negotiateCapabilities(adapter, modelId)`, `function synthesizeNegotiatedCapabilitiesFromRegistry(adapter, modelId, source)`.
    - `NegotiationAuthError` shape mirrors `AgentDeniedError` (verifiable via `grep -E 'readonly kind = "negotiation-auth-failed"' packages/lattice/src/capabilities/negotiate.ts` returns a match).
    - `NegotiatedCapabilities.source` is the closed union `"live" | "registry-fallback" | "registry"` -- verifiable via grep for the union literal in negotiate.ts.
    - The 4 vitest tests in capabilities-negotiate-helper.test.ts pass.
    - packages/lattice/src/tracing/tracing.ts contains `"capabilities.negotiation.fallback"` in the RunEventKind union (verifiable via grep).
    - packages/lattice/src/index.ts re-exports `NegotiatedCapabilities`, `NegotiationAuthError`, `negotiateCapabilities`, `synthesizeNegotiatedCapabilitiesFromRegistry`.
    - The temporary stub from Task 2 in provider.ts is removed; provider.ts imports `NegotiatedCapabilities` from `../capabilities/negotiate.js` (verifiable via grep).
    - `pnpm --filter @full-self-browsing/lattice typecheck` exits 0.
    - `pnpm --filter @full-self-browsing/lattice test:types` exits 0.
    - test-d/quirks-negotiation.test-d.ts contains the SanitizerKey exhaustive switch (from Task 1), AdapterQuirks discriminant tests (from Task 2), AND NegotiatedCapabilities.source + RunEventKind tests (Task 3).
    - The Pitfall 5 mitigation is enforced: helper has NO inflight-coalescing logic, NO new source values; verifiable via short LOC count + grep absence of `new Map<` in the helper top-level function.
  </acceptance_criteria>
  <done>NegotiatedCapabilities + NegotiationAuthError + top-level helper + RunEventKind addition shipped. Plans 02-05 can now implement per-adapter negotiate() against this contract.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer code -> Lattice public API | Untrusted modelId strings cross this boundary. Used in registry lookup (Map key) and (in Plans 02-04) URL path / filter parameter for /models calls. |
| Lattice public API -> Phase 33 registry | Type-safe boundary. KnownFailureMode + CapabilityAdapter are closed unions; consumer can't inject novel values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | Map<string, ...> in registry lookup keyed by user-supplied modelId | mitigate | Phase 33 already uses `Map<string, ...>` (verified lookup.ts:72) which is `__proto__`-safe -- prototype pollution via `__proto__` keys cannot poison the registry. No additional mitigation needed in 34-01; documented as inherited from Phase 33. |
| T-34-01-02 | Information Disclosure | NegotiationAuthError carries httpStatus + adapter + modelId to caller | accept | These three fields are non-sensitive (adapter id is public via package exports; modelId was supplied by caller; httpStatus is HTTP standard). Message field set by adapter implementations in Plans 02-04 MUST NOT include the apiKey. Mitigation enforced in Plans 02-04 acceptance criteria, not 34-01. |
| T-34-01-03 | Tampering | Helper synthesizes NegotiatedCapabilities from registry; consumer-controlled modelId becomes Map lookup key | accept | Map-based lookup safe per T-34-01-01. The synthesis path is read-only on Phase 33's frozen registry data; no write side effect. |
| T-34-01-04 | Denial of Service | RunEventKind union expansion is a typed breaking change for exhaustive-switch consumers | accept | Pitfall 6 documents this; Phase 17 set precedent with 3 recovery.* events; documented in changeset and SECURITY.md. Not a runtime DoS; consumer typecheck breaks at upgrade time. |
| T-34-01-05 | Tampering | Top-level helper drift from adapter-level negotiate() (Pitfall 5) | mitigate | Helper has ZERO logic in the live path -- pure delegation to `adapter.negotiateCapabilities`. JSDoc + Acceptance Criteria enforce the no-inflight-no-source-values policy in 34-01 Task 3. Verifiable via LOC count + absence of `new Map<` in the public negotiate() helper. |
| T-34-01-06 | Information Disclosure | New typed errors (NegotiationAuthError) extending Error -- stack traces in production logs | accept | Standard JS Error semantics. Caller's logging discipline determines disclosure boundary. Consistent with AgentDeniedError v1.2 precedent (verified agent/types.ts:172). |

</threat_model>

<verification>

Final phase-wide checks beyond per-task automated commands:

1. **Full workspace typecheck**: `pnpm typecheck` exits 0 (both lattice + lattice-cli green).
2. **Full lattice test suite**: `pnpm --filter @full-self-browsing/lattice test` -- all existing tests still pass; new tests (sanitizer-recommendations + negotiate-helper) added without regression.
3. **Type tests**: `pnpm test:types` -- workspace-wide tsd passes; new quirks-negotiation.test-d.ts is registered + executed.
4. **Public surface lint**: `pnpm -r lint:packages` (publint + arethetypeswrong) -- the 7 new index.ts exports must be visible in the published .d.ts; no dangling internal-only references.
5. **Zero new runtime dependencies**: `git diff packages/lattice/package.json` shows no `dependencies` block change (only optional Devs).
6. **REQUIREMENTS.md grep**: `grep -E "QUIRK-0[123]|NEG-0[12]" .planning/REQUIREMENTS.md | wc -l` >= 10 (5 REQ rows + 5 traceability rows minimum).

</verification>

<success_criteria>

Plan 34-01 is COMPLETE when:

- [ ] All 5 new REQ-IDs (QUIRK-01, QUIRK-02, QUIRK-03, NEG-01, NEG-02) are authored in REQUIREMENTS.md with bullet phrasing styled per CAPS-01..05 precedent + Traceability rows + Coverage table updated.
- [ ] `packages/lattice/src/providers/quirks.ts` exists with 8 interfaces (1 base + 7 narrowed); all use `readonly`.
- [ ] `packages/lattice/src/capabilities/sanitizer-recommendations.ts` exists with SanitizerKey + SANITIZER_BY_FAILURE_MODE + getRecommendedSanitizers; exhaustive over KnownFailureMode; the 6 vitest tests pass.
- [ ] `packages/lattice/src/capabilities/negotiate.ts` exists with NegotiatedCapabilities + NegotiationAuthError + top-level helper + synthesizeNegotiatedCapabilitiesFromRegistry; the 4 vitest tests pass; the helper has ZERO live-path logic (pure delegation per Pitfall 5).
- [ ] `packages/lattice/src/tracing/tracing.ts` RunEventKind union includes `"capabilities.negotiation.fallback"` as the last literal with the documented JSDoc.
- [ ] `packages/lattice/src/providers/provider.ts` `ProviderAdapter` interface adds OPTIONAL `quirks?: AdapterQuirks` and `negotiateCapabilities?(modelId): Promise<NegotiatedCapabilities>` fields; existing 4-field consumer adapter literals still satisfy the interface (D-01/D-02 non-breaking).
- [ ] `packages/lattice/src/index.ts` re-exports 13 new symbols (8 quirks interfaces + NegotiatedCapabilities + NegotiationAuthError + negotiateCapabilities + synthesizeNegotiatedCapabilitiesFromRegistry + SanitizerKey + SANITIZER_BY_FAILURE_MODE + getRecommendedSanitizers).
- [ ] `packages/lattice/test-d/quirks-negotiation.test-d.ts` contains 6+ tsd assertions: SanitizerKey exhaustive switch, AdapterQuirks discriminant, NegotiatedCapabilities.source exhaustive, RunEventKind includes new literal.
- [ ] Zero new runtime deps; pnpm typecheck + test + test:types + lint:packages all green.
- [ ] All decisions D-01, D-02, D-03, D-04 (definition), D-09 (registry-fallback source value), D-10 (NegotiationAuthError class), D-12 (RunEventKind addition), D-13, D-14, D-15, D-16 are implemented or have their TYPED SURFACE shipped (the dynamic fetch behavior lands in Plans 02-04).

</success_criteria>

<output>
After completion, create `.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-01-SUMMARY.md` documenting:
- The 5 REQ-IDs authored (QUIRK-01, QUIRK-02, QUIRK-03, NEG-01, NEG-02) with the final REQUIREMENTS.md row text
- File-by-file delta (new files + modified files + LOC counts)
- Test count delta (existing -> new)
- Verification of every D-XX implemented at the type level
- Hand-off note for Plans 02-05: "AdapterQuirks + NegotiatedCapabilities + NegotiationAuthError + synthesizeNegotiatedCapabilitiesFromRegistry are now importable. Plans 02-05 implement per-adapter negotiate() against this contract."
</output>
