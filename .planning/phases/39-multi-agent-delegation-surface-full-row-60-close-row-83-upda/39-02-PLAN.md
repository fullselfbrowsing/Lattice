---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/agent/infra/rate-limit-group.ts
  - packages/lattice/src/agent/infra/rate-limit-group.test.ts
autonomous: true
requirements: [DELEG-05]
must_haves:
  truths:
    - "A consumer can create a standalone dual-dimension (RPM + input-TPM) rate-limit group and gate any async work through acquire/release leases"
    - "The bucket drains continuously (per-ms smoothing) with lazy refill — no setInterval, no event-loop pinning"
    - "An AgentTransport wrapped with the group shares one bucket across every caller holding the same group instance"
  artifacts:
    - path: "packages/lattice/src/agent/infra/rate-limit-group.ts"
      provides: "createRateLimitGroup + withRateLimit transport wrapper"
      exports: ["createRateLimitGroup", "withRateLimit"]
      min_lines: 80
    - path: "packages/lattice/src/agent/infra/rate-limit-group.test.ts"
      provides: "fake-timer drain/lease/wrapper coverage"
      min_lines: 80
  key_links:
    - from: "packages/lattice/src/agent/infra/rate-limit-group.ts"
      to: "packages/lattice/src/agent/host.ts AgentTransport"
      via: "withRateLimit wraps transport.call / provider.execute"
      pattern: "AgentTransport"
    - from: "rate-limit-group.test.ts"
      to: "vi.useFakeTimers + advanceTimersByTimeAsync"
      via: "deterministic drain tests"
      pattern: "advanceTimersByTimeAsync"
---

<objective>
Ship `createRateLimitGroup` — the standalone, zero-dependency, dual-dimension token-bucket infra primitive (D-12..D-17) — plus the `withRateLimit` AgentTransport wrapper that the crew (39-06) and plain `runAgent` consumers compose.

Purpose: Rate-limit-group coordination is roadmap success criterion 3 and the explicitly flagged 429-cascade risk. The primitive has no dependency on any other Phase 39 work, so it lands in Wave 1.
Output: `agent/infra/rate-limit-group.ts` with lease-based interface, conservative Anthropic-Tier-1 defaults, and fake-timer test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-PATTERNS.md

Key research sections: Pattern 4 (full interface sketch + transport composition + verified Tier 1 defaults), Code Examples (fake-timer bucket test skeleton), Anti-Patterns (no setInterval refill).
</context>

<interfaces>
Target public surface (research Pattern 4, names locked here for downstream plans):

- `RateLimitGroupOptions { requestsPerMinute?: number; tokensPerMinute?: number; now?: () => number }` — defaults 50 RPM / 30_000 input TPM (Anthropic Tier 1, verified 2026-06-10).
- `RateLimitLease { release(actual: { promptTokens: number }): void }` — reconciles estimate vs actual `Usage.promptTokens`.
- `RateLimitGroup { readonly kind: "rate-limit-group"; acquire(estimate: { inputTokens: number }): Promise<RateLimitLease> }`.
- `withRateLimit(group: RateLimitGroup, inner?: AgentTransport): AgentTransport` — wraps `call(provider, request)`; falls through to `provider.execute(request)` when `inner` is undefined.

From packages/lattice/src/agent/host.ts (verified):
- `AgentTransport` at host.ts:83-88: `call(provider: ProviderAdapter, request: ProviderRunRequest): Promise<ProviderRunResponse>`.
- Pass-through reference impl in `createNoopAgentHost` at host.ts:144-156 (throws when `provider.execute === undefined` — replicate the same guard message shape, provider id only).

From packages/lattice/src/agent/infra/cost-tracker.ts (module-shape analog, D-12 locked precedent):
- Doc-header comment, `import type` only deps, `kind`-tagged closure factory, no agent-runtime imports.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createRateLimitGroup dual-dimension lease bucket</name>
  <files>packages/lattice/src/agent/infra/rate-limit-group.ts, packages/lattice/src/agent/infra/rate-limit-group.test.ts</files>
  <read_first>
    - packages/lattice/src/agent/infra/cost-tracker.ts (module shape: doc header, kind-tagged closure factory, type-only imports)
    - packages/lattice/src/providers/adapters.test.ts lines 500-560 (fake-timer precedent: vi.useFakeTimers + advanceTimersByTimeAsync)
    - packages/lattice/src/providers/anthropic.ts lines 25-45 (lazy-expiry / no-background-timer rule documented there)
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md Pattern 4 (interface + algorithm)
  </read_first>
  <behavior>
    - Test 1: defaults — `createRateLimitGroup()` allows 50 request acquisitions within a simulated minute, the 51st waits; token dimension caps at 30_000 input tokens/min
    - Test 2: dual-dimension independence — with `{ requestsPerMinute: 100, tokensPerMinute: 1000 }`, a single `acquire({ inputTokens: 1000 })` exhausts the token bucket while requests remain; next acquire waits on tokens
    - Test 3: continuous drain — `{ requestsPerMinute: 2, tokensPerMinute: 1000 }`: two acquires of 400 tokens succeed; third acquire of 400 pends; `await vi.advanceTimersByTimeAsync(30_000)` (half-minute refill) resolves it (RESEARCH skeleton verbatim)
    - Test 4: lease reconciliation — acquire with estimate 1000, `release({ promptTokens: 200 })` refunds the 800-token difference (a follow-up acquire of 800 proceeds without waiting); `release({ promptTokens: 1500 })` debits the extra 500
    - Test 5: FIFO fairness — two pending acquires resolve in acquisition order after refill
    - Test 6: injectable `now` — pure-logic test drives refill with a manual clock, no fake timers
  </behavior>
  <action>
    Create `packages/lattice/src/agent/infra/rate-limit-group.ts` implementing the interfaces block exactly. Algorithm: closure state holds two buckets `{ available, capacity, perMsRate, lastRefillAt }` (requests bucket: capacity = requestsPerMinute, rate = requestsPerMinute/60_000; tokens bucket likewise). On every `acquire`, lazily refill both from `now()` delta (`available = min(capacity, available + elapsed * perMsRate)`), then: if both dimensions have capacity (1 request + estimate.inputTokens tokens), debit and resolve immediately; otherwise compute the exact deficit wait in ms across both dimensions (max of the two deficits / rate), schedule ONE `setTimeout` for that wait, and queue the waiter FIFO. NO `setInterval`, no recurring timers (anthropic.ts lazy-expiry rule). `release(actual)`: refund/debit the difference `estimate - actual.promptTokens` on the token bucket only (requests are consumed at acquire, never refunded). Burn-on-failure policy is the CALLER's via release semantics — document in the doc header: "on provider failure, release with the original estimate (no refund) — quota may have been consumed despite the error". Module doc header `/** RateLimitGroup — Phase 39 (v1.3). ... */` with the CostTracker-style provenance note and zero-dep statement (D-12, D-17, D-18). Constant names: `DEFAULT_REQUESTS_PER_MINUTE = 50`, `DEFAULT_TOKENS_PER_MINUTE = 30_000`, each with a comment citing Anthropic Tier 1 (fetched 2026-06-10). No imports from agent runtime; only `import type` from providers/host where needed by Task 2.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/infra/rate-limit-group.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behaviors pass under fake timers; suite completes in < 5s wall-clock (no real waits)
    - `grep -c 'setInterval' packages/lattice/src/agent/infra/rate-limit-group.ts` returns 0
    - `grep -c 'DEFAULT_REQUESTS_PER_MINUTE = 50' packages/lattice/src/agent/infra/rate-limit-group.ts` returns 1 and `grep -c '30_000' ...` >= 1
    - `RateLimitGroup.kind` is the literal `"rate-limit-group"`
    - `pnpm --filter @full-self-browsing/lattice typecheck` passes
  </acceptance_criteria>
  <done>Standalone primitive complete: dual-dimension continuous-drain bucket with lease reconciliation, deterministic under fake timers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: withRateLimit AgentTransport wrapper</name>
  <files>packages/lattice/src/agent/infra/rate-limit-group.ts, packages/lattice/src/agent/infra/rate-limit-group.test.ts</files>
  <read_first>
    - packages/lattice/src/agent/host.ts (AgentTransport interface :83-88; noop transport guard :144-156)
    - packages/lattice/src/agent/runtime.ts lines 221-235 (transport call site + normalizedUsage consumption; error message rule `err.message` only)
    - packages/lattice/src/agent/infra/transcript-store.ts (TokenEstimator type to align the estimator signature with, if exported)
  </read_first>
  <behavior>
    - Test 1: `withRateLimit(group)` calling a fake provider routes through `provider.execute` and releases the lease with `response.normalizedUsage.promptTokens`
    - Test 2: `withRateLimit(group, innerTransport)` nests — inner transport's `call` is invoked, proving composability with consumer transports
    - Test 3: two sequential calls through the same group instance share one bucket (second call waits when the first exhausted capacity; advanceTimersByTimeAsync releases it)
    - Test 4: provider throw → lease released with the ORIGINAL estimate (no refund — conservative burn policy), and the error propagates unchanged; error path never serializes the request object or headers
    - Test 5: `normalizedUsage` missing/null promptTokens → release falls back to the estimate (no NaN arithmetic; `costUsd: null` discipline analog)
  </behavior>
  <action>
    Add to the same module: `export function withRateLimit(group: RateLimitGroup, inner?: AgentTransport): AgentTransport` per the research Pattern 4 sketch, with the burn-on-failure refund policy fixed as: on throw, `lease.release({ promptTokens: estimate })` (burns the reservation — provider may have consumed quota; documented decision resolving research Open Question 5). Token estimation: internal `estimateInputTokens(task: string): number` = `Math.ceil(task.length / 4)` chars/4 heuristic (release reconciles to actuals, A2). When `inner` is undefined fall through to `provider.execute(request)` guarded by the host.ts:150 undefined-execute check (error message names `provider.id` only — never options/headers). Export `withRateLimit` and the estimator stays private. Extend the test file with behaviors 1-5 using a minimal fake `ProviderAdapter` literal (id/kind/capabilities/execute).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/infra/rate-limit-group.test.ts && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behaviors 1-5 pass; failure-path test asserts the SAME error instance propagates and a subsequent acquire reflects the burned estimate
    - `ProviderAdapter` interface is not modified anywhere in the diff (INV-03; `git diff --name-only` does not contain providers/provider.ts)
    - Wrapper error messages contain provider id only — test asserts message does not contain "apiKey" or header content
  </acceptance_criteria>
  <done>Transport seam wrapper complete; 39-06 can wrap parent + child hosts with one shared group per adapter instance (D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crew/agent code → provider HTTP API | every provider call crosses the rate-gated transport |
| consumer config → bucket sizing | misconfigured limits cause 429 cascades or starvation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | DoS | provider quota (429 cascade) | mitigate | Conservative Tier-1 defaults (50 RPM / 30k TPM); continuous drain matches Anthropic's own token-bucket enforcement; explicit `limits` override only (D-16) |
| T-39-05 | DoS | event loop (timer pinning) | mitigate | Lazy refill from `now()` delta; single deficit setTimeout; grep gate forbids setInterval |
| T-39-06 | Information disclosure | wrapper error paths | mitigate | Task 2 test asserts errors carry provider id only, never apiKey/headers (T-34-02-01 rule) |
| T-39-07 | DoS | quota under-release (leak → starvation) | mitigate | Lease always released (success: actuals; failure: burned estimate); FIFO test proves pending waiters resolve |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (D-17 rejected bottleneck; D-18 zero new runtime deps) |
</threat_model>

<verification>
- `pnpm --filter @full-self-browsing/lattice test -- src/agent/infra/rate-limit-group.test.ts` green under fake timers, < 5s
- `pnpm --filter @full-self-browsing/lattice typecheck` green
- No changes outside `src/agent/infra/`
</verification>

<success_criteria>
- Dual-dimension lease bucket with verified Tier-1 defaults, lazy continuous drain, FIFO waiters
- `withRateLimit` composes over AgentTransport without touching ProviderAdapter (INV-03 intact)
- Refund policy decided (burn on failure), documented, and tested
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-02-SUMMARY.md` when done.
</output>
