---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 08
type: execute
wave: 5
depends_on: ["39-06", "39-07"]
files_modified:
  - AGENTS.md
  - docs/fsb-integration-gaps.md
  - packages/lattice/test-d/agent-crew.test-d.ts
  - packages/lattice/test-d/index.test-d.ts
  - .changeset/v1.3.0-agent-crew.md
  - packages/lattice/src/index.ts
autonomous: true
requirements: [DELEG-08]
must_haves:
  truths:
    - "AGENTS.md no longer claims multi-agent is Out of Scope anywhere — all three stale surfaces (What Not To Use row, policy header, Multi-Agent Crews section + Rationale) flipped to first-class via opt-in AgentHost capability"
    - "docs/fsb-integration-gaps.md Row 60 reads Covered with Phase 39 backlink; Row 83 reads Covered with the v1.2 Phase 20 commit 3794896 backlink"
    - "Every new public symbol has tsd coverage and the package passes publint/attw; a changeset documents the crew surface"
  artifacts:
    - path: "AGENTS.md"
      provides: "policy flip (3 surfaces)"
      contains: "First-class via opt-in"
    - path: "docs/fsb-integration-gaps.md"
      provides: "Row 60 + Row 83 Covered flips with backlinks"
      contains: "3794896"
    - path: "packages/lattice/test-d/agent-crew.test-d.ts"
      provides: "tsd coverage for AgentSpec/CrewPolicy/CrewResult/RateLimitGroup/receiptCid"
    - path: ".changeset/v1.3.0-agent-crew.md"
      provides: "release notes for the crew surface"
  key_links:
    - from: "docs/fsb-integration-gaps.md Row 83"
      to: "v1.2 Phase 20 recovery markers"
      via: "commit 3794896 backlink"
      pattern: "3794896"
    - from: "packages/lattice/test-d/agent-crew.test-d.ts"
      to: "packages/lattice/src/index.ts exports (39-06)"
      via: "package-root type imports"
      pattern: "@full-self-browsing/lattice"
---

<objective>
Close the phase's public-contract surfaces: flip all three stale AGENTS.md policy texts, flip `docs/fsb-integration-gaps.md` Row 60 (→ Covered, Phase 39) and Row 83 (→ Covered, v1.2 Phase 20 commit `3794896` backlink), add tsd type coverage for every new public symbol, write the changeset, and run the full phase gate suite (DELEG-08).

Purpose: The AGENTS.md flip is the public-contract change the roadmap flags as a risk — once advertised, consumers pin to it. RESEARCH.md §Doc Edits carries exact current→replacement text; this plan applies it verbatim.
Output: doc flips, `test-d/agent-crew.test-d.ts`, extended `index.test-d.ts`, changeset, all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-06-SUMMARY.md

Key research sections: §Doc Edits (EXACT current text → proposed replacement for all five doc surfaces — apply these), Pitfall 7 (three AGENTS.md surfaces, not one).
</context>

<interfaces>
Public symbols requiring tsd coverage (exported in 39-06):
- `defineAgent`, `AgentSpec` (kind:"agent" discriminant, childAgents tree, summaryReturnSchema)
- `CrewPolicy` (budget: BudgetInvariant, structural caps, limits record, coordination union)
- `RunAgentCrewOptions`, `CrewResult` (perAgent array shape, totalIterations, receipts, crewRootCid)
- `createRateLimitGroup`, `withRateLimit`, `RateLimitGroup` (kind literal), `RateLimitLease`, `RateLimitGroupOptions`
- `receiptCid` (returns Promise<string>); `CapabilityReceiptBody["parentReceiptCid"]` is `string | undefined` (extend receipt-v12.test-d.ts only if 39-01 did not already)
- `AI["runAgentCrew"]` facade member on createAI's return type
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AGENTS.md three-surface policy flip + gaps-doc Row 60/83 flips</name>
  <files>AGENTS.md, docs/fsb-integration-gaps.md</files>
  <read_first>
    - AGENTS.md (line ~127 "What Not To Use" OpenAI Agents SDK row; lines ~136-137 policy-flip header; lines ~151-153 "Multi-Agent Crews — Out of Scope" section; line ~157 Rationale paragraph)
    - docs/fsb-integration-gaps.md (Row 60 at line ~60; §Delegation surface-inventory paragraph at line ~55; Row 83 at line ~83; rows 70/72 survivability entries citing commits a4609bc/109d6ae)
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md §Doc Edits (the exact replacement text — apply verbatim, adjusting only line drift)
  </read_first>
  <action>
    Apply the RESEARCH.md §Doc Edits replacements exactly:
    AGENTS.md (Pitfall 7 — all THREE surfaces plus Rationale): (1) line ~127 What-Not-To-Use parenthetical → "Lattice ships its own opt-in multi-agent crew surface (`defineAgent` + `runAgentCrew`) on top of the same primitives; see Agent Execution Policy below."; (2) lines ~136-137 → append the second flip note: "**Policy flip in v1.3 (Phase 39).** Multi-agent crews are now first-class via the opt-in `AgentHost` capability: `runAgentCrew({ root, hosts: { childHost }, policy })` with `defineAgent` specs. Single-agent `ai.runAgent` remains the zero-config default."; (3) retitle section to "### Multi-Agent Crews — First-class via opt-in `AgentHost` capability (v1.3+)" and replace the body with the crew description (parent-child loops with structured summary-return `{ summary, artifacts, receipts }` per `summaryReturnSchema`; serial children in v1.3; crew-level `BudgetInvariant` + structural caps; shared rate-limit group per provider key via the `AgentTransport` seam; per-agent receipts chained via `parentReceiptCid`; cache-prefix sharing on Anthropic `cache_control` + OpenAI automatic; explicitly: opt-in only, no behavior change for `ai.runAgent` consumers); (4) Rationale final sentence → the crew surface ships as a thin composition over the same primitives (CrewDispatcher + existing loop), kept opt-in so the public model stays capability-first rather than graph/crew-first. Also flip the stale multi-agent comment near src/index.ts:52-54 if 39-06 left it (check; it is part of the same policy surface — prose comment only).
    docs/fsb-integration-gaps.md: Row 60 status "Out of scope"/"Blocker" → "Covered" / "n/a" with the RESEARCH-drafted Notes text naming `defineAgent` + `runAgentCrew`, `createRateLimitGroup`, cache-prefix sharing, `parentReceiptCid`, and the Phase 39 commit refs (use `git log --oneline` to fill actual short SHAs of the executed Phase 39 commits); also update the §Delegation surface-inventory paragraph at line ~55 restating the old stance. Row 83 "Needs addition"/"Important" → "Covered" / "n/a" with the verbatim RESEARCH Notes: retroactively covered in v1.2 Phase 20 — `recovery.start`/`recovery.complete`/`recovery.failed` in `RunEventKind`, closing TRACE-EXT-01, Lattice commit `3794896` (backlink missed at the time; recorded by v1.3 Phase 39), paired survivability commits `a4609bc`/`109d6ae` (rows 70/72).
  </action>
  <verify>
    <automated>bash -c '! grep -qi "remain Out of Scope" AGENTS.md && grep -q "First-class via opt-in" AGENTS.md && grep -q "3794896" docs/fsb-integration-gaps.md && ! grep -E "Multi-Agent Crews . Out of Scope" AGENTS.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "out of scope" AGENTS.md` returns no line mentioning multi-agent/crews (other Out-of-Scope items may remain)
    - AGENTS.md contains the exact substring "Policy flip in v1.3 (Phase 39)" and the retitled section heading
    - Row 60 line contains "Covered" and no longer contains "Blocker"; Row 83 line contains "Covered" and "3794896"
    - The §Delegation inventory paragraph (~line 55) no longer restates the old exclusion
  </acceptance_criteria>
  <done>Row 60 fully closed, Row 83 retroactively closed with backlink, AGENTS.md consistent across all surfaces — the public policy contract is flipped exactly once, in one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tsd type coverage + changeset</name>
  <files>packages/lattice/test-d/agent-crew.test-d.ts, packages/lattice/test-d/index.test-d.ts, .changeset/v1.3.0-agent-crew.md</files>
  <read_first>
    - packages/lattice/test-d/receipt-v12.test-d.ts (tsd file shape: expectType/expectAssignable patterns, package-root imports)
    - packages/lattice/test-d/index.test-d.ts (public-surface smoke assertions to extend)
    - packages/lattice/src/index.ts (final 39-06 export list — cover every new symbol)
    - .changeset/ existing v1.3.0-*.md files (naming + frontmatter convention: which package, bump type)
  </read_first>
  <behavior>
    - Type test: `defineAgent` returns AgentSpec with kind "agent" literal; childAgents accepts a nested AgentSpec tree; missing summaryReturnSchema is a type error
    - Type test: CrewPolicy.budget is assignable from a BudgetInvariant literal; coordination only accepts "managed" | "unmanaged"; limits record member shape enforced
    - Type test: CrewResult.perAgent element shape (id/usage/iterations/receiptCids); totalIterations is number; receipts is a readonly envelope array
    - Type test: createRateLimitGroup() returns RateLimitGroup with kind "rate-limit-group" literal; acquire returns Promise<RateLimitLease>; withRateLimit returns AgentTransport
    - Type test: receiptCid returns Promise<string>; createAI(...).runAgentCrew exists and returns Promise<CrewResult>
  </behavior>
  <action>
    Create `test-d/agent-crew.test-d.ts` importing every new symbol from the package root (`@full-self-browsing/lattice` or the relative dist path matching existing test-d convention — copy receipt-v12.test-d.ts's import style) and assert the behaviors above with `expectType`/`expectAssignable`/`expectError`. Extend `index.test-d.ts` with presence smoke checks for `defineAgent`, `createRateLimitGroup`, `withRateLimit`, `receiptCid`, and the `runAgentCrew` facade member. Write `.changeset/v1.3.0-agent-crew.md` following the repo's existing changeset conventions (minor bump on `@full-self-browsing/lattice`): summarize the opt-in multi-agent crew surface — `defineAgent`/`runAgentCrew`, serial parent-child delegation with schema-validated summary-return, `createRateLimitGroup` shared per-provider-key RPM+TPM bucket via the AgentTransport seam, cache-prefix sharing (Anthropic `cache_control`, OpenAI automatic), `parentReceiptCid` receipt chaining + `receiptCid` helper on the v1.2 body, and the AGENTS.md policy flip; state explicitly that single-agent `ai.runAgent` behavior is unchanged (opt-in only).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test:types</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:types` green with the new file included; an `expectError` case proves negative coverage compiles as a failure
    - `.changeset/v1.3.0-agent-crew.md` exists with a valid frontmatter block naming `@full-self-browsing/lattice` and contains the strings `runAgentCrew`, `createRateLimitGroup`, `parentReceiptCid`
  </acceptance_criteria>
  <done>Every new public symbol type-audited; release notes staged.</done>
</task>

<task type="auto">
  <name>Task 3: full phase gate</name>
  <files>(verification only — no new files)</files>
  <read_first>
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-VALIDATION.md (phase-gate command set)
  </read_first>
  <action>
    Run the complete phase gate from 39-VALIDATION.md: `pnpm test && pnpm typecheck && pnpm test:types && pnpm -r lint:packages` (publint + attw) at workspace root, then `pnpm --filter @full-self-browsing/lattice build && node examples/agent-crew/index.mjs` (exit 0). If any gate fails, fix the regression within this plan's file ownership (or report the owning plan in the SUMMARY for gap closure — do not paper over with skipped tests). Confirm `git grep -l "parentReceiptCid" packages/lattice/src` covers receipts + crew only (no accidental leakage into adapter code).
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm test:types && pnpm -r lint:packages && node examples/agent-crew/index.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All five gate commands exit 0
    - No test marked skip/todo was added anywhere in the phase diff: `git diff ca24e8b.. -- '*.test.ts' | grep -c "\.skip\|\.todo"` returns 0. Phase-start ref is pinned to `ca24e8b` ("docs(39): create phase plans for multi-agent delegation surface" — the commit immediately preceding all Phase 39 execution commits). If that SHA is unavailable (history rewrite), re-derive it via `git log --oneline --grep="docs(39): create phase plans"`.
  </acceptance_criteria>
  <done>Phase 39 surface fully green: tests, types, package gates, showcase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENTS.md policy text → external consumers | the flip is a public contract; consumers pin to it |
| package type surface → downstream compile contracts | tsd-audited exports ship in stable v1.3.0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-27 | Repudiation | gap-tracking audit trail | mitigate | Row 60/83 flips carry concrete commit backlinks (Phase 39 SHAs; verified `3794896` for Row 83) — claims are git-resolvable |
| T-39-28 | Tampering | inconsistent policy surfaces | mitigate | Pitfall 7 grep gates assert no residual "Out of Scope" multi-agent text on any of the three AGENTS.md surfaces |
| T-39-29 | Information disclosure | public surface drift | mitigate | publint + attw + tsd gates on every export; internal seam confirmed non-exported (39-06 gate) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase (D-18) |
</threat_model>

<verification>
- Task 3 full gate: `pnpm test && pnpm typecheck && pnpm test:types && pnpm -r lint:packages` + showcase exit 0
- Doc grep gates from Task 1 pass
</verification>

<success_criteria>
- AGENTS.md consistent (3 surfaces + Rationale), Row 60 + Row 83 Covered with backlinks
- tsd coverage for all new symbols; changeset staged; all phase gates green
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-08-SUMMARY.md` when done.
</output>
