---
phase: 44-streaming-adapter-implementations
plan: 04
type: execute
wave: 3
depends_on: ["44-01", "44-02", "44-03"]
files_modified:
  - packages/lattice/src/providers/parity.test.ts
  - packages/lattice/src/runtime/create-ai.test.ts
  - .changeset/streaming-adapters.md
autonomous: true
requirements: [SADAPT-01, SADAPT-02, SADAPT-03, SADAPT-04]
tags:
  - parity
  - runtime
  - changeset
must_haves:
  truths:
    - "SADAPT-04: streaming parity covers all seven v1.2 logical providers"
    - "Runtime policy.stream succeeds against real provider factories with fake SSE fetches"
    - "Release note documents provider streaming adapter implementations"
    - "No provider SDK dependency is added"
  artifacts:
    - path: "packages/lattice/src/providers/parity.test.ts"
      provides: "cross-provider executeStream parity tests"
    - path: "packages/lattice/src/runtime/create-ai.test.ts"
      provides: "runtime integration test against a real adapter factory"
    - path: ".changeset/streaming-adapters.md"
      provides: "release note for Phase 44"
---

<objective>
Close Phase 44 by proving streaming parity across the provider set, validating runtime integration with real adapter factories, and adding release notes.
</objective>

<context>
@.planning/phases/44-streaming-adapter-implementations/44-CONTEXT.md
@.planning/phases/44-streaming-adapter-implementations/44-RESEARCH.md
@.planning/phases/44-streaming-adapter-implementations/44-PATTERNS.md
@.planning/phases/44-streaming-adapter-implementations/44-VALIDATION.md
@.planning/phases/44-streaming-adapter-implementations/44-01-PLAN.md
@.planning/phases/44-streaming-adapter-implementations/44-02-PLAN.md
@.planning/phases/44-streaming-adapter-implementations/44-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-provider streaming parity</name>
  <files>packages/lattice/src/providers/parity.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/parity.test.ts
    - packages/lattice/src/providers/streaming.ts
    - packages/lattice/src/providers/sse.ts
  </read_first>
  <action>
    Extend parity tests with fake SSE response bodies per provider row. Add a test named `INV-03 streaming parity: seven logical providers expose executeStream`.

    The test should cover the seven v1.2 logical providers:
    - OpenAI
    - OpenAI-compatible
    - Anthropic
    - Gemini
    - xAI
    - OpenRouter
    - LM Studio

    LiteLLM may be included as an additional inherited gateway row, but SADAPT-04's seven-provider assertion must be explicit.

    For each row:
    - build the adapter with fake fetch,
    - assert `typeof adapter.executeStream === "function"`,
    - call `collectStream(await adapter.executeStream!(request))`,
    - assert `rawOutputs.text` is a non-empty string.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- parity streaming</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/parity.test.ts` contains `INV-03 streaming parity: seven logical providers expose executeStream`.
    - `pnpm --filter @full-self-browsing/lattice test -- parity streaming` exits 0.
  </acceptance_criteria>
  <done>Provider parity proves streaming availability across the v1.2 provider set.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime integration with a real streaming adapter factory</name>
  <files>packages/lattice/src/runtime/create-ai.test.ts</files>
  <read_first>
    - packages/lattice/src/runtime/create-ai.test.ts
    - packages/lattice/src/providers/adapters.ts
    - packages/lattice/src/runtime/create-ai.ts
  </read_first>
  <action>
    Add a runtime test named `runs policy.stream through a real OpenAI-compatible adapter factory`.

    Use `createOpenAICompatibleProvider` with fake SSE fetch, call `ai.run({ policy: { stream: true } })`, and assert:
    - `result.ok === true`,
    - output text matches assembled stream text,
    - events include `stream.start` and `stream.complete`,
    - plan attempt status is succeeded.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- create-ai streaming</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/runtime/create-ai.test.ts` contains `runs policy.stream through a real OpenAI-compatible adapter factory`.
    - `pnpm --filter @full-self-browsing/lattice test -- create-ai streaming` exits 0.
  </acceptance_criteria>
  <done>Runtime integration covers a real provider factory instead of only local literals.</done>
</task>

<task type="auto">
  <name>Task 3: Add changeset and run closure gates</name>
  <files>.changeset/streaming-adapters.md</files>
  <read_first>
    - .changeset/config.json
    - packages/lattice/package.json
    - scripts/check-core-package-boundary.mjs
  </read_first>
  <action>
    Create `.changeset/streaming-adapters.md`:
    ```markdown
    ---
    "@full-self-browsing/lattice": minor
    ---

    Implement normalized provider streaming adapters for Anthropic, Gemini, and the OpenAI-compatible provider family, including OpenAI, xAI, OpenRouter, LM Studio, and LiteLLM-compatible gateways.
    ```

    Run the full Phase 44 gate.
  </action>
  <verify>
    <automated>bash -c 'pnpm --filter @full-self-browsing/lattice test -- sse adapters anthropic gemini xai openrouter lm-studio litellm parity create-ai streaming && pnpm --filter @full-self-browsing/lattice test:types && pnpm --filter @full-self-browsing/lattice typecheck && node scripts/check-core-package-boundary.mjs'</automated>
  </verify>
  <acceptance_criteria>
    - `.changeset/streaming-adapters.md` contains `"@full-self-browsing/lattice": minor`.
    - `.changeset/streaming-adapters.md` contains `Anthropic`.
    - `.changeset/streaming-adapters.md` contains `Gemini`.
    - final Phase 44 gate exits 0.
  </acceptance_criteria>
  <done>Phase 44 has parity proof, runtime integration, release notes, and package validation.</done>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- sse adapters anthropic gemini xai openrouter lm-studio litellm parity create-ai streaming
pnpm --filter @full-self-browsing/lattice test:types
pnpm --filter @full-self-browsing/lattice typecheck
node scripts/check-core-package-boundary.mjs
```
</verification>
