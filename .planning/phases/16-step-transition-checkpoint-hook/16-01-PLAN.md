# Plan 16-01: Add `"step.transition"` literal to `RunEventKind`

**Status:** Complete (retro).
**Wave:** 1
**REQ-IDs:** TRACE-01
**Originating commit:** `fd254c4` feat(tracing): add step.transition event kind to RunEventKind.

## Goal

One-line additive change to `packages/lattice/src/tracing/tracing.ts`: add `"step.transition"` as the final literal in the `RunEventKind` union. `RunEvent` interface unchanged.

## Acceptance

- `RunEventKind` accepts `"step.transition"`.
- No semantic change to existing event flow.
- Existing tracer consumers compile unchanged.

## Outcome

Land cherry-pick `fd254c4` on this phase branch.
