---
phase: 45-multimodal-request-shaping-realtime-direction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/plan/plan.ts
  - packages/lattice/src/providers/packaging.ts
  - packages/lattice/src/providers/packaging.test.ts
autonomous: true
requirements: [MMRT-03]
tags:
  - multimodal
  - provider-packaging
  - execution-plan
must_haves:
  truths:
    - "MMRT-03: provider packaging records enough evidence to explain inline/base64/upload/file-reference choices"
    - "Transport remains the stable route-facing enum; provider-native shape details are additive metadata"
    - "Restricted artifacts and noUpload/noPublicUrl policies block unsafe media packaging"
    - "Large media artifacts prefer file references and fail with a clear warning when no safe reference exists"
  artifacts:
    - path: "packages/lattice/src/providers/packaging.ts"
      provides: "shared transport choice and provider request evidence"
    - path: "packages/lattice/src/plan/plan.ts"
      provides: "additive ProviderPackagedArtifactPlan metadata"
    - path: "packages/lattice/src/providers/packaging.test.ts"
      provides: "branch coverage for multimodal packaging decisions"
---

<objective>
Extend shared provider packaging so multimodal artifacts carry inspectable provider-request evidence before Anthropic and Gemini consume it.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-CONTEXT.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-RESEARCH.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-PATTERNS.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider request evidence to packaging plans</name>
  <files>packages/lattice/src/plan/plan.ts, packages/lattice/src/providers/packaging.ts</files>
  <action>
    Add optional provider request metadata to `ProviderPackagedArtifactPlan`, including native shape, source type, reason, media type, and size bytes. Populate the same metadata on derived packaged artifact lineage metadata. Do not include raw base64 payloads or API keys.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- packaging</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Make transport choice metadata-aware</name>
  <files>packages/lattice/src/providers/packaging.ts, packages/lattice/src/providers/packaging.test.ts</files>
  <action>
    Teach `chooseTransport` to prefer file references when provider-specific file ID/URI metadata exists, prefer URLs when artifact value/metadata is HTTP(S), and prefer base64 only for Blob values, data URLs, or explicit base64 metadata. Add policy branches for `noUpload`, `noPublicUrl`, `restricted`, MIME preservation, and oversized media.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- packaging</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- packaging
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Packaging tests cover inline/base64, URL, file-id/file-reference, MIME, privacy, and size-policy branches.
- Plan metadata explains every transport choice without raw payload leakage.
</success_criteria>
