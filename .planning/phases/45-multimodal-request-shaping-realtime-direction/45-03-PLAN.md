---
phase: 45-multimodal-request-shaping-realtime-direction
plan: 03
type: execute
wave: 3
depends_on: [45-01, 45-02]
files_modified:
  - packages/lattice/src/providers/gemini.ts
  - packages/lattice/src/providers/gemini.test.ts
  - packages/lattice/src/providers/multimodal.ts
autonomous: true
requirements: [MMRT-02, MMRT-03]
tags:
  - gemini
  - image
  - audio
  - video
  - multimodal
must_haves:
  truths:
    - "MMRT-02: Gemini request shaping maps image, audio, and video artifacts into contents[].parts[]"
    - "Inline media uses inlineData with MIME and base64 data"
    - "URL/file-reference media uses fileData with MIME and fileUri"
    - "Streaming and non-streaming Gemini requests share the same multimodal body builder"
  artifacts:
    - path: "packages/lattice/src/providers/gemini.ts"
      provides: "native Gemini media parts"
    - path: "packages/lattice/src/providers/gemini.test.ts"
      provides: "Gemini image/audio/video request body coverage"
---

<objective>
Map Lattice image, audio, and video artifacts into Gemini `parts[]` using inline data or file references according to packaging metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-CONTEXT.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-RESEARCH.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-PATTERNS.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Gemini native media parts</name>
  <files>packages/lattice/src/providers/gemini.ts, packages/lattice/src/providers/multimodal.ts</files>
  <action>
    Make the Gemini body builder async and append media parts after task text. Use `inlineData` for base64/inline image/audio/video artifacts and `fileData` for URL or file URI references. Preserve existing generation config and safety settings.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- gemini</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Cover Gemini media request branches</name>
  <files>packages/lattice/src/providers/gemini.test.ts</files>
  <action>
    Add tests named:
    - `Gemini packages image artifacts as inlineData parts`
    - `Gemini packages audio file references as fileData parts`
    - `Gemini packages video URL artifacts as fileData parts`
    - `Gemini streaming uses the multimodal request body`
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- gemini</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- gemini packaging
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Gemini non-streaming and streaming requests support inline image/audio/video data and fileData URL/URI references.
- Request shape and packaging plan metadata agree.
</success_criteria>
