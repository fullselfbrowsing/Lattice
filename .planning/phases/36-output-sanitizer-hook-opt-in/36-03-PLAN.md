---
phase: 36
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - packages/lattice/src/providers/anthropic.ts
  - packages/lattice/src/providers/gemini.ts
  - packages/lattice/src/providers/anthropic.test.ts
  - packages/lattice/src/providers/gemini.test.ts
  - packages/lattice/src/providers/parity.test.ts
  - .changeset/v1.3.0-output-sanitizers.md
autonomous: true
requirements:
  - SANITIZE-01
  - SANITIZE-03
  - SANITIZE-04
tags:
  - provider-adapters
  - direct-providers
  - parity
  - changeset
must_haves:
  truths:
    - "AnthropicProviderOptions accepts sanitizeOutput?: SanitizeOutputOption"
    - "GeminiProviderOptions accepts sanitizeOutput?: SanitizeOutputOption"
    - "Anthropic and Gemini apply sanitizers after text extraction and before return"
    - "All seven real provider factories accept and apply sanitizeOutput"
    - "OpenRouter anchor reproduction for session_1780792387779 round-trips to Greeted the user."
    - "Phase 36 changeset documents the opt-in sanitizer API"
  artifacts:
    - path: "packages/lattice/src/providers/anthropic.ts"
      provides: "Anthropic sanitizer option and execution wiring"
    - path: "packages/lattice/src/providers/gemini.ts"
      provides: "Gemini sanitizer option and execution wiring"
    - path: "packages/lattice/src/providers/parity.test.ts"
      provides: "All-seven adapter sanitizer parity coverage"
    - path: ".changeset/v1.3.0-output-sanitizers.md"
      provides: "Release note for sanitizer API addition"
      min_lines: 6
  key_links:
    - from: "packages/lattice/src/providers/anthropic.ts"
      to: "packages/lattice/src/sanitizers/index.ts"
      via: "imports SanitizeOutputOption and applyOutputSanitizers"
      pattern: "applyOutputSanitizers"
    - from: "packages/lattice/src/providers/gemini.ts"
      to: "packages/lattice/src/sanitizers/index.ts"
      via: "imports SanitizeOutputOption and applyOutputSanitizers"
      pattern: "applyOutputSanitizers"
---

<objective>
Wire the opt-in sanitizer option through Anthropic and Gemini, then close Phase 36 with all-seven adapter parity coverage and a changeset.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-CONTEXT.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-RESEARCH.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-PATTERNS.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-01-PLAN.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-02-PLAN.md
@packages/lattice/src/sanitizers/index.ts
@packages/lattice/src/providers/anthropic.ts
@packages/lattice/src/providers/gemini.ts
@packages/lattice/src/providers/parity.test.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Direct adapters sanitize different output names or fewer outputs than shared adapter | high | Add parity test over all requested output names and all seven adapters |
| Provider-specific raw responses are accidentally replaced with sanitized strings | medium | Adapter tests assert `rawResponse` remains the original JSON body |
| Anthropic/Gemini option type diverges from OpenAI-compatible option | medium | Reuse `SanitizeOutputOption`; package typecheck catches incompatible signatures |
| Changeset overpromises sanitizer safety | medium | Wording says opt-in output cleanup; avoid claiming security filtering or validation |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire Anthropic sanitizer option</name>
  <files>packages/lattice/src/providers/anthropic.ts</files>
  <read_first>
    - packages/lattice/src/providers/anthropic.ts
    - packages/lattice/src/sanitizers/index.ts (Plan 36-01 output)
    - packages/lattice/src/providers/provider.ts
  </read_first>
  <action>
    1. Import `applyOutputSanitizers` and `type SanitizeOutputOption` from `../sanitizers/index.js`.
    2. Add `sanitizeOutput?: SanitizeOutputOption;` to `AnthropicProviderOptions`.
    3. After extracting response text and creating the `rawOutputs` map, call `applyOutputSanitizers(rawOutputs, options.sanitizeOutput, { providerId: id, modelId: options.model })`.
    4. Return the sanitized output map.
    5. Keep `rawResponse` as the original Anthropic body.
    6. Preserve existing error handling, usage normalization, fetch override, and signal behavior.
  </action>
  <verify>
    <automated>rg -n 'sanitizeOutput|SanitizeOutputOption|applyOutputSanitizers|rawResponse' packages/lattice/src/providers/anthropic.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AnthropicProviderOptions` includes `sanitizeOutput?: SanitizeOutputOption`.
    - Anthropic execute invokes `applyOutputSanitizers`.
    - `rawResponse` remains provider body.
    - Existing Anthropic request/usage behavior is unchanged.
  </acceptance_criteria>
  <done>Anthropic adapter applies opt-in sanitizers.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Gemini sanitizer option</name>
  <files>packages/lattice/src/providers/gemini.ts</files>
  <read_first>
    - packages/lattice/src/providers/gemini.ts
    - packages/lattice/src/sanitizers/index.ts (Plan 36-01 output)
    - packages/lattice/src/providers/provider.ts
  </read_first>
  <action>
    1. Import `applyOutputSanitizers` and `type SanitizeOutputOption` from `../sanitizers/index.js`.
    2. Add `sanitizeOutput?: SanitizeOutputOption;` to `GeminiProviderOptions`.
    3. After extracting response text and creating the `rawOutputs` map, call `applyOutputSanitizers(rawOutputs, options.sanitizeOutput, { providerId: id, modelId: options.model })`.
    4. Return the sanitized output map.
    5. Keep `rawResponse` as the original Gemini body.
    6. Preserve existing error handling, usage normalization, fetch override, and signal behavior.
  </action>
  <verify>
    <automated>rg -n 'sanitizeOutput|SanitizeOutputOption|applyOutputSanitizers|rawResponse' packages/lattice/src/providers/gemini.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GeminiProviderOptions` includes `sanitizeOutput?: SanitizeOutputOption`.
    - Gemini execute invokes `applyOutputSanitizers`.
    - `rawResponse` remains provider body.
    - Existing Gemini request/usage behavior is unchanged.
  </acceptance_criteria>
  <done>Gemini adapter applies opt-in sanitizers.</done>
</task>

<task type="auto">
  <name>Task 3: Add direct adapter and all-seven parity tests</name>
  <files>
    packages/lattice/src/providers/anthropic.test.ts,
    packages/lattice/src/providers/gemini.test.ts,
    packages/lattice/src/providers/parity.test.ts
  </files>
  <read_first>
    - packages/lattice/src/providers/anthropic.test.ts
    - packages/lattice/src/providers/gemini.test.ts
    - packages/lattice/src/providers/parity.test.ts
    - packages/lattice/src/sanitizers/index.ts
    - .planning/phases/36-output-sanitizer-hook-opt-in/36-RESEARCH.md (test strategy)
  </read_first>
  <action>
    Add tests:

    1. Anthropic with `sanitizeOutput: unwrapInternalEnvelope({ field: "summary" })` converts provider text `{"summary":"Greeted the user."}` to `Greeted the user.`.
    2. Gemini with the same sanitizer converts the same embedded provider text to `Greeted the user.`.
    3. Anthropic and Gemini preserve `rawResponse`.
    4. A parity test iterates over all seven real adapter factories:
       - OpenAI-compatible
       - OpenAI
       - Anthropic
       - Gemini
       - xAI
       - OpenRouter
       - LM Studio
    5. The parity test configures each provider with `sanitizeOutput: unwrapInternalEnvelope({ field: "summary" })`.
    6. Each fake provider response should expose the same visible provider text shape, `{"summary":"Greeted the user."}`, adapted to that provider's response schema.
    7. Assert every adapter returns the visible output `Greeted the user.` for every requested string output name.
    8. Include `session_1780792387779` in the OpenRouter parity case name or comment to preserve the anchor reproduction link.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test anthropic gemini parity</automated>
  </verify>
  <acceptance_criteria>
    - Anthropic test contains `unwrapInternalEnvelope({ field: "summary" })`.
    - Gemini test contains `unwrapInternalEnvelope({ field: "summary" })`.
    - Parity test covers all seven provider ids.
    - Parity test contains `session_1780792387779`.
    - Tests assert `Greeted the user.`.
    - Verification command exits 0.
  </acceptance_criteria>
  <done>Direct adapters and all-seven parity coverage prove the sanitizer option works across the provider surface.</done>
</task>

<task type="auto">
  <name>Task 4: Add changeset and run final gates</name>
  <files>.changeset/v1.3.0-output-sanitizers.md</files>
  <read_first>
    - .changeset/ (existing changeset style)
    - .planning/REQUIREMENTS.md (SANITIZE requirements)
    - .planning/phases/36-output-sanitizer-hook-opt-in/36-01-PLAN.md
    - .planning/phases/36-output-sanitizer-hook-opt-in/36-02-PLAN.md
  </read_first>
  <action>
    1. Create `.changeset/v1.3.0-output-sanitizers.md` for `@full-self-browsing/lattice`.
    2. Mark it as a minor or patch according to existing v1.3 changeset convention. If the repo's active changesets for v1.3 use patch for additive SDK helpers, use patch.
    3. Mention:
       - opt-in `sanitizeOutput` adapter option
       - built-in output sanitizer factories
       - OpenRouter/internal-envelope reproduction
    4. Do not describe the feature as a security filter or model-output validator.
    5. Run final verification commands.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test sanitizers adapters openrouter xai lm-studio anthropic gemini parity public-surface && pnpm --filter @full-self-browsing/lattice test:types && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Changeset file exists.
    - Changeset mentions `sanitizeOutput`.
    - Changeset mentions built-in sanitizers.
    - Final test, type-test, and typecheck commands exit 0.
  </acceptance_criteria>
  <done>Phase 36 has release notes and final verification has passed.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
pnpm --filter @full-self-browsing/lattice test sanitizers adapters openrouter xai lm-studio anthropic gemini parity public-surface
pnpm --filter @full-self-browsing/lattice test:types
pnpm --filter @full-self-browsing/lattice typecheck
```

After successful execution, mark `SANITIZE-01` through `SANITIZE-04` complete in `.planning/REQUIREMENTS.md`, update Phase 36 progress in `.planning/ROADMAP.md`, and record completion in `.planning/STATE.md`.
</verification>
