# Quick Task 260615-6t9: Record FSB-via-npm dogfood validation and capture Lattice version-stamping follow-up - Summary

**Date:** 2026-06-15
**Status:** Complete

## Completed

- Recorded that FSB validates `@full-self-browsing/lattice@1.3.0` as a real downstream npm consumer, with registry install only and no local/git/workspace refs.
- Captured the final FSB dogfood result: `npm run test:lattice` exit 0 with 426 PASS / 0 FAIL.
- Noted the added FSB coverage for receipt v1.2 `modelClass`, proving the field round-trips through the signed receipt body.
- Reclassified the old canary blocker as closed and tracked the remaining Lattice-side version-stamping issue as low severity.

## Evidence

- FSB package baseline: published npm tarball, pinned package version and integrity, no local refs.
- FSB runtime validation: offscreen bundle inlined Lattice; fake/offline bridge path passed; seven provider adapter smoke passed; survivability, checkpoint, tripwire, receipt, and step-emitter suites passed.
- Lattice follow-up: `packages/lattice/src/version.ts` and `packages/lattice-cli/src/version.ts` still hardcode `"0.0.0"`, so `latticeVersion` and the CLI banner do not reflect `1.3.0`.

## Verification

- Updated `.planning/STATE.md`.
- Updated `.planning/PROJECT.md`.
- Updated `.planning/ROADMAP.md`.
- Ran `git diff --check`.
