---
quick_id: 260615-5m0
title: Author IEEE LaTeX paper on Lattice capability receipts and verifiable replay
date: 2026-06-15
status: complete
---

# Quick Task 260615-5m0 Summary

## What was built

A top-level `paper/` directory containing a submission-ready IEEE-conference LaTeX
paper about Lattice, with its headline contribution being capability receipts and
deterministic verifiable replay.

Files:

- `paper/main.tex` — the full two-column `IEEEtran` paper (Abstract, Introduction,
  Background and Related Work, Threat Model and Goals, Design, Implementation,
  Evaluation, Discussion, Limitations, Future Work, Conclusion, References). Includes
  four tables (the four guarantees, the seven verify error kinds, the seven-provider
  parity assertions, plus the receipt pipeline) and two figures (the receipt
  construction pipeline and the DSSE envelope).
- `paper/refs.bib` — 19 curated bibliography entries (RFC 8785, RFC 8032, RFC 7493,
  DSSE, in-toto, Sigstore, SLSA, TUF, Merkle, Model Cards, Datasheets, MCP, Standard
  Schema, Vercel AI SDK, LangChain, LiteLLM, OpenAI Agents SDK, OpenTelemetry, ML
  reproducibility). No citation was fabricated beyond this curated list.
- `paper/README.md` — build instructions (latexmk or manual pdflatex + bibtex).
- `paper/Makefile` — `make` (latexmk -pdf), `make clean`, `make distclean`.
- `paper/.gitignore` — LaTeX build artifacts.

## Accuracy: claims verified against the codebase

All quantitative claims were verified by four parallel codebase-exploration agents
before any prose was written. Stale planning-doc figures were corrected:

- Tests: 960 across 82 files (816 core + 144 CLI), vitest 4.1.5. (The v1.2-era
  "733 tests" figure was stale and is NOT used.)
- Capability profiles: 332 (328 generated from the OpenRouter feed + 4 static). (The
  "~337" figure was inaccurate and is NOT used.)
- 7 logical providers (openai, openai-compatible, anthropic, gemini, xai, openrouter,
  lm-studio) under the INV-03 parity contract.
- `verifyReceipt`: 7 error kinds; `MaterializationError`: 3 kinds; PII detectors: 4.
- Core runtime dependencies: 3 (`canonicalize`, `@standard-schema/spec`, `mime`);
  `@noble/ed25519` is a dev-only parity oracle; Node >= 24; packages at v1.3.0;
  ~21,584 lines of production TypeScript.
- `lattice eval --agent` and a `receipt diff` subcommand do NOT exist yet; the paper
  references them only as future work.

## Style compliance

Hard style rule honored: zero em dashes (U+2014), zero en dashes (U+2013), and no
` -- ` clause separators in the prose. `main.tex` is pure ASCII. Established
hyphenated API identifiers (`capability-first`, `lm-studio`, `schema-version-too-low`)
remain because they are names, not clause connectors. No emojis in any file.

## Build status

No TeX toolchain is installed in this environment (latexmk, pdflatex, bibtex, and
tectonic are all absent), so the PDF was NOT compiled. The document was verified
structurally instead: correct `\documentclass[conference]{IEEEtran}` preamble, all
sections present, balanced environments, `\bibliographystyle{IEEEtran}` and
`\bibliography{refs}` wired, and refs.bib keys matching the `\cite` calls. To build,
install a TeX distribution and run `make` from `paper/` (see `paper/README.md`).

## Execution note

The gsd-executor subagent created the scaffolding (refs.bib, README, Makefile,
.gitignore) and then terminated on a transient socket error before writing the paper
body. The orchestrator completed the quality-critical writing (`main.tex`), ran the
self-check pass, and committed, rather than risk a repeat failure on a publishable
artifact.

## Commit

Paper committed atomically to branch `recon`. See the `paper:` commit.
