# Local Codex review #2 — quick task 260616-g8h

Reviewer: `codex exec` (read-only sandbox). Scope: `git diff e7a76a2..HEAD -- packages/` (the g8h fix set).
Date: 2026-06-16

---

**Findings**

- **P2** [adapters.ts](/Users/lakshman/conductor/workspaces/lattice/dubai/packages/lattice/src/providers/adapters.ts:330): `noPublicUrl` is still bypassable through OpenAI-compatible-style adapters. Packaging can skip `url` and fall back to `inline` for `artifact.url("https://...")`, but `createOpenAICompatibleRequestBody` ignores the selected transport and still serializes `{ url: inputArtifact.value }` for URL artifacts. It also serializes string `value` for non-URL artifacts, so `artifact.image/document/file("https://...")` can still put a public URL in the provider request after a non-URL transport is selected. This affects `createOpenAICompatibleProvider` and wrappers built on it, such as OpenRouter/xAI/LM Studio-style paths. The Gemini/Anthropic `file-id` guard is correct for their native media branches, but P2-B is not complete globally.

**Prior Findings**

- **P2-B:** Partially resolved. The new `file-id` guard in [packaging.ts](/Users/lakshman/conductor/workspaces/lattice/dubai/packages/lattice/src/providers/packaging.ts:140) correctly blocks HTTP(S) metadata for Gemini `fileUri`/`geminiFileUri`/`providerFileUri` and also covers Anthropic-style `anthropicFileId`/`providerFileId`/`fileId` when selected as `file-id`. Provider-internal handles remain allowed. The remaining bypass above is outside that guard.

- **P2-A:** Resolved. Both post-packaging non-success receipt sites now pass `lineageArtifacts: [...built.artifacts, ...attemptPackaging.packagedArtifacts]` for `validation-failed` and `tripwire-violated` in [create-ai.ts](/Users/lakshman/conductor/workspaces/lattice/dubai/packages/lattice/src/runtime/create-ai.ts:512) and [create-ai.ts](/Users/lakshman/conductor/workspaces/lattice/dubai/packages/lattice/src/runtime/create-ai.ts:590). Earlier branches that occur before packaging are reasonably excluded.

- **P3:** Resolved. The added runtime test drives `ai.run({ policy: { stream: true } })` through the OpenAI-compatible streaming adapter and asserts non-zero usage on both `result.usage` and the verified signed receipt body in [create-ai.test.ts](/Users/lakshman/conductor/workspaces/lattice/dubai/packages/lattice/src/runtime/create-ai.test.ts:680).
