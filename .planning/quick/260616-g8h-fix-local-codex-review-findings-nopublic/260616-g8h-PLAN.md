---
phase: quick-260616-g8h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/providers/packaging.ts
  - packages/lattice/src/providers/packaging.test.ts
  - packages/lattice/src/runtime/create-ai.ts
  - packages/lattice/src/runtime/create-ai.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "noPublicUrl + fileUri=https:// metadata blocks the file-id transport to Gemini"
    - "noPublicUrl + fileUri=files/audio-123 (provider-internal) still passes via file-id"
    - "tripwire-violated receipt lineageMerkleRoot includes packaged artifacts"
    - "validation-failed receipt lineageMerkleRoot includes packaged artifacts"
    - "ai.run({ policy: { stream: true } }) surfaces non-zero usage in result.usage and the signed receipt"
  artifacts:
    - path: packages/lattice/src/providers/packaging.ts
      provides: "noPublicUrl file-id URL guard"
    - path: packages/lattice/src/providers/packaging.test.ts
      provides: "two new noPublicUrl+file-id tests"
    - path: packages/lattice/src/runtime/create-ai.ts
      provides: "lineageArtifacts at validation-failed and tripwire-violated sites"
    - path: packages/lattice/src/runtime/create-ai.test.ts
      provides: "tripwire-violated lineage test + streaming usage end-to-end test"
  key_links:
    - from: packages/lattice/src/providers/packaging.ts
      to: chooseTransport guard
      via: "candidate.reference.metadataKey lookup + isHttpUrl check"
    - from: packages/lattice/src/runtime/create-ai.ts
      to: maybeIssueReceipt calls at lines 506 and 583
      via: "lineageArtifacts: [...built.artifacts, ...attemptPackaging.packagedArtifacts]"
---

<objective>
Fix three confirmed findings from the local Codex review of the eu5 fix set. All three are on the `recon` branch feeding release PR #12 for v1.4 / 1.4.0. No version bump, no new changeset.

Purpose: Close a security/privacy bypass (noPublicUrl + file-id leak), two incomplete lineage fixes (non-success receipts), and a coverage gap (streaming usage end-to-end).
Output: Three atomic commits, each with a failing test committed first, then the fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260616-eu5-fix-codex-pr-12-review-findings-openai-s/260616-eu5-REVIEW.md

Branch: recon. All commits land on recon — do NOT switch branches.
No version bump. No new changeset files. No changes to `.changeset/`.
CI mirror: pnpm -r build && pnpm -r typecheck && pnpm -r test && pnpm -r test:types && pnpm -r lint:packages.
After all three tasks, also run scripts/check-tarball-refs.ts, scripts/check-install-scripts.ts, scripts/check-heavy-deps.ts if present.

Atomic commit order per task:
  1. Commit the failing test only (RED)
  2. Commit the fix (GREEN)

Commit message style (no Co-Authored-By, no AI attribution):
  fix(packaging): block noPublicUrl file-id transport when metadata value is a public URL
  fix(create-ai): add lineageArtifacts to validation-failed and tripwire-violated receipts
  test(create-ai): assert streaming usage surfaces into result.usage and signed receipt end-to-end
</context>

<interfaces>
From packages/lattice/src/providers/packaging.ts (chooseTransport, lines 106-182):

  - `candidate` has shape `{ transport: ProviderTransportMode; reason: string; reference?: ProviderPackagedArtifactReferencePlan }`
  - `ProviderPackagedArtifactReferencePlan` = `{ kind: "url"|"file-id"|"file-uri"|"storage"; metadataKey?: string }`
  - `isHttpUrl(value: unknown): boolean` — local function at line 430; checks `http:` or `https:` protocol
  - `inputArtifact.metadata` is `Record<string, unknown> | undefined`
  - Guard location: after the existing `if (policy?.noPublicUrl === true && transport === "url")` block at line 139

From packages/lattice/src/runtime/create-ai.ts:
  - `attemptPackaging` declared at line 396; in scope at lines 506 and 583
  - `attemptPackaging.packagedArtifacts` is `readonly ArtifactInput[]`
  - Success-path pattern (line 674): `lineageArtifacts: [...built.artifacts, ...attemptPackaging.packagedArtifacts, ...artifactRefs]`
  - `validation-failed` site: maybeIssueReceipt call at line 506 — add `lineageArtifacts: [...built.artifacts, ...attemptPackaging.packagedArtifacts]`
  - `tripwire-violated` site: maybeIssueReceipt call at line 583 — add same
  - Do NOT touch line 712 (fires before packaging) or line 750 (outside loop)

From packages/lattice/src/runtime/create-ai.test.ts:
  - `describe("Phase 9 receipts integration", ...)` block contains `makeSignerAndKeySet()`, `localTemplateProvider()`, and all T-prefixed receipt tests; file ends at line 1280
  - `describe("Phase 43 streaming runtime", ...)` at line 503 contains `streamingProvider()`, `streamFrom()`, `openAICompatibleSseData()`, `makeOpenAICompatibleStreamingFetch()`
  - Existing streaming test at line 595 feeds `usage: { prompt_tokens: 2, completion_tokens: 3 }` in the stream but does NOT assert `result.usage`
  - `computeArtifactLineageMerkleRoot` is already imported at line 21
  - `createFakeProvider`, `artifact`, `createInMemorySigner`, `generateEd25519KeyPairJwk`, `createMemoryKeySet`, `verifyReceipt` are all already imported
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Finding 1, P2-B): Block noPublicUrl bypass via file-id URL-valued metadata</name>
  <files>
    packages/lattice/src/providers/packaging.test.ts
    packages/lattice/src/providers/packaging.ts
  </files>
  <behavior>
    - Test A: artifact with `metadata.fileUri = "https://cdn.example.test/clip.mp4"`, providerId "gemini", policy `{ noPublicUrl: true }` → `result.blocked` includes the artifact ID; it is NOT sent as a Gemini fileData.fileUri.
    - Test B: artifact with `metadata.geminiFileUri = "files/audio-123"` (provider-internal handle), providerId "gemini", policy `{ noPublicUrl: true }` → transport is "file-id" (passes through); `result.blocked` is empty.
  </behavior>
  <action>
    Step 1 — RED commit: In `packaging.test.ts`, inside the existing `it("blocks public URL media when noPublicUrl policy is set", ...)` describe scope or immediately after it, add two new `it(...)` tests:

    Test A — "blocks file-id transport when metadata value is a public URL under noPublicUrl":
      Create an audio artifact with `metadata: { fileUri: "https://cdn.example.test/clip.mp4" }` and no inline bytes. Call `packageArtifactsForProvider` with `route({ providerId: "gemini" })` and `policy: { noPublicUrl: true }`. Assert `result.blocked` is non-empty (contains the artifact's id). Assert `result.plan.artifacts` is empty (no transport was chosen).

    Test B — "allows file-id transport when metadata value is a provider-internal handle under noPublicUrl":
      Create an audio artifact with `metadata: { geminiFileUri: "files/audio-123" }`. Call `packageArtifactsForProvider` with `route({ providerId: "gemini" })` and `policy: { noPublicUrl: true }`. Assert `result.blocked` is empty. Assert `result.plan.artifacts[0]?.transport` is `"file-id"`.

    Run `pnpm --filter lattice test -- --reporter=verbose packaging.test` — Test A must FAIL (currently passes transport), Test B must pass.

    Commit (RED): `test(packaging): add failing noPublicUrl+file-id URL-bypass tests`

    Step 2 — GREEN commit: In `packaging.ts`, in `chooseTransport`, DELETE the inaccurate comment block at lines 132-138 (the "not a bug" comment). Replace it with this accurate inline note immediately before the `noPublicUrl` URL guard:

      // noPublicUrl blocks "url" transport and also blocks "file-id" transport
      // when the referenced metadata value resolves to a public HTTP/HTTPS URL.
      // Provider-internal handles (e.g. "files/audio-123") are not public URLs and
      // are permitted. See Codex PR #12 finding P2-B.

    Immediately after the existing `if (policy?.noPublicUrl === true && transport === "url") { continue; }` guard (line 139), add a second guard for the file-id transport:

      if (
        policy?.noPublicUrl === true &&
        transport === "file-id" &&
        candidate.reference !== undefined &&
        candidate.reference.metadataKey !== undefined &&
        isHttpUrl(inputArtifact.metadata?.[candidate.reference.metadataKey])
      ) {
        continue;
      }

    This resolves the metadata value via the key stored in `candidate.reference.metadataKey` and checks if it is an HTTP/HTTPS URL. If it is, the candidate is skipped — exactly as the `url` transport is skipped. A provider-internal handle ("files/audio-123") is not an HTTP URL and passes `isHttpUrl` as false, so Test B continues to pass.

    Run `pnpm --filter lattice test -- --reporter=verbose packaging.test` — both tests must PASS.

    Commit (GREEN): `fix(packaging): block noPublicUrl file-id transport when metadata value is a public URL`
  </action>
  <verify>
    <automated>cd /Users/lakshman/conductor/workspaces/lattice/dubai && pnpm --filter lattice test -- --reporter=verbose packaging.test</automated>
  </verify>
  <done>
    Both new tests pass. `result.blocked` is non-empty for the public-URL file-id case; `result.plan.artifacts` is empty. Provider-internal handle passes unblocked. The inaccurate "not a bug" comment is gone; the accurate comment is in place.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Finding 2, P2-A): Add lineageArtifacts to validation-failed and tripwire-violated receipts</name>
  <files>
    packages/lattice/src/runtime/create-ai.test.ts
    packages/lattice/src/runtime/create-ai.ts
  </files>
  <behavior>
    - Test: a `tripwire-violated` run with lineage artifacts → `verifyResult.body.lineageMerkleRoot` must differ from the root computed over raw `built.artifacts` alone (proving packagedArtifacts are folded in). Mirror the existing T10b success-path guard pattern.
    - Regression guard: the test should fail if `lineageArtifacts` is removed from the tripwire-violated `maybeIssueReceipt` call.
  </behavior>
  <action>
    Step 1 — RED commit: In `create-ai.test.ts`, inside the `describe("Phase 9 receipts integration", ...)` block, after the existing T4 tripwire receipt test (around line 801), add a new test:

    "T4b: tripwire-violated receipt lineageMerkleRoot includes packaged artifacts":
      - Use `makeSignerAndKeySet()` and `localTemplateProvider({ rawOutputs: { text: "Contact alice@example.com please" }, normalizedUsage: { promptTokens: 1, completionTokens: 1, costUsd: 0 } })`.
      - Reset inv counter with `inv.__resetCounterForTests()`.
      - Create source artifact: `artifact.text("source", { id: "artifact:text:trip-lineage-source" })`.
      - Run `ai.run({ task: "x", outputs: { text: "text" as const }, artifacts: [source], contract: contract({ invariants: [inv.noPII("text")] }) })`.
      - Assert `result.ok` is false and `result.error.kind` is `"tripwire-violated"`.
      - Assert `result.receipt` is defined.
      - Call `verifyReceipt(result.receipt!, keySet)` — assert `verifyResult.ok` is true.
      - Compute `inputOnlyRoot = await computeArtifactLineageMerkleRoot([source])`.
      - Assert `verifyResult.body.lineageMerkleRoot` matches `/^sha256:[a-f0-9]{64}$/u`.
      - Assert `verifyResult.body.lineageMerkleRoot` is NOT equal to `inputOnlyRoot` — this is the regression guard (fails if lineageArtifacts is missing from the tripwire-violated receipt site).

    Run `pnpm --filter lattice test -- --reporter=verbose create-ai.test` — the new test must FAIL (lineageMerkleRoot currently equals inputOnlyRoot for the tripwire path).

    Commit (RED): `test(create-ai): add failing tripwire-violated lineage merkle regression guard`

    Step 2 — GREEN commit: In `create-ai.ts`:

    At the `validation-failed` `maybeIssueReceipt` call (line ~506), add `lineageArtifacts: [...built.artifacts, ...attemptPackaging.packagedArtifacts]` alongside the existing `artifacts: built.artifacts` field. Do not add `...artifactRefs` here — those are output artifact references that are only available on the success path.

    At the `tripwire-violated` `maybeIssueReceipt` call (line ~583), add the same `lineageArtifacts: [...built.artifacts, ...attemptPackaging.packagedArtifacts]` field.

    Do NOT touch the `anyExecutableAdapter === false` site (line ~712) or the post-loop site (line ~750).

    Run `pnpm --filter lattice test -- --reporter=verbose create-ai.test` — the new test must PASS. All existing receipt tests must still pass.

    Commit (GREEN): `fix(create-ai): add lineageArtifacts to validation-failed and tripwire-violated receipts`
  </action>
  <verify>
    <automated>cd /Users/lakshman/conductor/workspaces/lattice/dubai && pnpm --filter lattice test -- --reporter=verbose create-ai.test</automated>
  </verify>
  <done>
    T4b passes: tripwire-violated receipt lineageMerkleRoot differs from the raw-artifact-only root. validation-failed and tripwire-violated maybeIssueReceipt calls both carry `lineageArtifacts`. No existing tests regressed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Finding 3, P3): End-to-end streaming usage assertion in create-ai.test.ts</name>
  <files>
    packages/lattice/src/runtime/create-ai.test.ts
  </files>
  <behavior>
    - Test: `ai.run({ policy: { stream: true } })` with a fake streaming provider whose final chunk carries `usage: { promptTokens: 5, completionTokens: 3 }` → `result.usage` is defined and both `promptTokens` and `completionTokens` are non-zero; if a signer is configured, the signed receipt body also carries non-zero usage.
    - The test must use a streaming fake provider (custom `executeStream`), not the OpenAI-compatible adapter.
  </behavior>
  <action>
    Step 1 — RED commit: In `create-ai.test.ts`, inside the `describe("Phase 43 streaming runtime", ...)` block (after the existing tests, before the closing `}`), add a new test:

    "surfaces non-zero usage from streaming final chunk into result.usage and signed receipt":
      - Call `makeSignerAndKeySet()` — NOTE: `makeSignerAndKeySet` is defined inside `describe("Phase 9 receipts integration", ...)`. Because it is not accessible from `describe("Phase 43 streaming runtime", ...)`, define a local copy using the same pattern (`generateEd25519KeyPairJwk` + `createInMemorySigner` + `createMemoryKeySet`). All needed imports are already at the top of the file.
      - Create a `streamingProvider` (using the existing local helper) with `executeStream` that yields:
          `{ kind: "text-delta", output: "answer", text: "streamed" }`
        followed by a usage chunk: `{ kind: "usage", promptTokens: 5, completionTokens: 3, costUsd: 0 }`.
        Check what the `ProviderStreamChunk` union type accepts for a usage chunk — use the correct `kind` string. If `ProviderStreamChunk` does not have a `"usage"` variant, instead yield a final `text-delta` chunk and separately verify that the provider's `executeStream` return contract includes a trailing `usage` field on the generator itself, OR use an `execute`-mode test. To avoid fragility, prefer using `makeOpenAICompatibleStreamingFetch` (already defined in this describe block) with a final SSE chunk that carries `usage: { prompt_tokens: 5, completion_tokens: 3 }`, together with `createOpenAICompatibleProvider`. This is the pattern from line 595 but with the usage assertions added.
      - Run `ai.run({ task: "x", outputs: { answer: "text" as const }, policy: { stream: true } })` with the signer configured.
      - Assert `result.ok` is true.
      - Assert `result.usage` is defined, `result.usage.promptTokens` is greater than 0, and `result.usage.completionTokens` is greater than 0.
      - Assert `result.receipt` is defined.
      - Call `verifyReceipt(result.receipt!, keySet)` and assert `verifyResult.ok` is true.
      - Assert `verifyResult.body.usage.promptTokens` (or the receipt body field name for tokens — check the receipt body type) is greater than 0.

    Inspect the receipt body type before writing the assertion. Run:
      `grep -n "usage\|promptTokens\|prompt_tokens" packages/lattice/src/receipts/types.ts`
    Use the exact field names from the ReceiptBody type.

    Run `pnpm --filter lattice test -- --reporter=verbose create-ai.test` — the usage assertions on `result.usage` and `verifyResult.body.usage` must FAIL (currently the existing streaming test at line 595 does not assert these).

    Commit (RED): `test(create-ai): add failing streaming usage end-to-end assertion`

    Step 2 — Verify GREEN without code changes: The eu5 fix set (commit from the previous quick task) already added `stream_options: { include_usage: true }` to the OpenAI-compatible streaming request and captures usage from the final SSE chunk. Run the test again to see if it passes after the RED commit. If it passes immediately (fix already in place), the RED commit was the only needed step — commit the test alone and note "no production fix needed, eu5 fix sufficient."

    If it fails, investigate whether the usage chunk from the fake SSE stream is being collected. Check `packages/lattice/src/providers/openai-compatible.ts` around the streaming collector. Fix there only if needed, commit as GREEN:
      `fix(openai-compatible): collect streaming usage into result.usage`

    Commit sequence: RED test commit, then GREEN if a code fix was needed.
  </action>
  <verify>
    <automated>cd /Users/lakshman/conductor/workspaces/lattice/dubai && pnpm --filter lattice test -- --reporter=verbose create-ai.test</automated>
  </verify>
  <done>
    New test passes: `result.usage.promptTokens > 0`, `result.usage.completionTokens > 0`, and the signed receipt body carries matching non-zero usage. All prior streaming tests unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 4: CI mirror — full build, type, test, lint, and script audits</name>
  <files></files>
  <action>
    Run the full CI mirror in sequence. Stop and report on first failure.

    1. pnpm -r build
    2. pnpm -r typecheck
    3. pnpm -r test
    4. pnpm -r test:types
    5. pnpm -r lint:packages
    6. If scripts/check-tarball-refs.ts exists: node --loader ts-node/esm scripts/check-tarball-refs.ts (or tsx scripts/check-tarball-refs.ts)
    7. If scripts/check-install-scripts.ts exists: same invocation pattern
    8. If scripts/check-heavy-deps.ts exists: same invocation pattern

    All must pass with zero errors. If any step fails, fix the root cause before proceeding. Do not suppress errors.
  </action>
  <verify>
    <automated>cd /Users/lakshman/conductor/workspaces/lattice/dubai && pnpm -r build && pnpm -r typecheck && pnpm -r test && pnpm -r test:types && pnpm -r lint:packages</automated>
  </verify>
  <done>
    All CI mirror steps pass with zero errors. Branch `recon` is green and ready for PR #12.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact.metadata → Gemini API | User-supplied metadata values flow through packaging and are sent as `fileData.fileUri` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g8h-01 | Information Disclosure | chooseTransport / Gemini file-id | mitigate | Task 1: reject file-id candidates where metadata value resolves to an HTTP/HTTPS URL under noPublicUrl |
| T-g8h-02 | Tampering | Receipt lineage (tripwire/validation paths) | mitigate | Task 2: fold attemptPackaging.packagedArtifacts into lineageArtifacts at non-success receipt issuance sites |
</threat_model>

<verification>
After all tasks complete, confirm on branch `recon`:
- `git log --oneline -8` shows the six new commits (3x RED + 3x GREEN, or fewer if some GREEN steps were no-ops)
- `pnpm -r test` passes with all new tests included
- No `.changeset/` files were added or modified
- No version fields in `package.json` were changed
- `git diff main -- .changeset/` shows no new changeset files
</verification>

<success_criteria>
1. `noPublicUrl` + `metadata.fileUri="https://..."` → artifact blocked; no Gemini fileData.fileUri leak.
2. `noPublicUrl` + `metadata.geminiFileUri="files/audio-123"` → passes unblocked via file-id.
3. `tripwire-violated` receipt `lineageMerkleRoot` != raw-artifact-only root (packaged artifacts folded in).
4. `validation-failed` receipt `maybeIssueReceipt` carries `lineageArtifacts` (code change only; test coverage from T4b mirrors success-path guard).
5. Streaming `ai.run` with usage in final chunk → `result.usage.promptTokens > 0` and signed receipt body usage non-zero.
6. Full CI mirror green on `recon`.
</success_criteria>

<output>
No SUMMARY.md needed for quick tasks. Return findings to the orchestrator as inline text.
</output>
