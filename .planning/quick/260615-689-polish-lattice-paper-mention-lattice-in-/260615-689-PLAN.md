---
quick_id: 260615-689
title: Polish Lattice paper (title, author, Times fonts, diagrams, graph)
date: 2026-06-15
status: ready
mode: quick
---

# Quick Task 260615-689: Polish the Lattice IEEE paper

## Objective

Apply five requested polish changes to the existing paper at `paper/main.tex`
(created by quick task 260615-5m0), then recompile the PDF with tectonic. This is an
edit of an existing artifact, not a new paper.

## Requested changes

1. Title must mention Lattice. New title: "Lattice: Capability Receipts for Verifiable
   and Reproducible Multimodal Model Orchestration".
2. Use professional Times fonts as IEEE papers use (newtxtext + newtxmath, fallback
   mathptmx).
3. Generate and add real diagrams and a graph where useful (TikZ + pgfplots), not
   decorative filler.
4. Author name: Venkat Lakshman Turlapati, preferred name Lakshman Turlapati.
5. Email: lakshmanturlapati@gmail.com.

## Constraints

- Preserve the hard style rule: no em or en dashes joining clauses in prose; no ` -- `.
  Established hyphenated identifiers stay.
- Do not change any verified quantitative claim (960 tests / 82 files, 332 profiles,
  7 providers, 7 verify error kinds, 3 materialization kinds, 4 PII detectors, 3 core
  runtime deps, Node >= 24, v1.3.0).
- No emojis anywhere.
- Keep diagrams accurate to the implementation.

## Diagrams and graph to add

1. fig:arch (figure*, full width): the run lifecycle. intent -> deterministic router
   -> execution pipeline (nine stages) -> tripwire (terminal) -> capability receipt,
   then the receipt is consumed by verify and by repro/replay.
2. fig:pipeline (TikZ vertical flow, replaces the old tabular figure): the six step
   receipt construction (assemble, redact, canonicalize JCS, base64, DSSE PAE, Ed25519
   sign), emphasizing that canonicalize only consumes redaction output.
3. fig:tests (pgfplots bar chart, Evaluation): automated test cases per package
   (core 816, CLI 144; 960 total).
4. Keep the DSSE envelope listing and the three tables.

## Tasks

1. Update preamble: add newtxtext + newtxmath, tikz (arrows.meta, positioning, calc),
   pgfplots (compat 1.18), and TikZ styles. Remove standalone amssymb (unused; avoids a
   newtxmath clash).
2. Update title and author block (name, preferred name, email).
3. Insert fig:arch after the Design heading; replace the pipeline table with the TikZ
   flow; add fig:tests in Evaluation. Wire \ref to each.
4. Update paper/README.md title, author, and email.
5. Compile with tectonic until zero errors and zero overfull boxes. If newtx is missing
   from the bundle, fall back to mathptmx.

## Verify / done

- `tectonic main.tex` exits 0 with no Overfull boxes; main.pdf regenerates.
- Title shows Lattice; author is Venkat Lakshman Turlapati with preferred name and the
  gmail address; fonts are Times; three visuals render.
- grep finds no em/en dash or ` -- ` in main.tex.

## Commit guidance

- Commit `paper/main.tex` and `paper/README.md` atomically:
  `docs(paper): retitle to Lattice, add Times fonts, TikZ diagrams and a test-suite chart`.
- Docs (.planning) committed separately by the orchestrator. main.pdf stays gitignored.
