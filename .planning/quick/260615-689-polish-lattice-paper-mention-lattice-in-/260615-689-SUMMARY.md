---
quick_id: 260615-689
title: Polish Lattice paper (title, author, Times fonts, diagrams, graph)
date: 2026-06-15
status: complete
---

# Quick Task 260615-689 Summary

## What changed in `paper/`

Edited the existing paper (from 260615-5m0); no new paper was created.

1. **Title now leads with Lattice:** "Lattice: Capability Receipts for Verifiable and
   Reproducible Multimodal Model Orchestration".
2. **Professional Times fonts:** added `newtxtext` + `newtxmath` (the Times look IEEE
   papers use). Removed the now-unused `amssymb` to avoid a `newtxmath` symbol clash.
3. **Author and email:** author is now "Venkat Lakshman Turlapati" with
   "Preferred name: Lakshman Turlapati" and email "lakshmanturlapati@gmail.com". The
   old email lakshman@infinitechoice.com is gone.
4. **Diagrams and a graph (TikZ + pgfplots):**
   - `fig:arch` (full-width `figure*`): the run lifecycle (intent, deterministic
     router, nine-stage execution, terminal tripwire, capability receipt, then verify
     and repro/replay).
   - `fig:pipeline`: the six-step receipt construction redrawn as a TikZ vertical flow
     (replaces the old tabular figure), emphasizing that canonicalization only consumes
     redaction output.
   - `fig:tests` (pgfplots bar chart, Evaluation): automated test cases per package
     (core 816, CLI 144; 960 total).
5. **README:** updated title, author, email, and the package prerequisites (newtx,
   tikz, pgfplots; plus a tectonic one-liner).

## Build and verification

- Installed `tectonic` 0.16.9 (single-binary LaTeX engine) earlier in the session.
- `tectonic main.tex` exits 0 with **0 overfull boxes**; `main.pdf` regenerates at
  **8 pages**. Two transient overfull boxes during iteration (a long identifier in the
  guarantees table and a 4pt-wide verify table) were fixed by rephrasing and a small
  column-width trim.
- Rendered all 8 pages to PNG with `pdftoppm` and visually confirmed: Times fonts,
  corrected title and author block, the full-width architecture diagram at the top of
  page 2, the receipt-construction flow on page 3, and the bar chart on page 6. Preview
  PNGs were deleted afterward.
- Style preserved: zero em or en dashes and no ` -- ` clause separators in prose. The
  only ` -- ` tokens are TikZ path operators inside `\draw` commands. No emojis.
- No verified quantitative claim changed (960 tests / 82 files, 332 profiles, 7
  providers, 7 verify error kinds, 3 materialization kinds, 4 PII detectors, 3 core
  runtime deps, Node >= 24, v1.3.0).

## Execution note

Done directly by the orchestrator within the gsd-quick structure (the prior task's
delegated executor had died on a socket error). This task needed tight compile-verify
loops against tectonic for the fonts and TikZ/pgfplots diagrams, which the orchestrator
could iterate on directly.

## Commit

Paper changes (`main.tex`, `README.md`) committed atomically to branch `recon`. The
built `main.pdf` stays gitignored.
