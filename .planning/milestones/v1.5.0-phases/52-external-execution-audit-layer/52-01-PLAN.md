# Phase 52 Plan: External Execution Audit Layer

## Scope

Add an audit-only helper that wraps an externally executed model call with Lattice receipt, sidecar, and replay artifacts.

## Tasks

### 52-01: External Audit Helper

- Add `audit/external-execution.ts`.
- Compute input hashes, output hash, contract hash, raw envelope hashes, and lineage Merkle root.
- Build a signed receipt through `createReceipt`.
- Build a compatible `lattice-sidecar/v1` sidecar with external raw metadata.
- Build a replay envelope with a synthetic completed plan.

### 52-02: Public Exports and Tests

- Export helper and types through `audit.ts` and root exports.
- Add tests for receipt verification, sidecar contents, replay, and v1.2 receipt compatibility.
- Add type-surface coverage for the audit subpath.

### 52-03: Verification and GSD Closure

- Run package tests/typechecks/boundary checks.
- Mark AUD-01..AUD-05 complete and advance state to Phase 53.
