---
id: 55-01
phase: 55
name: Compatibility and Dogfood Evidence
wave: 1
depends_on: []
files_modified:
  - scripts/check-lattice-node20-modular.mjs
  - package.json
  - packages/lattice/test/gitfly-dogfood.test.ts
  - examples/external-consumer/index.mjs
  - docs/modular-entrypoints.md
requirements:
  - COMP-01
  - COMP-02
  - DOG-01
  - DOG-02
  - DOG-03
  - DOG-04
requirements_addressed:
  - COMP-01
  - COMP-02
  - DOG-01
  - DOG-02
  - DOG-03
  - DOG-04
autonomous: true
---

# Plan 55-01: Compatibility and Dogfood Evidence

<objective>
Add executable compatibility and dogfood evidence proving v1.5.0 modular adoption works incrementally for external consumers without promising Node 20 support for the full runtime.
</objective>

<requirements>
- COMP-01: Node 20 compatibility is tested for every modular layer that does not require Node 24-only APIs.
- COMP-02: Node 24 remains the supported full-runtime baseline where required APIs cannot be polyfilled or safely supported on Node 20.
- DOG-01: A GitFly-style dogfood scenario proves provider-only native tools and structured outputs do not regress compared with Vercel AI SDK-style flows.
- DOG-02: A GitFly-style dogfood scenario proves Lattice audit/receipts/replay can wrap an external executor behind a feature flag.
- DOG-03: A generic external-consumer example proves module-by-module adoption for at least two independent slices.
- DOG-04: Documentation explains recommended adoption paths: provider-only, audit-only, context/artifact-only, routing advisory, MCP/tools-only, eval-only, and full runtime.
</requirements>

<threat_model>
- T-55-01: Node 20 compatibility is asserted but not executed. Mitigation: add a script that spawns a real Node 20 binary and imports built node20-compatible facades.
- T-55-02: Full runtime accidentally appears Node 20-compatible. Mitigation: assert root engine remains `>=24` and `./agents` metadata remains `node24-runtime`.
- T-55-03: Dogfood tests duplicate adapter unit tests without host-owned execution shape. Mitigation: call provider adapters directly and avoid `createAI()`/`runAgent()` in the GitFly-style provider fixture.
- T-55-04: Audit dogfood lacks feature-flag realism. Mitigation: include feature-flag metadata and verify it appears in sidecar/replay metadata.
- T-55-05: Examples silently import workspace source. Mitigation: import built `packages/lattice/dist/*` subpaths.
</threat_model>

<must_haves>
- `scripts/check-lattice-node20-modular.mjs` spawns Node 20 and imports every `node20-compatible` built facade.
- Root scripts include `check:node20-modules`.
- GitFly-style provider dogfood verifies native tool request shaping, structured output materialization, and returned tool-call validation without using runtime/agent APIs.
- GitFly-style audit dogfood verifies signed receipt, sidecar metadata, replay envelope, and feature-flag metadata.
- `examples/external-consumer/index.mjs` runs after build and demonstrates at least two independent modular slices.
- `docs/modular-entrypoints.md` covers all required adoption paths and validation commands.
</must_haves>

<tasks>
<task id="55-01-01">
<title>Add Node 20 modular compatibility smoke</title>
<read_first>
- `packages/lattice/package.json`
- `scripts/check-lattice-module-boundaries.mjs`
- `docs/modular-entrypoints.md`
</read_first>
<action>
Create `scripts/check-lattice-node20-modular.mjs` and add a root `check:node20-modules` script that builds Lattice then runs the checker. The checker must locate Node 20, assert the child process is actually Node 20, import all built facades labelled `node20-compatible`, check representative exports, and assert `./agents` remains `node24-runtime` while root engines remain `>=24`.
</action>
<acceptance_criteria>
- `scripts/check-lattice-node20-modular.mjs` contains `NODE20_BIN`.
- `scripts/check-lattice-node20-modular.mjs` imports `dist/tools.js`.
- `package.json` contains `check:node20-modules`.
- `node scripts/check-lattice-node20-modular.mjs` exits 0 after build.
</acceptance_criteria>
</task>

<task id="55-01-02">
<title>Add GitFly-style dogfood fixtures</title>
<read_first>
- `packages/lattice/src/providers/adapters.test.ts`
- `packages/lattice/src/audit/external-execution.test.ts`
- `packages/lattice/src/providers/adapters.ts`
- `packages/lattice/src/audit/external-execution.ts`
</read_first>
<action>
Create `packages/lattice/test/gitfly-dogfood.test.ts` with two scenarios:
1. Provider-only host-owned execution using `createOpenAICompatibleProvider` directly with a GitFly-style model ID, native tool declaration, tool choice, structured output schema, and returned tool-call validation.
2. External audit feature-flag wrapping using `createExternalExecutionAudit`, receipt verification, replay, raw request/response hashes, and metadata asserting the feature flag.
</action>
<acceptance_criteria>
- `packages/lattice/test/gitfly-dogfood.test.ts` contains `describe("GitFly-style dogfood"`.
- Provider scenario does not call `createAI` or `runAgent`.
- Audit scenario contains `featureFlag`.
- `pnpm --filter @full-self-browsing/lattice test -- gitfly-dogfood` exits 0.
</acceptance_criteria>
</task>

<task id="55-01-03">
<title>Add generic external-consumer example</title>
<read_first>
- `examples/v14-validation/index.mjs`
- `docs/modular-entrypoints.md`
- `packages/lattice/src/core/standalone.ts`
- `packages/lattice/src/tools/mcp-artifacts.ts`
</read_first>
<action>
Create `examples/external-consumer/index.mjs` importing built modular dist subpaths. Demonstrate core-only preparation, tools/MCP artifact conversion and validation, audit-only wrapping, and eval-only scoring. Add a root `example:external-consumer` script that builds first.
</action>
<acceptance_criteria>
- `examples/external-consumer/index.mjs` imports from `packages/lattice/dist/core.js`.
- `examples/external-consumer/index.mjs` imports from `packages/lattice/dist/tools.js`.
- `examples/external-consumer/index.mjs` prints `scenario=external-consumer`.
- `node examples/external-consumer/index.mjs` exits 0 after build.
</acceptance_criteria>
</task>

<task id="55-01-04">
<title>Document adoption paths and verify</title>
<read_first>
- `docs/modular-entrypoints.md`
- `package.json`
- `scripts/check-lattice-node20-modular.mjs`
- `examples/external-consumer/index.mjs`
</read_first>
<action>
Update `docs/modular-entrypoints.md` with explicit provider-only, audit-only, context/artifact-only, routing advisory, MCP/tools-only, eval-only, and full-runtime adoption paths plus validation commands. Run the full Phase 55 verification suite.
</action>
<acceptance_criteria>
- `docs/modular-entrypoints.md` contains `Eval-Only`.
- `docs/modular-entrypoints.md` contains `check:node20-modules`.
- `pnpm --filter @full-self-browsing/lattice typecheck` exits 0.
- `pnpm --filter @full-self-browsing/lattice test:types` exits 0.
- `pnpm --filter @full-self-browsing/lattice lint:packages` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Focused dogfood: `pnpm --filter @full-self-browsing/lattice test -- gitfly-dogfood`
- Build: `pnpm --filter @full-self-browsing/lattice build`
- Node 20 modular: `node scripts/check-lattice-node20-modular.mjs`
- External example: `node examples/external-consumer/index.mjs`
- Typecheck: `pnpm --filter @full-self-browsing/lattice typecheck`
- Type/public package: `pnpm --filter @full-self-browsing/lattice test:types`
- Package lint: `pnpm --filter @full-self-browsing/lattice lint:packages`
</verification>
