---
phase: 50-module-boundary-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/providers.ts
  - packages/lattice/src/audit.ts
  - packages/lattice/src/context.ts
  - packages/lattice/src/artifacts.ts
  - packages/lattice/src/routing.ts
  - packages/lattice/src/tools.ts
  - packages/lattice/src/storage.ts
  - packages/lattice/src/eval.ts
  - packages/lattice/src/agents.ts
  - packages/lattice/src/core.ts
  - packages/lattice/tsdown.config.ts
  - packages/lattice/package.json
  - docs/modular-entrypoints.md
  - scripts/check-lattice-module-boundaries.mjs
  - package.json
  - packages/lattice/test/modular-entrypoints.test.ts
  - packages/lattice/test-d/modular-entrypoints.test-d.ts
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04]
requirements_addressed: [MOD-01, MOD-02, MOD-03, MOD-04]
tags:
  - modularity
  - package-exports
  - boundaries
must_haves:
  truths:
    - "MOD-01: package subpaths identify provider, audit, context, artifact, routing, MCP/tools, storage, eval, agent, and core surfaces"
    - "MOD-02: docs and package metadata label each module node20-compatible, node24-runtime, or adapter-specific"
    - "MOD-03: provider-only and audit-only imports are usable without createAI or runAgent"
    - "MOD-04: boundary checks fail if provider-only, audit-only, or core-only entrypoints import src/agent or src/agent/crew"
    - "D-Public Module Shape: subpath files are thin explicit re-export facades and root exports stay backward compatible"
    - "D-Compatibility Labels: package metadata and docs are the source of truth for per-module compatibility"
    - "D-Boundary Enforcement: deterministic import graph scanning protects provider-only, audit-only, and core-only entrypoints"
    - "D-Adoption Framing: GitFly-style incremental use does not require initializing createAI or runAgent"
  artifacts:
    - path: "packages/lattice/package.json"
      provides: "subpath exports and lattice.modules compatibility metadata"
    - path: "docs/modular-entrypoints.md"
      provides: "human-readable modular adoption contract"
    - path: "scripts/check-lattice-module-boundaries.mjs"
      provides: "deterministic source/package boundary verification"
    - path: "packages/lattice/test/modular-entrypoints.test.ts"
      provides: "source-level modular entrypoint assertions"
    - path: "packages/lattice/test-d/modular-entrypoints.test-d.ts"
      provides: "built package subpath type smoke tests"
---

<objective>
Add the Phase 50 modular adoption contract: public package subpaths, compatibility metadata/docs, and boundary checks proving provider-only, audit-only, and core-only paths do not pull in agent runtime code.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-module-boundary-contract/50-CONTEXT.md
@.planning/phases/50-module-boundary-contract/50-RESEARCH.md
@.planning/phases/50-module-boundary-contract/50-PATTERNS.md
@.planning/phases/50-module-boundary-contract/50-VALIDATION.md
</context>

<threat_model>
No external network, auth, or persistence changes are introduced. The primary risk is a misleading package contract: subpaths could accidentally import agent runtime code or overpromise Node 20 compatibility. Mitigate with deterministic source import graph checks, package metadata tests, docs that keep the package-level Node 24 baseline explicit, and no broad root API removals.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add modular source facades and build entries</name>
  <files>packages/lattice/src/providers.ts packages/lattice/src/audit.ts packages/lattice/src/context.ts packages/lattice/src/artifacts.ts packages/lattice/src/routing.ts packages/lattice/src/tools.ts packages/lattice/src/storage.ts packages/lattice/src/eval.ts packages/lattice/src/agents.ts packages/lattice/src/core.ts packages/lattice/tsdown.config.ts</files>
  <read_first>
    - packages/lattice/src/index.ts
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/context/context-pack.ts
    - packages/lattice/src/routing/router.ts
    - packages/lattice/src/routing/catalog.ts
    - packages/lattice/src/tools/tools.ts
    - packages/lattice/src/storage/storage.ts
    - packages/lattice/src/agent/eval.ts
    - packages/lattice/tsdown.config.ts
  </read_first>
  <action>
    Create explicit subpath source entry files: `providers.ts`, `audit.ts`, `context.ts`, `artifacts.ts`, `routing.ts`, `tools.ts`, `storage.ts`, `eval.ts`, `agents.ts`, and `core.ts`. Each file must use named exports only. `providers.ts` must export provider factories, `collectStream`, provider contracts, quirk types, and capability negotiation helpers. `audit.ts` must export receipt/sign/verify/replay/materialization helpers and related audit types. `context.ts`, `artifacts.ts`, `routing.ts`, `tools.ts`, `storage.ts`, and `eval.ts` must expose their existing standalone helpers. `agents.ts` is the only subpath that may export `runAgent`, `runAgentCrew`, `defineAgent`, agent host, and agent infra helpers. `core.ts` must avoid `runtime/public-types.ts`, `runtime/create-ai.ts`, and `src/agent/**`; it should expose artifacts, context, outputs, contract/preflight/tripwire, routing/catalog primitives, policy types, provider contract types, storage contract types, and result error helpers. Update `packages/lattice/tsdown.config.ts` entry array to include all new source entry files.
  </action>
  <acceptance_criteria>
    - `packages/lattice/src/providers.ts` exists and contains `createOpenAIProvider`
    - `packages/lattice/src/audit.ts` exists and contains `createReceipt`
    - `packages/lattice/src/core.ts` exists and does not contain `./agent/`
    - `packages/lattice/tsdown.config.ts` contains `src/providers.ts`
    - `packages/lattice/tsdown.config.ts` contains `src/core.ts`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add package exports, metadata, and adoption docs</name>
  <files>packages/lattice/package.json docs/modular-entrypoints.md</files>
  <read_first>
    - packages/lattice/package.json
    - .planning/phases/50-module-boundary-contract/50-CONTEXT.md
    - .planning/phases/50-module-boundary-contract/50-RESEARCH.md
  </read_first>
  <action>
    Add package `exports` entries for `./providers`, `./audit`, `./context`, `./artifacts`, `./routing`, `./tools`, `./storage`, `./eval`, `./agents`, and `./core`, each with `types`, `import`, and `default` pointing to the matching `dist/<name>.d.ts` and `dist/<name>.js` files. Add a `lattice.modules` object where every subpath has `compatibility` set to one of `node20-compatible`, `node24-runtime`, or `adapter-specific`, plus a concise `description`. Create `docs/modular-entrypoints.md` with a module table, compatibility labels, package-level Node `>=24` caveat, and provider-only, audit-only, and core-only import examples that do not call `createAI()` or `runAgent()`.
  </action>
  <acceptance_criteria>
    - `packages/lattice/package.json` contains `"./providers"`
    - `packages/lattice/package.json` contains `"./core"`
    - `packages/lattice/package.json` contains `"lattice"`
    - `docs/modular-entrypoints.md` contains `node20-compatible`
    - `docs/modular-entrypoints.md` contains `Provider-only`
    - `docs/modular-entrypoints.md` contains `does not lower the package-level engine`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-lattice-module-boundaries.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add deterministic boundary check script</name>
  <files>scripts/check-lattice-module-boundaries.mjs package.json packages/lattice/package.json</files>
  <read_first>
    - scripts/check-core-package-boundary.mjs
    - packages/lattice/package.json
    - package.json
  </read_first>
  <action>
    Add `scripts/check-lattice-module-boundaries.mjs`. The script must verify that required subpaths exist in `packages/lattice/package.json`, each required subpath has `types`, `import`, and `default`, and each required subpath has a `lattice.modules` compatibility label. The script must recursively scan static relative imports/re-exports from `packages/lattice/src/providers.ts`, `packages/lattice/src/audit.ts`, and `packages/lattice/src/core.ts`, resolving `.ts` and `index.ts` files inside `packages/lattice/src`, and fail if any path under `packages/lattice/src/agent/` or `packages/lattice/src/agent/crew/` is reached. Wire the script into root `package.json` as `check:module-boundaries` and into `packages/lattice/package.json` `lint:packages` before existing package lint checks.
  </action>
  <acceptance_criteria>
    - `scripts/check-lattice-module-boundaries.mjs` exists
    - `package.json` contains `check:module-boundaries`
    - `packages/lattice/package.json` `lint:packages` contains `check-lattice-module-boundaries.mjs`
    - `node scripts/check-lattice-module-boundaries.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-lattice-module-boundaries.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Protect source and package public surfaces</name>
  <files>packages/lattice/test/modular-entrypoints.test.ts packages/lattice/test-d/modular-entrypoints.test-d.ts</files>
  <read_first>
    - packages/lattice/test/public-surface.test.ts
    - packages/lattice/test-d/package-types.test-d.ts
    - packages/lattice/package.json
  </read_first>
  <action>
    Add Vitest coverage in `packages/lattice/test/modular-entrypoints.test.ts` that imports representative source subpaths, asserts their key values are functions, verifies every required package export and `lattice.modules` compatibility label exists, and checks `providers`, `audit`, and `core` are not classified as agent surfaces. Add `packages/lattice/test-d/modular-entrypoints.test-d.ts` with `tsd` smoke imports from every package subpath and representative `expectType` assertions for provider, audit, core, storage, tools, eval, and agents types/values.
  </action>
  <acceptance_criteria>
    - `packages/lattice/test/modular-entrypoints.test.ts` contains `REQUIRED_MODULES`
    - `packages/lattice/test-d/modular-entrypoints.test-d.ts` imports `@full-self-browsing/lattice/providers`
    - `packages/lattice/test-d/modular-entrypoints.test-d.ts` imports `@full-self-browsing/lattice/core`
    - `pnpm --filter @full-self-browsing/lattice test -- modular` exits 0
    - `pnpm --filter @full-self-browsing/lattice test:types` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- modular</automated>
    <automated>pnpm --filter @full-self-browsing/lattice test:types</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
node scripts/check-lattice-module-boundaries.mjs
pnpm --filter @full-self-browsing/lattice test -- modular
pnpm --filter @full-self-browsing/lattice typecheck
pnpm --filter @full-self-browsing/lattice test:types
pnpm --filter @full-self-browsing/lattice lint:packages
```
</verification>

<success_criteria>
- Required package subpaths exist and build to JS/declaration outputs.
- Provider-only, audit-only, and core-only source entrypoints do not transitively import agent runtime or crew modules.
- Compatibility labels are present in package metadata and human docs.
- Source and built package tests protect the modular entrypoint contract.
- Existing root public exports remain backward compatible.
</success_criteria>
