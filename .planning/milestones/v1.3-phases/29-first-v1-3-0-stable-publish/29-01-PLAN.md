---
phase: 29-first-v1-3-0-stable-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .github/workflows/release.yml
  - scripts/extract-release-notes.mjs
autonomous: true
requirements:
  - PUB-04
tags:
  - release-readiness
  - docs
  - github-actions
must_haves:
  truths:
    - "README.md reflects the full v1.3 stable surface: Phases 24-28 and 33-39 complete, Phases 30-32 still canary/audit follow-up"
    - ".github/workflows/release.yml creates GitHub Release v1.3.0 using changelog-derived notes, not only generic generated release notes"
    - "Release workflow keeps root permissions contents: read, id-token: write only in publish job, environment: npm-publish, and no NODE_AUTH_TOKEN/NPM_TOKEN"
  artifacts:
    - path: "README.md"
      provides: "stable v1.3 public status and provenance copy"
      contains: "v1.3.0"
    - path: "scripts/extract-release-notes.mjs"
      provides: "deterministic changelog-section extraction for GitHub Release body"
      contains: "Usage: node scripts/extract-release-notes.mjs <version> <output>"
    - path: ".github/workflows/release.yml"
      provides: "tag publish job with changelog-derived release body"
      contains: "body_path: .release-notes.md"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "packages/lattice/CHANGELOG.md"
      via: "scripts/extract-release-notes.mjs"
      pattern: "Extract changelog release notes"
---

<objective>
Prepare the public release surfaces before the stable version PR: refresh README's v1.3 status so it no longer describes Phases 35-39 as future work, and harden `release.yml` so GitHub Release `v1.3.0` uses changelog-derived notes from Changesets output.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-first-v1-3-0-stable-publish/29-CONTEXT.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-RESEARCH.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-PATTERNS.md
@.planning/phases/29-first-v1-3-0-stable-publish/29-VALIDATION.md
@.github/workflows/release.yml
@README.md
@packages/lattice/CHANGELOG.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-29-01: OIDC blast radius widens if id-token: write escapes the publish job | high | Preserve root `permissions: contents: read`; keep `id-token: write` only under `jobs.publish.permissions`; run `node scripts/check-workflow-safety.mjs` |
| T-29-02: token fallback bypasses Trusted Publisher and provenance | high | Verify `release.yml` contains no `NODE_AUTH_TOKEN` or `NPM_TOKEN`; publish remains OIDC-only |
| T-29-06: GitHub Release notes omit package changelog content | medium | Add deterministic changelog extractor and pass `.release-notes.md` as the release body |
| T-29-08: README advertises stale v1.3 scope at stable cut | medium | Replace stale Phase 33-34-only and multi-agent-out-of-scope language with the completed Phase 33-39 surface |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README stable-release status</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-08-SUMMARY.md
    - .changeset/v1.3.0-capability-registry.md
    - .changeset/v1.3.0-adapter-quirks-negotiation.md
    - .changeset/v1.3.0-prompt-scaffolds.md
    - .changeset/v1.3.0-output-sanitizers.md
    - .changeset/v1.3.0-tool-call-validation.md
    - .changeset/v1.3.0-receipt-v12-model-class.md
    - .changeset/v1.3.0-agent-crew.md
  </read_first>
  <action>
    Update README.md with concrete stable v1.3 release text:
    1. Replace the top version badge text `version-1.3.0--rc.0` with `version-1.3.0`.
    2. Replace the status block beginning with `> **Status:** v1.3.0-rc.0 is live on npm` so it says:
       `> **Status:** v1.3.0 is the first stable public release under @full-self-browsing. The release pipeline track (Phases 24-28), model-aware SDK track (Phases 33-38), and opt-in multi-agent crew surface (Phase 39) are complete. Stable publish is handled by npm OIDC Trusted Publisher with provenance; canary validation and cross-repo dispatch continue in Phases 30-32.`
    3. Replace the "Model-aware SDK (Phases 33 to 34, shipped)" heading and table with "Model-aware SDK + multi-agent surface (Phases 33 to 39, shipped)" and include rows for Phases 33, 34, 35, 36, 37, 38, and 39. The Phase 39 row must contain `defineAgent`, `runAgentCrew`, `createRateLimitGroup`, and `parentReceiptCid`.
    4. In "What is still coming in v1.3", remove Phase 35-39 rows and leave only Phases 30-32. Keep Phase 29 described as stable publish if this README is edited before the publish lands.
    5. Replace any remaining README sentence that says multi-agent crews remain out of scope with wording that crews are first-class via the opt-in `AgentHost`/`runAgentCrew` surface.
    6. Update the release provenance paragraph so it states stable `1.3.0` will be verified with `npm view @full-self-browsing/lattice@1.3.0 --json | jq .dist` after Phase 29. Do not claim post-publish proof before it exists.
  </action>
  <verify>
    <automated>bash -c 'rg "version-1\\.3\\.0" README.md && rg "Phases 33 to 39" README.md && rg "runAgentCrew|createRateLimitGroup|parentReceiptCid" README.md && ! rg -i "multi-agent crews remain out of scope|Phase 35 \\| Prompt scaffolding helpers|Phase 39 \\| Multi-agent delegation surface \\(currently Out of Scope" README.md'</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `version-1.3.0`.
    - README.md contains `Model-aware SDK + multi-agent surface (Phases 33 to 39, shipped)`.
    - README.md contains `runAgentCrew`, `createRateLimitGroup`, and `parentReceiptCid`.
    - README.md no longer contains `multi-agent crews remain Out of Scope`.
    - README.md no longer lists Phases 35-39 as upcoming work.
  </acceptance_criteria>
  <done>README is stable-release ready without claiming post-publish proof prematurely.</done>
</task>

<task type="auto">
  <name>Task 2: Add changelog-derived GitHub Release notes</name>
  <files>scripts/extract-release-notes.mjs, .github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml
    - packages/lattice/CHANGELOG.md
    - packages/lattice-cli/CHANGELOG.md
    - .changeset/config.json
  </read_first>
  <action>
    Create `scripts/extract-release-notes.mjs` with this behavior:
    - Usage string exactly: `Usage: node scripts/extract-release-notes.mjs <version> <output>`.
    - Read `packages/lattice/CHANGELOG.md`.
    - Accept version argument with or without leading `v`; normalize `v1.3.0` to `1.3.0`.
    - Extract the markdown section beginning at a heading that matches `## [1.3.0]` and ending before the next `## [` heading.
    - Fail exit 1 with a clear stderr message if the section is missing or empty.
    - Write output file with heading `# @full-self-browsing/lattice v1.3.0`, the extracted section body, then a final section:
      `## Packages`
      `- https://www.npmjs.com/package/@full-self-browsing/lattice/v/1.3.0`
      `- https://www.npmjs.com/package/@full-self-browsing/lattice-cli/v/1.3.0`
    - Use only Node built-ins.

    Update `.github/workflows/release.yml` inside `jobs.publish.steps`:
    1. After `Publish to npm with provenance`, add:
       ```yaml
      - name: Extract changelog release notes
        run: node scripts/extract-release-notes.mjs "${GITHUB_REF_NAME}" .release-notes.md
       ```
    2. In `Create GitHub Release`, remove `generate_release_notes: true` and add:
       ```yaml
          body_path: .release-notes.md
       ```
       Keep `tag_name: ${{ github.ref_name }}` and `name: ${{ github.ref_name }}`.
    3. Preserve all existing permissions and OIDC comments.
  </action>
  <verify>
    <automated>bash -c 'node scripts/check-workflow-safety.mjs && node scripts/extract-release-notes.mjs 1.2.0 /tmp/lattice-release-notes-test.md && rg "@full-self-browsing/lattice v1\\.2\\.0|Packages" /tmp/lattice-release-notes-test.md && rg "Extract changelog release notes|body_path: \\.release-notes\\.md" .github/workflows/release.yml && ! rg "generate_release_notes: true" .github/workflows/release.yml && ! rg "NODE_AUTH_TOKEN|NPM_TOKEN" .github/workflows/release.yml'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/extract-release-notes.mjs` exists and contains the usage string.
    - `node scripts/extract-release-notes.mjs 1.2.0 /tmp/lattice-release-notes-test.md` exits 0 against the current changelog.
    - `.github/workflows/release.yml` contains `Extract changelog release notes`.
    - `.github/workflows/release.yml` contains `body_path: .release-notes.md`.
    - `.github/workflows/release.yml` no longer contains `generate_release_notes: true`.
    - `node scripts/check-workflow-safety.mjs` exits 0.
    - `.github/workflows/release.yml` contains no `NODE_AUTH_TOKEN` or `NPM_TOKEN`.
  </acceptance_criteria>
  <done>GitHub Release body is sourced from package changelog and npm links.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
node scripts/check-workflow-safety.mjs
node scripts/extract-release-notes.mjs 1.2.0 /tmp/lattice-release-notes-test.md
pnpm changeset status --verbose
```
</verification>

<success_criteria>
- README reflects stable v1.3 scope and completed Phase 33-39 surface.
- `release.yml` creates GitHub Release notes from changelog extraction.
- Workflow safety and token absence gates pass.
</success_criteria>

<output>
Create `.planning/phases/29-first-v1-3-0-stable-publish/29-01-SUMMARY.md` when done.
</output>
