---
phase: 34
plan: 04
type: execute
wave: 1
depends_on:
  - "34-01"
files_modified:
  - packages/lattice/src/providers/gemini.ts
  - packages/lattice/src/providers/gemini.test.ts
  - packages/lattice/src/providers/openrouter.ts
  - packages/lattice/src/providers/openrouter.test.ts
  - packages/lattice/test/__fixtures__/quirks/gemini-models-ok.json
  - packages/lattice/test/__fixtures__/quirks/gemini-models-401.json
  - packages/lattice/test/__fixtures__/quirks/gemini-models-503.json
  - packages/lattice/test/__fixtures__/quirks/openrouter-models-ok.json
  - packages/lattice/test/__fixtures__/quirks/openrouter-models-503.json
autonomous: true
requirements:
  - QUIRK-02
  - NEG-01
  - NEG-02
must_haves:
  truths:
    - "createGeminiProvider's return narrows to expose `quirks: GeminiQuirks` + `negotiateCapabilities(modelId)`"
    - "createOpenRouterProvider's return narrows to expose `quirks: OpenRouterQuirks` + `negotiateCapabilities(modelId)`"
    - "Gemini quirks populated per RESEARCH §Q6/§Q3: 5 universal + responseSchemaSupported + safetySettingsConfigurable + systemInstructionSupported"
    - "OpenRouter quirks populated per RESEARCH §Q6/§Q5: 5 universal + providerRoutingArraySupported + floorPricingHints + allowFallbacks"
    - "Gemini negotiate(): /v1beta/models GET with x-goog-api-key header (NOT query-param); MEDIUM-THICK derivation -- uses supportedGenerationMethods + thinking + inputTokenLimit to populate supports.* from upstream; falls through to registry for missing facts"
    - "OpenRouter negotiate(): /api/v1/models GET (NO auth header); rich response per RESEARCH §Q5; supports OpenRouter id suffix-strip via stripOpenRouterVariant from Phase 33; intersects with registry"
    - "ANCHOR CASE STUDY (session_1780792387779) PASSES: negotiateCapabilities('openai/gpt-oss-120b:free') resolves to result.knownFailureModes.includes('internal_envelope_leak') AND result.recommendedSanitizers.includes('unwrapInternalEnvelope') AND result.source === 'live'"
    - "Per-instance TTL cache + inflight coalescing + retry + auth + fallback + event patterns mirror Plan 34-02 (no duplication)"
  artifacts:
    - path: "packages/lattice/src/providers/gemini.ts"
      provides: "Extended createGeminiProvider with quirks + negotiateCapabilities"
    - path: "packages/lattice/src/providers/openrouter.ts"
      provides: "Extended createOpenRouterProvider with quirks + negotiateCapabilities including anchor case study handling"
    - path: "packages/lattice/src/providers/gemini.test.ts"
      provides: "Gemini quirks + negotiate tests"
      min_lines: 200
    - path: "packages/lattice/src/providers/openrouter.test.ts"
      provides: "OpenRouter quirks + negotiate tests INCLUDING the anchor case study assertion"
      min_lines: 200
    - path: "packages/lattice/test/__fixtures__/quirks/gemini-models-ok.json"
      provides: "Verified Gemini /v1beta/models response with supportedGenerationMethods + thinking + inputTokenLimit"
    - path: "packages/lattice/test/__fixtures__/quirks/openrouter-models-ok.json"
      provides: "OpenRouter /api/v1/models response containing openai/gpt-oss-120b:free for the anchor case study assertion"
  key_links:
    - from: "packages/lattice/src/providers/gemini.ts"
      to: "Gemini /v1beta/models endpoint"
      via: "HTTP GET with x-goog-api-key header (preferred over ?key= query param)"
      pattern: "/v1beta/models"
    - from: "packages/lattice/src/providers/openrouter.ts"
      to: "OpenRouter /api/v1/models endpoint"
      via: "HTTP GET with NO auth header (unauthenticated public surface)"
      pattern: "/api/v1/models"
    - from: "packages/lattice/src/providers/openrouter.ts"
      to: "packages/lattice/src/capabilities/lookup.ts"
      via: "stripOpenRouterVariant + getCapabilityProfile for variant-suffix handling"
      pattern: "stripOpenRouterVariant|getCapabilityProfile"
    - from: "Both factories"
      to: "packages/lattice/src/capabilities/sanitizer-recommendations.ts"
      via: "getRecommendedSanitizers"
      pattern: "getRecommendedSanitizers"
---

<objective>
Wire Phase 34's contract surface onto Gemini and OpenRouter -- the two adapters with the richest /models responses among the non-Anthropic set. Gemini's `/v1beta/models` exposes `supportedGenerationMethods`, `thinking`, and per-model token limits; OpenRouter's `/api/v1/models` exposes the rich Phase 33 schema (canonical_slug, top_provider, supported_parameters, pricing). Both adapters do MEDIUM-THICK derivation -- they consume upstream truth where structured and fall back to registry for the rest.

Purpose: This plan owns the ANCHOR CASE STUDY (`session_1780792387779`) -- the regression bar for the entire Phase 34 milestone. OpenRouter's negotiate() must resolve `openai/gpt-oss-120b:free` to `recommendedSanitizers: ["unwrapInternalEnvelope"]`. This proves the full flow: /models live fetch -> id suffix-strip via Phase 33's `stripOpenRouterVariant` -> registry intersection -> `getRecommendedSanitizers` derivation. If this fails, the Phase 34 contract is broken.

Output: Extended `gemini.ts` + `openrouter.ts`; 5 frozen fixtures (3 Gemini + 2 OpenRouter); extended test files; the anchor case study assertion. NO changes to OpenAI / OpenAI-compat / xAI / Anthropic / LM Studio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-01-PLAN.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-02-PLAN.md
@.planning/phases/33-model-capability-registry-200-via-openrouter-feed/33-SUMMARY.md

@packages/lattice/src/providers/gemini.ts
@packages/lattice/src/providers/gemini.test.ts
@packages/lattice/src/providers/openrouter.ts
@packages/lattice/src/providers/openrouter.test.ts
@packages/lattice/src/providers/quirks.ts
@packages/lattice/src/capabilities/negotiate.ts
@packages/lattice/src/capabilities/lookup.ts
@packages/lattice/src/capabilities/registry.generated.ts

<interfaces>
<!-- Gemini /v1beta/models shape (RESEARCH §Q3 live-verified) -->

```json
{
  "models": [
    {
      "name": "models/gemini-2.0-flash",
      "baseModelId": "gemini-2.0-flash",
      "version": "2.0",
      "displayName": "Gemini 2.0 Flash",
      "description": "...",
      "inputTokenLimit": 1000000,
      "outputTokenLimit": 4096,
      "supportedGenerationMethods": ["generateContent"],
      "thinking": false,
      "temperature": 1.0,
      "maxTemperature": 2.0,
      "topP": 0.95,
      "topK": 40
    }
  ],
  "nextPageToken": "..."
}
```

Auth: `x-goog-api-key: $GEMINI_API_KEY` (header form preferred over `?key=` -- RESEARCH §Q3 caveat).

<!-- OpenRouter /api/v1/models shape (RESEARCH §Q5 live-verified -- rich shape) -->

```json
{
  "data": [
    {
      "id": "openai/gpt-oss-120b:free",
      "canonical_slug": "openai/gpt-oss-120b-20251231",
      "context_length": 131072,
      "architecture": { "modality": "text->text", "tokenizer": "OpenAI" },
      "pricing": { "prompt": "0", "completion": "0" },
      "top_provider": {
        "context_length": 131072,
        "max_completion_tokens": 32768,
        "is_moderated": false
      },
      "supported_parameters": ["tools", "tool_choice", "max_tokens", "..."],
      "default_parameters": { ... }
    }
  ]
}
```

NO auth header required. The endpoint is the same one Phase 33's refresh script uses.

<!-- Phase 34-01 imports -->

```typescript
// From packages/lattice/src/providers/quirks.ts (Plan 34-01)
export interface GeminiQuirks extends AdapterQuirks {
  readonly responseSchemaSupported: boolean;       // CITED: Gemini API responseSchema
  readonly safetySettingsConfigurable: boolean;    // verified gemini.ts:50-55 BLOCK_NONE 4-category
  readonly systemInstructionSupported: boolean;    // gemini-1.5+
}

export interface OpenRouterQuirks extends AdapterQuirks {
  readonly providerRoutingArraySupported: boolean; // CITED openrouter.ai/docs provider routing
  readonly floorPricingHints: boolean;             // CITED max_price / sort options
  readonly allowFallbacks: boolean;                // CITED allow_fallbacks boolean
}
```

```typescript
// From packages/lattice/src/capabilities/lookup.ts (Phase 33)
// CRITICAL FOR ANCHOR CASE STUDY:
export function stripOpenRouterVariant(id: string): string;
// "openai/gpt-oss-120b:free" -> "openai/gpt-oss-120b"
// "openrouter:openai/gpt-oss-120b" canonical key form
```

<!-- Phase 33 registry contains openrouter:openai/gpt-oss-120b with: -->
//   trainingClass: "open_weight_instruct"
//   knownFailureModes: ["internal_envelope_leak", "system_prompt_echo", "malformed_tool_arguments"]
// Verified in Phase 33 SUMMARY anchor case study section.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gemini quirks + negotiate() with medium-thick derivation + fixtures + tests</name>
  <files>
    packages/lattice/src/providers/gemini.ts,
    packages/lattice/src/providers/gemini.test.ts,
    packages/lattice/test/__fixtures__/quirks/gemini-models-ok.json,
    packages/lattice/test/__fixtures__/quirks/gemini-models-401.json,
    packages/lattice/test/__fixtures__/quirks/gemini-models-503.json
  </files>
  <read_first>
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 314-319 -- GeminiQuirks vocabulary; lines 715-740 -- Q3 Gemini shape verbatim; line 738 -- header preference over query-param caveat),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (D-05..D-12),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-02-PLAN.md (Anthropic closure pattern -- mirror exactly),
    packages/lattice/src/providers/gemini.ts (full -- existing closure shape; note the existing query-param auth at line 95 per RESEARCH that should be migrated to header form),
    packages/lattice/src/providers/gemini.test.ts (full -- makeFakeFetch pattern at lines 18-30),
    packages/lattice/src/capabilities/registry.static.ts (gemini:gemini-2.5-pro is the supplemental profile for fallback)
  </read_first>
  <behavior>
    - Test 1: factory return narrows to `quirks: GeminiQuirks` + `negotiateCapabilities`.
    - Test 2: quirks block populated: `{ supportsToolChoice: true, parallelToolCalls: true, structuredOutputs: true, responseFormatHonored: true, streamingDiverges: false, responseSchemaSupported: true, safetySettingsConfigurable: true, systemInstructionSupported: true }` per RESEARCH §Q6 Gemini vocabulary.
    - Test 3 (happy path with medium-thick derivation): mocked fetch returns gemini-models-ok.json. `negotiateCapabilities("gemini-2.0-flash")` resolves with:
      - `source: "live"`
      - `modelId: "gemini-2.0-flash"`
      - `contextWindow: 1000000` (FROM /models inputTokenLimit -- THICK derivation; this is upstream truth)
      - `supports.nativeToolCalling: true` (Gemini supports tools per generateContent method)
      - `supports.streaming: true` (Gemini supports streamGenerateContent; in the test fixture, ensure supportedGenerationMethods includes "streamGenerateContent" -- planner: add it to the fixture)
      - `supports.structuredOutputs: true` (Gemini-1.5+ supports responseSchema -- per quirks block which is the adapter posture)
      - `supports.parallelToolCalls: true`
      - `supports.extendedThinking: false` (the fixture sets thinking: false for gemini-2.0-flash; gemini-2.5-pro has thinking: true)
      - `knownFailureModes`: from registry profile (gemini-2.0-flash registry profile -- check what Phase 33 has; if empty, supports.extendedThinking is the only test signal)
      - `recommendedSanitizers: []` (assuming frontier_rlhf)
    - Test 4 (thinking: true model): mocked fetch returns a Gemini model with `thinking: true`. `negotiateCapabilities` returns `supports.extendedThinking: true` -- derived directly from upstream.
    - Test 5 (401 auth): NegotiationAuthError with `adapter: "gemini"`.
    - Test 6 (503 fallback): registry-fallback + event.
    - Test 7 (cache + inflight + retry): mirror Plan 34-02 patterns.
    - Test 8 (header auth verification): the fetch is called with `x-goog-api-key` HEADER (not `?key=` query-string). Verifiable via capture.urls (no `?key=` in URL) and capture.inits.headers (has `x-goog-api-key`).
    - Test 9 (lenient parse): missing `thinking` field doesn't crash; `supports.extendedThinking` falls through to false.
  </behavior>
  <action>
    1. **Read packages/lattice/src/providers/gemini.ts** to understand the existing closure structure. Note the line 95 reference in RESEARCH -- the existing `execute` path may use `?key=` query-param auth. Phase 34 NEGOTIATE uses HEADER auth (preferred per RESEARCH §Q3); the EXISTING execute() path is NOT changed by Phase 34 (Phase 34 is purely additive; execute() migration to header auth is out-of-scope for Phase 34 -- can be a separate future PR).
    2. **Mirror Plan 34-02 closure pattern** in `createGeminiProvider`:
       - Add `modelsCacheTtlMs?`, `modelsRetryCount?`, `runEventSink?` to `GeminiProviderOptions`
       - Add closure state: `cache`, `inflight`
       - Add `async function negotiate(modelId)` -- standard pattern
       - Add `async function fetchAndNegotiate(modelId)`: URL `${baseUrl}/v1beta/models`; headers `{ "x-goog-api-key": options.apiKey, "accept": "application/json" }`; retry [0, 200, 1000]; 401/403 NegotiationAuthError; transient fallback. NOTE the URL path differs from OpenAI -- `/v1beta/` not `/v1/`. Document in JSDoc.
       - Add `function mergeGeminiModelsWithRegistry(modelId, body)`:
         - LENIENT-PARSE body: `const models = body?.models;` (Gemini uses `models` key, not `data` -- different from OpenAI/Anthropic/OpenRouter)
         - `const found = Array.isArray(models) ? models.find?.((m: any) => m?.name === `models/${modelId}` || m?.baseModelId === modelId || m?.name === modelId) : undefined;` (Gemini's name is `models/${id}` prefix; lenient match against multiple fields)
         - If not found: registry-fallback + event
         - If found:
           - `contextWindow = found.inputTokenLimit ?? (registryProfile?.contextWindow ?? 0)` (THICK -- prefer upstream)
           - `supports.extendedThinking = !!found.thinking` (THICK -- from upstream)
           - `supports.streaming = (Array.isArray(found.supportedGenerationMethods) && found.supportedGenerationMethods.includes("streamGenerateContent")) ?? true` (THICK)
           - `supports.nativeToolCalling = (Array.isArray(found.supportedGenerationMethods) && found.supportedGenerationMethods.includes("generateContent"))` (Gemini's tools surface)
           - `supports.structuredOutputs = registryProfile?.toolCallSurface === "native_strict" || true` (Gemini supports responseSchema on 1.5+; rely on quirks-block-style posture for adapter; per-model truth lives in registry)
           - `supports.parallelToolCalls = true` (Gemini supports)
           - `knownFailureModes = registryProfile?.knownFailureModes ?? []`
           - `recommendedSanitizers = getRecommendedSanitizers(knownFailureModes)`
           - `source = "live"`
       - Add `emitFallbackEvent` + `stringifyErr` -- identical pattern.
    3. **Add quirks block** per Test 2.
    4. **Narrow factory return**: `createGeminiProvider(options): ProviderAdapter & { readonly quirks: GeminiQuirks; readonly negotiateCapabilities: ... }`.
    5. **Create 3 fixtures**:
       - gemini-models-ok.json: VERBATIM RESEARCH §Q3 shape with 3 models:
         - `models/gemini-2.0-flash`: `inputTokenLimit: 1000000`, `supportedGenerationMethods: ["generateContent", "streamGenerateContent", "countTokens"]`, `thinking: false`
         - `models/gemini-2.5-pro`: `inputTokenLimit: 1000000`, `supportedGenerationMethods: ["generateContent", "streamGenerateContent", "countTokens"]`, `thinking: true`
         - `models/gemini-1.5-flash-001`: legacy model; `inputTokenLimit: 32768`, `supportedGenerationMethods: ["generateContent"]`, NO `thinking` field
       - gemini-models-401.json: realistic Gemini error shape `{ "error": { "code": 401, "message": "API key not valid", "status": "UNAUTHENTICATED" } }`
       - gemini-models-503.json: `{ "error": { "code": 503, "message": "The service is currently unavailable", "status": "UNAVAILABLE" } }`
    6. **Extend gemini.test.ts** with `describe("Phase 34: Gemini quirks + negotiateCapabilities", () => { ... })` covering Tests 1-9.
    7. For Test 8 (header auth verification): assert `capture.urls[0].includes("?key=")` is FALSE; `capture.inits[0].headers["x-goog-api-key"]` is the apiKey.
    8. Run typecheck + tests.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test gemini.test</automated>
  </verify>
  <acceptance_criteria>
    - Gemini factory return narrows to expose quirks + negotiateCapabilities as required fields.
    - Quirks block matches Test 2 verbatim.
    - 3 fixtures created.
    - Medium-thick derivation: `inputTokenLimit`, `thinking`, `supportedGenerationMethods` flow from upstream to NegotiatedCapabilities.supports.*. Verifiable via Tests 3 + 4.
    - Header auth (`x-goog-api-key`) NOT query-param -- verified in Test 8.
    - Closure structure mirrors Plan 34-02 (cache, inflight with .finally, retry, auth-throw, transient-fallback, event-emit).
    - Auth error throws with `adapter: "gemini"`.
    - All 9 tests pass; existing gemini.test.ts tests continue to pass (backward compat).
  </acceptance_criteria>
  <done>Gemini factory ships quirks + medium-thick negotiate() with upstream-truth derivation for context window + thinking + streaming. Plan 34-04 Task 2 is the OpenRouter anchor case study.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenRouter quirks + negotiate() + ANCHOR CASE STUDY assertion + fixtures + tests</name>
  <files>
    packages/lattice/src/providers/openrouter.ts,
    packages/lattice/src/providers/openrouter.test.ts,
    packages/lattice/test/__fixtures__/quirks/openrouter-models-ok.json,
    packages/lattice/test/__fixtures__/quirks/openrouter-models-503.json
  </files>
  <read_first>
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 329-333 -- OpenRouterQuirks vocabulary; lines 767-793 -- Q5 OpenRouter shape + advisory; lines 534-535 -- Anti-pattern "Sending Authorization Bearer to OpenRouter /models"; lines 952-983 -- ANCHOR CASE STUDY test pattern verbatim),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (entire <specifics> section -- anchor case study verification protocol),
    .planning/phases/33-model-capability-registry-200-via-openrouter-feed/33-SUMMARY.md (lines 60-94 -- anchor case study verification + the registry profile for openrouter:openai/gpt-oss-120b),
    packages/lattice/src/providers/openrouter.ts (full -- existing closure shape; ALSO existing usage in Phase 33's refresh-model-registry.mjs of the same endpoint),
    packages/lattice/src/providers/openrouter.test.ts (full -- makeFakeFetch),
    packages/lattice/src/capabilities/lookup.ts (stripOpenRouterVariant + getCapabilityProfile + findCapabilityProfile),
    packages/lattice/src/capabilities/registry.generated.ts (search for "openai/gpt-oss-120b" -- verify the row exists with knownFailureModes including internal_envelope_leak),
    scripts/capabilities/__fixtures__/openrouter-models-snapshot.json (Phase 33's frozen fixture -- contains the gpt-oss-120b row; reuse for Phase 34 fixture)
  </read_first>
  <behavior>
    - Test 1: factory return narrows to `quirks: OpenRouterQuirks` + `negotiateCapabilities`.
    - Test 2: quirks block populated: `{ supportsToolChoice: true, parallelToolCalls: true, structuredOutputs: true, responseFormatHonored: true, streamingDiverges: false, providerRoutingArraySupported: true, floorPricingHints: true, allowFallbacks: true }` per RESEARCH §Q6 OpenRouter vocabulary.
    - Test 3 (NO auth header sent -- Anti-pattern check): fetch is called WITHOUT `Authorization: Bearer ...`. Verifiable via `capture.inits[0].headers` does NOT contain `Authorization`. Document the citation in JSDoc.
    - Test 4 (ANCHOR CASE STUDY -- session_1780792387779): mocked fetch returns openrouter-models-ok.json containing the openai/gpt-oss-120b:free row. `adapter.negotiateCapabilities("openai/gpt-oss-120b:free")` resolves with:
      - `result.modelId === "openai/gpt-oss-120b:free"` (preserves the input modelId verbatim)
      - `result.source === "live"`
      - `result.knownFailureModes.includes("internal_envelope_leak")` (from Phase 33 registry profile)
      - `result.recommendedSanitizers.includes("unwrapInternalEnvelope")` (from getRecommendedSanitizers mapping)
      - `result.contextWindow === 131072` (from /models top_provider.context_length)
    - Test 5 (variant suffix-strip): `negotiateCapabilities("openai/gpt-oss-120b")` (without `:free`) also resolves -- the canonical-stripped form maps to the same registry profile. Result: same shape as Test 4 except `result.modelId === "openai/gpt-oss-120b"`.
    - Test 6 (id NOT in /models response): mocked fetch returns 200 with only other models. `negotiateCapabilities("openai/gpt-oss-120b")` -- not found in body. Falls back to registry: `source: "registry-fallback"`. STILL contains the failure mode + sanitizer recommendation (the registry has the profile).
    - Test 7 (cache + inflight + retry): mirror Plan 34-02.
    - Test 8 (503 fallback): registry-fallback + event.
    - Test 9 (lenient parse): missing `top_provider.context_length` -- fall through to `context_length` per RESEARCH §Q5 / Pitfall 3.
  </behavior>
  <action>
    1. **Mirror Plan 34-02 closure pattern** in `createOpenRouterProvider`:
       - Add `modelsCacheTtlMs?`, `modelsRetryCount?`, `runEventSink?` to `OpenRouterProviderOptions`
       - Closure state: cache + inflight
       - `async function negotiate(modelId)` standard pattern
       - `async function fetchAndNegotiate(modelId)`: URL `${baseUrl}/api/v1/models` (NOTE: `/api/v1/models` -- different prefix from OpenAI's `/v1/models`); headers `{ "accept": "application/json" }` ONLY -- NO `Authorization` header per RESEARCH Anti-pattern. Document in inline comment. Retry [0, 200, 1000]; 401/403 still throws NegotiationAuthError (defensive -- even though endpoint is unauthenticated, OpenRouter MAY add auth later); transient fallback.
       - `function mergeOpenRouterModelsWithRegistry(modelId, body)`:
         - LENIENT-PARSE: `const rows = body?.data; const found = Array.isArray(rows) ? rows.find?.((m: any) => m?.id === modelId || m?.id === stripOpenRouterVariant(modelId)) : undefined;`
         - If not found in /models response: registry-fallback (NOT throw); event emit
         - If found:
           - `contextWindow = found.top_provider?.context_length ?? found.context_length ?? (registryProfile?.contextWindow ?? 0)` (Pitfall 3 / A1 precedence chain)
           - Build canonical registry key: `const canonicalKey = `openrouter:${stripOpenRouterVariant(modelId)}`;` `const registryProfile = getCapabilityProfile(canonicalKey);`
           - Populate `supports.*`:
             - `nativeToolCalling = Array.isArray(found.supported_parameters) && found.supported_parameters.includes("tools")` (THICK from upstream)
             - `structuredOutputs = Array.isArray(found.supported_parameters) && found.supported_parameters.includes("response_format")` (THICK)
             - `parallelToolCalls = Array.isArray(found.supported_parameters) && found.supported_parameters.includes("tool_choice")` (heuristic; assumes tool_choice implies parallel)
             - `extendedThinking = Array.isArray(found.supported_parameters) && (found.supported_parameters.includes("reasoning") || found.supported_parameters.includes("thinking"))` (some OpenRouter rows expose these)
             - `streaming = true` (OpenRouter supports streaming on virtually all models)
           - `knownFailureModes = registryProfile?.knownFailureModes ?? []` (from registry -- the ANCHOR CASE STUDY depends on this)
           - `recommendedSanitizers = getRecommendedSanitizers(knownFailureModes)` (THIS is what produces "unwrapInternalEnvelope" for gpt-oss-120b)
           - `modelId: modelId` (PRESERVE the input modelId verbatim per Test 4 -- if consumer passed `:free`, the result modelId has `:free`)
           - `source: "live"`
       - `emitFallbackEvent` + `stringifyErr` -- identical pattern.
    2. **Add quirks block** per Test 2.
    3. **Narrow factory return**: `createOpenRouterProvider(options): ProviderAdapter & { readonly quirks: OpenRouterQuirks; readonly negotiateCapabilities: ... }`.
    4. **Create openrouter-models-ok.json fixture**: REUSE Phase 33's `scripts/capabilities/__fixtures__/openrouter-models-snapshot.json` as a base -- it ALREADY contains the openai/gpt-oss-120b:free row used for Phase 33's anchor case study (verified per Phase 33 SUMMARY lines 60-94). Copy to `packages/lattice/test/__fixtures__/quirks/openrouter-models-ok.json`. Ensure the gpt-oss-120b row has:
       - `id: "openai/gpt-oss-120b:free"`
       - `top_provider.context_length: 131072`
       - `supported_parameters: ["tools", "tool_choice", "max_tokens"]`
       Also include 2 additional rows for variety: `anthropic/claude-3.5-sonnet` and `openai/gpt-4o` (frontier_rlhf models -- empty knownFailureModes per Phase 33 default).
    5. **Create openrouter-models-503.json**: `{ "error": { "message": "Service unavailable", "code": 503 } }` (OpenRouter's exact 503 shape isn't documented; this is realistic).
    6. **Extend openrouter.test.ts** with `describe("Phase 34: OpenRouter quirks + negotiateCapabilities", () => { ... })` covering Tests 1-9. Test 4 is the ANCHOR CASE STUDY -- name it explicitly: `it("anchor case study session_1780792387779: openai/gpt-oss-120b:free yields recommendedSanitizers: ['unwrapInternalEnvelope']", async () => { ... })`.
    7. For Test 3, assert `capture.inits[0].headers` does NOT contain the key `"Authorization"` (case-sensitive grep). Note: headers may be a Record or a Headers instance; handle both.
    8. Run typecheck + tests.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test openrouter.test</automated>
  </verify>
  <acceptance_criteria>
    - OpenRouter factory return narrows to expose quirks + negotiateCapabilities as required fields.
    - Quirks block matches Test 2 verbatim.
    - 2 fixtures created; openrouter-models-ok.json contains the openai/gpt-oss-120b:free row with the exact shape needed for the anchor case study.
    - The /models fetch sends NO `Authorization` header (Test 3 asserts).
    - ANCHOR CASE STUDY assertion PASSES: Test 4 explicitly named with session_1780792387779 reference; assertions verify `result.knownFailureModes.includes("internal_envelope_leak")` AND `result.recommendedSanitizers.includes("unwrapInternalEnvelope")` AND `result.source === "live"`.
    - Suffix-strip path (Test 5) resolves both `openai/gpt-oss-120b` and `openai/gpt-oss-120b:free` to the same registry profile -- the variant-stripped canonical key.
    - Lenient parsing: missing `top_provider.context_length` falls through to `context_length` per Pitfall 3 / A1 precedence chain.
    - Closure structure mirrors Plan 34-02 (cache + inflight + retry + auth-throw + transient-fallback + event).
    - All 9 tests pass + existing openrouter.test.ts tests pass.
    - `pnpm --filter @full-self-browsing/lattice typecheck` + `test:types` + `lint:packages` all green.
  </acceptance_criteria>
  <done>OpenRouter factory ships quirks + rich negotiate() with stripOpenRouterVariant integration. The anchor case study session_1780792387779 is verified end-to-end through the live-fetch + registry-intersection + sanitizer-derivation flow. This is the regression bar for the Phase 34 milestone.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer -> Gemini adapter | modelId untrusted; used as registry key + response-body filter (matched via multiple Gemini name forms). |
| Gemini adapter -> Gemini /v1beta/models | HTTPS GET with x-goog-api-key HEADER (preferred over query-param to avoid leaking the key in upstream logs). |
| consumer -> OpenRouter adapter | modelId untrusted; passed through stripOpenRouterVariant for registry lookup; used as response-body filter. |
| OpenRouter adapter -> OpenRouter /api/v1/models | HTTPS GET with NO auth header. Public-discovery surface verified Phase 33. |
| Both adapters -> RunEventSink | Same as Plans 02 and 03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Information Disclosure | apiKey leak via Gemini query-string auth (existing execute path uses ?key=) | accept (out-of-scope) | Phase 34 NEGOTIATE uses header auth (x-goog-api-key). EXISTING execute() may still use query-string -- migration is out-of-scope. Documented as a Phase 34 deferred follow-up. |
| T-34-04-02 | Information Disclosure | apiKey leak via fallback event payload | mitigate | Identical mitigation to T-34-02-01: stringifyErr returns err.message only. |
| T-34-04-03 | Tampering | OpenRouter's /api/v1/models is unauthenticated -- MITM could forge a response | accept | TLS gates this. The registry-intersection path (Test 6 fallback) provides a known-good baseline regardless of the live response. |
| T-34-04-04 | Tampering | OpenRouter suffix-strip via stripOpenRouterVariant -- could be confused by a maliciously-crafted modelId like "openai/foo:bar:baz" | accept | stripOpenRouterVariant is a Phase 33 verified utility; Phase 33 D-11 limits suffix-strip to `:free` and `:thinking` only; arbitrary suffixes pass through verbatim. No injection vector. |
| T-34-04-05 | Information Disclosure | NegotiationAuthError carries OpenRouter modelId (which may include `:free`) | accept | Same posture as T-34-02-04: non-sensitive. |
| T-34-04-06 | Denial of Service | OpenRouter /api/v1/models returns ~407 KB; under TTL pressure (modelsCacheTtlMs: 0) this is 407 KB per call | accept | Default 5-min TTL means cache absorbs most calls. Test verifies cache hit. If consumer disables cache (`modelsCacheTtlMs: 0` -- testing) they accept the bandwidth cost. |
| T-34-04-07 | Denial of Service | Inflight Map leak (Pitfall 4) inherited risk | mitigate | Both closures use `.finally`. Verifiable via grep. |
| T-34-04-08 | Tampering | RunEventKind union expansion -- inherited from Plan 34-01 | accept | Same posture as 34-01. |

</threat_model>

<verification>

Phase-wide checks beyond per-task automated commands:

1. **Wave 1 file-ownership: Plan 34-04 owns gemini.ts + openrouter.ts only**. Verify no overlap with Plans 02, 03, 05.
2. **ANCHOR CASE STUDY REGRESSION BAR**: Plan 34-04 Task 2 Test 4 MUST PASS. This is the Phase 34 milestone gate. If the registry profile in registry.generated.ts (or registry.static.ts) for `openrouter:openai/gpt-oss-120b` does NOT include `internal_envelope_leak` in `knownFailureModes`, Phase 33 has regressed and Phase 34 cannot proceed. Sub-check: `grep -E "openai/gpt-oss-120b" packages/lattice/src/capabilities/registry.generated.ts | head -3` should reveal the row with the failure mode.
3. **`pnpm --filter @full-self-browsing/lattice test` full suite passes** -- cross-adapter regression check.
4. **OpenRouter /api/v1/models call sends NO Authorization header** -- verifiable via Test 3.
5. **Gemini /v1beta/models call uses x-goog-api-key HEADER**, not `?key=` query-string -- verifiable via Test 8.
6. **`getRecommendedSanitizers` is the ONLY derivation path for `recommendedSanitizers` across both adapters**.

</verification>

<success_criteria>

Plan 34-04 is COMPLETE when:

- [ ] Gemini factory: quirks block (8 booleans); negotiate() with medium-thick derivation (inputTokenLimit + thinking + supportedGenerationMethods from upstream); cache + inflight + retry + auth-throw + transient-fallback + event; HEADER auth.
- [ ] OpenRouter factory: quirks block (8 booleans); negotiate() with rich /models intersection; NO auth header; uses stripOpenRouterVariant for canonical-key normalization.
- [ ] 5 fixtures created (3 Gemini + 2 OpenRouter).
- [ ] **ANCHOR CASE STUDY ASSERTION PASSES**: `it("anchor case study session_1780792387779: openai/gpt-oss-120b:free yields recommendedSanitizers: ['unwrapInternalEnvelope']", async () => { ... })` test resolves with:
   - `result.knownFailureModes.includes("internal_envelope_leak")` -> TRUE
   - `result.recommendedSanitizers.includes("unwrapInternalEnvelope")` -> TRUE
   - `result.source === "live"` -> TRUE
   - `result.contextWindow === 131072` -> TRUE
   - `result.modelId === "openai/gpt-oss-120b:free"` -> TRUE (preserves the input verbatim)
- [ ] Variant-strip path (Test 5) resolves both `openai/gpt-oss-120b` and `openai/gpt-oss-120b:free` to the same profile.
- [ ] All decisions D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 verified per adapter test.
- [ ] `pnpm --filter @full-self-browsing/lattice typecheck` + `test` + `test:types` + `lint:packages` all green.

</success_criteria>

<output>
After completion, create `.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-04-SUMMARY.md` documenting:
- Per-adapter quirks blocks with citation references
- Per-adapter negotiate() derivation depth (Gemini medium-thick; OpenRouter rich /models)
- Test count delta for gemini.test.ts and openrouter.test.ts
- **ANCHOR CASE STUDY VERIFICATION OUTPUT**: include the actual test output line showing the gpt-oss-120b assertion passing
- Note for Plan 34-05: "Plan 34-04 closed the 4 /models-capable adapters (Anthropic + OpenAI + Gemini + OpenRouter). Plan 34-05 ships LM Studio (no remote /models -- registry-only pattern like OpenAI-compat) + integration suite + changeset."
</output>
