---
phase: 34
plan: 05
type: execute
wave: 2
depends_on:
  - "34-02"
  - "34-03"
  - "34-04"
files_modified:
  - packages/lattice/src/providers/lm-studio.ts
  - packages/lattice/src/providers/lm-studio.test.ts
  - packages/lattice/test/capabilities-negotiate-integration.test.ts
  - .changeset/v1.3.0-adapter-quirks-negotiation.md
autonomous: true
requirements:
  - QUIRK-02
  - NEG-01
must_haves:
  truths:
    - "createLmStudioProvider's return narrows to expose `quirks: LmStudioQuirks` + `negotiateCapabilities(modelId)`"
    - "LM Studio quirks populated per RESEARCH §Q6/§Pattern 2: 5 universal + customChatTemplateRiskFlag + noAuthRequired"
    - "LM Studio negotiate() is REGISTRY-ONLY (no fetch, no cache, no inflight, no event); always returns source: 'registry'"
    - "Integration suite exercises the TOP-LEVEL helper (Plan 34-01) with a synthetic consumer adapter (NO `negotiateCapabilities` method, NO `quirks` field) to prove the D-04 fallback path works"
    - "Integration suite includes a CROSS-ADAPTER test exercising all 7 first-party adapters' quirks + negotiate() in a single suite (smoke check)"
    - "Changeset entry documents the public-API additions + the RunEventKind breaking change for exhaustive-switch consumers (Pitfall 6)"
  artifacts:
    - path: "packages/lattice/src/providers/lm-studio.ts"
      provides: "Extended createLmStudioProvider with quirks + registry-only negotiate()"
    - path: "packages/lattice/src/providers/lm-studio.test.ts"
      provides: "LM Studio quirks + negotiate tests"
      min_lines: 100
    - path: "packages/lattice/test/capabilities-negotiate-integration.test.ts"
      provides: "Integration suite covering top-level helper consumer-fallback path + 7-adapter quirks smoke"
      min_lines: 150
    - path: ".changeset/v1.3.0-adapter-quirks-negotiation.md"
      provides: "Changeset entry for Phase 34's public-API additions + RunEventKind breaking note"
  key_links:
    - from: "packages/lattice/src/providers/lm-studio.ts"
      to: "packages/lattice/src/capabilities/negotiate.ts (synthesizeNegotiatedCapabilitiesFromRegistry)"
      via: "Direct synthesis -- mirror Plan 34-03 Task 2 OpenAI-compat pattern"
      pattern: "synthesizeNegotiatedCapabilitiesFromRegistry"
    - from: "packages/lattice/test/capabilities-negotiate-integration.test.ts"
      to: "packages/lattice/src/index.ts (public negotiateCapabilities helper)"
      via: "Tests the consumer-facing import path (validates surface index discipline)"
      pattern: "from \"\\.\\./src/index\\.js\"|from \"\\.\\./\\.\\./lattice/src/index\\.js\""
---

<objective>
Close Phase 34 by (a) shipping the LM Studio adapter's quirks + registry-only negotiate(), (b) shipping an integration suite that proves the D-04 consumer-adapter fallback path through the top-level helper, (c) shipping a smoke test that exercises all 7 first-party adapters' quirks blocks in one suite, and (d) authoring the changeset entry that documents the public-API additions + the RunEventKind breaking change.

Purpose: LM Studio is the prototypical "no remote /models endpoint" adapter -- the registry-only pattern from Plan 34-03 Task 2 (OpenAI-compat) applies verbatim. The integration suite is the missing piece from Wave 1 -- Plans 02/03/04 each verified their OWN adapter; this plan verifies that (1) the TOP-LEVEL helper from Plan 34-01 works for a synthetic adapter with NO `negotiateCapabilities` method (D-04 consumer-adapter fallback) and (2) all 7 first-party adapters compose cleanly with the same helper. The changeset captures the public-surface change for v1.3.0 release notes.

Output: Extended `lm-studio.ts`; new integration test file; new changeset entry. NO changes to any other adapter files (those are owned by Plans 02-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-01-PLAN.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-02-PLAN.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-03-PLAN.md
@.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-04-PLAN.md

@packages/lattice/src/providers/lm-studio.ts
@packages/lattice/src/providers/lm-studio.test.ts
@packages/lattice/src/providers/quirks.ts
@packages/lattice/src/capabilities/negotiate.ts
@packages/lattice/src/capabilities/registry.static.ts
@packages/lattice/src/index.ts
@.changeset/v1.3.0-capability-registry.md

<interfaces>
<!-- LmStudioQuirks from Plan 34-01 -->

```typescript
export interface LmStudioQuirks extends AdapterQuirks {
  readonly customChatTemplateRiskFlag: boolean;    // CITED lmstudio-bug-tracker issue 1342
  readonly noAuthRequired: boolean;                // verified lm-studio.ts:35-37 -- apiKey optional
}
```

<!-- Plan 34-01 top-level helper -->

```typescript
export async function negotiateCapabilities(
  adapter: ProviderAdapter,
  modelId: string,
): Promise<NegotiatedCapabilities>;
// If adapter.negotiateCapabilities exists -> delegates verbatim
// Otherwise -> synthesizeNegotiatedCapabilitiesFromRegistry(adapter.id, modelId, "registry")
```

<!-- LM Studio static profile available in registry -->

```typescript
// From packages/lattice/src/capabilities/registry.static.ts (Phase 33)
// lm-studio:local-template (local_quantized, 8K conservative default)
```

<!-- Registry-only pattern from Plan 34-03 Task 2 -->

```typescript
const negotiate = async (modelId: string): Promise<NegotiatedCapabilities> => {
  return synthesizeNegotiatedCapabilitiesFromRegistry("lm-studio", modelId, "registry");
};
```

<!-- All 7 first-party factories that should expose quirks + negotiate() after Plans 02-04 -->

- createAnthropicProvider (Plan 34-02): quirks AnthropicQuirks; live negotiate
- createOpenAIProvider (Plan 34-03): quirks OpenAIQuirks; live negotiate
- createOpenAICompatibleProvider (Plan 34-03): quirks OpenAICompatQuirks; registry-only negotiate
- createXaiProvider (Plan 34-03): quirks XaiQuirks; live negotiate
- createGeminiProvider (Plan 34-04): quirks GeminiQuirks; live negotiate
- createOpenRouterProvider (Plan 34-04): quirks OpenRouterQuirks; live negotiate
- createLmStudioProvider (this plan): quirks LmStudioQuirks; registry-only negotiate
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LM Studio quirks + registry-only negotiate() + tests</name>
  <files>
    packages/lattice/src/providers/lm-studio.ts,
    packages/lattice/src/providers/lm-studio.test.ts
  </files>
  <read_first>
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 335-339 -- LmStudioQuirks vocabulary; lines 1033-1037 -- Open Question 5 advisory on event-emission for source: "registry"),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (D-04 -- registry source; D-12 -- event NOT fired for intentional no-endpoint),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-03-PLAN.md (Task 2 -- OpenAI-compat registry-only pattern; mirror this verbatim),
    packages/lattice/src/providers/lm-studio.ts (full -- existing closure shape; note the apiKey is OPTIONAL per RESEARCH §verified lm-studio.ts:35-37),
    packages/lattice/src/providers/lm-studio.test.ts (full -- makeFakeFetch pattern at lines 14-28),
    packages/lattice/src/capabilities/registry.static.ts (lm-studio:local-template profile)
  </read_first>
  <behavior>
    - Test 1: factory return narrows to `quirks: LmStudioQuirks` + `negotiateCapabilities: (id) => Promise<NegotiatedCapabilities>`.
    - Test 2: quirks block populated: `{ supportsToolChoice: false, parallelToolCalls: false, structuredOutputs: false, responseFormatHonored: false, streamingDiverges: true, customChatTemplateRiskFlag: true, noAuthRequired: true }` per RESEARCH §Q6 LM Studio vocabulary. NOTE: conservative defaults (false) on the 5 universal because LM Studio runs LOCAL quantized models whose capabilities vary wildly by template + model file. `streamingDiverges: true` because some LM Studio chat templates produce different output streaming vs buffered.
    - Test 3: `negotiateCapabilities("local-template")` (which exists in registry.static.ts as `lm-studio:local-template`) resolves to:
      - `source: "registry"` (NOT "live", NOT "registry-fallback")
      - `modelId: "local-template"`
      - `contextWindow: 8192` (per Phase 33 static profile -- conservative default)
      - All supports.* populated from the registry profile (LM Studio's local_quantized has limited capabilities)
      - `knownFailureModes` from registry (local_quantized class -- 5 modes per Phase 33 D-14)
      - `recommendedSanitizers` derived via getRecommendedSanitizers -- expect non-empty (template_artifact_leak + internal_envelope_leak both present in local_quantized class -> sanitizer keys: stripChatTemplateArtifacts + unwrapInternalEnvelope)
    - Test 4: `negotiateCapabilities("unknown-local-model-xyz")` -- not in registry. Returns empty-stub NegotiatedCapabilities with `source: "registry"`, contextWindow: 0, all supports.* false, knownFailureModes empty, recommendedSanitizers empty.
    - Test 5 (no /models fetch): mocked fetch is NEVER called for negotiate(). Verify capture.urls is empty.
    - Test 6 (no event emitted): runEventSink (if provided) receives zero events when source is "registry" -- per RESEARCH §Open Question 5 recommendation.
    - Test 7: backward-compat -- existing lm-studio.test.ts tests (no Phase 34 changes) still pass.
  </behavior>
  <action>
    1. **Read packages/lattice/src/providers/lm-studio.ts** to understand the existing factory closure. Confirm apiKey is OPTIONAL (per RESEARCH §verified).
    2. **Add factory options** to `LmStudioProviderOptions` for uniformity (NOT USED in registry-only path):
       - `modelsCacheTtlMs?` -- ignored; JSDoc note: "Reserved for future /models discovery; LM Studio currently has no remote /models endpoint."
       - `runEventSink?` -- accepted but NEVER fired (source: "registry" is the documented happy path).
    3. **Add quirks block** to the factory's returned literal per Test 2 values.
    4. **Add `negotiateCapabilities`** as registry-only delegation (mirror Plan 34-03 Task 2):
       ```typescript
       const negotiate = async (modelId: string): Promise<NegotiatedCapabilities> => {
         return synthesizeNegotiatedCapabilitiesFromRegistry("lm-studio", modelId, "registry");
       };
       ```
    5. **Narrow factory return**: `createLmStudioProvider(options): ProviderAdapter & { readonly quirks: LmStudioQuirks; readonly negotiateCapabilities: (modelId: string) => Promise<NegotiatedCapabilities>; }`.
    6. Imports at top of lm-studio.ts: `import type { LmStudioQuirks } from "./quirks.js";`, `import type { NegotiatedCapabilities } from "../capabilities/negotiate.js";`, `import { synthesizeNegotiatedCapabilitiesFromRegistry } from "../capabilities/negotiate.js";`.
    7. **Extend packages/lattice/src/providers/lm-studio.test.ts** with `describe("Phase 34: LM Studio quirks + negotiateCapabilities (registry-only)", () => { ... })` covering Tests 1-7. Reuse existing makeFakeFetch.
    8. For Test 5, instantiate the adapter with a mocked fetch and assert `capture.urls.length === 0` after the negotiate() resolves.
    9. For Test 3, verify `result.recommendedSanitizers` includes at minimum `stripChatTemplateArtifacts` AND `unwrapInternalEnvelope` -- assuming Phase 33 D-14 maps local_quantized's `template_artifact_leak` and `internal_envelope_leak` to those sanitizers. If the registry.static.ts profile for lm-studio:local-template has different knownFailureModes, adjust the assertion to match the profile.
    10. Run typecheck + tests.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test lm-studio.test</automated>
  </verify>
  <acceptance_criteria>
    - LM Studio factory return narrows to expose quirks + negotiateCapabilities as required fields.
    - Quirks block: 7 values per Test 2 verbatim; `noAuthRequired: true` reflects the apiKey-optional design; `customChatTemplateRiskFlag: true` cites lmstudio-bug-tracker.
    - `negotiateCapabilities` performs NO fetch (capture.urls empty in Test 5).
    - Source is ALWAYS `"registry"` (never `"live"` or `"registry-fallback"`).
    - NO event emitted (runEventSink sees zero calls in Test 6).
    - Test 3 returns non-empty recommendedSanitizers (the local_quantized class has multiple sanitizable failure modes per Phase 33 D-14).
    - JSDoc cites D-04 + Open Question 5 (no-event-for-registry-source).
    - All 7 tests pass; existing lm-studio.test.ts tests continue to pass.
  </acceptance_criteria>
  <done>LM Studio factory ships quirks + registry-only negotiate(). The pattern from Plan 34-03 Task 2 (OpenAI-compat) is now replicated; the "intentional-no-endpoint" adapter set is complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration suite -- consumer-adapter fallback + 7-adapter quirks smoke</name>
  <files>
    packages/lattice/test/capabilities-negotiate-integration.test.ts
  </files>
  <read_first>
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (D-04 -- consumer-adapter fallback path; the central contract this plan verifies),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 612-617 -- Pitfall 5 helper-vs-adapter drift; the integration suite verifies the helper has no logic of its own),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-01-PLAN.md (Task 3 -- top-level helper + behavior),
    packages/lattice/src/index.ts (verify the public negotiateCapabilities helper export),
    packages/lattice/src/capabilities/negotiate.ts (the helper implementation Plan 34-01 shipped),
    packages/lattice/src/providers/anthropic.ts (verify post-Plan-02 quirks + negotiate),
    packages/lattice/src/providers/adapters.ts (verify post-Plan-03 OpenAI + OpenAI-compat),
    packages/lattice/src/providers/xai.ts (verify post-Plan-03 xAI),
    packages/lattice/src/providers/gemini.ts (verify post-Plan-04 Gemini),
    packages/lattice/src/providers/openrouter.ts (verify post-Plan-04 OpenRouter + anchor case study)
  </read_first>
  <behavior>
    - Test 1 (consumer adapter D-04 fallback): construct a synthetic ProviderAdapter with NO `negotiateCapabilities` method, NO `quirks` field, just the 4 required fields `{ id: "anthropic", kind: "provider-adapter" }`. Call top-level `negotiateCapabilities(adapter, "claude-opus-4")` (importing from `@full-self-browsing/lattice` or `../../src/index.js`). The helper detects no method, synthesizes from registry. Assert:
       - `result.source === "registry"` (NOT "registry-fallback" -- this is the intentional consumer-fallback path)
       - `result.modelId === "claude-opus-4"`
       - `result.knownFailureModes` matches the Phase 33 anthropic:claude-opus-4 profile (empty for frontier_rlhf)
    - Test 2 (consumer adapter, unknown model): same synthetic adapter, modelId `"nonexistent-model-xyz"`. Helper returns empty-stub with `source: "registry"`.
    - Test 3 (helper delegates verbatim for first-party adapters): create a real Anthropic adapter with a mocked fetch returning a 200 /v1/models response. Call top-level `negotiateCapabilities(anthropicAdapter, "claude-opus-4-6")`. Assert the helper RETURNS the adapter's result verbatim -- `result.source === "live"`. The helper did NOT bypass; it delegated.
    - Test 4 (helper does NOT add inflight-coalescing or extra logic -- Pitfall 5 verification): call the helper twice with the SAME adapter + modelId. The mock fetch is called TWICE if the adapter's TTL cache is `0`, or ONCE if TTL is non-zero. Assert the count matches the ADAPTER's cache behavior, not any helper-level cache. This verifies Pitfall 5's "no drift" -- the helper does not introduce its own caching.
    - Test 5 (7-adapter quirks smoke): construct one instance of each of the 7 first-party adapters with minimal config; assert each `adapter.quirks` is defined and has the 5 universal booleans. Iterate the list:
      ```typescript
      const adapters = [
        createAnthropicProvider({ model: "...", apiKey: "..." }),
        createOpenAIProvider({ model: "...", apiKey: "..." }),
        createOpenAICompatibleProvider({ baseUrl: "http://localhost", model: "..." }),
        createXaiProvider({ model: "...", apiKey: "..." }),
        createGeminiProvider({ model: "...", apiKey: "..." }),
        createOpenRouterProvider({ model: "...", apiKey: "..." }),
        createLmStudioProvider({ model: "..." }),
      ];
      for (const a of adapters) {
        expect(a.quirks).toBeDefined();
        expect(typeof a.quirks.supportsToolChoice).toBe("boolean");
        expect(typeof a.quirks.parallelToolCalls).toBe("boolean");
        expect(typeof a.quirks.structuredOutputs).toBe("boolean");
        expect(typeof a.quirks.responseFormatHonored).toBe("boolean");
        expect(typeof a.quirks.streamingDiverges).toBe("boolean");
        expect(typeof a.negotiateCapabilities).toBe("function");
      }
      ```
    - Test 6 (discriminant narrowing smoke): given an adapter with `id === "anthropic"`, the `adapter.quirks` cast to AnthropicQuirks exposes `promptCachingSupported`. RESEARCH §Pattern 2 doc clarifies TypeScript can't auto-narrow on `adapter.id` without explicit cast -- this test asserts the runtime VALUES are present, not the typescript narrowing (which the tsd test from Plan 34-01 already covers).
    - Test 7 (anchor case study via top-level helper): construct a real OpenRouter adapter with a mocked fetch returning the openrouter-models-ok.json fixture. Call top-level `negotiateCapabilities(openrouterAdapter, "openai/gpt-oss-120b:free")`. Assert the anchor case study result -- verifies the FULL stack: top-level helper -> adapter delegate -> /models fetch -> stripOpenRouterVariant -> registry intersection -> getRecommendedSanitizers.
  </behavior>
  <action>
    1. **Create packages/lattice/test/capabilities-negotiate-integration.test.ts**. Layout:
       - Import path: `import { negotiateCapabilities, createAnthropicProvider, createOpenAIProvider, createOpenAICompatibleProvider, createXaiProvider, createGeminiProvider, createOpenRouterProvider, createLmStudioProvider } from "../src/index.js";` (THIS IS DELIBERATE -- importing from the public surface index verifies PKG-01/INDEX-01 re-export discipline).
       - Import test fixtures from `./__fixtures__/quirks/` (anthropic-models-ok.json, openrouter-models-ok.json).
       - Reuse the makeFakeFetch helper -- planner clarification: define it locally in this test file, or extract to a shared test helper module if a precedent exists. Planner advisory: define locally; the helper is 15 LOC and inlining keeps the test file self-contained.
       - Top-level describe: `describe("Phase 34: end-to-end negotiate integration", () => { ... })`.
       - Sub-describe per test family: "consumer adapter fallback (D-04)", "first-party delegation (Pitfall 5)", "7-adapter quirks smoke", "anchor case study via top-level helper".
    2. Implement Test 1 (consumer adapter D-04):
       ```typescript
       const synthetic: ProviderAdapter = { id: "anthropic", kind: "provider-adapter" };
       const result = await negotiateCapabilities(synthetic, "claude-opus-4");
       expect(result.source).toBe("registry");
       expect(result.modelId).toBe("claude-opus-4");
       // claude-opus-4 is in registry.static.ts as anthropic:claude-opus-4 with frontier_rlhf
       expect(result.knownFailureModes).toEqual([]);
       ```
    3. Implement Test 2 (unknown model): similar but with `modelId: "nonexistent-model-xyz"`; expect empty-stub.
    4. Implement Test 3 (helper delegation): construct real Anthropic adapter with mocked fetch + anthropic-models-ok.json fixture; call top-level helper; assert `source === "live"`.
    5. Implement Test 4 (Pitfall 5 no-drift): two sequential calls with `modelsCacheTtlMs: 60_000` -- expect fetch called ONCE (adapter cache works); then with `modelsCacheTtlMs: 0` -- expect fetch called TWICE. This verifies the helper has NO caching of its own (otherwise the second case would still show 1 call).
    6. Implement Test 5 (7-adapter smoke) iterating the 7 factories. Use minimal config -- the adapters' quirks blocks should be populated WITHOUT calling negotiate() or execute().
    7. Implement Test 6 (discriminant narrowing): runtime assertions; tsd narrowing was verified in Plan 34-01.
    8. Implement Test 7 (anchor case study via top-level helper): construct OpenRouter adapter; call top-level helper; verify the anchor case study assertions hold (same as Plan 34-04 Task 2 Test 4 but exercised through the public helper).
    9. Run typecheck + tests:
       ```
       pnpm --filter @full-self-browsing/lattice typecheck
       pnpm --filter @full-self-browsing/lattice test capabilities-negotiate-integration
       ```
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test capabilities-negotiate-integration</automated>
  </verify>
  <acceptance_criteria>
    - All imports in capabilities-negotiate-integration.test.ts go through `../src/index.js` -- this verifies PKG-01/INDEX-01 re-export discipline for ALL Phase 34 public symbols.
    - Test 1 (D-04 consumer-adapter fallback) passes: synthetic 4-field ProviderAdapter without `negotiateCapabilities` triggers the helper's registry-only path with `source: "registry"`.
    - Test 3 (first-party delegation) passes: real Anthropic adapter's `negotiateCapabilities` is called THROUGH the helper, returning `source: "live"`.
    - Test 4 (Pitfall 5 no-drift) passes: fetch-count matches the adapter's cache config, NOT a helper-level cache.
    - Test 5 (7-adapter quirks smoke) passes: all 7 first-party adapters expose quirks + negotiateCapabilities; the 5 universal booleans are all typed `boolean`.
    - Test 7 (anchor case study via helper) passes: the FULL stack works through the public surface.
    - All 7 tests in the suite pass.
    - `pnpm --filter @full-self-browsing/lattice typecheck` + `test:types` + `lint:packages` all green.
  </acceptance_criteria>
  <done>The integration suite verifies the Plan 34-01 + Plans 02-04 contract end-to-end through the public surface. Phase 34's must-haves are now fully verifiable in a single test run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Changeset entry + full-suite green confirmation</name>
  <files>
    .changeset/v1.3.0-adapter-quirks-negotiation.md
  </files>
  <read_first>
    .changeset/v1.3.0-capability-registry.md (Phase 33 precedent -- mirror style + frontmatter),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-CONTEXT.md (the Phase 34 scope statement),
    .planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-RESEARCH.md (lines 618-624 -- Pitfall 6: RunEventKind expansion is breaking for exhaustive-switch consumers; lines 60-69 -- the 5 REQ-IDs and their phasing)
  </read_first>
  <behavior>
    - Changeset frontmatter follows Phase 33's pattern: minor bump for `@full-self-browsing/lattice`; `@full-self-browsing/lattice-cli` auto-couples via changesets `fixed` config.
    - Changeset body summarizes:
      - Public-API additions (7 quirks interfaces, NegotiatedCapabilities, NegotiationAuthError, top-level negotiateCapabilities helper, SanitizerKey, SANITIZER_BY_FAILURE_MODE, getRecommendedSanitizers)
      - 7 first-party adapter factories now narrow their return types to expose `quirks` + `negotiateCapabilities` as REQUIRED fields
      - 4 adapters with /models endpoints (Anthropic, OpenAI, Gemini, OpenRouter) do live discovery; 3 adapters without (OpenAI-compat, xAI -- wait, xAI does have /models; revise: 2 without -- OpenAI-compat, LM Studio) fall back to registry
      - NEW typed error `NegotiationAuthError` thrown on 401/403 from /models
      - **BREAKING CHANGE NOTE for exhaustive-switch consumers** (Pitfall 6): `RunEventKind` union gains `"capabilities.negotiation.fallback"` as a new literal; consumers writing exhaustive switches over RunEventKind must add the new case. Phase 17 set the precedent with 3 recovery.* events; v1.3 has no prior stable consumers (rc.0 is staged).
    - Final action: run full workspace test suite + lint and verify all green.
  </behavior>
  <action>
    1. **Create .changeset/v1.3.0-adapter-quirks-negotiation.md** mirroring Phase 33's changeset structure:
       ```markdown
       ---
       "@full-self-browsing/lattice": minor
       ---

       feat(capabilities): adapter quirk flags + capability negotiation API (Phase 34)

       Each of the 7 first-party provider adapters now ships:
       - A typed `quirks` field documenting known behavioral deviations from
         OpenAI-canonical shape (`AdapterQuirks` base + 7 per-adapter narrowed
         sub-interfaces: `AnthropicQuirks`, `OpenAIQuirks`, `OpenAICompatQuirks`,
         `GeminiQuirks`, `XaiQuirks`, `OpenRouterQuirks`, `LmStudioQuirks`).
       - A runtime `negotiateCapabilities(modelId): Promise<NegotiatedCapabilities>`
         method that queries the provider's `/models` endpoint where available
         and intersects the response with Phase 33's static `ModelCapabilityProfile`
         registry; falls back to registry-only synthesis when the endpoint is
         absent (OpenAI-compat, LM Studio) or transient errors occur.

       Public API additions (all re-exported from the package index per PKG-01/INDEX-01):
       - Types: `AdapterQuirks`, 7 narrowed quirks interfaces, `NegotiatedCapabilities`, `SanitizerKey`
       - Functions: top-level `negotiateCapabilities(adapter, modelId)` helper, `getRecommendedSanitizers`
       - Constants: `SANITIZER_BY_FAILURE_MODE`
       - Error class: `NegotiationAuthError` (thrown on 401/403 from /models)

       Reliability features:
       - Per-instance TTL cache (`modelsCacheTtlMs` factory option; default 5 min)
       - Single-flight inflight-request coalescing (concurrent calls share one fetch)
       - 2 retries with exponential backoff on transient errors (5xx, network, timeout)
       - Auth errors (401/403) throw the typed `NegotiationAuthError` -- they never silently fall back

       BREAKING CHANGE NOTE (exhaustive-switch consumers): The `RunEventKind` union
       gains a new literal `"capabilities.negotiation.fallback"`. Consumers writing
       exhaustive `switch` statements over `RunEventKind` must add the new case.
       Phase 17 set the precedent with the `recovery.*` events; v1.3 has no prior
       stable consumers (rc.0 is staged but not stable).

       The new event fires when an adapter's `negotiateCapabilities` falls back
       from the live `/models` endpoint to the Phase 33 registry due to transient
       failure. Auth errors do NOT fire this event (they throw `NegotiationAuthError`).

       Anchor case study verified end-to-end (session_1780792387779):
       `negotiateCapabilities(openrouterAdapter, "openai/gpt-oss-120b:free")` yields
       `result.recommendedSanitizers` including `"unwrapInternalEnvelope"` -- proving
       the live-fetch + Phase 33 registry-intersection + sanitizer-derivation pipeline.

       Phase 36 will register the actual sanitizer implementations under the
       `SanitizerKey` union shipped in this release.
       ```
    2. **Run the full lattice test suite** to verify NO regressions across Plans 02-05:
       ```
       pnpm --filter @full-self-browsing/lattice typecheck
       pnpm --filter @full-self-browsing/lattice test
       pnpm --filter @full-self-browsing/lattice test:types
       pnpm -r lint:packages
       ```
    3. **Run a phase-wide grep verification**:
       - `grep -rE "negotiateCapabilities" packages/lattice/src/providers/*.ts | wc -l` -- expect non-zero, all 7 adapter files referenced
       - `grep -rE "quirks:" packages/lattice/src/providers/*.ts | wc -l` -- expect 7 (one per adapter)
       - `grep -rE "capabilities.negotiation.fallback" packages/lattice/src/tracing/tracing.ts` -- expect 1 match
    4. **Verify no missing artifacts**: the 5 plans collectively touched the documented file set; no file is missing.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck &amp;&amp; pnpm --filter @full-self-browsing/lattice test &amp;&amp; pnpm --filter @full-self-browsing/lattice test:types &amp;&amp; pnpm -r lint:packages</automated>
  </verify>
  <acceptance_criteria>
    - .changeset/v1.3.0-adapter-quirks-negotiation.md exists with the documented body.
    - Frontmatter declares minor bump for `@full-self-browsing/lattice` (per Phase 33 precedent).
    - BREAKING CHANGE NOTE documents the RunEventKind expansion explicitly.
    - Full workspace test suite passes: `pnpm --filter @full-self-browsing/lattice typecheck` + `test` + `test:types` + `pnpm -r lint:packages` all exit 0.
    - Phase-wide grep confirms quirks + negotiateCapabilities present in all 7 adapter files.
    - `capabilities.negotiation.fallback` exists in tracing.ts exactly once.
    - Anchor case study test from Plan 34-04 PASSES in the full-suite run (no regression from Plans 03/04/05 landing).
    - Integration suite from Task 2 PASSES.
  </acceptance_criteria>
  <done>Phase 34 is COMPLETE. Changeset captures the public-API additions + breaking change note. The 5 plans + the typed surface from Plan 34-01 collectively deliver QUIRK-01..03 + NEG-01..02 with the anchor case study verified end-to-end.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer -> LM Studio adapter | modelId untrusted; passed verbatim to registry lookup. Local-only adapter -- no network egress. |
| consumer -> top-level negotiateCapabilities helper | adapter parameter is consumer-provided; if it's a consumer-built adapter (no `negotiateCapabilities` method), the helper synthesizes from registry. The synthetic D-04 path is the consumer-side ergonomic. |
| LM Studio adapter -> (no upstream) | No network. Pure local registry lookup. |
| Top-level helper -> consumer's adapter.negotiateCapabilities | Helper does VERBATIM delegation; any threat in adapter.negotiateCapabilities is owned by the adapter, not the helper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05-01 | Tampering | Consumer constructs synthetic adapter with malicious `id` string outside CapabilityAdapter closed enum | mitigate | synthesizeNegotiatedCapabilitiesFromRegistry (Plan 34-01) handles unknown adapter gracefully -- returns empty-stub NegotiatedCapabilities with `source: "registry"`. Test 2 in Task 2 verifies this. |
| T-34-05-02 | Denial of Service | Top-level helper delegates to a consumer adapter that hangs indefinitely | accept | Caller controls the adapter; if they want timeout protection, they should wrap with their own AbortSignal or Promise.race. The helper has no opinion on this. Document in JSDoc. |
| T-34-05-03 | Tampering | Pitfall 5 helper drift -- a future contributor adds inflight-coalescing to the helper | mitigate | Test 4 in Task 2 (Pitfall 5 no-drift verification) catches this regression: it asserts the helper has NO caching by checking fetch-count matches adapter's TTL setting. |
| T-34-05-04 | Information Disclosure | NegotiationAuthError thrown from a consumer-built adapter caught at the helper boundary | accept | Errors propagate verbatim through the helper. Consumer's adapter code is the security boundary. |
| T-34-05-05 | Spoofing | LM Studio adapter could fake `source: "live"` if a contributor accidentally mis-implements the registry-only path | mitigate | Test 3 + Test 5 (no fetch, no event) explicitly assert `source === "registry"`. Acceptance criterion enforced. |

</threat_model>

<verification>

Phase-wide checks beyond per-task automated commands:

1. **Wave 2 file-ownership: Plan 34-05 owns lm-studio.ts + capabilities-negotiate-integration.test.ts + the new changeset only**. NO overlap with Plans 02-04.
2. **CROSS-PLAN VERIFICATION GATE**: Plan 34-05 Task 3 runs the FULL workspace test suite, including the anchor case study test from Plan 34-04 Task 2. If Plan 34-04 has regressed by the time Plan 34-05 runs (Wave 2), this catches it.
3. **`pnpm --filter @full-self-browsing/lattice test` full suite passes** -- catches cross-plan regressions.
4. **Public-surface verification**: the integration suite imports from `../src/index.js` -- PKG-01/INDEX-01 re-export discipline is enforced at the test layer.
5. **No new runtime deps**: `git diff packages/lattice/package.json` shows no `dependencies` block changes from any of the 5 plans collectively.

</verification>

<success_criteria>

Plan 34-05 is COMPLETE when:

- [ ] LM Studio factory: quirks block (7 values); negotiate() registry-only (no fetch, no cache, no inflight, no event); always returns `source: "registry"`.
- [ ] Integration suite covers:
   - D-04 consumer-adapter fallback path with synthetic ProviderAdapter
   - First-party delegation (helper -> adapter.negotiateCapabilities verbatim)
   - Pitfall 5 no-drift verification (helper has no caching of its own)
   - 7-adapter quirks smoke (all 7 first-party factories expose quirks + negotiateCapabilities)
   - Anchor case study via the public top-level helper (full-stack regression bar)
- [ ] Changeset entry created with: public-API additions; breaking-change note on RunEventKind expansion; anchor case study reference.
- [ ] Full workspace test suite (typecheck + test + test:types + lint:packages) ALL GREEN.
- [ ] Phase-wide grep verification: quirks + negotiateCapabilities present in all 7 first-party adapter files; the `capabilities.negotiation.fallback` literal exists exactly once in tracing.ts.
- [ ] No regression to the anchor case study assertion from Plan 34-04 Task 2.

</success_criteria>

<output>
After completion, create `.planning/phases/34-adapter-quirk-flags-capability-negotiation-api/34-05-SUMMARY.md` documenting:
- LM Studio quirks block with citation references
- Integration suite test count + per-test verification of D-04 + Pitfall 5 + 7-adapter smoke + anchor case study end-to-end
- Phase-wide totals: lines added per plan, total new tests, total new file count, public-API surface delta
- The exact changeset entry text (for review)
- Phase 34 COMPLETION CONFIRMATION: all 5 REQ-IDs verified, anchor case study verified through public helper, full workspace green.
- Hand-off note for Phase 35 (Prompt Scaffolding Helpers): "Phase 34 ships the dispatch keys (SanitizerKey) and the negotiation API (NegotiatedCapabilities) that Phase 36 will consume. Phase 35 (prompt scaffolds) is INDEPENDENT of Phase 34 -- it reads Phase 33's `recommendedPromptStrategy` enum and ships strategy-tuned prompt fragments. Phase 35 can proceed in parallel with Phase 36."
</output>
