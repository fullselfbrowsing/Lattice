---
phase: 37
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - packages/lattice/src/providers/anthropic.ts
  - packages/lattice/src/providers/gemini.ts
  - packages/lattice/src/providers/anthropic.test.ts
  - packages/lattice/src/providers/gemini.test.ts
  - packages/lattice/src/providers/parity.test.ts
  - .changeset/v1.3.0-tool-call-validation.md
autonomous: true
requirements:
  - VALID-01
  - VALID-02
  - VALID-03
tags:
  - provider-adapters
  - direct-providers
  - parity
  - changeset
must_haves:
  truths:
    - "AnthropicProviderOptions accepts validateToolCalls?: ValidateToolCallsOption"
    - "GeminiProviderOptions accepts validateToolCalls?: ValidateToolCallsOption"
    - "Anthropic and Gemini preserve rawOutputs and rawResponse while populating toolCalls"
    - "All seven real provider factories accept validateToolCalls"
    - "All three failure modes are covered by direct or parity tests"
    - "Phase 37 changeset documents the opt-in tool-call validation API"
  artifacts:
    - path: "packages/lattice/src/providers/anthropic.ts"
      provides: "Anthropic validation option and execution wiring"
    - path: "packages/lattice/src/providers/gemini.ts"
      provides: "Gemini validation option and execution wiring"
    - path: "packages/lattice/src/providers/parity.test.ts"
      provides: "All-seven adapter validation parity coverage"
    - path: ".changeset/v1.3.0-tool-call-validation.md"
      provides: "Release note for validation API addition"
      min_lines: 6
  key_links:
    - from: "packages/lattice/src/providers/anthropic.ts"
      to: "packages/lattice/src/tools/tool-call-validation.ts"
      via: "imports ValidateToolCallsOption and validateToolCallRequests"
      pattern: "validateToolCallRequests"
    - from: "packages/lattice/src/providers/gemini.ts"
      to: "packages/lattice/src/tools/tool-call-validation.ts"
      via: "imports ValidateToolCallsOption and validateToolCallRequests"
      pattern: "validateToolCallRequests"
---

<objective>
Wire `validateToolCalls` through Anthropic and Gemini, add all-seven provider
parity coverage, and close Phase 37 planning with release notes and final gates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-CONTEXT.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-RESEARCH.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-PATTERNS.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-01-PLAN.md
@.planning/phases/37-tool-call-validation-layer-opt-in/37-02-PLAN.md
@packages/lattice/src/tools/tool-call-validation.ts
@packages/lattice/src/providers/anthropic.ts
@packages/lattice/src/providers/gemini.ts
@packages/lattice/src/providers/parity.test.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Direct adapters diverge from shared OpenAI-compatible semantics | high | All-seven parity test asserts matching valid/drop/throw behavior |
| Provider raw bodies are replaced by normalized validation data | medium | Direct adapter tests assert rawResponse remains the original provider body |
| Callback/drop modes hide all malformed behavior from tests | medium | Parity/direct tests assert callback count and dropped-call list shape |
| Changeset overpromises native tool-use support | medium | Wording says opt-in validation of returned tool-call envelopes, not native provider tool APIs |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire Anthropic validation option</name>
  <files>packages/lattice/src/providers/anthropic.ts</files>
  <read_first>
    - packages/lattice/src/providers/anthropic.ts
    - packages/lattice/src/tools/tool-call-validation.ts
    - packages/lattice/src/agent/format-tools.ts
    - packages/lattice/src/providers/provider.ts
  </read_first>
  <action>
    1. Import `type ValidateToolCallsOption` and `validateToolCallRequests` from `../tools/tool-call-validation.js`.
    2. Import `parseToolUseEnvelope` from `../agent/format-tools.js`.
    3. Add `validateToolCalls?: ValidateToolCallsOption;` to `AnthropicProviderOptions`.
    4. After extracting response `text`, parse tool-call requests with `parseToolUseEnvelope(text)`.
    5. If parsed requests are non-null, validate them with `validateToolCallRequests(parsed, options.validateToolCalls)`.
    6. Return `toolCalls` only when the helper returns a defined list.
    7. Preserve existing sanitized `rawOutputs`, `normalizedUsage`, `usage`, `rawResponse`, fetch override, and signal behavior.
  </action>
  <verify>
    <automated>rg -n 'validateToolCalls|ValidateToolCallsOption|validateToolCallRequests|parseToolUseEnvelope|toolCalls' packages/lattice/src/providers/anthropic.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AnthropicProviderOptions` includes `validateToolCalls?: ValidateToolCallsOption`.
    - Anthropic execute calls `parseToolUseEnvelope`.
    - Anthropic execute calls `validateToolCallRequests`.
    - Anthropic preserves `rawResponse`.
  </acceptance_criteria>
  <done>Anthropic adapter produces validated normalized tool calls when opted in.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Gemini validation option</name>
  <files>packages/lattice/src/providers/gemini.ts</files>
  <read_first>
    - packages/lattice/src/providers/gemini.ts
    - packages/lattice/src/tools/tool-call-validation.ts
    - packages/lattice/src/agent/format-tools.ts
    - packages/lattice/src/providers/provider.ts
  </read_first>
  <action>
    1. Import `type ValidateToolCallsOption` and `validateToolCallRequests` from `../tools/tool-call-validation.js`.
    2. Import `parseToolUseEnvelope` from `../agent/format-tools.js`.
    3. Add `validateToolCalls?: ValidateToolCallsOption;` to `GeminiProviderOptions`.
    4. After extracting response `text`, parse tool-call requests with `parseToolUseEnvelope(text)`.
    5. If parsed requests are non-null, validate them with `validateToolCallRequests(parsed, options.validateToolCalls)`.
    6. Return `toolCalls` only when the helper returns a defined list.
    7. Preserve existing sanitized `rawOutputs`, `normalizedUsage`, `usage`, `rawResponse`, fetch override, and signal behavior.
  </action>
  <verify>
    <automated>rg -n 'validateToolCalls|ValidateToolCallsOption|validateToolCallRequests|parseToolUseEnvelope|toolCalls' packages/lattice/src/providers/gemini.ts</automated>
  </verify>
  <acceptance_criteria>
    - `GeminiProviderOptions` includes `validateToolCalls?: ValidateToolCallsOption`.
    - Gemini execute calls `parseToolUseEnvelope`.
    - Gemini execute calls `validateToolCallRequests`.
    - Gemini preserves `rawResponse`.
  </acceptance_criteria>
  <done>Gemini adapter produces validated normalized tool calls when opted in.</done>
</task>

<task type="auto">
  <name>Task 3: Add direct adapter and all-seven parity tests</name>
  <files>
    packages/lattice/src/providers/anthropic.test.ts
    packages/lattice/src/providers/gemini.test.ts
    packages/lattice/src/providers/parity.test.ts
  </files>
  <read_first>
    - packages/lattice/src/providers/anthropic.test.ts
    - packages/lattice/src/providers/gemini.test.ts
    - packages/lattice/src/providers/parity.test.ts
    - packages/lattice/src/tools/tool-call-validation.test.ts
    - .planning/phases/37-tool-call-validation-layer-opt-in/37-RESEARCH.md
  </read_first>
  <action>
    Add tests:

    1. Anthropic valid tool envelope returns `response.toolCalls[0]` with id `c1`, name `search`, and args `{ query: "ok" }`.
    2. Gemini valid tool envelope returns the same normalized tool-call shape.
    3. Anthropic and Gemini preserve `rawOutputs.text` and `rawResponse`.
    4. Anthropic and Gemini throw `ToolCallValidationError` for the roadmap typo `{ name: "search_database", args: { quer: "..." } }`.
    5. Parity test iterates all seven real adapter factories:
       - OpenAI-compatible
       - OpenAI
       - Anthropic
       - Gemini
       - xAI
       - OpenRouter
       - LM Studio
    6. Parity test asserts valid calls populate `toolCalls`.
    7. Parity test asserts drop mode omits the invalid roadmap typo call.
    8. Parity test asserts throw mode rejects with reason `unknown_tool` or `invalid_args` as appropriate for the scenario.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test anthropic gemini parity</automated>
  </verify>
  <acceptance_criteria>
    - Anthropic test contains `validateToolCalls`.
    - Gemini test contains `validateToolCalls`.
    - Parity test covers all seven provider ids.
    - Tests contain `search_database` and `{ quer: "..." }`.
    - Tests assert `rawResponse` is preserved.
    - Verification command exits 0.
  </acceptance_criteria>
  <done>Direct adapters and all-seven parity coverage prove validation works across the provider surface.</done>
</task>

<task type="auto">
  <name>Task 4: Add changeset and run final gates</name>
  <files>.changeset/v1.3.0-tool-call-validation.md</files>
  <read_first>
    - .changeset/ (existing changeset style)
    - .planning/REQUIREMENTS.md (VALID requirements)
    - .planning/phases/37-tool-call-validation-layer-opt-in/37-01-PLAN.md
    - .planning/phases/37-tool-call-validation-layer-opt-in/37-02-PLAN.md
  </read_first>
  <action>
    1. Create `.changeset/v1.3.0-tool-call-validation.md` for `@full-self-browsing/lattice`.
    2. Use the same bump convention as adjacent v1.3 additive SDK features.
    3. Mention:
       - opt-in `validateToolCalls` adapter option
       - `ToolCallValidationError`
       - normalized validated `toolCalls`
       - returned tool-call envelope validation, not native provider tool APIs
    4. Do not claim hosted tool execution or provider-native tool-use support.
    5. Run final verification commands.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test tool-call-validation format-tools adapters openrouter xai lm-studio agent/runtime anthropic gemini parity public-surface && pnpm --filter @full-self-browsing/lattice build && pnpm --filter @full-self-browsing/lattice test:types && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Changeset file exists.
    - Changeset mentions `validateToolCalls`.
    - Changeset mentions `ToolCallValidationError`.
    - Changeset avoids claiming native provider tool-use support.
    - Final test, build, type-test, and typecheck commands exit 0.
  </acceptance_criteria>
  <done>Phase 37 has release notes and final verification has passed.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
pnpm --filter @full-self-browsing/lattice test tool-call-validation format-tools adapters openrouter xai lm-studio agent/runtime anthropic gemini parity public-surface
pnpm --filter @full-self-browsing/lattice build
pnpm --filter @full-self-browsing/lattice test:types
pnpm --filter @full-self-browsing/lattice typecheck
```

After successful execution, mark `VALID-01` through `VALID-03` complete in
`.planning/REQUIREMENTS.md`, update Phase 37 progress in `.planning/ROADMAP.md`,
and record completion in `.planning/STATE.md`.
</verification>
