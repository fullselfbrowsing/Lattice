---
phase: 36
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - packages/lattice/src/providers/adapters.ts
  - packages/lattice/src/providers/openrouter.ts
  - packages/lattice/src/providers/xai.ts
  - packages/lattice/src/providers/lm-studio.ts
  - packages/lattice/src/providers/adapters.test.ts
  - packages/lattice/src/providers/openrouter.test.ts
  - packages/lattice/src/providers/xai.test.ts
  - packages/lattice/src/providers/lm-studio.test.ts
autonomous: true
requirements:
  - SANITIZE-01
  - SANITIZE-03
  - SANITIZE-04
tags:
  - provider-adapters
  - openai-compatible
  - wrapper-providers
must_haves:
  truths:
    - "OpenAICompatibleProviderOptions accepts sanitizeOutput?: SanitizeOutputOption"
    - "OpenAI-compatible execute applies sanitizers after rawOutputs extraction and before return"
    - "OpenAI, OpenRouter, xAI, and LM Studio inherit the OpenAI-compatible sanitizer path"
    - "Wrapper providers do not apply a second sanitizer pass"
    - "rawResponse remains the original provider response body"
    - "Absent sanitizeOutput preserves current behavior"
  artifacts:
    - path: "packages/lattice/src/providers/adapters.ts"
      provides: "Shared sanitizer wiring for OpenAI-compatible and OpenAI adapters"
    - path: "packages/lattice/src/providers/openrouter.test.ts"
      provides: "OpenRouter anchor reproduction test for session_1780792387779 envelope shape"
    - path: "packages/lattice/src/providers/xai.test.ts"
      provides: "xAI wrapper no-double-sanitization regression"
    - path: "packages/lattice/src/providers/lm-studio.test.ts"
      provides: "LM Studio pass-through sanitizer regression"
  key_links:
    - from: "packages/lattice/src/providers/adapters.ts"
      to: "packages/lattice/src/sanitizers/index.ts"
      via: "imports SanitizeOutputOption and applyOutputSanitizers"
      pattern: "applyOutputSanitizers"
    - from: "packages/lattice/src/providers/openrouter.ts"
      to: "packages/lattice/src/providers/adapters.ts"
      via: "delegates sanitizer option through OpenAI-compatible options"
      pattern: "createOpenAICompatibleProvider"
---

<objective>
Wire the opt-in sanitizer option through the OpenAI-compatible adapter family: OpenAI-compatible, OpenAI, OpenRouter, xAI, and LM Studio. The sanitizer should apply exactly once in the shared OpenAI-compatible execution path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-CONTEXT.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-RESEARCH.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-PATTERNS.md
@.planning/phases/36-output-sanitizer-hook-opt-in/36-01-PLAN.md
@packages/lattice/src/sanitizers/index.ts
@packages/lattice/src/providers/adapters.ts
@packages/lattice/src/providers/openrouter.ts
@packages/lattice/src/providers/xai.ts
@packages/lattice/src/providers/lm-studio.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Wrapper providers double-sanitize output | high | Apply the pipeline only inside `createOpenAICompatibleProvider`; wrapper tests use a mutating sanitizer to prove one pass |
| Sanitized values diverge from replay/debug raw provider body | medium | Preserve `rawResponse` exactly and assert it in tests |
| Absent option causes behavior regression | high | Add tests for current no-option output path |
| Sanitizer option is typed on base adapter but not wrapper factories | medium | Wrapper option types inherit the base option; typecheck confirms factory options accept it |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add sanitizeOutput to OpenAI-compatible options and execution</name>
  <files>packages/lattice/src/providers/adapters.ts</files>
  <read_first>
    - packages/lattice/src/providers/adapters.ts (current shared adapter implementation)
    - packages/lattice/src/sanitizers/index.ts (Plan 36-01 output)
    - packages/lattice/src/providers/provider.ts (ProviderRunResponse shape)
  </read_first>
  <action>
    1. Import `applyOutputSanitizers` and `type SanitizeOutputOption` from `../sanitizers/index.js`.
    2. Add `sanitizeOutput?: SanitizeOutputOption;` to `OpenAICompatibleProviderOptions`.
    3. After extracting `text` and building `rawOutputs`, call:
       ```ts
       const sanitizedOutputs = await applyOutputSanitizers(rawOutputs, options.sanitizeOutput, {
         providerId: id,
         modelId: options.model,
       });
       ```
    4. Return `rawOutputs: sanitizedOutputs`.
    5. Preserve `rawResponse: body` unchanged.
    6. Preserve no-option behavior as closely as possible; when `options.sanitizeOutput` is undefined, `applyOutputSanitizers` should return the original output map.
  </action>
  <verify>
    <automated>rg -n 'sanitizeOutput|SanitizeOutputOption|applyOutputSanitizers|rawResponse: body' packages/lattice/src/providers/adapters.ts</automated>
  </verify>
  <acceptance_criteria>
    - `OpenAICompatibleProviderOptions` contains `sanitizeOutput?: SanitizeOutputOption`.
    - `createOpenAICompatibleProvider` invokes `applyOutputSanitizers`.
    - Sanitizer context uses `providerId: id` and `modelId: options.model`.
    - `rawResponse` still returns `body`.
  </acceptance_criteria>
  <done>OpenAI-compatible adapter applies opt-in sanitizers.</done>
</task>

<task type="auto">
  <name>Task 2: Verify wrapper providers pass through without double application</name>
  <files>
    packages/lattice/src/providers/openrouter.ts,
    packages/lattice/src/providers/xai.ts,
    packages/lattice/src/providers/lm-studio.ts
  </files>
  <read_first>
    - packages/lattice/src/providers/openrouter.ts
    - packages/lattice/src/providers/xai.ts
    - packages/lattice/src/providers/lm-studio.ts
    - packages/lattice/src/providers/adapters.ts (Task 1 output)
  </read_first>
  <action>
    Inspect wrapper factories and make only necessary edits:

    1. Ensure option types continue to inherit `sanitizeOutput` from `OpenAICompatibleProviderOptions`.
    2. Ensure wrapper calls pass `...options` into `createOpenAICompatibleProvider`.
    3. Do not add wrapper-local calls to `applyOutputSanitizers`.
    4. For xAI, preserve existing reasoning-token usage normalization while leaving `rawOutputs` from the inner adapter untouched.
    5. If TypeScript requires explicit type imports or option widening, keep edits minimal and local.
  </action>
  <verify>
    <automated>rg -n 'sanitizeOutput|applyOutputSanitizers|createOpenAICompatibleProvider' packages/lattice/src/providers/openrouter.ts packages/lattice/src/providers/xai.ts packages/lattice/src/providers/lm-studio.ts</automated>
  </verify>
  <acceptance_criteria>
    - Wrapper provider files do not contain wrapper-local `applyOutputSanitizers` calls.
    - Wrapper provider options accept `sanitizeOutput` via inherited base option type.
    - xAI still preserves its reasoning token normalization behavior.
  </acceptance_criteria>
  <done>Wrapper providers delegate sanitizer behavior through the shared adapter once.</done>
</task>

<task type="auto">
  <name>Task 3: Add OpenAI-compatible family tests</name>
  <files>
    packages/lattice/src/providers/adapters.test.ts,
    packages/lattice/src/providers/openrouter.test.ts,
    packages/lattice/src/providers/xai.test.ts,
    packages/lattice/src/providers/lm-studio.test.ts
  </files>
  <read_first>
    - packages/lattice/src/providers/adapters.test.ts
    - packages/lattice/src/providers/openrouter.test.ts
    - packages/lattice/src/providers/xai.test.ts
    - packages/lattice/src/providers/lm-studio.test.ts
    - packages/lattice/src/sanitizers/index.ts
  </read_first>
  <action>
    Add focused tests:

    1. OpenAI-compatible adapter with `sanitizeOutput: unwrapInternalEnvelope({ field: "summary" })` converts provider text `{"summary":"Greeted the user."}` to `Greeted the user.`.
    2. OpenAI adapter inherits the same behavior.
    3. OpenAI-compatible adapter without `sanitizeOutput` returns the current raw string unchanged.
    4. OpenAI-compatible adapter preserves `rawResponse`.
    5. OpenRouter test named with `session_1780792387779` verifies the anchor envelope shape unwraps to `Greeted the user.`.
    6. xAI wrapper test uses a sanitizer that appends a marker and asserts the marker appears once.
    7. LM Studio wrapper test verifies `sanitizeOutput` is accepted and applied through the delegated adapter.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test adapters openrouter xai lm-studio</automated>
  </verify>
  <acceptance_criteria>
    - Tests contain `session_1780792387779`.
    - Tests contain `unwrapInternalEnvelope({ field: "summary" })`.
    - xAI test would fail if sanitizer ran twice.
    - Tests assert `rawResponse` is not sanitized.
    - Verification command exits 0.
  </acceptance_criteria>
  <done>OpenAI-compatible family sanitizer behavior is covered by adapter tests.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
pnpm --filter @full-self-browsing/lattice test adapters openrouter xai lm-studio
pnpm --filter @full-self-browsing/lattice typecheck
```

Plan 36-03 adds Anthropic, Gemini, all-seven parity, changeset, and final phase verification.
</verification>
