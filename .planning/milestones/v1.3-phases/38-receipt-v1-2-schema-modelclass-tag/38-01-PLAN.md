---
phase: 38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/receipts/types.ts
  - packages/lattice/src/receipts/receipt.ts
  - packages/lattice/src/receipts/verify.ts
  - packages/lattice/src/receipts/receipt.test.ts
  - packages/lattice/src/receipts/verify.test.ts
  - packages/lattice/src/receipts/canonical.test.ts
autonomous: true
requirements:
  - RECEIPT12-01
  - RECEIPT12-02
  - RECEIPT12-04
tags:
  - receipts
  - crypto
  - schema-version
must_haves:
  truths:
    - "createReceipt always mints version lattice-receipt/v1.2"
    - "CapabilityReceiptBody.version includes v1, v1.1, and v1.2"
    - "CapabilityReceiptBody.modelClass is optional and typed as TrainingClass"
    - "CreateReceiptInput accepts optional modelClass but not version"
    - "verifyReceipt accepts signed v1.1 and v1.2 receipts"
    - "verifyReceipt still rejects v1 and absent-version downgrade attempts before key lookup where structurally possible"
    - "Unknown future versions such as lattice-receipt/v2 still fail as version-mismatch"
  artifacts:
    - path: "packages/lattice/src/receipts/types.ts"
      provides: "Receipt v1.2 body type and optional modelClass field"
    - path: "packages/lattice/src/receipts/receipt.ts"
      provides: "v1.2 minting and modelClass body assembly"
    - path: "packages/lattice/src/receipts/verify.ts"
      provides: "v1.1/v1.2 verifier compatibility with CRYPTO-01 floor"
    - path: "packages/lattice/src/receipts/verify.test.ts"
      provides: "Compatibility and downgrade regression tests"
  key_links:
    - from: "packages/lattice/src/receipts/types.ts"
      to: "packages/lattice/src/capabilities/profile.ts"
      via: "imports TrainingClass type"
      pattern: "TrainingClass"
    - from: "packages/lattice/src/receipts/receipt.ts"
      to: "packages/lattice/src/receipts/redact.ts"
      via: "modelClass is included before redactReceiptBody"
      pattern: "redactReceiptBody"
---

<objective>
Update the core Capability Receipt schema to v1.2, add optional modelClass to
the signed body, preserve v1.1 verification compatibility, and prove the
CRYPTO-01 downgrade defense still rejects v1 receipts even when a forged
modelClass field is present.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-CONTEXT.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-RESEARCH.md
@.planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-PATTERNS.md
@packages/lattice/src/receipts/types.ts
@packages/lattice/src/receipts/receipt.ts
@packages/lattice/src/receipts/verify.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| v1 downgrade receipts regain acceptance when v1.2 is added | high | Keep CRYPTO-01 minimum-version floor and add forged v1+modelClass regression |
| Unknown future receipt schemas are accidentally accepted | high | Keep unknown literal path returning `version-mismatch` before key lookup |
| modelClass is signed after redaction/canonicalization | high | Add field during initial body assembly before `redactReceiptBody` |
| v1.1 receipts stop verifying after schema bump | medium | Add hand-crafted signed v1.1 positive test |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Widen receipt body type to v1.2 and optional modelClass</name>
  <files>packages/lattice/src/receipts/types.ts</files>
  <read_first>
    - .planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-CONTEXT.md
    - packages/lattice/src/receipts/types.ts
    - packages/lattice/src/capabilities/profile.ts
  </read_first>
  <action>
    Update `packages/lattice/src/receipts/types.ts`:

    1. Add `import type { TrainingClass } from "../capabilities/profile.js";`.
    2. Change `CapabilityReceiptBody.version` to:
       ```ts
       readonly version:
         | "lattice-receipt/v1"
         | "lattice-receipt/v1.1"
         | "lattice-receipt/v1.2";
       ```
    3. Add:
       ```ts
       readonly modelClass?: TrainingClass;
       ```
       near `model` / `route` so it is part of the signed body.
    4. Update nearby comments so they say v1.2 carries `modelClass`; do not say
       step-marker fields are the only v1.1+ extension.
  </action>
  <verify>
    <automated>rg -n 'TrainingClass|lattice-receipt/v1\\.2|modelClass\\?: TrainingClass' packages/lattice/src/receipts/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` imports `TrainingClass` as a type.
    - `CapabilityReceiptBody.version` contains `lattice-receipt/v1.2`.
    - `CapabilityReceiptBody` contains `readonly modelClass?: TrainingClass`.
    - No runtime import from `capabilities/profile.ts` is introduced.
  </acceptance_criteria>
  <done>Receipt body type exposes the v1.2 schema and optional model class.</done>
</task>

<task type="auto">
  <name>Task 2: Mint v1.2 receipts and preserve optional modelClass in signed body</name>
  <files>packages/lattice/src/receipts/receipt.ts</files>
  <read_first>
    - packages/lattice/src/receipts/receipt.ts
    - packages/lattice/src/receipts/types.ts
    - packages/lattice/src/receipts/redact.ts
    - packages/lattice/src/receipts/canonical.ts
  </read_first>
  <action>
    Update `packages/lattice/src/receipts/receipt.ts`:

    1. Import `TrainingClass` as a type from `../capabilities/profile.js`.
    2. In `CreateReceiptInput`, add:
       ```ts
       readonly modelClass?: TrainingClass;
       ```
    3. Update the `CreateReceiptInput` doc comment so `version` is forced to
       `"lattice-receipt/v1.2"`.
    4. Replace the current v1.1 constant with:
       ```ts
       const version: CapabilityReceiptBody["version"] = "lattice-receipt/v1.2";
       ```
    5. Add conditional body assembly:
       ```ts
       ...(input.modelClass !== undefined ? { modelClass: input.modelClass } : {}),
       ```
       before noRouteReasons/tripwire/step-marker spreads.
    6. Do not add `version` to `CreateReceiptInput`.
  </action>
  <verify>
    <automated>rg -n 'modelClass\\?: TrainingClass|lattice-receipt/v1\\.2|input\\.modelClass|CapabilityReceiptBody\\[\"version\"\\]' packages/lattice/src/receipts/receipt.ts</automated>
  </verify>
  <acceptance_criteria>
    - `CreateReceiptInput` contains optional `modelClass`.
    - `createReceipt` assigns version `lattice-receipt/v1.2`.
    - `createReceipt` conditionally spreads `input.modelClass`.
    - `CreateReceiptInput` still has no `version` property.
    - The redact -> canonicalize -> PAE -> sign ordering remains unchanged.
  </acceptance_criteria>
  <done>Receipt minting emits v1.2 and signs modelClass when supplied.</done>
</task>

<task type="auto">
  <name>Task 3: Extend verifier accepted literals without weakening CRYPTO-01</name>
  <files>packages/lattice/src/receipts/verify.ts</files>
  <read_first>
    - packages/lattice/src/receipts/verify.ts
    - packages/lattice/src/receipts/types.ts
    - .planning/phases/26-release-hygiene-docs-receipt-downgrade-defense/26-CONTEXT.md
  </read_first>
  <action>
    Update `packages/lattice/src/receipts/verify.ts`:

    1. In `asReceiptBody`, add `"lattice-receipt/v1.2"` to the accepted known
       literal list.
    2. Keep `undefined` and `"lattice-receipt/v1"` accepted by the structural
       gate so they reach the CRYPTO-01 minimum-version branch.
    3. Keep unknown future literals such as `"lattice-receipt/v2"` returning
       `undefined` from `asReceiptBody`, which maps to `version-mismatch`.
    4. Keep the minimum floor condition exactly:
       ```ts
       body.version === undefined || body.version === "lattice-receipt/v1"
       ```
    5. Update the error message to say v1.1 or v1.2 is required.
    6. Do not import `getCapabilityProfile`, `findCapabilityProfile`, or any
       registry module in `verify.ts`.
  </action>
  <verify>
    <automated>rg -n 'lattice-receipt/v1\\.2|v1\\.1.*v1\\.2|schema-version-too-low|getCapabilityProfile|findCapabilityProfile' packages/lattice/src/receipts/verify.ts</automated>
  </verify>
  <acceptance_criteria>
    - `verify.ts` accepts the v1.2 literal in `asReceiptBody`.
    - `schema-version-too-low` branch still checks absent version and v1.
    - Error message mentions `lattice-receipt/v1.1` and `lattice-receipt/v1.2`.
    - `verify.ts` contains no capability registry import.
  </acceptance_criteria>
  <done>Verifier handles v1.2 while preserving downgrade and future-version gates.</done>
</task>

<task type="auto">
  <name>Task 4: Add receipt schema, compatibility, and downgrade tests</name>
  <files>
    packages/lattice/src/receipts/receipt.test.ts
    packages/lattice/src/receipts/verify.test.ts
    packages/lattice/src/receipts/canonical.test.ts
  </files>
  <read_first>
    - packages/lattice/src/receipts/receipt.test.ts
    - packages/lattice/src/receipts/verify.test.ts
    - packages/lattice/src/receipts/canonical.test.ts
    - packages/lattice/src/receipts/envelope.ts
    - packages/lattice/src/receipts/sign.ts
  </read_first>
  <action>
    Update tests to cover:

    1. Fresh `createReceipt(minimalInput(), signer)` verifies with
       `body.version === "lattice-receipt/v1.2"`.
    2. `createReceipt(minimalInput({ modelClass: "local_quantized" }), signer)`
       preserves `body.modelClass === "local_quantized"`.
    3. Fixed `receiptId`, `issuedAt`, `kid`, signer, and `modelClass` produce
       byte-equal envelope payload and signature across two calls.
    4. A hand-crafted signed v1.1 `CapabilityReceiptBody` verifies cleanly.
    5. A hand-crafted signed v1.2 `CapabilityReceiptBody` with
       `modelClass: "frontier_rlhf"` verifies cleanly.
    6. A hand-crafted signed v1 body with `modelClass: "frontier_rlhf"` fails
       with `schema-version-too-low`.
    7. A v2 body still fails with `version-mismatch`.
    8. Existing step-marker tests are updated to expect v1.2 minting while
       preserving all step fields.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test receipts/receipt receipts/verify receipts/canonical</automated>
  </verify>
  <acceptance_criteria>
    - Receipt tests assert fresh minting returns `lattice-receipt/v1.2`.
    - Receipt tests assert modelClass survives signed payload decode.
    - Verify tests include v1.1 positive control.
    - Verify tests include v1.2 positive control with modelClass.
    - Verify tests include forged v1 + modelClass downgrade rejection.
    - Unknown v2 version still returns `version-mismatch`.
    - Focused receipt test command exits 0.
  </acceptance_criteria>
  <done>Core receipt schema and downgrade behavior are covered by tests.</done>
</task>

</tasks>

<verification>
Run after this plan:

```bash
pnpm --filter @full-self-browsing/lattice test receipts/receipt receipts/verify receipts/canonical
pnpm --filter @full-self-browsing/lattice typecheck
```

Do not mark `RECEIPT12-*` complete yet; runtime population and public-surface
gates are covered by Plans 38-02 and 38-03.
</verification>
