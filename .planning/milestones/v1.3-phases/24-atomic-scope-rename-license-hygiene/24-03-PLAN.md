---
phase: 24-atomic-scope-rename-license-hygiene
plan: 03
type: execute
wave: 1
depends_on:
  - 24-01
  - 24-02
files_modified:
  - packages/lattice/package.json
  - packages/lattice-cli/src/io/keyset-loader.ts
  - packages/lattice-cli/src/io/sidecar-loader.ts
  - packages/lattice-cli/src/io/sidecar-walker.ts
  - packages/lattice-cli/src/io/receipt-loader.ts
  - packages/lattice-cli/src/io/receipt-walker.ts
  - packages/lattice-cli/src/io/artifact-loader.ts
  - packages/lattice-cli/src/commands/repro.ts
  - packages/lattice-cli/src/commands/verify.ts
  - packages/lattice-cli/src/eval/runner.ts
  - packages/lattice-cli/test/repro.test.ts
  - packages/lattice-cli/test/verify.test.ts
  - packages/lattice-cli/test/eval-runner.test.ts
  - packages/lattice-cli/test/sidecar-walker.test.ts
  - packages/lattice-cli/test/receipt-walker.test.ts
  - packages/lattice-cli/test/showcase-e2e.test.ts
  - packages/lattice/test-d/package-types.test-d.ts
  - .changeset/v1.3.0-initial.md
  - pnpm-lock.yaml
autonomous: true
requirements:
  - RENAME-04
  - RENAME-05
  - PKG-05
requirements_addressed:
  - RENAME-04
  - RENAME-05
  - PKG-05

must_haves:
  truths:
    - "Every import string in packages/lattice-cli/src, packages/lattice-cli/test, and packages/lattice/test-d that previously read 'lattice' now reads '@fullselfbrowsing/lattice'"
    - "packages/lattice/package.json#tsd.compilerOptions.paths uses key '@fullselfbrowsing/lattice' (not the bare 'lattice')"
    - "Pre-seeded .changeset/v1.3.0-initial.md exists, frontmatter references both scoped package names with minor bump"
    - "pnpm install regenerates pnpm-lock.yaml with @fullselfbrowsing/lattice-cli depending on workspace @fullselfbrowsing/lattice; no stale unscoped 'lattice' workspace package referenced"
    - "pnpm -r typecheck, pnpm -r test (733/733 tests), pnpm -r test:types, pnpm -r lint:packages all exit 0 after the rename"
    - "pnpm pack on each publishable package produces tarballs whose package.json contains zero standalone unscoped 'lattice' references in deps, exports, types, or tsd paths"
    - "All four files staged across Plans 24-01, 24-02, 24-03 land in ONE atomic commit titled exactly: refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)"
  artifacts:
    - path: "packages/lattice/test-d/package-types.test-d.ts"
      provides: "tsd package-types test importing from scoped name"
      contains: "@fullselfbrowsing/lattice"
    - path: ".changeset/v1.3.0-initial.md"
      provides: "Pre-seeded changeset entry for v1.3.0 initial public release"
      contains: "@fullselfbrowsing/lattice"
    - path: "pnpm-lock.yaml"
      provides: "Regenerated lockfile reflecting scoped workspace package + scoped cli dep + workspace:^ flip"
      contains: "@fullselfbrowsing/lattice"
  key_links:
    - from: "packages/lattice-cli/src/**/*.ts"
      to: "@fullselfbrowsing/lattice workspace package"
      via: "import { ... } from '@fullselfbrowsing/lattice'"
      pattern: "from \"@fullselfbrowsing/lattice\""
    - from: "packages/lattice/package.json#tsd.compilerOptions.paths"
      to: "./dist/index.d.ts"
      via: "tsd type-resolver alias keyed on scoped name"
      pattern: "\"@fullselfbrowsing/lattice\":\\s*\\[\\s*\"\\./dist/index\\.d\\.ts\"\\s*\\]"
    - from: "pnpm-lock.yaml"
      to: "packages/lattice-cli depends on workspace @fullselfbrowsing/lattice"
      via: "pnpm install resolves scoped name to local workspace package"
      pattern: "@fullselfbrowsing/lattice-cli"
    - from: "single atomic commit"
      to: "all 4 file groups (lattice pkg, lattice-cli pkg, imports + tsd + changeset, lockfile)"
      via: "git commit -m 'refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)' (one commit, all files)"
      pattern: "refactor\\(scope\\): rename to @fullselfbrowsing/\\* \\(PHASE-24\\)"
---

<objective>
Complete the atomic scope rename by (a) rewriting every `'lattice'` import string in CLI source + tests + tsd test-d files to `'@fullselfbrowsing/lattice'`, (b) updating the tsd `compilerOptions.paths` map in packages/lattice/package.json, (c) creating the pre-seeded `.changeset/v1.3.0-initial.md` entry referencing the new scope, (d) regenerating the lockfile via `pnpm install`, (e) verifying everything still passes (typecheck + 733/733 tests + test:types + lint:packages publint + attw), (f) running the tarball inspection gate (`pnpm pack` + grep), and (g) producing the SINGLE atomic commit that lands ALL changes from Plans 24-01, 24-02, and 24-03 together.

Purpose: This plan owns RENAME-04 (tsd paths + import rewrites + test:types pass), RENAME-05 (pnpm pack tarball-inspection gate), and PKG-05 (publint + attw clean on both publishable packages). Most critically, this plan owns the atomic-commit gate that ensures the rename and metadata land together so the tarball inspection (RENAME-05) and publint validation (PKG-05) see BOTH simultaneously.

Output:
- Every `from "lattice"` and `vi.doMock("lattice", ...)` / `importOriginal<typeof import("lattice")>()` rewritten to use `@fullselfbrowsing/lattice`
- packages/lattice/package.json tsd paths key updated
- `.changeset/v1.3.0-initial.md` created
- pnpm-lock.yaml regenerated
- 733/733 tests passing, typecheck + test:types + lint:packages all clean
- Tarballs inspected (zero stale `"lattice"` strings in critical fields)
- ONE atomic commit `refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)` covering all Phase 24 files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-atomic-scope-rename-license-hygiene/24-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@packages/lattice/package.json
@packages/lattice-cli/package.json

<interfaces>
**Full list of import sites to rewrite** (already discovered via grep — executor does NOT need to re-grep, just use this list):

`packages/lattice/test-d/package-types.test-d.ts` (lines 10 and 25): two `} from "lattice";` closures of type imports.

`packages/lattice-cli/src/io/keyset-loader.ts:27`: `import { createMemoryKeySet, type KeyEntry, type KeySet } from "lattice";`
`packages/lattice-cli/src/io/sidecar-loader.ts:30`: `} from "lattice";` (multi-line import)
`packages/lattice-cli/src/io/sidecar-walker.ts:34`: `import type { ReceiptEnvelope } from "lattice";`
`packages/lattice-cli/src/io/receipt-loader.ts:17`: `import type { ReceiptEnvelope } from "lattice";`
`packages/lattice-cli/src/io/receipt-walker.ts:27`: `import type { ReceiptEnvelope } from "lattice";`
`packages/lattice-cli/src/io/artifact-loader.ts:19`: `import type { ArtifactInput } from "lattice";`
`packages/lattice-cli/src/commands/repro.ts:44`: `} from "lattice";` (multi-line)
`packages/lattice-cli/src/commands/verify.ts:28`: `import { verifyReceipt, type ReceiptEnvelope } from "lattice";`
`packages/lattice-cli/src/eval/runner.ts:55`: `} from "lattice";` (multi-line)

`packages/lattice-cli/test/repro.test.ts`: line 29 closes a multi-line import; lines 112, 117 use `vi.doUnmock("lattice")`; lines 173, 212, 313, 394, 470, 531, 779 use `vi.doMock("lattice", ...)` AND `importOriginal<typeof import("lattice")>()`.
`packages/lattice-cli/test/verify.test.ts:26`: `} from "lattice";`
`packages/lattice-cli/test/eval-runner.test.ts`: line 38 closes import; lines 210, 298, 356, 421, 558, 655, 745 use `vi.doMock("lattice", ...)` AND `importOriginal<typeof import("lattice")>()`; lines 231, 235 use `vi.doUnmock("lattice")`.
`packages/lattice-cli/test/sidecar-walker.test.ts:29`: `} from "lattice";`
`packages/lattice-cli/test/receipt-walker.test.ts:29`: `} from "lattice";`
`packages/lattice-cli/test/showcase-e2e.test.ts:204`: a string literal `"lattice"` — needs context check (could be referring to the bin name like `spawn("lattice", ...)`, in which case it MUST stay as `"lattice"`; or could be referring to the package import name, in which case it changes).

The tsd block in `packages/lattice/package.json` currently reads:
```json
"tsd": {
  "compilerOptions": {
    "baseUrl": ".",
    "exactOptionalPropertyTypes": true,
    "module": "ESNext",
    "noUncheckedIndexedAccess": true,
    "paths": {
      "lattice": ["./dist/index.d.ts"]
    },
    "strict": true,
    "target": "ES2024"
  }
}
```

The path key `"lattice"` must become `"@fullselfbrowsing/lattice"`. The array value `["./dist/index.d.ts"]` stays unchanged.

**What does NOT change:**
- `bin: { "lattice": "./dist/cli.js" }` in packages/lattice-cli/package.json — that's the user-facing binary name (RENAME-02 success criterion).
- `examples/work-inbox/index.mjs` and `examples/agent-loop/index.mjs` — verified they import from `../../packages/lattice/dist/index.js` (relative path), NOT from the bare `'lattice'` specifier.
- `examples/work-inbox/` has no `package.json` (uses relative dist imports only).
- `examples/agent-loop/package.json` has `"lattice": "workspace:*"` — this DOES change to `"@fullselfbrowsing/lattice": "workspace:^"`.
- CHANGELOG.md history (Phase 26 owns retroactive seeding).
- `packages/lattice-cli/dist/**` — these are build artifacts; they'll be regenerated by `pnpm -r build` during `pnpm -r lint:packages`. Do NOT manually edit dist files.

**Pre-seeded changeset content** for `.changeset/v1.3.0-initial.md` (the YAML-fence lines below are LITERAL triple-dashes `---` that must appear in the created file; they are written here using `<DASH3>` placeholder ONLY to avoid confusing the planning-file frontmatter parser — when writing the actual file, use real triple-dash lines):

```text
<DASH3>
"@fullselfbrowsing/lattice": minor
"@fullselfbrowsing/lattice-cli": minor
<DASH3>

Initial public release under the @fullselfbrowsing scope.
```

Where each `<DASH3>` represents a literal line containing exactly three dash characters and a newline (the standard YAML frontmatter delimiter). The file ends with a single trailing newline.

That is the entire file. No extra prose, no version header, no commit-message-like content. Changesets reads the YAML frontmatter for the bump map and the body for the CHANGELOG entry text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite tsd paths key + every 'lattice' import string + create pre-seeded changeset + audit examples/agent-loop</name>
  <files>
    packages/lattice/package.json
    packages/lattice/test-d/package-types.test-d.ts
    packages/lattice-cli/src/io/keyset-loader.ts
    packages/lattice-cli/src/io/sidecar-loader.ts
    packages/lattice-cli/src/io/sidecar-walker.ts
    packages/lattice-cli/src/io/receipt-loader.ts
    packages/lattice-cli/src/io/receipt-walker.ts
    packages/lattice-cli/src/io/artifact-loader.ts
    packages/lattice-cli/src/commands/repro.ts
    packages/lattice-cli/src/commands/verify.ts
    packages/lattice-cli/src/eval/runner.ts
    packages/lattice-cli/test/repro.test.ts
    packages/lattice-cli/test/verify.test.ts
    packages/lattice-cli/test/eval-runner.test.ts
    packages/lattice-cli/test/sidecar-walker.test.ts
    packages/lattice-cli/test/receipt-walker.test.ts
    packages/lattice-cli/test/showcase-e2e.test.ts
    examples/agent-loop/package.json
    .changeset/v1.3.0-initial.md
  </files>
  <read_first>
    - packages/lattice/package.json (to see existing tsd.compilerOptions.paths block before edit)
    - packages/lattice-cli/test/showcase-e2e.test.ts (lines around 200-210 to determine whether the `"lattice"` literal at line 204 is a bin-name reference or a package-import reference)
    - examples/agent-loop/package.json (to confirm dependency line that needs flipping)
    - .planning/phases/24-atomic-scope-rename-license-hygiene/24-CONTEXT.md sections "Atomic commit shape" and "Tarball inspection gate"
    - All 16 source/test files listed in <files> (each must be opened and the specific lines rewritten with the executor having seen current context)
  </read_first>
  <action>
    Perform the following edits. Each is targeted (use Edit tool, not Write):

    **A. packages/lattice/package.json — update tsd paths key only.**
    In the `tsd.compilerOptions.paths` block, change the key `"lattice"` to `"@fullselfbrowsing/lattice"`. The array value `["./dist/index.d.ts"]` stays the same. Final block reads:
    ```
    "paths": {
      "@fullselfbrowsing/lattice": ["./dist/index.d.ts"]
    }
    ```
    Do NOT touch any other field. Plan 24-01 already changed `name`, added `license`, `repository`, `bugs`, `homepage`, `publishConfig` — those edits are already staged. This edit is additive in the same file.

    **B. Rewrite every `from "lattice"` to `from "@fullselfbrowsing/lattice"` in the 9 source files and 5 simple test files.**
    For each file in:
    - packages/lattice/test-d/package-types.test-d.ts (2 occurrences: lines ~10 and ~25, both closing `} from "lattice";`)
    - packages/lattice-cli/src/io/keyset-loader.ts (line 27)
    - packages/lattice-cli/src/io/sidecar-loader.ts (line 30 — multi-line import closing)
    - packages/lattice-cli/src/io/sidecar-walker.ts (line 34)
    - packages/lattice-cli/src/io/receipt-loader.ts (line 17)
    - packages/lattice-cli/src/io/receipt-walker.ts (line 27)
    - packages/lattice-cli/src/io/artifact-loader.ts (line 19)
    - packages/lattice-cli/src/commands/repro.ts (line 44)
    - packages/lattice-cli/src/commands/verify.ts (line 28)
    - packages/lattice-cli/src/eval/runner.ts (line 55)
    - packages/lattice-cli/test/verify.test.ts (line 26)
    - packages/lattice-cli/test/sidecar-walker.test.ts (line 29)
    - packages/lattice-cli/test/receipt-walker.test.ts (line 29)

    Change the string `"lattice"` to `"@fullselfbrowsing/lattice"` ONLY when it appears as the source of an `import ... from "..."` or `} from "...";` line. Do NOT change comments, strings, or identifiers that happen to contain "lattice".

    **C. packages/lattice-cli/test/repro.test.ts — broader rewrite.**
    This file has both `} from "lattice";` on ~line 29 AND vi.mock helpers on multiple lines. Rewrite:
    - Line ~29 closing import: `"lattice"` → `"@fullselfbrowsing/lattice"`
    - All `vi.doMock("lattice", ...)` (lines 173, 212, 313, 394, 470, 531, 779) → `vi.doMock("@fullselfbrowsing/lattice", ...)`
    - All `vi.doUnmock("lattice")` (lines 112, 117) → `vi.doUnmock("@fullselfbrowsing/lattice")`
    - All `typeof import("lattice")` (same lines as doMock blocks) → `typeof import("@fullselfbrowsing/lattice")`

    The simplest mechanical approach: for this file ONLY, replace every quoted occurrence of `"lattice"` and `"lattice"` (both single and double quotes if any) where the literal appears as a module specifier — i.e. surrounded by `import("`, `from "`, `doMock("`, `doUnmock("`. Do NOT change any unrelated `"lattice"` literal that may exist as a fixture string (open the file, scan once, confirm no false positives before editing).

    **D. packages/lattice-cli/test/eval-runner.test.ts — same pattern as repro.test.ts.**
    Lines ~38 (import closure), 210/298/356/421/558/655/745 (doMock + importOriginal), 231/235 (doUnmock). Same rewrite rule.

    **E. packages/lattice-cli/test/showcase-e2e.test.ts line ~204 — context-dependent.**
    Open the file, read the surrounding ~10 lines around line 204. If the literal `"lattice"` is being passed as the first argument to a `spawn(...)`, `execSync(...)`, child-process command, or similar (meaning it refers to the user-facing CLI binary name), then it MUST STAY as `"lattice"` — that is the bin name, preserved per RENAME-02. If instead it is being used as a package import specifier (e.g. inside `import(...)`, `require(...)`, `vi.mock(...)`, `vi.doMock(...)`, `typeof import(...)`, `from "..."`), then rewrite it to `"@fullselfbrowsing/lattice"`. Document the decision (bin vs. import) in the task SUMMARY.

    **F. examples/agent-loop/package.json — workspace dep flip.**
    Change the dependencies block from:
    ```
    "dependencies": {
      "lattice": "workspace:*"
    }
    ```
    to:
    ```
    "dependencies": {
      "@fullselfbrowsing/lattice": "workspace:^"
    }
    ```
    Same dual rename + caret flip rationale as Plan 24-02 (RENAME-03 risk: workspace:* republishes as exact pin). Even though examples/agent-loop is `"private": true` and never publishes, the workspace resolution must use the new scoped name.

    **G. Create `.changeset/v1.3.0-initial.md`.**
    Create the file `.changeset/v1.3.0-initial.md` with EXACTLY these contents (no leading whitespace, exact YAML frontmatter, blank line, then body):
    ```
    ---
    "@fullselfbrowsing/lattice": minor
    "@fullselfbrowsing/lattice-cli": minor
    ---

    Initial public release under the @fullselfbrowsing scope.
    ```
    If `.changeset/` directory does not exist, create it first. The `config.json` inside .changeset (if it exists) is unrelated to this file and stays unchanged.

    **H. After all edits, stage everything but do NOT commit yet:**
    `git add packages/lattice/package.json packages/lattice/test-d/ packages/lattice-cli/src/ packages/lattice-cli/test/ examples/agent-loop/package.json .changeset/v1.3.0-initial.md`

    Verify staging via `git diff --cached --name-only`. Expect the union of Plans 24-01, 24-02, and 24-03's file lists.
  </action>
  <verify>
    <automated>bash -c 'set -e; node -e "const p=require(\"./packages/lattice/package.json\"); if (p.tsd.compilerOptions.paths[\"@fullselfbrowsing/lattice\"]?.[0] !== \"./dist/index.d.ts\") process.exit(1); if (p.tsd.compilerOptions.paths[\"lattice\"] !== undefined) process.exit(2);"; UNSC=$(grep -rn "from \"lattice\"" packages/lattice/test-d packages/lattice-cli/src packages/lattice-cli/test 2>/dev/null | grep -v dist/ | wc -l | tr -d " "); if [ "$UNSC" != "0" ]; then echo "FAIL: $UNSC unscoped from \"lattice\" still present"; grep -rn "from \"lattice\"" packages/lattice/test-d packages/lattice-cli/src packages/lattice-cli/test | grep -v dist/; exit 3; fi; DOMOCK=$(grep -rn "doMock(\"lattice\"\|doUnmock(\"lattice\"\|import(\"lattice\")" packages/lattice-cli/test 2>/dev/null | wc -l | tr -d " "); if [ "$DOMOCK" != "0" ]; then echo "FAIL: $DOMOCK unscoped vi.mock/import refs to \"lattice\" remain"; exit 4; fi; test -f .changeset/v1.3.0-initial.md || { echo "FAIL: changeset missing"; exit 5; }; grep -q "@fullselfbrowsing/lattice" .changeset/v1.3.0-initial.md && grep -q "@fullselfbrowsing/lattice-cli" .changeset/v1.3.0-initial.md || { echo "FAIL: changeset content"; exit 6; }; node -e "const p=require(\"./examples/agent-loop/package.json\"); if (p.dependencies?.[\"@fullselfbrowsing/lattice\"] !== \"workspace:^\" || p.dependencies?.lattice !== undefined) process.exit(7);"; echo "ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from \"lattice\"" packages/lattice/test-d packages/lattice-cli/src packages/lattice-cli/test 2>/dev/null | grep -v dist/ | wc -l` returns 0
    - `grep -rn "doMock(\"lattice\"\|doUnmock(\"lattice\"\|typeof import(\"lattice\")" packages/lattice-cli/test 2>/dev/null | wc -l` returns 0
    - `grep -rn "from \"@fullselfbrowsing/lattice\"" packages/lattice-cli/src 2>/dev/null | wc -l` returns at least 9 (one per source file rewritten)
    - `node -e "const p=require('./packages/lattice/package.json'); process.exit((p.tsd.compilerOptions.paths['@fullselfbrowsing/lattice']?.[0]==='./dist/index.d.ts' && p.tsd.compilerOptions.paths['lattice']===undefined)?0:1)"` exits 0
    - `node -e "const p=require('./examples/agent-loop/package.json'); process.exit((p.dependencies['@fullselfbrowsing/lattice']==='workspace:^' && p.dependencies['lattice']===undefined)?0:1)"` exits 0
    - `test -f .changeset/v1.3.0-initial.md` succeeds
    - `grep -F '"@fullselfbrowsing/lattice": minor' .changeset/v1.3.0-initial.md` succeeds
    - `grep -F '"@fullselfbrowsing/lattice-cli": minor' .changeset/v1.3.0-initial.md` succeeds
    - `grep -F 'Initial public release under the @fullselfbrowsing scope.' .changeset/v1.3.0-initial.md` succeeds
    - `git diff --cached --name-only | sort` includes (at minimum): `.changeset/v1.3.0-initial.md`, `examples/agent-loop/package.json`, `package.json` (root), `packages/lattice-cli/package.json`, `packages/lattice/package.json`, every file under `packages/lattice-cli/src/` and `packages/lattice-cli/test/` that was rewritten, and `packages/lattice/test-d/package-types.test-d.ts`
    - For `showcase-e2e.test.ts` line 204: the SUMMARY explicitly documents whether the literal was interpreted as bin-name (kept as `"lattice"`) or package-import (rewritten to `"@fullselfbrowsing/lattice"`), with a quote of surrounding code as justification
  </acceptance_criteria>
  <done>
    Every unscoped `"lattice"` module specifier (import / from / vi.doMock / vi.doUnmock / typeof import) across the lattice-cli source + tests + lattice test-d is rewritten to `"@fullselfbrowsing/lattice"`. tsd paths key updated. examples/agent-loop dep flipped (both scope rename AND workspace:^). Pre-seeded changeset exists with exact content. All files staged. The showcase-e2e.test.ts line 204 disposition is documented in the task SUMMARY.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate lockfile, run typecheck + tests + test:types + lint:packages, tarball-inspection gate, then atomic commit</name>
  <files>
    pnpm-lock.yaml
  </files>
  <read_first>
    - packages/lattice/package.json (post-edit, to confirm all manifest fields present before validation)
    - packages/lattice-cli/package.json (post-edit, to confirm scoped name + workspace:^ dep)
    - .planning/phases/24-atomic-scope-rename-license-hygiene/24-CONTEXT.md section "Tarball inspection gate"
    - The previous task's verification output (all import rewrites confirmed)
  </read_first>
  <action>
    Run the full validation sequence and produce the atomic commit. Execute these steps in order, halting on first failure:

    **Step 1 — Regenerate the lockfile.**
    Run `pnpm install` from the repo root. This regenerates `pnpm-lock.yaml` to reflect:
    - packages/lattice publishing as `@fullselfbrowsing/lattice`
    - packages/lattice-cli depending on `@fullselfbrowsing/lattice: workspace:^`
    - examples/agent-loop depending on `@fullselfbrowsing/lattice: workspace:^`
    Stage the updated lockfile: `git add pnpm-lock.yaml`. If `pnpm install` fails, fix the failure (likely a leftover unscoped `lattice` reference somewhere) before proceeding — do NOT commit a broken lockfile.

    **Step 2 — Typecheck.**
    Run `pnpm -r typecheck`. Must exit 0. If failures, the most likely cause is a missed import rewrite in a file Task 1 didn't catch. Re-grep `grep -rn "from \"lattice\"" packages/ | grep -v dist/`, fix, re-stage, re-run.

    **Step 3 — Full test suite.**
    Run `pnpm -r test`. Must exit 0 with all 733 tests passing (or whatever the current count is — the criterion is "matches the count from STATE.md, currently 733, with zero failures"). If failures, diagnose. Common causes: vi.doMock("lattice") still referenced somewhere; or showcase-e2e.test.ts line 204 was rewritten when it should have stayed as bin-name (or vice versa).

    **Step 4 — Type tests.**
    Run `pnpm -r test:types`. Must exit 0. This validates that the tsd paths key rewrite (Task 1, edit A) successfully resolves `@fullselfbrowsing/lattice` to `./dist/index.d.ts` for the test-d files.

    **Step 5 — Publint + attw on both publishable packages (PKG-05).**
    Run `pnpm -r lint:packages`. Must exit 0 on both lattice and lattice-cli. This runs:
    - `pnpm build` (tsdown emits dist for each package)
    - `publint` (validates package.json metadata, exports map, files glob)
    - `attw --pack . --profile esm-only` (validates types-on-publish, ESM-only profile, scoped name resolution)
    - For lattice only: `node ./scripts/check-cli-deps.mjs` (already in repo; validates no CLI-only deps leak into runtime)

    If publint flags missing metadata, the cause is in Plan 24-01 or 24-02's manifest fields — fix the field, re-stage, re-run. If attw flags type-resolution failures under the scoped name, the cause is the tsd paths key or a missed export — fix and re-run.

    **Step 6 — Tarball inspection gate (RENAME-05).**
    For each publishable package, produce a packed tarball and grep for any stale unscoped `"lattice"` string in the published package.json:
    ```
    cd packages/lattice && pnpm pack --pack-destination /tmp/lattice-pack && cd ../..
    cd packages/lattice-cli && pnpm pack --pack-destination /tmp/lattice-pack && cd ../..
    ```
    Then for each tarball:
    ```
    tar -xzf /tmp/lattice-pack/fullselfbrowsing-lattice-0.0.0.tgz package/package.json -O > /tmp/lattice-pack/lattice-pkg.json
    tar -xzf /tmp/lattice-pack/fullselfbrowsing-lattice-cli-0.0.0.tgz package/package.json -O > /tmp/lattice-pack/lattice-cli-pkg.json
    ```
    (Adjust file names if pnpm pack names them differently; the actual naming is `<scope>-<name>-<version>.tgz` for scoped pkgs after pnpm flattens the scope.)

    Then grep each extracted package.json for any standalone `"lattice"` in critical fields (dep keys, exports keys, types value, tsd paths). The check:
    ```
    node -e '
      const lat = require("/tmp/lattice-pack/lattice-pkg.json");
      const cli = require("/tmp/lattice-pack/lattice-cli-pkg.json");
      const badKeys = [];
      for (const [k, _] of Object.entries(lat.dependencies || {})) if (k === "lattice") badKeys.push("lattice/deps/lattice");
      for (const [k, _] of Object.entries(cli.dependencies || {})) if (k === "lattice") badKeys.push("lattice-cli/deps/lattice");
      for (const [k, _] of Object.entries(lat.tsd?.compilerOptions?.paths || {})) if (k === "lattice") badKeys.push("lattice/tsd/lattice");
      if (lat.name !== "@fullselfbrowsing/lattice") badKeys.push("lattice/name not scoped");
      if (cli.name !== "@fullselfbrowsing/lattice-cli") badKeys.push("lattice-cli/name not scoped");
      // bin.lattice is EXPECTED to remain unchanged (user-facing command)
      if (cli.bin?.lattice !== "./dist/cli.js") badKeys.push("lattice-cli/bin/lattice missing (user-facing command broken!)");
      if (badKeys.length) { console.error("TARBALL INSPECTION FAILED:", badKeys); process.exit(1); }
      console.log("tarball inspection passed");
    '
    ```
    If the grep finds ANY stale unscoped `"lattice"` in a dependency key, exports key, types path, or tsd paths key, the phase fails. The ONLY allowed unscoped `"lattice"` is `bin.lattice` in the CLI tarball — that's the user-facing binary name (RENAME-02 success criterion).

    Clean up the temp tarballs: `rm -rf /tmp/lattice-pack`.

    **Step 7 — Atomic commit.**
    With the lockfile staged and all validations green, produce ONE git commit covering every change from Plans 24-01, 24-02, 24-03:
    ```
    git add pnpm-lock.yaml
    git status --porcelain
    ```
    Verify `git status --porcelain` shows ONLY staged files (no unstaged modifications). The staged set should include:
    - `package.json` (root, from 24-01)
    - `packages/lattice/package.json` (from 24-01 + this plan's tsd paths key edit)
    - `packages/lattice-cli/package.json` (from 24-02)
    - `packages/lattice/test-d/package-types.test-d.ts`
    - All packages/lattice-cli/src/* rewrites
    - All packages/lattice-cli/test/* rewrites
    - `examples/agent-loop/package.json`
    - `.changeset/v1.3.0-initial.md`
    - `pnpm-lock.yaml`

    Then commit with EXACTLY this message (use HEREDOC as required by the commit guidance):
    ```
    git commit -m "$(cat <<'EOF'
    refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)

    Atomic scope rename + license/metadata hygiene per Phase 24 plan.

    - packages/lattice -> @fullselfbrowsing/lattice
    - packages/lattice-cli -> @fullselfbrowsing/lattice-cli
    - packages/lattice-cli dep: "lattice": "workspace:*" -> "@fullselfbrowsing/lattice": "workspace:^"
    - tsd compilerOptions.paths key rewritten to scoped name
    - Every "from \"lattice\"" import in packages/lattice-cli/src, packages/lattice-cli/test, packages/lattice/test-d rewritten to "@fullselfbrowsing/lattice"
    - examples/agent-loop dependency flipped to scoped + workspace:^
    - Added license: MIT, repository (with directory), bugs, homepage, publishConfig.access: public to both publishable packages
    - Added license: MIT to workspace root (private: true preserved; no publishConfig at root)
    - Pre-seeded .changeset/v1.3.0-initial.md (minor bump on both scoped packages)
    - CLI bin name "lattice" preserved (user-facing command unchanged)
    - 733/733 tests passing; pnpm -r typecheck + test:types + lint:packages (publint + attw) all clean
    - pnpm pack tarball inspection: zero stale unscoped "lattice" in dep keys, exports, types, or tsd paths

    Addresses: RENAME-01, RENAME-02, RENAME-03, RENAME-04, RENAME-05, PKG-01, PKG-02, PKG-03, PKG-04, PKG-05

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    DO NOT use `--amend`. DO NOT split into multiple commits. DO NOT skip hooks. If a pre-commit hook fails, diagnose the underlying issue, fix it, re-stage the fix, and create a NEW commit (per gsd commit safety protocol).

    **Step 8 — Confirm commit.**
    Run `git log -1 --stat` and confirm:
    - Commit subject is exactly `refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)`
    - The file list in `--stat` includes every Phase 24 file
    - The commit is a single commit on the current branch (no merge, no amend)
  </action>
  <verify>
    <automated>bash -c 'set -e; pnpm -r typecheck >/dev/null 2>&1 || { echo "typecheck FAILED"; exit 1; }; pnpm -r test >/dev/null 2>&1 || { echo "tests FAILED"; exit 2; }; pnpm -r test:types >/dev/null 2>&1 || { echo "test:types FAILED"; exit 3; }; pnpm -r lint:packages >/dev/null 2>&1 || { echo "lint:packages FAILED"; exit 4; }; LAST_MSG=$(git log -1 --pretty=%s); if [ "$LAST_MSG" != "refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)" ]; then echo "commit subject mismatch: $LAST_MSG"; exit 5; fi; LAST_FILES=$(git log -1 --name-only --pretty=); for f in package.json packages/lattice/package.json packages/lattice-cli/package.json .changeset/v1.3.0-initial.md pnpm-lock.yaml examples/agent-loop/package.json; do echo "$LAST_FILES" | grep -qx "$f" || { echo "missing $f in last commit"; exit 6; }; done; echo "ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r typecheck` exits 0
    - `pnpm -r test` exits 0 and reports 733 (or current) tests passing with 0 failures
    - `pnpm -r test:types` exits 0
    - `pnpm -r lint:packages` exits 0 (publint clean on both packages, attw clean on both packages under esm-only profile, check-cli-deps clean for lattice)
    - `pnpm pack` succeeds in BOTH `packages/lattice` and `packages/lattice-cli`
    - Tarball inspection script (Step 6) exits 0 — zero standalone `"lattice"` in dep keys, exports keys, types value, or tsd paths in either tarball's package.json
    - `cli.bin.lattice === "./dist/cli.js"` confirmed in the CLI tarball (user-facing command preserved)
    - `git log -1 --pretty=%s` outputs exactly `refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)`
    - `git log -1 --name-only` lists ALL of: `package.json`, `packages/lattice/package.json`, `packages/lattice-cli/package.json`, `.changeset/v1.3.0-initial.md`, `pnpm-lock.yaml`, `examples/agent-loop/package.json`, `packages/lattice/test-d/package-types.test-d.ts`, and all the lattice-cli/src + lattice-cli/test rewrite files
    - `git log -1 --pretty=%B` body contains `Addresses: RENAME-01, RENAME-02, RENAME-03, RENAME-04, RENAME-05, PKG-01, PKG-02, PKG-03, PKG-04, PKG-05`
    - `git status --porcelain` returns empty (clean tree after commit)
    - `/tmp/lattice-pack` directory cleaned up (no leftover .tgz files)
    - `grep -rE '(from|import\(|doMock\(|doUnmock\()\s*"lattice"' packages/ examples/ --include="*.ts" --include="*.mjs" --include="*.json" 2>/dev/null | grep -v dist/ | grep -v 'bin"\?:\s*{' | wc -l` returns 0 (no stale module-specifier `"lattice"` anywhere outside dist/ build artifacts and the CLI bin name)
  </acceptance_criteria>
  <done>
    Lockfile regenerated and committed. All four test gates (typecheck, test, test:types, lint:packages) green. Tarball inspection passed on both publishable packages with zero stale unscoped `"lattice"` strings in critical fields and `bin.lattice` preserved in the CLI tarball. Single atomic commit landed on the current branch with the exact required subject line and a body listing all 10 REQ-IDs addressed. Tree is clean post-commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workspace → published npm tarball | The tarballs produced by `pnpm pack` are what external consumers will eventually receive; any unscoped `"lattice"` leak in critical fields would mislead consumers into pulling a different (potentially squatted) package |
| current commit → future v1.3.0 publish (Phase 28) | The atomic commit produced here is consumed by every subsequent release-phase commit; a partial rename would propagate into provenance attestations and `package.json` snapshots on the registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `packages/lattice-cli/package.json#dependencies` | mitigate | Task 1 edit D + Task 2 tarball-inspection grep gate confirm the unscoped `"lattice"` dep key is removed in BOTH the workspace manifest AND the packed tarball; per PITFALLS RENAME-1, `workspace:*` republishes through changesets as a literal value, so a missed rewrite would silently publish a manifest pointing at the unscoped (potentially squatted) `lattice` package |
| T-24-02 | Spoofing | external consumers of @fullselfbrowsing/lattice-cli `lattice` binary | mitigate | `bin: { lattice: "./dist/cli.js" }` preservation is explicitly tested in Task 2 Step 6 tarball inspection (`cli.bin.lattice === "./dist/cli.js"`); without this check, a misguided full-replace would rename the binary and break every user's `lattice --version` invocation in the documented quick-start (PITFALLS RENAME-2 — documentation trap) |
| T-24-03 | Information Disclosure | workspace root package.json | mitigate | Plan 24-01 Task 2 acceptance criteria assert `p.private === true && p.publishConfig === undefined` so `.planning/`, `tools/`, internal scripts cannot accidentally publish if a future contributor flips access; PKG-04 enforced at the gate |
| T-24-04 | Tampering | `.changeset/v1.3.0-initial.md` | accept | The changeset is checked into git and reviewed in the PR; downstream tampering would surface as a changeset diff in the next CHANGELOG.md. Pre-publish review process out of scope for Phase 24 |
| T-24-05 | Repudiation | atomic commit author identity | accept | The single commit lands on `main` via the user's git config; Phase 28 will introduce OIDC-bound publish provenance that ties the commit SHA to the release. Phase 24 itself uses standard git authorship |
| T-24-06 | Denial of Service | pnpm install lockfile regeneration | mitigate | Task 2 Step 1 runs `pnpm install` and fails the phase if lockfile generation errors; this catches any leftover unscoped reference before it can break downstream CI |
</threat_model>

<verification>
After this plan completes (end of Phase 24):

1. `git log -1 --pretty=oneline` shows a single commit `refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)` on the current branch.
2. `git log -1 --name-only | wc -l` is large enough to include all 18+ modified files (15 rewrite files + 2 manifest files + lockfile + changeset).
3. `grep -rE '(from|import\(|doMock\(|doUnmock\()\s*"lattice"' packages/ examples/ --include="*.ts" --include="*.mjs" --include="*.json" 2>/dev/null | grep -v dist/ | wc -l` returns 0.
4. `grep -c '"lattice"' packages/lattice-cli/package.json` returns exactly 1 (the bin entry).
5. `node -e "const p=require('./packages/lattice/package.json'); console.log(p.name)"` prints `@fullselfbrowsing/lattice`.
6. `node -e "const p=require('./packages/lattice-cli/package.json'); console.log(p.name, JSON.stringify(p.dependencies))"` prints `@fullselfbrowsing/lattice-cli {"@fullselfbrowsing/lattice":"workspace:^","citty":"catalog:"}`.
7. `cat .changeset/v1.3.0-initial.md` matches the required template exactly.
8. `pnpm -r test 2>&1 | tail -5` confirms all tests passing.
9. The phase work is on the working branch, ready for Phase 25 (CI workflow scaffolding) to consume.
</verification>

<success_criteria>
- Single atomic commit `refactor(scope): rename to @fullselfbrowsing/* (PHASE-24)` lands on the branch with ALL Phase 24 file changes (lattice package, lattice-cli package, root package, all import rewrites, tsd paths, agent-loop example, changeset, lockfile).
- Zero stale unscoped `"lattice"` references remain as module specifiers anywhere in `packages/lattice/test-d`, `packages/lattice-cli/src`, `packages/lattice-cli/test`, or `examples/`. The ONLY allowed unscoped `"lattice"` is `bin.lattice` in `packages/lattice-cli/package.json` (user-facing command, preserved per RENAME-02).
- `pnpm -r typecheck`, `pnpm -r test` (733/733 passing), `pnpm -r test:types`, `pnpm -r lint:packages` (publint + attw + check-cli-deps) all exit 0.
- `pnpm pack` succeeds for both publishable packages and the extracted package.json from each tarball shows the scoped name and zero stale unscoped `"lattice"` in dep keys, exports keys, types path, or tsd paths.
- `.changeset/v1.3.0-initial.md` exists with the exact template content (minor bump on both scoped packages, body "Initial public release under the @fullselfbrowsing scope.").
- RENAME-04 (tsd paths + imports + test:types pass), RENAME-05 (tarball inspection gate), and PKG-05 (publint + attw clean) all addressed by this plan.
- Combined with Plans 24-01 and 24-02 staging, this plan's atomic commit covers all 10 Phase 24 REQ-IDs: RENAME-01..05 + PKG-01..05.
</success_criteria>

<output>
After completion, create `.planning/phases/24-atomic-scope-rename-license-hygiene/24-03-SUMMARY.md` documenting:
- The complete list of files rewritten (with line counts of `"lattice"` → `"@fullselfbrowsing/lattice"` replacements per file)
- The disposition of `packages/lattice-cli/test/showcase-e2e.test.ts` line 204 (bin-name preserved vs. module-specifier rewritten, with the quoted code context that justified the decision)
- The 733/733 (or current count) test pass confirmation
- The tarball inspection result (both tarballs grep-clean of stale unscoped `"lattice"` in critical fields; CLI tarball's `bin.lattice` preserved)
- The single atomic commit SHA and subject line
- Confirmation that Phase 24 closed all 10 REQ-IDs (RENAME-01..05 + PKG-01..05)
- Any anomalies discovered (e.g., additional `"lattice"` references found beyond the planned list, with disposition)
</output>
