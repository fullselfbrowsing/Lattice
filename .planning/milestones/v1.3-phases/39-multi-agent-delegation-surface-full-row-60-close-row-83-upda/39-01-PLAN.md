---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - packages/lattice/src/receipts/cid.ts
  - packages/lattice/src/receipts/cid.test.ts
  - packages/lattice/src/receipts/types.ts
  - packages/lattice/src/receipts/receipt.ts
  - packages/lattice/src/receipts/receipt.test.ts
  - packages/lattice/src/receipts/verify.test.ts
  - packages/lattice/test-d/receipt-v12.test-d.ts
autonomous: true
requirements: [DELEG-06]
must_haves:
  truths:
    - "DELEG-01..08 are authored in .planning/REQUIREMENTS.md with traceability rows, so the milestone audit can count 87/87"
    - "A receipt envelope yields a deterministic content-addressed CID (sha256:<hex> of the decoded DSSE payload bytes) without keys or re-signing"
    - "A v1.2 receipt minted with parentReceiptCid verifies cleanly; a forged v1/v1.1 downgrade carrying parentReceiptCid is rejected (CRYPTO-01 non-regression)"
  artifacts:
    - path: "packages/lattice/src/receipts/cid.ts"
      provides: "receiptCid(envelope): Promise<string> helper"
      exports: ["receiptCid"]
    - path: "packages/lattice/src/receipts/types.ts"
      provides: "CapabilityReceiptBody.parentReceiptCid?: string (additive optional, no schema bump)"
      contains: "parentReceiptCid"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DELEG-01..08 authored + traceability rows"
      contains: "DELEG-01"
  key_links:
    - from: "packages/lattice/src/receipts/receipt.ts"
      to: "CapabilityReceiptBody.parentReceiptCid"
      via: "conditional spread in body assembly (modelClass precedent at receipt.ts:103)"
      pattern: "parentReceiptCid !== undefined"
    - from: "packages/lattice/src/receipts/cid.ts"
      to: "envelope.payload"
      via: "atob decode + crypto.subtle.digest SHA-256"
      pattern: "crypto\\.subtle\\.digest"
---

<objective>
Author the 8 missing DELEG requirement IDs into REQUIREMENTS.md, and land the receipt-chain substrate: a content-addressed `receiptCid` helper plus the additive-optional `parentReceiptCid` field on the v1.2 receipt body — with a CRYPTO-01 downgrade-defense non-regression matrix.

Purpose: Phase 39's receipt chaining (DELEG-06) and the milestone audit's 87/87 REQ-ID count both depend on this. The receipts module is independent of the agent code, so this lands in Wave 1.
Output: DELEG-01..08 authored; `receipts/cid.ts` + tests; `parentReceiptCid` on `CapabilityReceiptBody` + `CreateReceiptInput`; extended verify/receipt test matrices; extended tsd coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-PATTERNS.md

Key research sections: `<phase_requirements>` (draft DELEG wording), Pattern 2 (Receipt CID + chain), Code Examples (receiptCid helper), Pitfall 6 (exactOptionalPropertyTypes).
</context>

<interfaces>
From packages/lattice/src/receipts (Phase 38 state, verified by research):

- `CapabilityReceiptBody.version` is the literal union `"lattice-receipt/v1" | "lattice-receipt/v1.1" | "lattice-receipt/v1.2"`; `modelClass?: TrainingClass` sits at types.ts:54-57 — `parentReceiptCid?: string` lands beside it.
- `createReceipt` body assembly uses conditional spread: `...(input.modelClass !== undefined ? { modelClass: input.modelClass } : {})` (receipt.ts:103) — copy this line shape for `parentReceiptCid`.
- `asReceiptBody` (verify.ts:36-63) checks only required fields; unknown optional fields pass structurally and are re-canonicalized byte-stably (verify.ts:149).
- CID building blocks: sha256-hex via `crypto.subtle.digest` (storage/fingerprint.ts:14-19, toHex at :48-50); Buffer-free base64 decode `Uint8Array.from(atob(envelope.payload), (c) => c.charCodeAt(0))` (contract/checkpoint.ts:254-256).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author DELEG-01..08 in .planning/REQUIREMENTS.md with traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current structure: RECEIPT12 section ends at line ~148; counts table at ~152-182; traceability table at ~220+)
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md `<phase_requirements>` table (draft wording for all 8 IDs)
  </read_first>
  <action>
    Author the 8 DELEG requirements using the RESEARCH.md draft wording (adjust phrasing for consistency with existing REQ-ID style — declarative, testable, one paragraph each):

    1. Insert a new section `### Multi-Agent Delegation Surface (\`DELEG-*\`)` immediately after the RECEIPT12 section (before the `---` / `## Total Requirements` divider), containing `- [ ] **DELEG-01**` through `- [ ] **DELEG-08**` with the RESEARCH draft wording. Key content per ID: DELEG-01 `defineAgent`/`AgentSpec`; DELEG-02 `runAgentCrew` + `CrewPolicy` + budget pool + cost aggregation; DELEG-03 CrewDispatcher + summary re-entry + failure routing + cycle prevention + `crew-budget-exceeded`; DELEG-04 cache-prefix sharing (Anthropic `cache_control` + OpenAI automatic) with assertable counters; DELEG-05 `createRateLimitGroup` dual-dimension lease bucket + transport injection; DELEG-06 `parentReceiptCid` + CID helper + CRYPTO-01 non-regression; DELEG-07 `examples/agent-crew/` + `evalAgentRun` gate; DELEG-08 AGENTS.md/gaps-doc flips + public exports + publint/attw/tsd + changeset.
    2. Update `## Total Requirements`: change "**79 authored REQ-IDs** across **19 categories**" to "**87 authored REQ-IDs** across **20 categories**"; update the "53 / 79" complete fraction to "53 / 87"; add row `| DELEG | 8 | Phase 39 |` to the authored-category table; DELETE the "Planned but not yet authored" table and its lead-in sentence.
    3. In `## Traceability`: change the intro sentence "Phase 39 still needs detailed REQ-ID authoring before execution." to note DELEG IDs were authored 2026-06-10 by the Phase 39 planner. Append 8 rows after RECEIPT12-04: DELEG-01 → Phase 39 / 39-03 / pending; DELEG-02 → 39-03 / 39-06 / pending; DELEG-03 → 39-03 / 39-05 / 39-06 / pending; DELEG-04 → 39-04 / 39-05 / pending; DELEG-05 → 39-02 / 39-06 / pending; DELEG-06 → 39-01 / 39-05 / pending; DELEG-07 → 39-07 / pending; DELEG-08 → 39-08 / pending.
    4. Append a footnote line at the bottom matching the existing convention: `*Phase 39 REQ-IDs (DELEG-01..08) added: 2026-06-10 — plan-phase prerequisite*`.

    Do NOT change any existing REQ-ID text or checkbox state.
  </action>
  <verify>
    <automated>grep -c '\*\*DELEG-0[1-8]\*\*' .planning/REQUIREMENTS.md | grep -qx 8 && grep -q '| DELEG | 8 | Phase 39 |' .planning/REQUIREMENTS.md && ! grep -q 'Planned but not yet authored' .planning/REQUIREMENTS.md && grep -c '^| DELEG-0[1-8] | Phase 39' .planning/REQUIREMENTS.md | grep -qx 8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\*\*DELEG-0[1-8]\*\*' .planning/REQUIREMENTS.md` returns exactly 8
    - All 8 DELEG entries use unchecked `- [ ]` boxes
    - Counts header reads "87 authored REQ-IDs" and "20 categories"; the "Planned but not yet authored" table is gone
    - Traceability table has 8 DELEG rows, each with status `pending` and the plan mapping listed in the action
    - No existing REQ-ID line was modified (git diff touches only the insertions/count edits)
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains all 87 v1.3 REQ-IDs; milestone-audit precondition satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: receiptCid content-address helper</name>
  <files>packages/lattice/src/receipts/cid.ts, packages/lattice/src/receipts/cid.test.ts</files>
  <read_first>
    - packages/lattice/src/storage/fingerprint.ts (sha256 digest + toHex + ArrayBuffer copy idiom, lines 14-19, 48-57)
    - packages/lattice/src/contract/checkpoint.ts (Buffer-free atob payload decode, lines 252-261)
    - packages/lattice/src/receipts/receipt.ts (envelope shape `{ payload, payloadType, signatures }`; how envelopes are minted, for test fixtures)
    - packages/lattice/src/receipts/verify.ts (payload byte-equality proof at lines 149-155 — why payload-digest CIDs are stable)
  </read_first>
  <behavior>
    - Test 1: `receiptCid(envelope)` returns a string matching `/^sha256:[0-9a-f]{64}$/`
    - Test 2: deterministic — calling twice on the same envelope returns identical strings
    - Test 3: two envelopes minted from different bodies produce different CIDs
    - Test 4: the CID equals an independently computed `"sha256:" + hex(SHA-256(Uint8Array.from(atob(envelope.payload), c => c.charCodeAt(0))))` in the test (re-derivation cross-check)
    - Test 5: CID is derivable from a verified envelope without any KeySet/signer (no key material passed)
  </behavior>
  <action>
    Create `packages/lattice/src/receipts/cid.ts` exporting `export async function receiptCid(envelope: ReceiptEnvelope): Promise<string>` per RESEARCH.md Code Examples: decode `envelope.payload` via `Uint8Array.from(atob(...), c => c.charCodeAt(0))`, digest with `crypto.subtle.digest("SHA-256", ...)` (copy bytes into a fresh `ArrayBuffer` using the fingerprint.ts:52-57 idiom), hex-encode with the padStart(2,"0") join pattern, return `` `sha256:${hex}` ``. Web-standard APIs only — no Node Buffer (checkpoint.ts:255 precedent). Module doc header: `/** Receipt CID — Phase 39 (v1.3). Content address = sha256 of the DSSE canonical payload bytes (the exact bytes that were signed). */`. Import `ReceiptEnvelope` as `import type` from the receipts types/receipt module (use whichever file exports it; check during read_first). Tests in colocated `cid.test.ts` mint real envelopes via `createReceipt` + an ephemeral Ed25519 signer (reuse the fixture helpers receipt.test.ts already uses).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/receipts/cid.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cid.test.ts` passes with all 5 behaviors covered
    - `grep -c 'Buffer' packages/lattice/src/receipts/cid.ts` returns 0
    - `receiptCid` output format is exactly `sha256:` + 64 lowercase hex chars
    - `pnpm --filter @full-self-browsing/lattice typecheck` passes
  </acceptance_criteria>
  <done>Content-addressed receipt CIDs derivable from any envelope; dispatcher (39-05) can chain receipts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: parentReceiptCid on the v1.2 body + CRYPTO-01 non-regression matrix</name>
  <files>packages/lattice/src/receipts/types.ts, packages/lattice/src/receipts/receipt.ts, packages/lattice/src/receipts/receipt.test.ts, packages/lattice/src/receipts/verify.test.ts, packages/lattice/test-d/receipt-v12.test-d.ts</files>
  <read_first>
    - packages/lattice/src/receipts/types.ts (modelClass field + doc comment at lines 54-57 — the additive-optional template)
    - packages/lattice/src/receipts/receipt.ts (CreateReceiptInput member at :39; conditional spread at :103)
    - packages/lattice/src/receipts/verify.test.ts (Phase 38 RECEIPT12-02 forged-downgrade test patterns to replicate)
    - packages/lattice/test-d/receipt-v12.test-d.ts (existing type-test shape to extend)
    - .planning/phases/38-receipt-v1-2-schema-modelclass-tag/38-CONTEXT.md (parentReceiptCid explicitly deferred to Phase 39 on v1.2 — no version bump)
  </read_first>
  <behavior>
    - Test 1: `createReceipt({ ..., parentReceiptCid: "sha256:<64hex>" })` mints a v1.2 body containing the field; omitting the input leaves the field absent from the canonical JSON (not `undefined`)
    - Test 2: a signed v1.2 receipt WITH `parentReceiptCid` verifies cleanly and round-trips DSSE/JCS byte-stably (serialize → deserialize → verify, payload bytes equal)
    - Test 3: forged v1 body carrying `parentReceiptCid` → rejected `schema-version-too-low`; absent-version body carrying the field → `schema-version-too-low` (CRYPTO-01 non-regression)
    - Test 4: forged `"lattice-receipt/v2"` literal → `version-mismatch`; previously-signed v1.1 receipt (no new field) still verifies
    - Test 5: tampering `parentReceiptCid` post-signing → `canonicalization-mismatch` or `signature-invalid`
    - Type test: `CapabilityReceiptBody["parentReceiptCid"]` is `string | undefined`; `CreateReceiptInput` accepts the optional member
  </behavior>
  <action>
    Add `readonly parentReceiptCid?: string;` to `CapabilityReceiptBody` in types.ts beside `modelClass`, with a Phase 39 doc comment noting: chain-link to the crew-root receipt's CID (`sha256:<hex>` of the parent envelope's canonical payload bytes); stable identifier, not user content (redaction-exempt per the step-marker-field rationale); absent on root/non-crew receipts. Add `readonly parentReceiptCid?: string;` to `CreateReceiptInput` in receipt.ts and one conditional-spread line in body assembly mirroring the modelClass line exactly: `...(input.parentReceiptCid !== undefined ? { parentReceiptCid: input.parentReceiptCid } : {})`. NEVER assign `undefined` literally (JCS throws — Pitfall 6). Extend receipt.test.ts (mint include/omit + byte-stability) and verify.test.ts (downgrade matrix per behaviors 3-5, replicating the Phase 38 RECEIPT12-02 test structure). Extend test-d/receipt-v12.test-d.ts with the new optional member assertions. No schema-version change anywhere — v1.2 literal union stays as-is.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/receipts/receipt.test.ts src/receipts/verify.test.ts && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All receipt + verify tests pass including the new matrix; no existing test modified to weaken assertions
    - `grep -c 'parentReceiptCid' packages/lattice/src/receipts/types.ts` >= 1 and `grep -c 'parentReceiptCid !== undefined' packages/lattice/src/receipts/receipt.ts` == 1
    - Forged v1 + parentReceiptCid test asserts the exact error kind string `schema-version-too-low`
    - `pnpm test:types` (tsd) passes for the lattice package
    - The version literal union in types.ts is unchanged (still ends at `"lattice-receipt/v1.2"`)
  </acceptance_criteria>
  <done>Receipt chain field live on v1.2 with proven downgrade-defense non-regression; DELEG-06 schema surface complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| receipt envelope → verifier | untrusted/forged envelopes cross into `verifyReceipt` |
| receipt body field values → chain consumers | `parentReceiptCid` is consumed by audit tooling resolving chains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | verifyReceipt downgrade gate | mitigate | Task 3 matrix: forged v1/absent-version bodies carrying `parentReceiptCid` rejected `schema-version-too-low` pre-crypto; `v2` literal → `version-mismatch` (CRYPTO-01 non-regression) |
| T-39-02 | Tampering/Repudiation | parentReceiptCid chain link | mitigate | Field lives inside the signed JCS body; CID is sha256 of signed payload bytes — tamper test asserts `canonicalization-mismatch`/`signature-invalid` |
| T-39-03 | Information disclosure | new receipt field | mitigate | `parentReceiptCid` is a hash, never free-form user content; doc comment forbids free-form crew names in receipt identifier fields |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH.md Package Legitimacy Audit: none; D-18 zero new runtime deps) |
</threat_model>

<verification>
- `pnpm --filter @full-self-browsing/lattice test -- src/receipts` green
- `pnpm --filter @full-self-browsing/lattice typecheck && pnpm --filter @full-self-browsing/lattice test:types` green
- REQUIREMENTS.md grep gates from Task 1 pass
</verification>

<success_criteria>
- DELEG-01..08 authored with traceability; 87/87 REQ-IDs present in REQUIREMENTS.md
- `receiptCid` helper exists, tested, Buffer-free
- `parentReceiptCid` mintable + verifiable on v1.2; CRYPTO-01 matrix green; v1.1 compatibility intact
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-01-SUMMARY.md` when done.
</output>
