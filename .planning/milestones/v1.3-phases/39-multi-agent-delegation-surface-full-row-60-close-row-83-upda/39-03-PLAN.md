---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/agent/crew/agent-spec.ts
  - packages/lattice/src/agent/crew/agent-spec.test.ts
  - packages/lattice/src/agent/crew/crew-policy.ts
  - packages/lattice/src/agent/crew/crew-policy.test.ts
  - packages/lattice/src/agent/types.ts
  - packages/lattice/src/agent/host.ts
  - packages/lattice/src/agent/runtime.ts
  - packages/lattice/src/agent/format-tools.ts
autonomous: true
requirements: [DELEG-01, DELEG-02, DELEG-03]
must_haves:
  truths:
    - "defineAgent(spec) returns a kind:'agent' AgentSpec as a literal sibling of defineTool, composing child agents by value as a tree"
    - "CrewPolicy normalizes with BudgetInvariant reused verbatim, maxDepth defaulting to 1, and maxConcurrentChildren > 1 rejected with a typed error"
    - "The single-agent loop exposes an internal injectable dispatch seam defaulting to the existing runTool path — existing runAgent behavior and tests unchanged"
  artifacts:
    - path: "packages/lattice/src/agent/crew/agent-spec.ts"
      provides: "defineAgent + AgentSpec (D-03)"
      exports: ["defineAgent", "AgentSpec"]
    - path: "packages/lattice/src/agent/crew/crew-policy.ts"
      provides: "CrewPolicy type + validateCrewPolicy normalizer (D-06, D-11)"
      exports: ["CrewPolicy", "validateCrewPolicy"]
    - path: "packages/lattice/src/agent/types.ts"
      provides: "AgentFailureKind extended with crew-budget-exceeded (D-10)"
      contains: "crew-budget-exceeded"
    - path: "packages/lattice/src/agent/host.ts"
      provides: "AgentSnapshot optional ancestry field (D-05, backward-compatible)"
      contains: "ancestry"
  key_links:
    - from: "packages/lattice/src/agent/runtime.ts"
      to: "injectable dispatchToolUse seam"
      via: "internal options parameter defaulting to runTool path"
      pattern: "dispatchToolUse"
    - from: "packages/lattice/src/agent/crew/crew-policy.ts"
      to: "packages/lattice/src/contract/contract.ts"
      via: "BudgetInvariant import (verbatim reuse)"
      pattern: "BudgetInvariant"
---

<objective>
Lay the crew type foundation and the internal dispatch seam: `defineAgent`/`AgentSpec`, `CrewPolicy` + validation, the `crew-budget-exceeded` failure kind, the optional `AgentSnapshot.ancestry` field, the injectable tool-use dispatch seam extracted from runtime.ts step 4g, and the format-tools body-only option for cache-prefix hoisting.

Purpose: These are the contracts every other crew plan (39-05, 39-06) builds against — interface-first. All edits are additive; existing single-agent behavior must be bit-for-bit unchanged.
Output: `agent/crew/{agent-spec,crew-policy}.ts` + tests; extended `agent/{types,host,runtime,format-tools}.ts` with full regression suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-PATTERNS.md

Key research sections: Pattern 1 (dispatch seam extraction, option (a) recommended), Pitfall 8 (AgentSnapshot versioning), Pattern 5 (reject vs clamp). PATTERNS.md carries the exact defineTool/contract()/AgentFailureKind/AgentSnapshot excerpts.
</context>

<interfaces>
Contracts this plan DEFINES for downstream plans (39-05, 39-06):

```typescript
// agent/crew/agent-spec.ts (D-03)
export interface AgentSpec {
  readonly kind: "agent";
  readonly id: string;
  readonly intent: string;
  readonly tools: ReadonlyArray<ToolDefinition<StandardSchemaV1>>;
  readonly childAgents?: ReadonlyArray<AgentSpec>;
  readonly summaryReturnSchema: StandardSchemaV1;
  readonly contract?: CapabilityContract;          // optional per-agent sub-budget (D-07)
}
export function defineAgent(definition: Omit<AgentSpec, "kind">): AgentSpec;

// agent/crew/crew-policy.ts (D-06, D-11, D-16)
export interface CrewPolicy {
  readonly budget?: BudgetInvariant;               // reused verbatim from contract/contract.ts
  readonly maxTotalIterations?: number;
  readonly maxIterationsPerAgent?: number;
  readonly maxConcurrentChildren?: number;         // type carries it; runtime rejects > 1
  readonly maxDepth?: number;                      // default 1
  readonly limits?: Readonly<Record<string, { readonly requestsPerMinute?: number; readonly tokensPerMinute?: number }>>; // keyed by adapter.id
  readonly coordination?: "managed" | "unmanaged"; // default "managed"
}
export function validateCrewPolicy(policy?: CrewPolicy): Required-defaults normalized frozen policy; throws TypeError on maxConcurrentChildren > 1
```

Existing code (verified by research, replicate exactly):
- `defineTool` at tools/tools.ts:29-36 — `Omit<…, "kind">` factory + spread.
- `contract()` frozen conditional-spread normalizer at contract/contract.ts:91-106 — template for validateCrewPolicy.
- `AgentFailureKind` union at agent/types.ts:134-138 — append `| "crew-budget-exceeded"`.
- `AgentSnapshot` at host.ts:47-54 — version literal stays `"agent-snapshot/v1"`; add `readonly ancestry?: readonly string[];` (absent = root).
- runtime.ts step 4g (lines ~295-346): tool lookup → runTool → role:"tool" push — the seam extraction site; serialization site for snapshots at runtime.ts:373-383.
- format-tools.ts:179-224: `systemBlock`, `buildTask` (pushes systemBlock first), `describeForSystem()`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: defineAgent + AgentSpec</name>
  <files>packages/lattice/src/agent/crew/agent-spec.ts, packages/lattice/src/agent/crew/agent-spec.test.ts</files>
  <read_first>
    - packages/lattice/src/tools/tools.ts (defineTool shape lines 12-36; import style `import type { StandardSchemaV1 } from "@standard-schema/spec"`)
    - packages/lattice/src/contract/contract.ts (CapabilityContract type for the optional contract member)
  </read_first>
  <behavior>
    - Test 1: `defineAgent({ id, intent, tools: [], summaryReturnSchema })` returns `{ kind: "agent", ...fields }`; `kind` is the literal `"agent"`
    - Test 2: tree composition by value — a parent spec with `childAgents: [childA, childB]` where childA itself has `childAgents` round-trips structurally (deep equality, no mutation of inputs)
    - Test 3: optional members absent from input are absent from output (no `undefined`-valued keys; `Object.keys` check)
    - Test 4: spec accepts a `~standard` stub schema for summaryReturnSchema (the examples/agent-loop/setup.mjs:54-60 stub pattern)
  </behavior>
  <action>
    Create `packages/lattice/src/agent/crew/agent-spec.ts` per the interfaces block, mirroring defineTool literally (D-03): `export function defineAgent(definition: Omit<AgentSpec, "kind">): AgentSpec { return { kind: "agent", ...definition }; }`. Module doc header `/** AgentSpec — Phase 39 (v1.3). Sibling of defineTool; crew member specification composing by value as a tree (D-03). */`. Relative `.js`-suffixed imports, `import type` for type-only. Colocated test per behaviors.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/agent-spec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 behaviors pass; `pnpm --filter @full-self-browsing/lattice typecheck` green
    - `grep -c 'kind: "agent"' packages/lattice/src/agent/crew/agent-spec.ts` >= 1
    - AgentSpec fields exactly: kind, id, intent, tools, childAgents?, summaryReturnSchema, contract? — no extras
  </acceptance_criteria>
  <done>DELEG-01 spec factory shipped (package-root export lands in 39-06; tsd in 39-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CrewPolicy + validation, crew-budget-exceeded kind, AgentSnapshot ancestry</name>
  <files>packages/lattice/src/agent/crew/crew-policy.ts, packages/lattice/src/agent/crew/crew-policy.test.ts, packages/lattice/src/agent/types.ts, packages/lattice/src/agent/host.ts</files>
  <read_first>
    - packages/lattice/src/contract/contract.ts (BudgetInvariant :28-33; frozen conditional-spread factory :91-106)
    - packages/lattice/src/agent/types.ts (AgentFailureKind union :134-138; AgentDeniedError terminal template :172-184)
    - packages/lattice/src/agent/host.ts (AgentSnapshot :47-54)
    - packages/lattice/src/agent/runtime.ts lines 370-390 (snapshot serialization site — confirm optional field is conditionally spread)
  </read_first>
  <behavior>
    - Test 1: `validateCrewPolicy()` (no arg) returns frozen defaults: `maxDepth: 1`, `maxConcurrentChildren: 1`, `coordination: "managed"`; budget absent
    - Test 2: `validateCrewPolicy({ maxConcurrentChildren: 2 })` throws `TypeError` whose message names `maxConcurrentChildren` and states serial-only v1.3 limit (D-11 reject, research Open Question 3 resolution)
    - Test 3: `validateCrewPolicy({ budget: { maxCostUsd: 1 }, maxTotalIterations: 20 })` preserves the BudgetInvariant verbatim and freezes the result; input object not mutated
    - Test 4: `limits` record keyed by adapter id round-trips; `coordination: "unmanaged"` accepted
    - Test 5 (types.ts): a value of type AgentFailureKind accepts `"crew-budget-exceeded"` (compile-level via test assignment)
    - Test 6 (host.ts): an AgentSnapshot WITHOUT ancestry deserializes/typechecks (v1 backward compat); one WITH `ancestry: ["root","child-a"]` round-trips
  </behavior>
  <action>
    Create `crew-policy.ts`: `CrewPolicy` interface per the interfaces block; `validateCrewPolicy(policy?: CrewPolicy)` returns an `Object.freeze`d normalized policy using the contract.ts:91-106 conditional-spread template, applying defaults `maxDepth: 1`, `maxConcurrentChildren: 1`, `coordination: "managed"` and throwing `TypeError("CrewPolicy.maxConcurrentChildren > 1 is not supported in v1.3 — children execute serially (D-11).")` for values > 1 (also reject non-integer or < 1 values for maxDepth/maxConcurrentChildren/iteration caps with TypeError naming the field). In `agent/types.ts` append `| "crew-budget-exceeded"` to `AgentFailureKind` (D-10). In `agent/host.ts` add `readonly ancestry?: readonly string[];` to `AgentSnapshot` with a Phase 39 doc comment (dispatch ancestry chain of spec ids; absent = root agent; optional to stay `agent-snapshot/v1` compatible — Pitfall 8). If the runtime.ts snapshot serialization site assigns fields positionally, add ancestry via conditional spread there ONLY if a chain is present in loop context (the chain is threaded by 39-05; here just the type + compat test). Colocated crew-policy.test.ts covers behaviors 1-6.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/crew-policy.test.ts src/agent && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behaviors 1-6 pass; full existing `src/agent` suite still green (no snapshot/serialization regression)
    - `grep -c '"crew-budget-exceeded"' packages/lattice/src/agent/types.ts` == 1
    - `grep -c 'ancestry?: readonly string\[\]' packages/lattice/src/agent/host.ts` == 1; `AgentSnapshot.version` literal unchanged (`grep -c '"agent-snapshot/v1"' packages/lattice/src/agent/host.ts` >= 1)
    - validateCrewPolicy throw message contains the exact substring `maxConcurrentChildren`
  </acceptance_criteria>
  <done>Policy contract + failure taxonomy + snapshot compat shipped; 39-05/39-06 build against these without renegotiation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: runtime.ts internal dispatch seam + format-tools body-only option</name>
  <files>packages/lattice/src/agent/runtime.ts, packages/lattice/src/agent/format-tools.ts</files>
  <read_first>
    - packages/lattice/src/agent/runtime.ts (FULL step 4g, lines ~295-346: lookup, BEFORE_TOOL/AFTER_TOOL pipeline, error stringify convention :309-311, role:"tool" push :334-339, toolCallRecords; runAgent signature + existing internal params)
    - packages/lattice/src/agent/format-tools.ts (systemBlock assembly :179-190, buildTask :191-212, describeForSystem :214-216)
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md Pattern 1 (seam shape (a): internal options with dispatchToolUse default)
  </read_first>
  <behavior>
    - Test 1: existing runtime tests pass UNCHANGED (seam default = current runTool path; zero behavior delta)
    - Test 2: a test-only injected `dispatchToolUse` intercepts a tool-use request by name and its returned `{ content }` lands in the conversation as the role:"tool" turn content (same toolCallId/toolName as today)
    - Test 3: when injected dispatcher declines (returns undefined/sentinel for non-child names), the default runTool path executes — proving fall-through
    - Test 4 (format-tools): the body-only variant emits the same turn rendering as buildTask minus the leading systemBlock; `describeForSystem()` + "\n" + body reconstructs buildTask output byte-for-byte
  </behavior>
  <action>
    runtime.ts — research Pattern 1 option (a): add an internal (NOT exported from src/index.ts) seam so step 4g routes each `ToolUseRequest` through an injectable `dispatchToolUse?: (req: ToolUseRequest, ctx: { iterationIndex: number; conversation; pipeline }) => Promise<{ content: string } | undefined>` carried on a non-public extra options parameter (e.g. exported `runAgentInternal`/extra param on `runAgent` consumed only in-package — pick the least-invasive shape that keeps the public `runAgent(intent, config)` signature identical). Semantics: if the injected dispatcher returns `{ content }`, push the role:"tool" turn with that content and record toolCallRecords as today (argsHash via existing stableHash); if it returns undefined, run the existing lookup/runTool path verbatim. BEFORE_TOOL/AFTER_TOOL hook band semantics for the default path are untouched; dispatched-agent calls bypass runTool but the dispatcher (39-05) owns its own pipeline events. format-tools.ts — add the body-only option: either `buildTask(conversation, { omitSystemBlock?: boolean })` or a `buildTaskBody()` sibling on the handle (research recommends sibling) that skips the `lines.push(systemBlock)` line; everything else identical so `describeForSystem()` remains the byte-stable prefix (cache hoist, 39-05). Add/extend colocated tests for behaviors 2-4 (runtime test file already exists — extend; do not fork the loop).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - ENTIRE existing agent suite green with zero test-expectation edits (behavior-preserving refactor)
    - Public `runAgent` signature unchanged (`git diff packages/lattice/src/index.ts` is empty for this plan)
    - Injected-dispatcher test proves content lands verbatim in the role:"tool" turn with original toolCallId
    - Byte-equality test: `describeForSystem() + separator + bodyOnly === buildTask()` output
  </acceptance_criteria>
  <done>Dispatch chokepoint exists for the CrewDispatcher (39-05) without any public-surface change; cache-prefix hoist has its non-duplicating body builder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer CrewPolicy input → runtime | untrusted config values (negative caps, >1 concurrency) |
| serialized AgentSnapshot → resume | snapshots cross storage; ancestry field must not break v1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | DoS | CrewPolicy validation | mitigate | validateCrewPolicy rejects >1 concurrency and non-positive caps with TypeError at entry (fail-fast); frozen output prevents post-validation mutation |
| T-39-09 | Tampering | AgentSnapshot resume | mitigate | ancestry optional (absent = root); v1 snapshot without field deserializes cleanly — tested |
| T-39-10 | Elevation of privilege | dispatch seam | mitigate | Seam is in-package only, never re-exported from src/index.ts (acceptance gate); default path identical to today |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase (D-18) |
</threat_model>

<verification>
- `pnpm --filter @full-self-browsing/lattice test -- src/agent` green (existing + new)
- `pnpm --filter @full-self-browsing/lattice typecheck` green
- `git diff --name-only` contains no `src/index.ts` (public surface untouched in this plan)
</verification>

<success_criteria>
- defineAgent/AgentSpec, CrewPolicy/validateCrewPolicy, crew-budget-exceeded, AgentSnapshot.ancestry all exist and are tested
- Dispatch seam injectable in-package; existing single-agent behavior byte-identical
- format-tools body-only variant reconstructs buildTask byte-for-byte when prefixed
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-03-SUMMARY.md` when done.
</output>
