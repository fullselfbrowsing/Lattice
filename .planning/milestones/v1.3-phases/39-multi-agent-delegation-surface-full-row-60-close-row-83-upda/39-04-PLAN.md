---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/providers/provider.ts
  - packages/lattice/src/providers/anthropic.ts
  - packages/lattice/src/providers/anthropic.test.ts
autonomous: true
requirements: [DELEG-04]
must_haves:
  truths:
    - "When ProviderRunRequest.cacheSystemPrefix is present, the Anthropic adapter emits it as a cache_control-marked system content block; when absent, the request body is byte-identical to today's"
    - "Anthropic cache-hit counters (cache_creation_input_tokens / cache_read_input_tokens) are readable from rawResponse without widening the Usage type"
    - "All 7 adapters remain INV-03 parity-compatible — no ProviderAdapter method/signature change"
  artifacts:
    - path: "packages/lattice/src/providers/provider.ts"
      provides: "additive optional ProviderRunRequest.cacheSystemPrefix?: string"
      contains: "cacheSystemPrefix"
    - path: "packages/lattice/src/providers/anthropic.ts"
      provides: "cache_control system-block emission gated on field presence"
      contains: "cache_control"
  key_links:
    - from: "packages/lattice/src/providers/anthropic.ts"
      to: "request.cacheSystemPrefix"
      via: "system block array emission with cache_control ephemeral"
      pattern: "cache_control"
    - from: "packages/lattice/src/providers/anthropic.test.ts"
      to: "mocked fetch request body"
      via: "JSON shape assertion"
      pattern: "ephemeral"
---

<objective>
Open the Anthropic prompt-cache path: add the additive-optional `ProviderRunRequest.cacheSystemPrefix` field and teach the Anthropic adapter to emit it as a `cache_control`-marked system block — the prerequisite for crew cache-prefix sharing (DELEG-04).

Purpose: Research verified Anthropic caching is content-block-granular and the current adapter sends `system: ""` with the whole prompt in one mutating user block — Anthropic cache hits are structurally impossible today (Pitfall 1). OpenAI needs zero changes (automatic token-prefix caching). This is provider-local work, independent of crew code: Wave 1.
Output: `provider.ts` request field; `anthropic.ts` opt-in emission; mocked-fetch shape tests + cache-counter fixture assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-PATTERNS.md

Key research sections: Pattern 3 (provider cache facts table + recommended mechanism + A1 assumption), Code Examples (Anthropic cache_control request JSON), Pitfall 1. PATTERNS.md `providers/anthropic.ts` section carries the exact current execute() body lines 388-410.
</context>

<interfaces>
Target shape (research Pattern 3, locked here for 39-05):

- `ProviderRunRequest` gains `readonly cacheSystemPrefix?: string;` — additive optional (Phase 37 `toolCalls`-on-response precedent; A1: request/response additive fields accepted, ProviderAdapter METHODS frozen).
- Anthropic emission when present: `system: [{ type: "text", text: request.cacheSystemPrefix, cache_control: { type: "ephemeral" } }]` replacing `system: ""`; `messages` unchanged (task carries only the body).
- When absent: request body byte-identical to current (`system: ""`).
- Gate for composition (consumed by 39-05, not here): `AnthropicQuirks.promptCachingSupported === true` at quirks.ts:47-51 / anthropic.ts:383.
- Counters: response `usage.cache_creation_input_tokens` / `usage.cache_read_input_tokens` available via `rawResponse` (anthropic.ts:441 preserves full body); `Usage` 3-field shape NOT widened.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProviderRunRequest.cacheSystemPrefix + Anthropic cache_control emission</name>
  <files>packages/lattice/src/providers/provider.ts, packages/lattice/src/providers/anthropic.ts</files>
  <read_first>
    - packages/lattice/src/providers/provider.ts (ProviderRunRequest definition; Phase 37 toolCalls additive-field precedent on ProviderRunResponse)
    - packages/lattice/src/providers/anthropic.ts (execute() body lines 388-443: current `system: ""` + single user message + rawResponse preservation; D-07 comment about top-level system field)
    - packages/lattice/src/providers/quirks.ts lines 40-55 (AnthropicQuirks.promptCachingSupported flag)
  </read_first>
  <behavior>
    - (Implemented here, tested in Task 2 via mocked fetch) presence → system block array with cache_control; absence → `system: ""` byte-identical body
  </behavior>
  <action>
    In provider.ts add `readonly cacheSystemPrefix?: string;` to `ProviderRunRequest` with a doc comment: Phase 39 opt-in prompt-cache prefix; adapters that support block-granular caching (Anthropic) hoist it to a `cache_control`-marked system block; adapters that ignore it MUST receive the prefix folded into `task` by the caller instead (the crew dispatcher gates on `quirks.promptCachingSupported`) — the field is advisory, additive, and absent for all existing callers. In anthropic.ts execute(), replace the static `system: ""` line with a conditional: when `request.cacheSystemPrefix !== undefined` emit `system: [{ type: "text", text: request.cacheSystemPrefix, cache_control: { type: "ephemeral" } }]`, else keep `system: ""` exactly. Use conditional logic, not conditional spread of the system key (the key is always present per the Messages API contract comment at anthropic.ts:390). Touch NO other adapter — OpenAI-family adapters ignore the field by design (token-granular automatic caching needs the prefix at the head of `task`, which the caller controls). Do not modify `ProviderAdapter` methods or any adapter signatures (INV-03).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/providers/anthropic.test.ts && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cacheSystemPrefix' packages/lattice/src/providers/provider.ts` >= 1 (type) and `grep -c 'cache_control' packages/lattice/src/providers/anthropic.ts` >= 1
    - Existing anthropic tests pass unmodified (absence path byte-identical)
    - `git diff --name-only` for this task touches only provider.ts and anthropic.ts
    - No change to the `Usage` type or any other adapter file
  </acceptance_criteria>
  <done>Anthropic cache hoist path exists behind an additive optional field; default behavior untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mocked-fetch shape tests + cache-counter fixture assertions</name>
  <files>packages/lattice/src/providers/anthropic.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/anthropic.test.ts (existing mocked-fetch harness: how fetch is stubbed, how request bodies are captured/asserted, fixture response shapes)
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md Pattern 3 + Code Examples (exact expected request JSON; counter field names)
  </read_first>
  <behavior>
    - Test 1: execute() with `cacheSystemPrefix: "CREW PREFIX"` → captured fetch body has `system` as an ARRAY with one element `{ type: "text", text: "CREW PREFIX", cache_control: { type: "ephemeral" } }`, and `messages[0].content` equals `request.task` (prefix NOT duplicated into messages)
    - Test 2: execute() without the field → captured body has `system: ""` (string), deep-equal to a pre-change golden body for the same inputs
    - Test 3: fixture response carrying `usage: { input_tokens, output_tokens, cache_creation_input_tokens: 1200, cache_read_input_tokens: 0 }` → `response.rawResponse` exposes both cache counters; second fixture with `cache_read_input_tokens: 1200` likewise readable — and `response.normalizedUsage` shape is unchanged (3 fields, no cache members)
  </behavior>
  <action>
    Extend anthropic.test.ts with a `describe("cacheSystemPrefix (Phase 39)")` block implementing the 3 behaviors using the file's existing mocked-fetch capture pattern. Assert exact JSON: `body.system[0].cache_control.type === "ephemeral"`. For Test 3, build the fixture from the existing success-response fixture plus the two cache counter fields, and assert via `response.rawResponse` property access (cast as the test file already does for raw bodies). Add a comment noting live cache-hit verification is nightly/manual-only per repo policy (A4) — these mocked tests are the PR-time proof (Pitfall 1).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/providers/anthropic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behaviors pass; full providers suite green (`pnpm --filter @full-self-browsing/lattice test -- src/providers`)
    - Test 1 asserts the literal string `"ephemeral"` in the captured request body
    - Test 3 asserts `normalizedUsage` has exactly the existing fields (no cache counters leaked into the normalized shape)
  </acceptance_criteria>
  <done>DELEG-04 provider-side proof complete: request shape + counters assertable at PR time; crew composition (39-05) can rely on the field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crew/consumer prefix string → provider HTTP body | prefix content crosses into the Anthropic request |
| provider response → rawResponse consumers | cache counters read off untrusted provider JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-11 | Tampering | request body assembly | mitigate | Absence path byte-identical golden test; presence path exact-shape asserted — no silent body drift for existing consumers |
| T-39-12 | Information disclosure | new error/log paths | mitigate | No new logging added; adapter error handling untouched (err.message-only rule preserved) |
| T-39-13 | DoS | cache-miss cost inflation (1.25x write cost on every call if prefix not byte-stable) | transfer | Byte-stability is the crew dispatcher's contract (39-05 snapshot test); adapter emits exactly what it is given |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase (D-18) |
</threat_model>

<verification>
- `pnpm --filter @full-self-browsing/lattice test -- src/providers` green
- `pnpm --filter @full-self-browsing/lattice typecheck` green
- No diff outside `src/providers/{provider,anthropic}.ts` + `anthropic.test.ts`
</verification>

<success_criteria>
- `cacheSystemPrefix` additive field live; Anthropic emits cache_control system block when present; byte-identical body when absent
- Cache counters assertable from rawResponse fixtures without Usage widening
- INV-03 intact: zero adapter method/signature changes
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-04-SUMMARY.md` when done.
</output>
