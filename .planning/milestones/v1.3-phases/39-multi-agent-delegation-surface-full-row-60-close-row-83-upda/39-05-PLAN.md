---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 05
type: execute
wave: 2
depends_on: ["39-01", "39-03", "39-04"]
files_modified:
  - packages/lattice/src/agent/crew/dispatcher.ts
  - packages/lattice/src/agent/crew/dispatcher.test.ts
  - packages/lattice/src/agent/crew/cache-prefix.test.ts
autonomous: true
requirements: [DELEG-03, DELEG-04, DELEG-06]
must_haves:
  truths:
    - "When the parent's model names a child agent, the runtime branches on the kind:'agent' discriminant at the CrewDispatcher chokepoint — never through a tool closure (D-01/D-02)"
    - "A completed child returns a schema-validated { summary, artifacts, receipts } envelope that re-enters the parent conversation as a role:'tool' turn over the existing prompt-reencoded tool protocol (D-04); recoverable failures return as structured { error: { kind, reason, terminal } } tool results (D-09)"
    - "Terminal child failures are never re-dispatched; cycle/depth violations are rejected from the ancestry chain (D-05/D-10)"
    - "Every child completion receipt carries parentReceiptCid = crew-root CID; the shared cache prefix is byte-identical across child dispatches and hoisted only when quirks.promptCachingSupported"
  artifacts:
    - path: "packages/lattice/src/agent/crew/dispatcher.ts"
      provides: "CrewDispatcher chokepoint: child dispatch, budgets, validation, failure routing, receipt minting, cache-prefix composition"
      exports: ["createCrewDispatcher"]
      min_lines: 120
    - path: "packages/lattice/src/agent/crew/dispatcher.test.ts"
      provides: "dispatch/cycle/failure/receipt coverage"
    - path: "packages/lattice/src/agent/crew/cache-prefix.test.ts"
      provides: "byte-equality across 3 dispatches + quirks-gated hoist coverage"
  key_links:
    - from: "packages/lattice/src/agent/crew/dispatcher.ts"
      to: "runtime.ts internal dispatch seam (39-03)"
      via: "dispatchToolUse injection branching on childAgents id match"
      pattern: "dispatchToolUse"
    - from: "packages/lattice/src/agent/crew/dispatcher.ts"
      to: "packages/lattice/src/receipts/cid.ts (39-01)"
      via: "receiptCid for parentReceiptCid chaining"
      pattern: "receiptCid"
    - from: "packages/lattice/src/agent/crew/dispatcher.ts"
      to: "ProviderRunRequest.cacheSystemPrefix (39-04)"
      via: "quirks.promptCachingSupported-gated prefix hoist"
      pattern: "promptCachingSupported"
---

<objective>
Build the CrewDispatcher — the single chokepoint where ALL crew concerns live (D-02): child-by-name dispatch through the kind:"agent" branch, ancestry/cycle/depth enforcement, per-child budget derivation, summary-return validation, classified failure routing with terminal semantics, per-agent receipt minting with `parentReceiptCid` chaining, and byte-stable cache-prefix composition gated on adapter quirks.

Purpose: This is the core of DELEG-03 and the crew-side halves of DELEG-04/06. It consumes the contracts from Wave 1 (seam, specs, policy, CID, cacheSystemPrefix) and is consumed by the orchestrator (39-06).
Output: `agent/crew/dispatcher.ts` + dispatcher and cache-prefix test suites, all against fake providers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-PATTERNS.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-03-SUMMARY.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-04-SUMMARY.md

Key research sections: Pattern 1 (re-entry turn shape + structured failure convention + synthetic child tool declarations), Pattern 2 (chain topology recommendation), Pattern 3 (prefix composition mechanism), Pitfalls 2/4/5.
</context>

<interfaces>
Contracts CONSUMED (defined in Wave 1):
- `AgentSpec` / `defineAgent` from `agent/crew/agent-spec.ts` (39-03): `{ kind: "agent", id, intent, tools, childAgents?, summaryReturnSchema, contract? }`.
- Normalized `CrewPolicy` from `validateCrewPolicy` (39-03): maxDepth (default 1), maxIterationsPerAgent, coordination, limits.
- Internal dispatch seam (39-03): `dispatchToolUse(req, ctx) => Promise<{ content: string } | undefined>`; returning undefined falls through to runTool.
- `receiptCid(envelope)` from `receipts/cid.ts` (39-01); `createReceipt({ ..., parentReceiptCid })` (39-01).
- `ProviderRunRequest.cacheSystemPrefix` + `AnthropicQuirks.promptCachingSupported` (39-04).
- `validateSchemaOutput` from `outputs/validate.ts` (Standard Schema kernel runTool uses).
- `runAgent` loop (existing) for child execution; `formatToolsForProvider` handle: `describeForSystem()` (byte-stable prefix) + body-only variant (39-03).

Contract DEFINED for 39-06 (orchestrator):
```typescript
// agent/crew/dispatcher.ts
export interface CrewDispatchContext {
  readonly policy: NormalizedCrewPolicy;
  readonly childHost: AgentHost;
  readonly ancestry: readonly string[];        // spec-id chain, parent-first (D-05)
  readonly crewRootCid?: string;               // chain anchor (absent when no signer)
  readonly signer?: ReceiptSigner;
  readonly recordUsage: (agentId: string, usage: Usage) => void;   // feeds per-agent tracker + crew aggregator exactly once
  readonly remainingBudget: () => BudgetInvariant | undefined;     // crew pool remaining (D-07)
  readonly sharedPrefix: string;               // byte-stable crew prefix
  readonly mintedReceipts: (envelope) => void; // collect per-agent envelopes for CrewResult
}
export function createCrewDispatcher(spec: AgentSpec, ctx: CrewDispatchContext): {
  dispatchToolUse: DispatchToolUseFn;          // plugs into the 39-03 seam
  childToolDeclarations: ReadonlyArray<ToolDefinition-shaped>;  // for the parent's intent.tools / formatToolsForProvider / Phase 37 registries
}
```
(Member names may be refined during implementation, but 39-06 consumes exactly: a seam-compatible dispatch function + synthesized child declarations + the usage/receipt callbacks.)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dispatch branch, child execution, summary validation</name>
  <files>packages/lattice/src/agent/crew/dispatcher.ts, packages/lattice/src/agent/crew/dispatcher.test.ts</files>
  <read_first>
    - packages/lattice/src/agent/runtime.ts (step 4g + the 39-03 seam shape; child loop entry `runAgent(intent, config)`; AgentIntent fields)
    - packages/lattice/src/agent/crew/agent-spec.ts + crew-policy.ts (39-03 outputs)
    - packages/lattice/src/outputs/validate.ts (validateSchemaOutput signature)
    - packages/lattice/src/agent/format-tools.ts (describeForSystem + body-only variant; TOOL_RESULT rendering :204-206)
    - examples/agent-loop/setup.mjs lines 42-94 (scripted fake provider + ~standard stub schema patterns for tests)
  </read_first>
  <behavior>
    - Test 1: parent model emits `{"tool_calls":[{"id":"d1","name":"researcher","args":{"task":"find X"}}]}` where `researcher` ∈ childAgents → dispatcher runs the child loop (scripted fake provider) and the parent conversation gains a role:"tool" turn with `toolCallId: "d1"`, `toolName: "researcher"`, content = `JSON.stringify({ summary, artifacts, receipts })`
    - Test 2: a name NOT in childAgents returns undefined from the dispatch fn (falls through to runTool — asserted by the seam contract)
    - Test 3: child success output validates against summaryReturnSchema; a child whose assembled envelope FAILS validation produces `JSON.stringify({ error: { kind: "summary-validation-failed", reason, terminal: false } })` (D-09 recoverable)
    - Test 4: child task input derived from `args.task` string; child AgentIntent maps spec.intent + task + spec.tools + childHost; child budget = min(spec.contract?.budget, ctx.remainingBudget()) per dimension with `costUsd: null`-safe arithmetic (Pitfall 4 — null never poisons min())
    - Test 5: synthesized childToolDeclarations are ToolDefinition-shaped (name = spec id, description embeds intent + summary shape, ~standard input schema) so formatToolsForProvider renders them and Phase 37 validateToolCalls registries accept them (Pitfall 5)
  </behavior>
  <action>
    Create `agent/crew/dispatcher.ts` implementing `createCrewDispatcher` per the interfaces block. Dispatch flow (research architecture diagram): on each ToolUseRequest, if `req.name` matches a `spec.childAgents[].id` → run the full child pipeline; else return undefined (fall-through). Child pipeline: (1) checks (Task 2), (2) derive effective budget per D-07 — per-dimension min of child `spec.contract?.budget` and `ctx.remainingBudget()`, applying cost-dimension min only when both sides are numbers (Pitfall 4), iterations also capped by `policy.maxIterationsPerAgent`; (3) build child AgentIntent `{ task: <args.task string>, tools: spec.tools, host: ctx.childHost, contract: { budget: effective }, signer: ctx.signer, ... }` and invoke the existing `runAgent` loop; (4) on AgentSuccess, assemble the summary envelope `{ summary: <child final output string>, artifacts: <ArtifactRef[] from result, else []>, receipts: <CIDs of receipts minted for this child> }`, validate via `validateSchemaOutput(spec.id, childSpec.summaryReturnSchema, envelope)` (children only — root return is NOT schema-validated, research Open Question 2 resolution); (5) record child usage via `ctx.recordUsage(childId, usage)` exactly once (no double-counting — Pitfall 3); (6) return `{ content: JSON.stringify(envelope) }`. Also export the childToolDeclarations synthesis (behavior 5; the `~standard` stub pattern from examples/agent-loop/setup.mjs:54-60, description embedding intent + result shape per research Pattern 1). All policy logic lives HERE, none in tool closures (D-01/D-02). Module doc header with Phase 39 provenance + D-01/D-02 citation.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/dispatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behaviors 1-5 pass against scripted fake providers (no network)
    - `grep -c 'runTool(' packages/lattice/src/agent/crew/dispatcher.ts` returns 0 (children never route through tool closures)
    - Null-cost test: crew with fake provider (`costUsd: null`) derives budgets without NaN (explicit assertion)
    - `pnpm --filter @full-self-browsing/lattice typecheck` green
  </acceptance_criteria>
  <done>Hybrid dispatch model live: model sees named tools, runtime branches at the chokepoint; summary-return validated and re-entering as a standard tool turn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cycle/depth enforcement, classified failure routing, receipt chaining</name>
  <files>packages/lattice/src/agent/crew/dispatcher.ts, packages/lattice/src/agent/crew/dispatcher.test.ts</files>
  <read_first>
    - packages/lattice/src/results/errors.ts (isTerminal semantics)
    - packages/lattice/src/agent/types.ts (AgentFailureKind incl. crew-budget-exceeded from 39-03; STUCK_REASONS source in agent/infra/action-history.ts)
    - packages/lattice/src/receipts/receipt.ts + cid.ts (createReceipt input shape incl. parentReceiptCid; receiptCid)
    - packages/lattice/src/contract/checkpoint.ts lines 125-145 (DEFAULT_ROUTE synthetic-identifier precedent for non-provider receipts)
  </read_first>
  <behavior>
    - Test 1 (cycle, D-05): dispatch whose target id already appears in `ctx.ancestry` → rejected with structured error `{ kind: "crew-cycle-rejected", ..., terminal: false }` content, child never runs
    - Test 2 (depth, D-05): with maxDepth 1, a child attempting to dispatch its own childAgents is rejected (ancestry length >= maxDepth gate)
    - Test 3 (recoverable, D-09): child failing with `agent-max-iterations` or a STUCK_REASONS stall → parent receives `{ error: { kind, reason, terminal: false } }` and MAY re-dispatch
    - Test 4 (terminal, D-10): child tripwire violation (isTerminal-true failure) → `{ error: { kind, reason, terminal: true } }`; a SECOND dispatch of the same child id is short-circuit rejected with the same terminal error without running the child
    - Test 5 (crew ceiling, D-10): `ctx.remainingBudget()` exhausted before or during dispatch → terminal `crew-budget-exceeded` error content; dispatcher signals the orchestrator to end the run
    - Test 6 (receipts): with a signer + crewRootCid present, child completion mints a receipt whose body carries `parentReceiptCid === ctx.crewRootCid`; the envelope verifies via verifyReceipt; the child's summary `receipts` array contains `await receiptCid(childEnvelope)`; no signer → no mint, summary receipts `[]`, run still succeeds
  </behavior>
  <action>
    Extend dispatcher.ts: (a) BEFORE running a child: ancestry-chain cycle check (target id ∈ ctx.ancestry → reject) and depth check (`ctx.ancestry.length >= policy.maxDepth` → reject) — both return structured tool-result errors, terminal: false (the parent can pick a different child); thread `[...ctx.ancestry, childId]` into the child's dispatch context and persist it on the child's AgentSnapshot ancestry field (39-03) when snapshots are captured. (b) Failure classification (D-09/D-10): map child AgentFailure → `{ error: { kind: failure.kind, reason: failure.reason, terminal: isTerminal-equivalent } }` where terminal is true for tripwire-violation kinds (consult results/errors.ts isTerminal) and crew-ceiling breaches, false for agent-max-iterations/stuck/summary-validation-failed; maintain a per-dispatcher terminal-block set by child id — re-dispatch of a terminally-failed child returns the cached terminal error without execution (D-10 "must not re-delegate"). (c) Crew ceiling: when remainingBudget() is exhausted (any dimension at/below zero, null-cost-safe), emit terminal `crew-budget-exceeded` (the 39-03 AgentFailureKind) and expose a flag/callback the orchestrator (39-06) reads to end the crew run. (d) Receipt minting at the seam (D-02 + research Pattern 2 topology): when ctx.signer present, mint the child completion receipt directly via `createReceipt` with `parentReceiptCid: ctx.crewRootCid`, synthetic route identifiers when no provider route applies (checkpoint.ts DEFAULT_ROUTE precedent — use providerId "lattice-crew", capabilityId "lattice-crew/agent-completion"), push the envelope via ctx.mintedReceipts, and include `await receiptCid(envelope)` in the child's summary receipts array. Checkpoint per-iteration receipts remain UNCHANGED (no parentReceiptCid — research Pattern 2 point 4).
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/dispatcher.test.ts src/receipts && pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Behaviors 1-6 pass; receipts suite still green (chain field non-regression)
    - Terminal re-dispatch test proves the child loop function is NOT invoked the second time (spy/counter assertion)
    - Minted child envelope round-trips verifyReceipt with the ephemeral test KeySet and `JSON.parse(atob(envelope.payload)).parentReceiptCid` equals the crew-root CID
    - Structured error content is exactly `{"error":{"kind":...,"reason":...,"terminal":...}}` JSON (D-09 shape) — asserted by parse
  </acceptance_criteria>
  <done>Failure routing, terminal semantics, cycle prevention, and the receipt chain all enforced at the single chokepoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: byte-stable cache-prefix composition + quirks-gated hoist</name>
  <files>packages/lattice/src/agent/crew/dispatcher.ts, packages/lattice/src/agent/crew/cache-prefix.test.ts</files>
  <read_first>
    - packages/lattice/src/agent/format-tools.ts (describeForSystem byte-stable prefix; body-only variant from 39-03)
    - packages/lattice/src/providers/quirks.ts (AnthropicQuirks.promptCachingSupported :47-51)
    - packages/lattice/src/prompts/scaffolds.ts (PROMPT_SCAFFOLD_VERSION pinning; getToolUseContract — optional composition source)
    - packages/lattice/src/providers/provider.ts (cacheSystemPrefix field from 39-04)
  </read_first>
  <behavior>
    - Test 1: the composed shared prefix is byte-identical (===) across 3 consecutive child dispatches in one crew (snapshot + strict equality)
    - Test 2: provider with `quirks.promptCachingSupported === true` → ProviderRunRequest carries `cacheSystemPrefix === sharedPrefix` and `task` contains ONLY the conversation body (prefix not duplicated — assembled via the 39-03 body-only variant)
    - Test 3: provider without the quirk (fake/OpenAI-compat) → no cacheSystemPrefix field on the request (conditional spread, never undefined-assigned — Pitfall 6) and the prefix sits at the HEAD of `task` (OpenAI automatic token-prefix path)
    - Test 4: prefix contains no timestamps/random ids — composing twice from the same spec yields identical bytes (anti-pattern guard)
  </behavior>
  <action>
    Add prefix composition to the dispatcher/crew context: `ctx.sharedPrefix` is composed ONCE per crew (by 39-06 at crew start; the composition helper lives here and is exported) as the `describeForSystem()` block for the member's tool surface — deterministic, version-pinned inputs only (Phase 35 scaffold discipline; NO Date.now/random/unsorted keys). Per-call request assembly: gate on the executing adapter's `quirks?.promptCachingSupported === true` → set `cacheSystemPrefix` via conditional spread and build `task` from the body-only variant; otherwise fold prefix into the head of `task` exactly as buildTask does today. Note: the hoist applies to how the crew composes ProviderRunRequests through the transport/loop path — implement at whichever point the dispatcher/orchestrator constructs or influences member intents, keeping ALL gating logic in crew code (adapters stay dumb). Write cache-prefix.test.ts covering behaviors 1-4 with a quirked fake (promptCachingSupported true) and a plain fake.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/cache-prefix.test.ts src/agent/crew/dispatcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Byte-equality test uses strict `===` across 3 dispatches (not just snapshot match)
    - Hoist test asserts BOTH: field present with exact prefix AND task lacks the prefix substring (no duplication)
    - Non-quirked path asserts the request object has NO `cacheSystemPrefix` own-property
    - `grep -c 'Date.now\|Math.random' <prefix composition code paths in dispatcher.ts>` — no nondeterministic inputs in prefix composition (review-level; test 4 is the executable proof)
  </acceptance_criteria>
  <done>Cache-prefix sharing implemented crew-side: byte-stable prefix, quirks-gated Anthropic hoist, OpenAI head-of-task path — DELEG-04 crew half complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child model output → parent conversation | untrusted model text crosses agent boundaries |
| child AgentFailure → parent loop decisions | failure metadata drives re-dispatch decisions |
| dispatcher → receipt chain | minted receipts become audit artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-14 | Elevation of privilege | child→parent summary re-entry | mitigate | ONLY the schema-validated `{summary, artifacts, receipts}` envelope re-enters (validateSchemaOutput); raw child transcripts never flow to the parent (anti-pattern guard); failures are structured objects, not raw model text |
| T-39-15 | Tampering/Repudiation | parentReceiptCid chain | mitigate | CID = sha256 of signed payload bytes (39-01); chain anchor is the crew-root receipt minted before children run (Pitfall 2); verifyReceipt round-trip asserted in tests |
| T-39-16 | DoS | runaway delegation | mitigate | Ancestry cycle rejection + maxDepth gate + crew-budget-exceeded terminal kind + maxIterationsPerAgent cap — all enforced at the chokepoint |
| T-39-17 | Elevation of privilege | terminal-failure re-dispatch | mitigate | Per-dispatcher terminal-block set: tripwire-rejected children short-circuit on re-dispatch (D-10), spy-asserted |
| T-39-18 | Information disclosure | dispatcher error paths | mitigate | Errors carry kind/reason strings only (err.message rule); never serialize request options/headers/keys |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase (D-18) |
</threat_model>

<verification>
- `pnpm --filter @full-self-browsing/lattice test -- src/agent/crew src/receipts` green
- `pnpm --filter @full-self-browsing/lattice typecheck` green
- No public-surface change in this plan (src/index.ts untouched; exports land in 39-06)
</verification>

<success_criteria>
- D-01/D-02/D-04/D-05/D-07/D-09/D-10 all observable in passing tests at the dispatcher chokepoint
- Receipt chain: child envelopes carry parentReceiptCid = crew-root CID and verify
- Cache prefix byte-stable across dispatches; hoist gated on quirks; no duplication
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-05-SUMMARY.md` when done.
</output>
