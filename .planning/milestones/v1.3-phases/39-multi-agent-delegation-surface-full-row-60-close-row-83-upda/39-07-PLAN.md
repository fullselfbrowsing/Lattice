---
phase: 39-multi-agent-delegation-surface-full-row-60-close-row-83-upda
plan: 07
type: execute
wave: 4
depends_on: ["39-06"]
files_modified:
  - examples/agent-crew/package.json
  - examples/agent-crew/setup.mjs
  - examples/agent-crew/index.mjs
  - packages/lattice/src/agent/crew/crew-eval.test.ts
autonomous: true
requirements: [DELEG-07]
must_haves:
  truths:
    - "node examples/agent-crew/index.mjs runs a parent-summarizer + 3 child-researchers crew (serial children) end-to-end and exits 0"
    - "Every per-agent receipt in the showcase is signed with a real Ed25519 key, written to a receipts dir, verified via verifyReceipt, and chain-linked via parentReceiptCid"
    - "An evalAgentRun-style regression test gates crew iterations-to-goal + cost against a committed baseline using a scripted fake provider"
  artifacts:
    - path: "examples/agent-crew/index.mjs"
      provides: "crew showcase entry: run, verify receipts + chain, eval gate line, exit codes"
      min_lines: 50
    - path: "examples/agent-crew/setup.mjs"
      provides: "signer/keyset, scripted fake provider, 1 parent + 3 child specs, tools"
    - path: "packages/lattice/src/agent/crew/crew-eval.test.ts"
      provides: "evalAgentRun crew regression gate vs committed baseline"
  key_links:
    - from: "examples/agent-crew/index.mjs"
      to: "packages/lattice/dist/index.js"
      via: "built-dist imports (agent-loop precedent)"
      pattern: "dist/index.js"
    - from: "packages/lattice/src/agent/crew/crew-eval.test.ts"
      to: "packages/lattice/src/agent/eval.ts evalAgentRun"
      via: "AgentRunSnapshot derived from CrewResult (totalIterations + aggregate usage)"
      pattern: "evalAgentRun"
---

<objective>
Ship the `examples/agent-crew/` showcase — parent-summarizer + 3 child-researchers, serial children, real Ed25519 signing and verification of every per-agent receipt — plus the `evalAgentRun`-style crew regression gate against a committed baseline (DELEG-07, roadmap success criterion 3).

Purpose: The showcase is the human-visible proof of the whole surface; the eval gate is the machine-enforced regression budget for crew iterations + cost.
Output: three example files mirroring `examples/agent-loop/`, one colocated regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-CONTEXT.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-PATTERNS.md
@.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-06-SUMMARY.md

Key research sections: Pattern 6 (evalAgentRun crew gate — no API change; snapshot from CrewResult), A4 (live cache verification deferred to env-keyed opt-in), Pattern 3 point 4 (prefix > 1024 tokens for live cacheability). PATTERNS.md `examples/agent-crew/` section carries the exact agent-loop excerpts (package.json, signer/keyset, scripted fake, ~standard stub, index.mjs exit-code pattern).
</context>

<interfaces>
From 39-06 (consume as-is):
- `CrewResult { result, perAgent[{ id, usage, iterations, receiptCids }], usage, totalIterations, receipts, crewRootCid }`.
- `ai.runAgentCrew({ root, hosts: { childHost }, policy, signer, ... })` on the createAI facade.
- `defineAgent`, `verifyReceipt`, `receiptCid`, `evalAgentRun`, `createInMemorySigner`, `createMemoryKeySet`, `generateEd25519KeyPairJwk` — all from the built dist.

From packages/lattice/src/agent/eval.ts (existing, unchanged):
- `AgentRunSnapshot { iterationsToGoal: number; usage: Usage }`; `evalAgentRun(baseline, current, options?)` defaults +1 iteration / +10% cost; `mixed-cost-unknown` guard fires when exactly one costUsd is null — scripted fakes must emit numeric costUsd on BOTH sides.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: examples/agent-crew showcase (parent-summarizer + 3 child-researchers)</name>
  <files>examples/agent-crew/package.json, examples/agent-crew/setup.mjs, examples/agent-crew/index.mjs</files>
  <read_first>
    - examples/agent-loop/package.json, examples/agent-loop/setup.mjs, examples/agent-loop/index.mjs (the complete structure to mirror: dist imports, signer/keyset, scripted fake, receipts dir, exit codes, eval gate line)
    - packages/lattice/src/agent/crew/run-crew.ts (final RunAgentCrewOptions/CrewResult shapes)
    - .planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-RESEARCH.md Pattern 3 point 4 + A4 (opt-in live-cache script posture)
  </read_first>
  <action>
    Mirror `examples/agent-loop/` exactly. package.json: name `lattice-example-agent-crew`, `private: true`, `type: "module"`, dependency `"@full-self-browsing/lattice": "workspace:^"`. setup.mjs: imports from `"../../packages/lattice/dist/index.js"` (built dist, agent-loop:25-40 precedent); real Ed25519 via `generateEd25519KeyPairJwk` + `createInMemorySigner` (kid `kid:agent-crew-showcase:01`) + `createMemoryKeySet`; receipts dir via `mkdtempSync(join(tmpdir(), "lattice-agent-crew-"))`; ONE scripted fake provider whose response sequence drives: parent dispatches researcher-1, then researcher-2, then researcher-3 (serial — D-11), each child runs a small bounded loop emitting a summary, parent synthesizes the final answer; specs built with `defineAgent` — 1 parent (`summarizer`) with `childAgents: [researcher-1, researcher-2, researcher-3]` and a `~standard` stub `summaryReturnSchema` validating `{ summary, artifacts, receipts }`; give the children a sizeable shared tool-description prefix (target > 1024 tokens of byte-stable text) and include a commented, env-keyed opt-in note for live Anthropic/OpenAI cache verification (skipped by default — A4; real keys never run at PR time). index.mjs: `createAI({ providers: [fake], signer })` → `await ai.runAgentCrew({ root, hosts: { childHost }, policy: { budget: {...}, maxTotalIterations: ... } , signer })`; on non-success exit 2 with stderr line (agent-loop:26-34 pattern); write every envelope from `CrewResult.receipts` to the receipts dir; `verifyReceipt` each (exit 3 on any failure); assert chain: every non-root envelope's parsed payload `parentReceiptCid === crewRootCid` (exit 4 on mismatch); derive `{ iterationsToGoal: result.totalIterations, usage: result.usage }` and print an `evalAgentRun` gate line against an inline baseline; exit 0. Use only public dist exports — no src deep imports.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice build && node examples/agent-crew/index.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node examples/agent-crew/index.mjs` exits 0; stdout shows per-agent receipt verification lines (4+ receipts: crew root + parent + 3 children) and the eval gate line
    - Chain assertion present in index.mjs: grep `parentReceiptCid` in examples/agent-crew/index.mjs >= 1
    - All imports resolve from `dist/index.js` — `grep -c 'packages/lattice/src' examples/agent-crew/*.mjs` returns 0
    - Children execute serially (the scripted response sequence only works in serial order — implicit proof)
  </acceptance_criteria>
  <done>Roadmap SC3 showcase live: crew of 4 agents, every receipt Ed25519-signed, verified, and chain-linked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evalAgentRun crew regression gate</name>
  <files>packages/lattice/src/agent/crew/crew-eval.test.ts</files>
  <read_first>
    - packages/lattice/src/agent/eval.ts (AgentRunSnapshot :20-23; tolerance defaults +1 iteration/+10% cost :65-66; mixed-cost-unknown guard :84-93)
    - packages/lattice/src/agent/crew/crew-integration.test.ts (39-06 scripted crew fixtures to reuse)
  </read_first>
  <behavior>
    - Test 1: the scripted parent + 3-researchers crew completes successfully and `evalAgentRun(baseline, currentSnapshot)` passes against a committed inline baseline `{ iterationsToGoal, usage }` (both sides numeric costUsd — fake provider emits priced usage so the mixed-cost-unknown guard never fires)
    - Test 2: a deliberately inflated current snapshot (baseline iterations + 2) FAILS the gate — proving the gate actually gates
    - Test 3: snapshot derivation — `iterationsToGoal === CrewResult.totalIterations` and `usage === CrewResult.usage` (the Pattern 6 derivation contract)
  </behavior>
  <action>
    Write `src/agent/crew/crew-eval.test.ts`: run the 4-agent scripted crew (reuse/extract the 39-06 integration fixture builders), derive the snapshot from CrewResult per research Pattern 6 (totalIterations + crew aggregate usage), commit the baseline as an inline constant with a comment stating it is the Phase 39 regression budget (update intentionally, never casually). Use `evalAgentRun` UNCHANGED — no API modification (Pattern 6: "No API change is needed"). The fake provider must report numeric costUsd on every iteration so both snapshots are priced.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/crew-eval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 behaviors pass; gate-failure test asserts the failing verdict shape evalAgentRun returns (not a thrown string)
    - `git diff packages/lattice/src/agent/eval.ts` is empty (kernel untouched)
    - Baseline constant carries an explanatory comment
  </acceptance_criteria>
  <done>Crew completes within iteration + cost budget against a fake provider, machine-gated — DELEG-07 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example code → consumers copying it | the showcase is reference material; insecure patterns would propagate |
| scripted fake → eval baseline | baseline drift hides real regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-24 | Repudiation | showcase receipts | mitigate | Real Ed25519 signing + verifyReceipt of every envelope + chain assertion with non-zero exit codes — the example demonstrates the audit-trail contract correctly |
| T-39-25 | Information disclosure | example env handling | mitigate | Live-provider cache check is env-keyed and skipped by default; no keys committed; example never logs key material |
| T-39-26 | Tampering | eval baseline | accept | Inline committed baseline with intent comment; casual drift is caught in code review (low risk, fake-provider determinism) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | Zero installs — example depends on workspace package only (D-18) |
</threat_model>

<verification>
- `pnpm --filter @full-self-browsing/lattice build && node examples/agent-crew/index.mjs` exit 0
- `pnpm --filter @full-self-browsing/lattice test -- src/agent/crew/crew-eval.test.ts` green
</verification>

<success_criteria>
- 4-agent showcase runs end-to-end on built dist with all receipts signed, verified, chained
- evalAgentRun gate green against committed baseline and demonstrably capable of failing
</success_criteria>

<output>
Create `.planning/phases/39-multi-agent-delegation-surface-full-row-60-close-row-83-upda/39-07-SUMMARY.md` when done.
</output>
