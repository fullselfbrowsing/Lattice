---
phase: 48-eval-diagnostics-cli-expansion
plan: 04
type: execute
wave: 4
depends_on: [48-01, 48-02, 48-03]
files_modified:
  - packages/lattice-cli/src/cli.ts
  - packages/lattice-cli/package.json
  - packages/lattice-cli/test/cli.test.ts
  - .changeset/eval-diagnostics-cli.md
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04]
tags:
  - cli
  - release
  - validation
must_haves:
  truths:
    - "CLI help exposes eval agent mode, receipt diff, and diagnostics"
    - "CLI package metadata reflects the broader command scope"
    - "changeset records the user-facing CLI additions"
    - "full CLI package test/type/build/package-lint gates pass"
  artifacts:
    - path: ".changeset/eval-diagnostics-cli.md"
      provides: "release note for CLI expansion"
---

<objective>
Close the Phase 48 CLI surface with help/package metadata, changeset, and full package validation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-eval-diagnostics-cli-expansion/48-CONTEXT.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-RESEARCH.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-PATTERNS.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish CLI metadata and help coverage</name>
  <files>packages/lattice-cli/src/cli.ts packages/lattice-cli/package.json packages/lattice-cli/test/cli.test.ts</files>
  <action>
    Update the CLI description from receipt-only wording to cover eval and diagnostics. Add smoke assertions that top-level help lists `receipt` and `diagnostics`, and targeted help can be printed for the new command groups.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- cli</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add changeset</name>
  <files>.changeset/eval-diagnostics-cli.md</files>
  <action>
    Add a minor changeset for `@full-self-browsing/lattice-cli` describing agent eval, receipt diff, and LM Studio diagnostics.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run full CLI verification</name>
  <files>packages/lattice-cli/*</files>
  <action>
    Run the full CLI test suite, typecheck, build, and package lint. Capture command results in the phase summary and fix any failures before closing.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test</automated>
    <automated>pnpm --filter @full-self-browsing/lattice-cli typecheck</automated>
    <automated>pnpm --filter @full-self-browsing/lattice-cli build</automated>
    <automated>pnpm --filter @full-self-browsing/lattice-cli lint:packages</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice-cli test
pnpm --filter @full-self-browsing/lattice-cli typecheck
pnpm --filter @full-self-browsing/lattice-cli build
pnpm --filter @full-self-browsing/lattice-cli lint:packages
```
</verification>

<success_criteria>
- All Phase 48 user-facing commands are discoverable from help output.
- The CLI package has a changeset.
- Full CLI package validation passes.
</success_criteria>
