---
phase: 48-eval-diagnostics-cli-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice-cli/src/eval/agent-types.ts
  - packages/lattice-cli/src/eval/agent-runner.ts
  - packages/lattice-cli/src/commands/eval.ts
  - packages/lattice-cli/test/agent-eval.test.ts
  - packages/lattice-cli/test/eval.test.ts
autonomous: true
requirements: [EVAL-01, EVAL-02]
tags:
  - cli
  - eval
  - agents
must_haves:
  truths:
    - "lattice eval --agent loads current agent snapshots from fixtures and baselines from JSON"
    - "agent eval calls evalAgentRun for every fixture with a matching baseline entry"
    - "agent eval emits version lattice-agent-eval/v1 and does not alter lattice-eval/v1"
    - "regressions exit 1 and malformed inputs exit 2"
  artifacts:
    - path: "packages/lattice-cli/src/eval/agent-runner.ts"
      provides: "agent eval fixture/baseline loader and report runner"
    - path: "packages/lattice-cli/src/commands/eval.ts"
      provides: "--agent dispatch and CLI arg mapping"
---

<objective>
Add the agent eval CLI mode while preserving the existing receipt replay eval contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-eval-diagnostics-cli-expansion/48-CONTEXT.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-RESEARCH.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-PATTERNS.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define agent eval file/report types</name>
  <files>packages/lattice-cli/src/eval/agent-types.ts</files>
  <action>
    Define versioned fixture, baseline, per-fixture report, summary, config, and session report types. Use `AgentRunSnapshot`, `EvalOptions`, and `EvalRegression` from `@full-self-browsing/lattice` instead of duplicating the kernel types.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli typecheck</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement fixture discovery and baseline loading</name>
  <files>packages/lattice-cli/src/eval/agent-runner.ts</files>
  <action>
    Load `*.json` fixtures in lexicographic order, validate the fixture and baseline JSON shapes, call `evalAgentRun()` for every valid fixture with a baseline entry, and return a `lattice-agent-eval/v1` report. Treat missing baseline entries as `new-fixture`; throw typed load errors for missing/malformed input files.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- agent-eval</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire --agent into lattice eval</name>
  <files>packages/lattice-cli/src/commands/eval.ts</files>
  <action>
    Add `agent`, `iterations-tolerance`, and agent config args to `RunEvalArgs`/citty args. In `runEval()`, dispatch to the agent runner when `args.agent === true`; otherwise keep the existing code path unchanged. Emit one JSON stdout line and compact stderr summaries.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- eval agent-eval</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Cover success, regression, malformed, and compatibility paths</name>
  <files>packages/lattice-cli/test/agent-eval.test.ts packages/lattice-cli/test/eval.test.ts</files>
  <action>
    Add tests for passing agent eval, iteration/cost regression, malformed fixture or baseline, missing baseline entry, and default `lattice eval` still returning `lattice-eval/v1`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- eval agent-eval</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice-cli test -- eval agent-eval
pnpm --filter @full-self-browsing/lattice-cli typecheck
```
</verification>

<success_criteria>
- `runEval({ agent: true })` emits a `lattice-agent-eval/v1` JSON report.
- Regression reports include iterations-to-goal and cost outcomes from `evalAgentRun`.
- Existing receipt replay eval tests still pass without report shape drift.
</success_criteria>
