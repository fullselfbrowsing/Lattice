---
phase: 48-eval-diagnostics-cli-expansion
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - packages/lattice-cli/src/receipt/diff.ts
  - packages/lattice-cli/src/commands/receipt.ts
  - packages/lattice-cli/src/cli.ts
  - packages/lattice-cli/test/receipt-diff.test.ts
  - packages/lattice-cli/test/cli.test.ts
autonomous: true
requirements: [EVAL-03]
tags:
  - cli
  - receipts
  - diff
must_haves:
  truths:
    - "lattice receipt diff compares two receipt envelopes without requiring key material"
    - "diff report includes model, route, usage, hashes, lineage, parent receipt, and signature/key fields"
    - "equal selected fields exit 0, differences exit 1, load failures exit 2"
  artifacts:
    - path: "packages/lattice-cli/src/receipt/diff.ts"
      provides: "receipt envelope decode and structural diff report"
    - path: "packages/lattice-cli/src/commands/receipt.ts"
      provides: "receipt command group with diff subcommand"
---

<objective>
Add `lattice receipt diff` for safe, scriptable comparison of signed receipt envelopes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-eval-diagnostics-cli-expansion/48-CONTEXT.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-RESEARCH.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-PATTERNS.md
@.planning/phases/48-eval-diagnostics-cli-expansion/48-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement structural receipt diff</name>
  <files>packages/lattice-cli/src/receipt/diff.ts</files>
  <action>
    Load two envelopes, decode each base64 payload to a `CapabilityReceiptBody`-like structure, project safe comparable fields, and return `lattice-receipt-diff/v1` with stable path-based differences. Include signature count, key ids, and signature values in the comparison.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- receipt-diff</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add receipt command group</name>
  <files>packages/lattice-cli/src/commands/receipt.ts packages/lattice-cli/src/cli.ts</files>
  <action>
    Register a new `receipt` top-level command group with `diff --left <path> --right <path>`. Preserve the existing main command description style and map typed diff/load failures to `FAIL kind=receipt-diff-...`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- receipt-diff cli</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Test diff equality, mismatch, malformed paths, and help</name>
  <files>packages/lattice-cli/test/receipt-diff.test.ts packages/lattice-cli/test/cli.test.ts</files>
  <action>
    Add direct handler tests for equal receipts, changed model/route/usage/hash/lineage/parent/signature fields, malformed payload failure, and CLI help registration for `receipt`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice-cli test -- receipt-diff cli</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice-cli test -- receipt-diff cli
pnpm --filter @full-self-browsing/lattice-cli typecheck
```
</verification>

<success_criteria>
- `lattice receipt diff` is available in help output.
- Diff report paths are deterministic and cover all required receipt fields.
- Bad inputs fail with exit 2 and no JSON report.
</success_criteria>
