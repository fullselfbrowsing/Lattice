---
phase: 47-opentelemetry-exporter-langfuse-phoenix-paths
plan: 03
type: execute
wave: 3
depends_on: [47-02]
files_modified:
  - packages/lattice/src/observability/otel.ts
  - packages/lattice/src/observability/otel.test.ts
  - docs/observability-otel.md
autonomous: true
requirements: [OTEL-04]
tags:
  - observability
  - otlp
  - docs
must_haves:
  truths:
    - "Langfuse and Phoenix setup paths use standard OTLP HTTP configuration"
    - "helpers return endpoint/header data only and do not instantiate SDK exporters"
    - "core has no hard Langfuse, Phoenix, or OpenTelemetry SDK dependency"
  artifacts:
    - path: "docs/observability-otel.md"
      provides: "host-app setup examples for Langfuse, Phoenix, and generic OTLP"
---

<objective>
Expose dependency-free OTLP configuration helpers and document Langfuse/Phoenix setup paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-CONTEXT.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-RESEARCH.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-PATTERNS.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Langfuse and Phoenix config helpers</name>
  <files>packages/lattice/src/observability/otel.ts, packages/lattice/src/observability/otel.test.ts</files>
  <action>
    Add `createLangfuseOtlpConfig()` and `createPhoenixOtlpConfig()` helpers returning `{ endpoint, headers }` for HTTP trace exporters. Langfuse should support base URL plus public/secret key Basic auth and ingestion-version header. Phoenix should support local/default, cloud/self-hosted base URL, API key header, and custom headers.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- otel</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add setup documentation</name>
  <files>docs/observability-otel.md</files>
  <action>
    Document wiring `createOtelRunEventSink()` into `createAI({ events: [...] })`, host-app ownership of OpenTelemetry SDK/exporter setup, Langfuse OTLP endpoint/auth, Phoenix local/cloud setup, and the default sanitizer/content-capture stance.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- otel</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- otel
node scripts/check-core-package-boundary.mjs
```
</verification>

<success_criteria>
- Helpers are deterministic pure functions with no external imports.
- Docs provide concrete OTLP paths for Langfuse and Phoenix.
- Boundary check remains clean.
</success_criteria>

