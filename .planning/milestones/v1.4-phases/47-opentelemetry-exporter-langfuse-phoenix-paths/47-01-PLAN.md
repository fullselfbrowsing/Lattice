---
phase: 47-opentelemetry-exporter-langfuse-phoenix-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/observability/otel.ts
  - packages/lattice/src/observability/otel.test.ts
autonomous: true
requirements: [OTEL-01, OTEL-02, OTEL-05]
tags:
  - observability
  - opentelemetry
  - tracing
must_haves:
  truths:
    - "createOtelRunEventSink is a RunEventSink over structural OTel-compatible tracer/span types"
    - "one low-cardinality run span is created per run id and every RunEvent becomes a span event"
    - "run.complete ends with OK status and run.failed ends with ERROR status"
    - "all current RunEventKind literals are covered by tests"
  artifacts:
    - path: "packages/lattice/src/observability/otel.ts"
      provides: "structural OTel run-event sink and attribute mapping"
---

<objective>
Add the structural OpenTelemetry-compatible run-event sink and core event-to-attribute mapping.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-CONTEXT.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-RESEARCH.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-PATTERNS.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structural OTel types and sink factory</name>
  <files>packages/lattice/src/observability/otel.ts</files>
  <action>
    Define structural tracer/span/status/attribute types and implement `createOtelRunEventSink()`. The sink should create a span on `run.start`, lazily create one if needed, call `span.addEvent()` for every `RunEvent`, set span attributes from run/provider/model metadata, and end on terminal run events.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- otel</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Cover run and event vocabulary mapping</name>
  <files>packages/lattice/src/observability/otel.test.ts</files>
  <action>
    Add fake tracer/span tests covering run.start/run.complete/run.failed, lazy span creation, status mapping, and an exhaustive list of every current `RunEventKind` producing a span event with stable `lattice.*` attributes.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- otel</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- otel
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- The exporter works with fake structural OTel objects and has no OTel imports.
- Every current event literal maps predictably to a span event.
- Terminal run events end the span with the expected status.
</success_criteria>

