---
phase: 47-opentelemetry-exporter-langfuse-phoenix-paths
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - packages/lattice/src/observability/otel.ts
  - packages/lattice/src/observability/otel.test.ts
autonomous: true
requirements: [OTEL-02, OTEL-03]
tags:
  - observability
  - sanitizer
  - receipts
must_haves:
  truths:
    - "default sanitizer excludes raw prompt/output/artifact content"
    - "secret-shaped keys remain excluded even with metadata capture enabled"
    - "receipt CID and signature references attach when event metadata carries an envelope"
    - "usage and gateway metadata map into gen_ai.*, lattice.*, and Phoenix-compatible token attributes"
  artifacts:
    - path: "packages/lattice/src/observability/otel.ts"
      provides: "sanitizer, receipt attributes, and safe metadata mapping"
---

<objective>
Add safe span attribute sanitization, usage/gateway mappings, and receipt reference attributes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-CONTEXT.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-RESEARCH.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-PATTERNS.md
@.planning/phases/47-opentelemetry-exporter-langfuse-phoenix-paths/47-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sanitizer and metadata mapping</name>
  <files>packages/lattice/src/observability/otel.ts</files>
  <action>
    Add `sanitizeRunEventAttributes()` with default content-safe behavior and explicit metadata capture mode. Map known metadata such as context counts, router counts, provider attempt status, stream status, tool call identifiers, gateway observed model, usage, and validation/failure reasons to stable attributes.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- otel</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add receipt reference attributes</name>
  <files>packages/lattice/src/observability/otel.ts, packages/lattice/src/observability/otel.test.ts</files>
  <action>
    Implement `createOtelReceiptAttributes()` using `receiptCid()` and detect receipt envelopes on event metadata. Add tests using a real test receipt envelope so the CID and signature attributes are deterministic enough to assert shape and key id.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- otel receipt</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- otel receipt
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Default attributes do not leak raw content or secrets.
- Metadata capture mode is explicit and bounded.
- Receipt references appear when event metadata includes a receipt envelope.
</success_criteria>

