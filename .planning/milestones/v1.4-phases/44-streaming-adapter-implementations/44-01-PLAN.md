---
phase: 44-streaming-adapter-implementations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/providers/sse.ts
  - packages/lattice/src/providers/sse.test.ts
  - packages/lattice/src/providers/adapters.ts
  - packages/lattice/src/providers/adapters.test.ts
  - packages/lattice/src/providers/xai.test.ts
  - packages/lattice/src/providers/openrouter.test.ts
  - packages/lattice/src/providers/lm-studio.test.ts
autonomous: true
requirements: [SADAPT-03, SADAPT-04]
tags:
  - streaming
  - openai-compatible
  - sse
must_haves:
  truths:
    - "SADAPT-03: xAI, OpenRouter, and LM Studio inherit executeStream? from the OpenAI-compatible path"
    - "SADAPT-04: OpenAI and generic OpenAI-compatible also expose executeStream? for seven-provider parity"
    - "SSE parsing handles comments, split frames, and [DONE] without leaking provider-specific events"
    - "Streaming text is yielded as normalized ProviderStreamChunk values and final sanitized rawOutputs override accumulated text"
    - "OpenAI-compatible tool-call deltas are accumulated and validated before becoming tool-call chunks"
  artifacts:
    - path: "packages/lattice/src/providers/sse.ts"
      provides: "small dependency-free SSE reader"
    - path: "packages/lattice/src/providers/adapters.ts"
      provides: "OpenAI-compatible executeStream? implementation shared by wrappers"
    - path: "packages/lattice/src/providers/adapters.test.ts"
      provides: "OpenAI-compatible stream parser tests"
  key_links:
    - from: "createOpenAICompatibleProvider.executeStream?"
      to: "xAI/OpenRouter/LM Studio inherited streaming"
      via: "wrapper spread of inner adapter"
      pattern: "one parser covers the OpenAI-compatible family"
---

<objective>
Build the shared streaming foundation: a dependency-free SSE reader plus `executeStream?` on `createOpenAICompatibleProvider`. This plan should make OpenAI, generic OpenAI-compatible, xAI, OpenRouter, LM Studio, and LiteLLM stream through the same parser without adding SDK dependencies.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-streaming-adapter-implementations/44-CONTEXT.md
@.planning/phases/44-streaming-adapter-implementations/44-RESEARCH.md
@.planning/phases/44-streaming-adapter-implementations/44-PATTERNS.md
@.planning/phases/44-streaming-adapter-implementations/44-VALIDATION.md
@.planning/phases/43-streaming-contract-collectstream/43-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependency-free SSE reader</name>
  <files>packages/lattice/src/providers/sse.ts, packages/lattice/src/providers/sse.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/streaming.ts
    - packages/lattice/src/providers/streaming.test.ts
    - .planning/phases/44-streaming-adapter-implementations/44-RESEARCH.md
  </read_first>
  <action>
    Create `packages/lattice/src/providers/sse.ts` exporting:
    ```typescript
    export interface SseEvent {
      readonly event?: string;
      readonly data: string;
    }

    export async function* readSseEvents(response: Response): AsyncIterable<SseEvent>
    ```

    Semantics:
    - Read `response.body` with `getReader()` when available; otherwise fall back to `await response.text()`.
    - Decode with `TextDecoder`.
    - Split SSE frames on blank lines.
    - Ignore comment lines beginning with `:`.
    - Support multiple `data:` lines by joining them with `\n`.
    - Preserve optional `event:` name.
    - Skip frames with no `data`.
    - Do not parse JSON in this helper.

    Add `packages/lattice/src/providers/sse.test.ts` covering:
    - split frames across multiple stream chunks,
    - comments ignored,
    - multiple `data:` lines joined,
    - `event:` value preserved.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- sse</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/sse.ts` contains `export async function* readSseEvents`.
    - `packages/lattice/src/providers/sse.test.ts` contains `split frames across chunks`.
    - `pnpm --filter @full-self-browsing/lattice test -- sse` exits 0.
  </acceptance_criteria>
  <done>SSE parsing is available without adding runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add OpenAI-compatible executeStream?</name>
  <files>packages/lattice/src/providers/adapters.ts, packages/lattice/src/providers/adapters.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/adapters.ts
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/providers/streaming.ts
    - packages/lattice/src/tools/tool-call-validation.ts
    - packages/lattice/src/agent/format-tools.ts
  </read_first>
  <action>
    In `adapters.ts`:
    - Import `readSseEvents`.
    - Add an unexported helper to build the existing chat-completions request body so `execute()` and `executeStream?` share the same base request shape.
    - Add `executeStream?` to the object returned by `createOpenAICompatibleProvider`.
    - The streaming request must call `${baseUrl}/chat/completions` with the same headers and body as `execute()`, plus `stream: true`.
    - Parse SSE `data` JSON chunks. Stop on `[DONE]`.
    - Yield text deltas for `choices[*].delta.content`; for every content delta, yield one `text-delta` per requested output name.
    - Accumulate final text locally and yield a final `complete` chunk whose `rawOutputs` are passed through `applyOutputSanitizers`.
    - Parse final accumulated text with `parseToolUseEnvelope` to preserve prompt-reencoded tool-call validation.
    - Accumulate native `delta.tool_calls` fragments by index; when `validateToolCalls` is configured, validate accumulated calls and yield `tool-call` chunks or include them in the final `complete.toolCalls`.
    - Parse `usage` if present on any chunk; last usage wins.
    - Preserve gateway metadata behavior: OpenRouter/xAI/OpenAI-compatible response `model` should become observed model when present, and LiteLLM/gateway policy should still surface under `gateway`.

    In `adapters.test.ts`, add tests for:
    - streaming request body includes `stream: true`,
    - text chunks collect to final output with `collectStream()`,
    - split SSE frames parse correctly,
    - native tool-call deltas validate into `toolCalls`,
    - non-OK streaming response throws `OpenAI-compatible provider failed with ...`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- adapters streaming</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/adapters.ts` contains `executeStream`.
    - `packages/lattice/src/providers/adapters.ts` contains `stream: true`.
    - `packages/lattice/src/providers/adapters.test.ts` contains `streaming request body includes stream true`.
    - `packages/lattice/src/providers/adapters.test.ts` contains `native tool-call deltas validate into toolCalls`.
    - `pnpm --filter @full-self-browsing/lattice test -- adapters streaming` exits 0.
  </acceptance_criteria>
  <done>OpenAI-compatible streaming produces normalized chunks and final sanitized outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Verify wrapper inheritance for xAI, OpenRouter, LM Studio, and LiteLLM</name>
  <files>packages/lattice/src/providers/xai.test.ts, packages/lattice/src/providers/openrouter.test.ts, packages/lattice/src/providers/lm-studio.test.ts, packages/lattice/src/providers/litellm.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/xai.ts
    - packages/lattice/src/providers/openrouter.ts
    - packages/lattice/src/providers/lm-studio.ts
    - packages/lattice/src/providers/litellm.ts
    - packages/lattice/src/providers/adapters.test.ts
  </read_first>
  <action>
    Add focused tests proving each wrapper exposes `executeStream` and that a fake OpenAI-compatible SSE response collects into final output. Use `collectStream()` directly in provider tests; do not go through runtime here.

    Include at least these test names:
    - `xAI inherits OpenAI-compatible executeStream`
    - `OpenRouter inherits OpenAI-compatible executeStream`
    - `LM Studio inherits OpenAI-compatible executeStream`
    - `LiteLLM inherits OpenAI-compatible executeStream`
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- xai openrouter lm-studio litellm</automated>
  </verify>
  <acceptance_criteria>
    - wrapper tests contain the four names above.
    - `pnpm --filter @full-self-browsing/lattice test -- xai openrouter lm-studio litellm` exits 0.
  </acceptance_criteria>
  <done>OpenAI-compatible wrapper adapters inherit the streaming path.</done>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- sse adapters streaming xai openrouter lm-studio litellm
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Shared SSE parser works with split frames and comments.
- OpenAI-compatible `executeStream?` yields normalized stream chunks.
- xAI, OpenRouter, LM Studio, and LiteLLM inherit the OpenAI-compatible streaming implementation.
</success_criteria>
