---
phase: 44-streaming-adapter-implementations
plan: 03
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - packages/lattice/src/providers/gemini.ts
  - packages/lattice/src/providers/gemini.test.ts
autonomous: true
requirements: [SADAPT-02, SADAPT-04]
tags:
  - streaming
  - gemini
  - sse
must_haves:
  truths:
    - "SADAPT-02: Gemini executeStream? maps streamGenerateContent text parts into normalized text-delta chunks"
    - "SADAPT-02: Gemini functionCall parts become validated tool-call chunks when validation is configured"
    - "SADAPT-04: Gemini remains compatible with existing non-streaming execute() behavior"
  artifacts:
    - path: "packages/lattice/src/providers/gemini.ts"
      provides: "Gemini native executeStream? parser"
    - path: "packages/lattice/src/providers/gemini.test.ts"
      provides: "Gemini streaming fake SSE tests"
---

<objective>
Implement Gemini `streamGenerateContent` streaming on the existing Gemini adapter without changing its non-streaming `execute()` path.
</objective>

<context>
@.planning/phases/44-streaming-adapter-implementations/44-CONTEXT.md
@.planning/phases/44-streaming-adapter-implementations/44-RESEARCH.md
@.planning/phases/44-streaming-adapter-implementations/44-PATTERNS.md
@.planning/phases/44-streaming-adapter-implementations/44-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gemini executeStream?</name>
  <files>packages/lattice/src/providers/gemini.ts</files>
  <read_first>
    - packages/lattice/src/providers/gemini.ts
    - packages/lattice/src/providers/sse.ts
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/tools/tool-call-validation.ts
  </read_first>
  <action>
    Add `executeStream?` to the Gemini provider object:
    - Reuse the existing `execute()` body shape (`contents`, `generationConfig`, `safetySettings`).
    - POST to `${baseUrl}/v1beta/models/${encodeURIComponent(model)}:streamGenerateContent?key=${key}&alt=sse` or equivalent query ordering.
    - Throw `Gemini provider failed with ${status}.` on non-OK responses.
    - Use `readSseEvents(response)`.
    - Parse each streamed response JSON object.
    - For `candidates[].content.parts[].text`, yield text deltas for every requested output name and accumulate final text.
    - For `candidates[].content.parts[].functionCall`, convert to Lattice tool-use request shape and validate through `validateToolCallRequests` when configured.
    - Parse `usageMetadata` into `usage` and `normalizedUsage`; last usage wins.
    - At stream end, yield `complete` with sanitized raw outputs, usage, normalized usage, validated tool calls if any, and raw response chunks.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/gemini.ts` contains `executeStream`.
    - `packages/lattice/src/providers/gemini.ts` contains `streamGenerateContent`.
    - `packages/lattice/src/providers/gemini.ts` contains `functionCall`.
    - `pnpm --filter @full-self-browsing/lattice typecheck` exits 0.
  </acceptance_criteria>
  <done>Gemini adapter exposes native streaming under the normalized contract.</done>
</task>

<task type="auto">
  <name>Task 2: Test Gemini streaming text, function calls, usage, and errors</name>
  <files>packages/lattice/src/providers/gemini.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/gemini.test.ts
    - packages/lattice/src/providers/streaming.ts
    - packages/lattice/src/providers/sse.test.ts
  </read_first>
  <action>
    Add fake SSE tests named:
    - `streams Gemini text parts through executeStream`
    - `streams Gemini functionCall parts into validated toolCalls`
    - `streams Gemini usageMetadata into final complete chunk`
    - `Gemini executeStream throws on non-OK response`

    Use `collectStream()` to assert final outputs and tool calls.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- gemini streaming</automated>
  </verify>
  <acceptance_criteria>
    - Gemini test file contains all four test names.
    - `pnpm --filter @full-self-browsing/lattice test -- gemini streaming` exits 0.
  </acceptance_criteria>
  <done>Gemini streaming behavior is covered with fake SSE fixtures.</done>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- gemini streaming
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>
