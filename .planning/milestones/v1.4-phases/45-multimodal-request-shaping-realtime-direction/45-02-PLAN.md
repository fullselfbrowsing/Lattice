---
phase: 45-multimodal-request-shaping-realtime-direction
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - packages/lattice/src/providers/anthropic.ts
  - packages/lattice/src/providers/anthropic.test.ts
  - packages/lattice/src/providers/multimodal.ts
autonomous: true
requirements: [MMRT-01, MMRT-03]
tags:
  - anthropic
  - image
  - multimodal
must_haves:
  truths:
    - "MMRT-01: Anthropic image artifacts map to native Messages image blocks"
    - "Anthropic file-id requests include the files API beta header"
    - "Text-only Anthropic requests preserve the old string content shape"
    - "Streaming and non-streaming Anthropic requests share the same multimodal body builder"
  artifacts:
    - path: "packages/lattice/src/providers/anthropic.ts"
      provides: "native image request shaping"
    - path: "packages/lattice/src/providers/anthropic.test.ts"
      provides: "Anthropic base64, URL, and file-id request body coverage"
---

<objective>
Map Lattice image artifacts into Anthropic Messages image content blocks using provider packaging metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-CONTEXT.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-RESEARCH.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-PATTERNS.md
@.planning/phases/45-multimodal-request-shaping-realtime-direction/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared multimodal artifact helpers</name>
  <files>packages/lattice/src/providers/multimodal.ts</files>
  <action>
    Add helper functions for safe media extraction: provider file ID/URI metadata lookup, HTTP(S) URL lookup, media type fallback, data URL/base64 extraction, and Blob/ArrayBuffer base64 conversion. Keep helpers provider-adjacent and dependency-free.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build Anthropic image content blocks</name>
  <files>packages/lattice/src/providers/anthropic.ts</files>
  <action>
    Make the Anthropic body builder async and add native image blocks before task text when artifacts have `base64`, `url`, or `file-id` transport. Use `media_type` for base64 blocks and `file_id` for file references. Add the Files API beta header only when file references are used. Preserve text-only body shape.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- anthropic</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Cover Anthropic request branches</name>
  <files>packages/lattice/src/providers/anthropic.test.ts</files>
  <action>
    Add tests named:
    - `Anthropic packages image artifacts as base64 image blocks`
    - `Anthropic packages image URL artifacts as url image blocks`
    - `Anthropic packages image file-id artifacts with files beta header`
    - `Anthropic streaming uses the multimodal request body`
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- anthropic</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- anthropic packaging
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Anthropic non-streaming and streaming requests support base64, URL, and file-id image blocks.
- The provider packaging plan remains the source of transport evidence.
</success_criteria>
