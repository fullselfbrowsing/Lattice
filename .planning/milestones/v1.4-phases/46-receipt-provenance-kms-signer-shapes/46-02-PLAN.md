---
phase: 46-receipt-provenance-kms-signer-shapes
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - packages/lattice/src/runtime/create-ai.ts
  - packages/lattice/src/runtime/create-ai.test.ts
  - packages/lattice/src/agent/types.ts
  - packages/lattice/src/agent/runtime.ts
  - packages/lattice/src/agent/crew/dispatcher.ts
  - packages/lattice/src/agent/crew/dispatcher.test.ts
  - packages/lattice/src/agent/crew/run-crew.ts
  - packages/lattice/src/agent/crew/run-crew.test.ts
autonomous: true
requirements: [REC-03]
tags:
  - runtime
  - streaming
  - crew
  - provenance
must_haves:
  truths:
    - "Runtime inputHashes remain input-only"
    - "Runtime lineage roots include available input and output artifact refs"
    - "Streaming receipt roots are computed after collection"
    - "Crew child completion receipts include lineage roots when child artifacts expose lineage"
---

<objective>
Wire lineage merkle roots into terminal receipt issuance for normal runs, streaming runs, and crew child completion receipts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-CONTEXT.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-PATTERNS.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runtime receipt lineage roots</name>
  <files>packages/lattice/src/runtime/create-ai.ts, packages/lattice/src/runtime/create-ai.test.ts</files>
  <action>
    Extend `maybeIssueReceipt()` with separate lineage artifact input, compute `lineageMerkleRoot` inside the best-effort receipt block, and pass input + output artifact refs on success. Add normal and streaming tests using derived artifacts with lineage.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- create-ai</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Crew child completion lineage roots</name>
  <files>packages/lattice/src/agent/types.ts, packages/lattice/src/agent/runtime.ts, packages/lattice/src/agent/crew/dispatcher.ts, packages/lattice/src/agent/crew/dispatcher.test.ts, packages/lattice/src/agent/crew/run-crew.ts, packages/lattice/src/agent/crew/run-crew.test.ts</files>
  <action>
    Surface optional final-response artifact refs on agent success, feed those refs into child completion receipt creation, and assert the child completion receipt carries a lineage root when a child response exposes derived artifact refs.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- dispatcher run-crew</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- create-ai dispatcher run-crew
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Runtime receipts include roots only when lineage exists.
- Streaming test proves root inclusion happens after stream collection.
- Crew child receipt test proves child artifact lineage is committed under `parentReceiptCid`.
</success_criteria>

