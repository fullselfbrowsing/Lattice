---
phase: 46-receipt-provenance-kms-signer-shapes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/receipts/types.ts
  - packages/lattice/src/receipts/receipt.ts
  - packages/lattice/src/receipts/verify.ts
  - packages/lattice/src/receipts/lineage.ts
  - packages/lattice/src/receipts/lineage.test.ts
  - packages/lattice/src/receipts/receipt.test.ts
  - packages/lattice/src/receipts/verify.test.ts
autonomous: true
requirements: [REC-01, REC-02]
tags:
  - receipts
  - provenance
  - verification
must_haves:
  truths:
    - "createReceipt mints lattice-receipt/v1.3"
    - "verifyReceipt accepts v1.1/v1.2/v1.3 and still rejects v1/undefined"
    - "lineageMerkleRoot is optional, additive, and canonicalized only when supplied"
    - "lineage helper hashes sanitized artifact refs, never raw artifact values"
  artifacts:
    - path: "packages/lattice/src/receipts/lineage.ts"
      provides: "deterministic content-free lineage merkle root helper"
---

<objective>
Add the v1.3 receipt provenance schema and deterministic lineage merkle-root helper while preserving verifier compatibility.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-CONTEXT.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-RESEARCH.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-PATTERNS.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen receipt schema to v1.3</name>
  <files>packages/lattice/src/receipts/types.ts, packages/lattice/src/receipts/receipt.ts, packages/lattice/src/receipts/verify.ts</files>
  <action>
    Add `lattice-receipt/v1.3` to the version union, add optional `lineageMerkleRoot?: string`, mint v1.3 in `createReceipt()`, and update verifier accepted-version/error text while preserving the existing v1 downgrade gate.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- receipt verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add lineage merkle-root helper</name>
  <files>packages/lattice/src/receipts/lineage.ts, packages/lattice/src/receipts/lineage.test.ts</files>
  <action>
    Implement `computeArtifactLineageMerkleRoot()` over sanitized artifact refs. Return `undefined` when no lineage exists. Cover determinism, order-insensitivity, changed lineage, nested parents, and value exclusion.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- lineage receipt verify</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- lineage receipt verify
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- v1.3 receipts round-trip through `verifyReceipt()`.
- Existing v1.1/v1.2 receipts continue to verify.
- Lineage roots are stable and payload-free.
</success_criteria>

