---
phase: 46-receipt-provenance-kms-signer-shapes
plan: 03
type: execute
wave: 3
depends_on: [46-01]
files_modified:
  - packages/lattice/src/receipts/remote-signer.ts
  - packages/lattice/src/receipts/remote-signer.test.ts
  - packages/lattice/src/index.ts
  - packages/lattice/src/runtime/public-types.ts
  - packages/lattice/test/public-surface.test.ts
  - packages/lattice/test-d/package-types.test-d.ts
autonomous: true
requirements: [REC-04, REC-05]
tags:
  - receipts
  - kms
  - signing
  - public-api
must_haves:
  truths:
    - "Remote signer factory returns existing ReceiptSigner"
    - "No cloud SDK dependency is added to core"
    - "Remote callback receives exact DSSE PAE bytes"
    - "Receipts signed through remote callback verify with existing KeySet"
---

<objective>
Add a public, cloud-SDK-free remote signer adapter shape for production KMS/HSM signing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-CONTEXT.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-RESEARCH.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-PATTERNS.md
@.planning/phases/46-receipt-provenance-kms-signer-shapes/46-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement remote receipt signer factory</name>
  <files>packages/lattice/src/receipts/remote-signer.ts, packages/lattice/src/receipts/remote-signer.test.ts</files>
  <action>
    Add `createRemoteReceiptSigner()` with request/result types that identify the bytes as DSSE PAE. Tests should record callback bytes, delegate signing to the in-memory signer, compare recorded bytes to `buildPae(PAYLOAD_TYPE, envelope.payload)`, and verify through `createMemoryKeySet`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- remote-signer receipt verify</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Export public signer types</name>
  <files>packages/lattice/src/index.ts, packages/lattice/src/runtime/public-types.ts, packages/lattice/test/public-surface.test.ts, packages/lattice/test-d/package-types.test-d.ts</files>
  <action>
    Export the factory and type surface from the package root, update strict value export allowlists, and add tsd assertions that a remote signer is assignable to `ReceiptSigner`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice build && pnpm --filter @full-self-browsing/lattice test:types</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- remote-signer public-surface public-types
pnpm --filter @full-self-browsing/lattice build
pnpm --filter @full-self-browsing/lattice test:types
node scripts/check-core-package-boundary.mjs
```
</verification>

<success_criteria>
- Remote signer callback receives canonical DSSE PAE bytes exactly.
- Public exports are type-tested.
- Core package boundary remains cloud-SDK-free.
</success_criteria>

