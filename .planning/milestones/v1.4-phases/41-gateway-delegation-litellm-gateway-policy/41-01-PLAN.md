---
phase: 41-gateway-delegation-litellm-gateway-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lattice/src/capabilities/profile.ts
  - packages/lattice/src/policy/policy.ts
  - packages/lattice/src/providers/adapters.ts
  - packages/lattice/src/providers/litellm.ts
  - packages/lattice/src/providers/quirks.ts
  - packages/lattice/src/providers/litellm.test.ts
autonomous: true
requirements: [GATE-01, GATE-02]
tags:
  - litellm
  - gateway-policy
  - openai-compatible
must_haves:
  truths:
    - "GATE-01: createLiteLLMProvider delegates to createOpenAICompatibleProvider and introduces no LiteLLM Python SDK or gateway runtime dependency"
    - "GATE-02: typed GatewayPolicy carries routeTags, providerPreferences, metadata, and allowFallbacks without using a raw bag"
    - "D-01: createLiteLLMProvider is a thin wrapper over createOpenAICompatibleProvider"
    - "D-02: LiteLLM defaults to provider id litellm and default base URL http://localhost:4000"
    - "D-03: no LiteLLM Python SDK, gateway process dependency, or new transport abstraction is added"
    - "D-04: apiKey remains optional and only a provided key becomes Authorization: Bearer <apiKey>"
    - "D-05: LiteLLM capability negotiation is registry-only in this phase"
    - "D-06: gateway routing hints use a typed additive GatewayPolicy object"
    - "D-07: gateway hints are advisory and do not affect Lattice route scoring"
    - "D-08: LiteLLM metadata serializes to the documented top-level metadata request-body field"
    - "D-09: API keys and custom auth data are never copied into metadata"
    - "D-14: LiteLLM defaults gateway allowFallbacks to false"
    - "D-15: gateway fallback/load-balancing hints are explicit opt-in fields"
  artifacts:
    - path: "packages/lattice/src/policy/policy.ts"
      provides: "GatewayPolicy and GatewayMetadataValue public policy types"
    - path: "packages/lattice/src/capabilities/profile.ts"
      provides: "typed first-party litellm capability adapter id"
    - path: "packages/lattice/src/providers/adapters.ts"
      provides: "OpenAI-compatible gateway metadata serialization and observed model extraction"
    - path: "packages/lattice/src/providers/litellm.ts"
      provides: "first-class LiteLLM provider helper"
    - path: "packages/lattice/src/providers/litellm.test.ts"
      provides: "fake-fetch coverage for LiteLLM request shape and usage"
  key_links:
    - from: "PolicySpec.gateway"
      to: "OpenAI-compatible request body"
      via: "metadata.lattice_gateway"
      pattern: "route_tags, provider_preferences, allow_fallbacks"
    - from: "createLiteLLMProvider"
      to: "createOpenAICompatibleProvider"
      via: "delegated inner provider"
      pattern: "id: litellm, baseUrl: http://localhost:4000"
---

<objective>
Add the typed gateway policy surface and first-class LiteLLM provider helper. The helper must remain a thin OpenAI-compatible wrapper, default to the documented local LiteLLM proxy URL, preserve optional auth semantics, and prove request body metadata/usage/error behavior with fake-fetch tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-CONTEXT.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-RESEARCH.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-PATTERNS.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-VALIDATION.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| T-41-01: LiteLLM helper becomes a separate transport with dependency drag | high | implement `createLiteLLMProvider` by delegating to `createOpenAICompatibleProvider`; add no dependency to any package manifest |
| T-41-02: raw gateway metadata accepts secrets by accident | high | type metadata as JSON-like values, do not accept headers in `GatewayPolicy`, and test that `sk-` does not appear in request body metadata |
| T-41-03: gateway fallback is enabled silently | high | default `gateway.allowFallbacks` to false in `createLiteLLMProvider` and include `allow_fallbacks: false` only inside metadata hints |
| T-41-04: registry-only negotiation emits noisy fallback events | low | mirror LM Studio and OpenAI-compatible no-endpoint behavior: source `"registry"`, no fetch, no event |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add typed gateway policy and OpenAI-compatible metadata serialization</name>
  <files>packages/lattice/src/policy/policy.ts, packages/lattice/src/providers/adapters.ts</files>
  <read_first>
    - packages/lattice/src/policy/policy.ts
    - packages/lattice/src/providers/adapters.ts
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/providers/adapters.test.ts
    - .planning/phases/41-gateway-delegation-litellm-gateway-policy/41-RESEARCH.md
  </read_first>
  <action>
    In `packages/lattice/src/policy/policy.ts`, add these exported types above `PolicySpec`:
    ```typescript
    export type GatewayMetadataValue =
      | string
      | number
      | boolean
      | null
      | readonly GatewayMetadataValue[]
      | { readonly [key: string]: GatewayMetadataValue };

    export interface GatewayPolicy {
      readonly routeTags?: readonly string[];
      readonly providerPreferences?: readonly string[];
      readonly metadata?: Record<string, GatewayMetadataValue>;
      readonly allowFallbacks?: boolean;
    }
    ```

    Add `readonly gateway?: GatewayPolicy;` to `PolicySpec`.

    In `packages/lattice/src/providers/adapters.ts`:
    1. Import `type GatewayPolicy` from `../policy/policy.js`.
    2. Add `readonly gateway?: GatewayPolicy;` to `OpenAICompatibleProviderOptions`.
    3. Add helper functions:
       - `readGatewayPolicy(policy: unknown): GatewayPolicy | undefined`
       - `mergeGatewayPolicy(providerGateway: GatewayPolicy | undefined, requestGateway: GatewayPolicy | undefined): GatewayPolicy | undefined`
       - `gatewayPolicyToMetadata(policy: GatewayPolicy | undefined): Record<string, unknown> | undefined`
       - `observedModelFromResponse(body: unknown): string | undefined`
    4. `readGatewayPolicy` must only return a value when `policy` is an object and `policy.gateway` is an object.
    5. `mergeGatewayPolicy` must concatenate `routeTags` and `providerPreferences`, merge `metadata` shallowly with request metadata winning, and let request `allowFallbacks` override provider `allowFallbacks`.
    6. `gatewayPolicyToMetadata` must omit undefined fields and return:
       ```typescript
       {
         ...policy.metadata,
         lattice_gateway: {
           route_tags: policy.routeTags,
           provider_preferences: policy.providerPreferences,
           allow_fallbacks: policy.allowFallbacks
         }
       }
       ```
       Omit `lattice_gateway` when all three nested fields are undefined or empty.
    7. In the request body, include `metadata: gatewayPolicyToMetadata(mergedGatewayPolicy)` only when the helper returns a value.
    8. Do not include `apiKey`, `authorization`, `headers`, or any secret-shaped field in request body metadata.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/policy/policy.ts` contains `export interface GatewayPolicy`.
    - `packages/lattice/src/policy/policy.ts` contains `readonly gateway?: GatewayPolicy`.
    - `packages/lattice/src/providers/adapters.ts` contains `readonly gateway?: GatewayPolicy`.
    - `packages/lattice/src/providers/adapters.ts` contains `function gatewayPolicyToMetadata`.
    - `packages/lattice/src/providers/adapters.ts` contains `lattice_gateway`.
    - `packages/lattice/src/providers/adapters.ts` does not contain `metadata: { authorization`.
  </acceptance_criteria>
  <done>OpenAI-compatible providers can serialize typed gateway metadata without changing route selection.</done>
</task>

<task type="auto">
  <name>Task 2: Add createLiteLLMProvider wrapper and quirks</name>
  <files>packages/lattice/src/capabilities/profile.ts, packages/lattice/src/providers/litellm.ts, packages/lattice/src/providers/quirks.ts</files>
  <read_first>
    - packages/lattice/src/capabilities/profile.ts
    - packages/lattice/src/providers/lm-studio.ts
    - packages/lattice/src/providers/openrouter.ts
    - packages/lattice/src/providers/adapters.ts
    - packages/lattice/src/providers/quirks.ts
    - packages/lattice/src/capabilities/negotiate.ts
  </read_first>
  <action>
    In `packages/lattice/src/capabilities/profile.ts`:
    1. Add `"litellm"` to the `CapabilityAdapter` union.
    2. Add `"litellm"` to the `CAPABILITY_ADAPTERS` runtime list.
    3. Update comments that say "7" first-party adapters to "8" where they describe the closed first-party adapter set.

    In `packages/lattice/src/providers/quirks.ts`, add:
    ```typescript
    export interface LiteLLMQuirks extends AdapterQuirks {
      readonly gatewayMetadataSupported: boolean;
      readonly gatewayFallbacksSupported: boolean;
      readonly openAIErrorMapping: boolean;
    }
    ```

    Create `packages/lattice/src/providers/litellm.ts` with:
    1. Imports for `ProviderAdapter`, `OpenAICompatibleProviderOptions`, `createOpenAICompatibleProvider`, `LiteLLMQuirks`, `NegotiatedCapabilities`, `synthesizeNegotiatedCapabilitiesFromRegistry`, and `GatewayPolicy`.
    2. `export interface LiteLLMProviderOptions extends Omit<OpenAICompatibleProviderOptions, "id" | "baseUrl" | "gateway">` with:
       ```typescript
       readonly id?: string;
       readonly baseUrl?: string;
       readonly gateway?: GatewayPolicy;
       ```
    3. `const DEFAULT_LITELLM_BASE_URL = "http://localhost:4000";`.
    4. `export function createLiteLLMProvider(options: LiteLLMProviderOptions): ProviderAdapter & { readonly quirks: LiteLLMQuirks; readonly negotiateCapabilities: (modelId: string) => Promise<NegotiatedCapabilities>; }`.
    5. Resolve `const resolvedId = options.id ?? "litellm";`.
    6. Resolve `const resolvedBaseUrl = options.baseUrl ?? DEFAULT_LITELLM_BASE_URL;`.
    7. Resolve gateway defaults as:
       ```typescript
       const gateway: GatewayPolicy = {
         allowFallbacks: false,
         ...options.gateway,
       };
       ```
    8. Build `inner` by calling `createOpenAICompatibleProvider({ ...options, id: resolvedId, baseUrl: resolvedBaseUrl, gateway })`.
    9. Implement registry-only `negotiateCapabilities` returning `synthesizeNegotiatedCapabilitiesFromRegistry("litellm", modelId, "registry")`.
    10. Return `{ ...inner, quirks: { supportsToolChoice: false, parallelToolCalls: false, structuredOutputs: false, responseFormatHonored: false, streamingDiverges: true, gatewayMetadataSupported: true, gatewayFallbacksSupported: true, openAIErrorMapping: true }, negotiateCapabilities }`.
    11. Add comments that LiteLLM is an OpenAI-compatible gateway and the wrapper does not start or embed a gateway process.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/litellm.ts` exists.
    - `packages/lattice/src/providers/litellm.ts` contains `DEFAULT_LITELLM_BASE_URL = "http://localhost:4000"`.
    - `packages/lattice/src/providers/litellm.ts` contains `createOpenAICompatibleProvider`.
    - `packages/lattice/src/providers/litellm.ts` contains `allowFallbacks: false`.
    - `packages/lattice/src/providers/litellm.ts` contains `synthesizeNegotiatedCapabilitiesFromRegistry`.
    - `packages/lattice/src/capabilities/profile.ts` contains `| "litellm"`.
    - `packages/lattice/src/capabilities/profile.ts` contains `"litellm",`.
    - `packages/lattice/src/providers/quirks.ts` contains `export interface LiteLLMQuirks`.
  </acceptance_criteria>
  <done>LiteLLM has a first-class provider helper that delegates to OpenAI-compatible HTTP.</done>
</task>

<task type="auto">
  <name>Task 3: Add LiteLLM fake-fetch provider tests</name>
  <files>packages/lattice/src/providers/litellm.test.ts</files>
  <read_first>
    - packages/lattice/src/providers/litellm.ts
    - packages/lattice/src/providers/lm-studio.test.ts
    - packages/lattice/src/providers/adapters.test.ts
    - packages/lattice/src/providers/parity.test.ts
  </read_first>
  <action>
    Create `packages/lattice/src/providers/litellm.test.ts`.

    Add a local `makeFakeFetch` helper that captures `url` and `init`, and returns a JSON response with:
    ```typescript
    {
      id: "chatcmpl-litellm",
      object: "chat.completion",
      model: "azure/gpt-4o",
      choices: [{ message: { content: "litellm ok" } }],
      usage: { prompt_tokens: 11, completion_tokens: 7, total_tokens: 18 }
    }
    ```

    Add tests proving:
    1. default `adapter.id` is `"litellm"` and execute posts to `http://localhost:4000/chat/completions`.
    2. custom `baseUrl: "https://gateway.example/v1/"` posts to `https://gateway.example/v1/chat/completions`.
    3. omitting `apiKey` sends no `authorization` or `Authorization` header.
    4. providing `apiKey: "sk-litellm"` sends `authorization: "Bearer sk-litellm"`.
    5. provider option `gateway: { routeTags: ["prod"], providerPreferences: ["openai"], metadata: { trace_id: "trace-1" } }` serializes request body `metadata.trace_id === "trace-1"` and `metadata.lattice_gateway.route_tags === ["prod"]`.
    6. run policy `policy: { gateway: { routeTags: ["run"], metadata: { generation_name: "case-1" }, allowFallbacks: true } }` overrides/merges provider gateway defaults in the request body.
    7. default LiteLLM request metadata has `metadata.lattice_gateway.allow_fallbacks === false` when the caller gives no gateway policy.
    8. request body JSON does not contain `"sk-litellm"` inside `metadata`.
    9. response raw output is `"litellm ok"` and `normalizedUsage` is `{ promptTokens: 11, completionTokens: 7, costUsd: null }`.
    10. non-OK response rejects with `/OpenAI-compatible provider failed with 500/`.
    11. `negotiateCapabilities("gpt-4o")` returns `source: "registry"` and does not call fetch.
    12. `adapter.quirks.gatewayMetadataSupported`, `.gatewayFallbacksSupported`, and `.openAIErrorMapping` are all `true`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- litellm</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/litellm.test.ts` contains `http://localhost:4000/chat/completions`.
    - `packages/lattice/src/providers/litellm.test.ts` contains `https://gateway.example/v1/chat/completions`.
    - `packages/lattice/src/providers/litellm.test.ts` contains `allow_fallbacks`.
    - `packages/lattice/src/providers/litellm.test.ts` contains `gatewayMetadataSupported`.
    - `packages/lattice/src/providers/litellm.test.ts` contains `OpenAI-compatible provider failed with 500`.
    - `pnpm --filter @full-self-browsing/lattice test -- litellm` exits 0.
  </acceptance_criteria>
  <done>LiteLLM wrapper behavior is covered without running a real gateway.</done>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- litellm
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- `createLiteLLMProvider` exists and delegates to `createOpenAICompatibleProvider`.
- No package manifest gains a LiteLLM or Python/gateway dependency.
- `GatewayPolicy` exists and can serialize route tags, provider preferences, metadata, and fallback hints.
- LiteLLM fake-fetch tests cover base URLs, auth headers, request body metadata, error behavior, and usage normalization.
</success_criteria>
