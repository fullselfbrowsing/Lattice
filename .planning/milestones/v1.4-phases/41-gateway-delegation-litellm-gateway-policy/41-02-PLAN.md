---
phase: 41-gateway-delegation-litellm-gateway-policy
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - packages/lattice/src/providers/provider.ts
  - packages/lattice/src/plan/plan.ts
  - packages/lattice/src/runtime/create-ai.ts
  - packages/lattice/test/runtime.test.ts
  - packages/lattice/test/planning-execution.test.ts
autonomous: true
requirements: [GATE-02, GATE-03]
tags:
  - route-accounting
  - run-events
  - replayability
must_haves:
  truths:
    - "GATE-02: gateway policy fields pass through without changing Lattice's deterministic route decision"
    - "GATE-03: execution plans and run events record gateway usage while preserving Lattice-selected adapter/model separately"
    - "D-07: gateway hints remain advisory and do not mutate route score or fallback chain"
    - "D-10: ExecutionPlan.route.selected remains the Lattice-selected provider/model"
    - "D-11: router.candidates and provider.attempt metadata expose gateway usage"
    - "D-12: response model is recorded as observed gateway metadata and does not replace route.modelId"
    - "D-13: receipts continue to attest to the Lattice route in Phase 41"
    - "D-14: default behavior preserves replayability and avoids silent gateway fallback"
    - "D-15: gateway fallback hints are recorded separately from Lattice fallback chain"
    - "D-16: router tests prove gateway fallback metadata does not make Lattice route choice opaque"
  artifacts:
    - path: "packages/lattice/src/providers/provider.ts"
      provides: "additive ProviderRunResponse gateway metadata type"
    - path: "packages/lattice/src/plan/plan.ts"
      provides: "additive ProviderAttemptRecord metadata slot"
    - path: "packages/lattice/src/runtime/create-ai.ts"
      provides: "plan and event gateway accounting"
    - path: "packages/lattice/test/runtime.test.ts"
      provides: "gateway policy pass-through and selected route preservation tests"
  key_links:
    - from: "PolicySpec.gateway"
      to: "ExecutionPlan.metadata.gateway"
      via: "buildPlan"
      pattern: "selectedProviderId, requestedModel, routeTags, providerPreferences, allowFallbacks"
    - from: "ProviderRunResponse.gateway.observedModel"
      to: "ProviderAttemptRecord.metadata.gateway.observedModel"
      via: "attemptSucceeded"
      pattern: "does not mutate route.selected.modelId"
---

<objective>
Wire typed gateway policy into runtime plan/event accounting. A LiteLLM run must show that Lattice selected provider/model `litellm` plus the configured model, while gateway hints and observed gateway response model are recorded only as additive metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-CONTEXT.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-RESEARCH.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-PATTERNS.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-VALIDATION.md
@.planning/phases/41-gateway-delegation-litellm-gateway-policy/41-01-PLAN.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| T-41-05: gateway observed model overwrites deterministic route model | high | tests assert `plan.route.selected.modelId` remains the configured LiteLLM model and observed model lives under metadata |
| T-41-06: gateway fallback hint becomes Lattice fallback chain | high | tests assert `plan.route.fallbackChain` is unchanged by `policy.gateway.allowFallbacks` |
| T-41-07: sensitive gateway metadata leaks through events | high | sanitize gateway metadata before plan/event emission and test metadata does not contain `sk-` |
| T-41-08: adding success provider.attempt event breaks existing event consumers | medium | make event additive, preserve existing event kinds, and adjust tests to use `arrayContaining` where needed |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add additive gateway response and attempt metadata types</name>
  <files>packages/lattice/src/providers/provider.ts, packages/lattice/src/plan/plan.ts</files>
  <read_first>
    - packages/lattice/src/providers/provider.ts
    - packages/lattice/src/plan/plan.ts
    - packages/lattice/src/runtime/create-ai.ts
    - packages/lattice/src/replay/replay.ts
  </read_first>
  <action>
    In `packages/lattice/src/providers/provider.ts`, add:
    ```typescript
    export interface ProviderGatewayMetadata {
      readonly used: boolean;
      readonly requestedModel?: string;
      readonly observedModel?: string;
      readonly policy?: Record<string, unknown>;
    }
    ```

    Add `readonly gateway?: ProviderGatewayMetadata;` to `ProviderRunResponse`.

    In `packages/lattice/src/plan/plan.ts`, add `readonly metadata?: Record<string, unknown>;` to `ProviderAttemptRecord`.

    Do not add a top-level gateway field to `SelectedRoute`, `RouteDecision`, or `ReceiptRoute` in this plan.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/providers/provider.ts` contains `export interface ProviderGatewayMetadata`.
    - `packages/lattice/src/providers/provider.ts` contains `readonly gateway?: ProviderGatewayMetadata`.
    - `packages/lattice/src/plan/plan.ts` contains `readonly metadata?: Record<string, unknown>;`.
    - `packages/lattice/src/plan/plan.ts` does not contain `readonly gateway?:`.
  </acceptance_criteria>
  <done>Provider responses and plan attempts can carry observed gateway metadata additively.</done>
</task>

<task type="auto">
  <name>Task 2: Record gateway hints and observed model in plan/events</name>
  <files>packages/lattice/src/providers/adapters.ts, packages/lattice/src/runtime/create-ai.ts</files>
  <read_first>
    - packages/lattice/src/providers/adapters.ts
    - packages/lattice/src/runtime/create-ai.ts
    - packages/lattice/src/policy/policy.ts
    - packages/lattice/src/tracing/tracing.ts
    - packages/lattice/src/plan/plan.ts
  </read_first>
  <action>
    In `packages/lattice/src/providers/adapters.ts`:
    1. Use the `observedModelFromResponse(body)` helper from Plan 01.
    2. Return `gateway` in `ProviderRunResponse` only when the provider id is `"litellm"` or a gateway policy was merged.
    3. The returned gateway object must be:
       ```typescript
       {
         used: true,
         requestedModel: options.model,
         ...(observedModel !== undefined ? { observedModel } : {}),
         ...(mergedGatewayPolicy !== undefined ? { policy: sanitizedGatewayPolicyForPlan(mergedGatewayPolicy) } : {})
       }
       ```
    4. `sanitizedGatewayPolicyForPlan` must include only `routeTags`, `providerPreferences`, `metadata`, and `allowFallbacks`.

    In `packages/lattice/src/runtime/create-ai.ts`:
    1. Add a small local `sanitizeGatewayPolicyForEvents(policy: PolicySpec["gateway"] | undefined): Record<string, unknown> | undefined` helper.
    2. In `buildPlan`, when `mergedPolicy.gateway` exists and `route.selected` exists, add:
       ```typescript
       gateway: {
         providerId: route.selected.providerId,
         requestedModel: route.selected.modelId,
         policy: sanitizeGatewayPolicyForEvents(mergedPolicy.gateway)
       }
       ```
       under `createExecutionPlan({ metadata })` without removing existing `tools` or `summaryArtifactIds` metadata.
    3. Add the same sanitized gateway object to `router.candidates` event metadata when present.
    4. Add the same gateway object to the started `provider.attempt` event metadata when present.
    5. After `const response = await adapter.execute(request);`, emit a second `provider.attempt` event with `metadata: { status: "succeeded", fallback: index > 0, ...(response.gateway !== undefined ? { gateway: response.gateway } : {}) }`.
    6. Update `attemptSucceeded` to accept optional `metadata?: Record<string, unknown>` and include it on `ProviderAttemptRecord`.
    7. Pass `{ gateway: response.gateway }` to `attemptSucceeded` when `response.gateway` is present.
    8. Do not change `maybeIssueReceipt` calls except for any type fixes required by the new function signature. Receipt `model.requested` and route capability id must still use `route.modelId`.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/src/runtime/create-ai.ts` contains `sanitizeGatewayPolicyForEvents`.
    - `packages/lattice/src/runtime/create-ai.ts` contains `status: "succeeded"`.
    - `packages/lattice/src/runtime/create-ai.ts` contains `response.gateway`.
    - `packages/lattice/src/runtime/create-ai.ts` still contains `model: { requested: route.modelId, observed: null }`.
    - `packages/lattice/src/runtime/create-ai.ts` still contains `capabilityId: route.modelId`.
  </acceptance_criteria>
  <done>Runtime accounting separates Lattice route fields from gateway hint/observation metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime tests for deterministic route preservation</name>
  <files>packages/lattice/test/runtime.test.ts, packages/lattice/test/planning-execution.test.ts</files>
  <read_first>
    - packages/lattice/test/runtime.test.ts
    - packages/lattice/test/planning-execution.test.ts
    - packages/lattice/src/runtime/create-ai.ts
    - packages/lattice/src/providers/litellm.ts
  </read_first>
  <action>
    Add runtime tests using `createLiteLLMProvider` with fake fetch.

    In `packages/lattice/test/runtime.test.ts`, add a test named `records gateway policy without changing the selected Lattice route`:
    1. Create a LiteLLM provider with `model: "gpt-4o"` and fake fetch returning `model: "azure/gpt-4o"` plus `choices` and `usage`.
    2. Run `createAI({ providers: [provider], events: sink }).run({ task: "Gateway case", outputs: { answer: "text" }, policy: { gateway: { routeTags: ["prod"], providerPreferences: ["azure"], metadata: { trace_id: "trace-41" }, allowFallbacks: true } } })`.
    3. Assert result is ok.
    4. Assert `result.plan.route.selected` matches `{ providerId: "litellm", modelId: "gpt-4o" }`.
    5. Assert `result.plan.route.fallbackChain` is `[]`.
    6. Assert `result.plan.metadata?.gateway` contains `providerId: "litellm"` and `requestedModel: "gpt-4o"`.
    7. Assert at least one `provider.attempt` event has `metadata.gateway.observedModel === "azure/gpt-4o"`.
    8. Assert `JSON.stringify(result.plan.metadata)` and `JSON.stringify(result.events)` do not contain `sk-`.

    In `packages/lattice/test/planning-execution.test.ts`, add or extend a planning test proving `ai.plan(...)` with `policy.gateway.allowFallbacks: true` still leaves `plan.route.selected.providerId === "litellm"` and does not add a Lattice fallback route when no second provider exists.
  </action>
  <verify>
    <automated>pnpm --filter @full-self-browsing/lattice test -- runtime planning-execution</automated>
  </verify>
  <acceptance_criteria>
    - `packages/lattice/test/runtime.test.ts` contains `records gateway policy without changing the selected Lattice route`.
    - `packages/lattice/test/runtime.test.ts` contains `observedModel`.
    - `packages/lattice/test/runtime.test.ts` contains `not.toContain("sk-")`.
    - `packages/lattice/test/planning-execution.test.ts` contains `allowFallbacks`.
    - `pnpm --filter @full-self-browsing/lattice test -- runtime planning-execution` exits 0.
  </acceptance_criteria>
  <done>Runtime tests prove gateway metadata does not make routing opaque or non-replayable.</done>
</task>

</tasks>

<verification>
Run:
```bash
pnpm --filter @full-self-browsing/lattice test -- runtime planning-execution
pnpm --filter @full-self-browsing/lattice typecheck
```
</verification>

<success_criteria>
- Plans/events include sanitized gateway metadata when a gateway policy is used.
- Lattice-selected route fields remain unchanged and deterministic.
- Observed gateway model is recorded only as metadata.
- Receipts still use the Lattice selected route in Phase 41.
</success_criteria>

