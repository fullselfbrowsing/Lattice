# Plan 19-01: AGENTS.md policy flip (Delegation Surface)

**Status:** Not started
**Wave:** 1 (independent — pure docs)
**REQ-IDs:** DELEG-01

## Goal

Flip Lattice's `AGENTS.md` policy stance from "multi-agent crews: Out of Scope" to "agent execution: First-class, runtime-agnostic." Record the rationale + boundary so future contributors understand what is in / out of scope.

## Tasks

1. Locate the "Out of Scope" / "Delegation" section in `AGENTS.md` that declares multi-agent OOS.
2. Replace with two clearly separated subsections:
   - **"Agent Execution — In Scope (v1.2+):"** A single-agent loop is first-class. The runtime is host-agnostic (Node, MV3 SW, edge worker, Lambda) via the `AgentHost` adapter shipping in Phase 20. Built on top of v1.1 capability runtime, hook pipeline, step-transition tracing, and v1.2 provider adapters.
   - **"Multi-Agent Crews — Out of Scope:"** Parent-child loops, summary-return, cache-prefix sharing, rate-limit-group coordination remain Out of Scope. Single-agent only.
3. Add a one-paragraph rationale referencing the audit trail at `automation/.planning/LATTICE-PIN.md` (FSB v0.10.0-attempt-2 → v1.2 retro Phases 14-18) and the runtime-agnostic design constraint.

## Acceptance

- `AGENTS.md` reflects the policy flip with both subsections.
- Audit narrative is grep-able for "Agent Execution — In Scope" and "Multi-Agent Crews — Out of Scope".
- DELEG-01 closed.

## Verification

Documentation change only. No tests.

## Files

- `AGENTS.md` (modified)
