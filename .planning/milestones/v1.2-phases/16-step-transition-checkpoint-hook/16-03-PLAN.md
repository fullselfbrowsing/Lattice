# Plan 16-03: Public surface re-exports for checkpoint hook + audit doc Phase 3 close

**Status:** Complete (retro).
**Wave:** 3 (closure)
**REQ-IDs:** INDEX-03
**Originating commits:** `acdbb8a` feat(api): re-export createCheckpointHook + checkpoint types from public surface; `7afd62f` docs(fsb-integration): close Phase 3 audit rows (observability/step-markers).

## Goal

Re-export `createCheckpointHook` value + `DEFAULT_CHECKPOINT_BAND` + `STEP_TRANSITION_EVENT_NAME` constants + type-only `CheckpointHookContext` + `CheckpointHookOptions` from `packages/lattice/src/index.ts`. Flip the 2 Observability/step-markers audit-doc rows to Covered.

## Tasks

1. `packages/lattice/src/index.ts` — 7 re-export lines for checkpoint surface.
2. `docs/fsb-integration-gaps.md` — flip rows: "step.transition event kind" → Covered (`fd254c4`); "Inspector envelope shape" → Covered (`a67f476` + `acdbb8a`).

## Acceptance

- `import { createCheckpointHook, DEFAULT_CHECKPOINT_BAND, STEP_TRANSITION_EVENT_NAME, type CheckpointHookContext, type CheckpointHookOptions } from "lattice"` resolves.
- Audit doc rows show Covered with backlinks.

## Outcome

Land cherry-picks `acdbb8a` and `7afd62f` on this phase branch.
