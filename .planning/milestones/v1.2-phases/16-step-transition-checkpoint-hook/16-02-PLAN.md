# Plan 16-02: `createCheckpointHook` factory + tests

**Status:** Complete (retro).
**Wave:** 2
**REQ-IDs:** CHECKPOINT-01, CHECKPOINT-02, CHECKPOINT-03, CHECKPOINT-04
**Originating commit:** `a67f476` feat(contract): add createCheckpointHook factory + per-step receipt mint.

## Goal

Ship `packages/lattice/src/contract/checkpoint.ts` exporting `createCheckpointHook(options)` factory plus context/options types plus `STEP_TRANSITION_EVENT_NAME` and `DEFAULT_CHECKPOINT_BAND` constants.

## Tasks

1. New `packages/lattice/src/contract/checkpoint.ts` (+261 lines). Factory returns `HookHandler<CheckpointHookContext>`. Per invocation:
   - Emit exactly one `step.transition` tracer event when a tracer is provided.
   - When `options.signer` is provided: mint exactly one v1.1 Capability Receipt with step-marker fields populated.
   - Signer failure surfaces as `metadata.mintError`; never throws upstream.
2. Module exports `STEP_TRANSITION_EVENT_NAME` (string constant) and `DEFAULT_CHECKPOINT_BAND` (= `BAND.OBSERVABILITY`).
3. Module exports types `CheckpointHookContext` and `CheckpointHookOptions`.
4. New `packages/lattice/src/contract/checkpoint.test.ts` (+239 lines, 15 cases across 7 describe blocks): factory identity, tracer-only mode, signer mode mint+verify, signer-throws fallback, 3-call linked-list threading, HookPipeline integration, tracer-absent mint-still-works.

## Acceptance

- 15 vitest cases pass.
- Composable with Phase 15 `HookPipeline` via `pipeline.register(BAND.OBSERVABILITY, createCheckpointHook({ signer, tracer }))`.

## Outcome

Land cherry-pick `a67f476` on this phase branch.
