# Plan 15-03: Public surface re-exports (band pipeline + lifecycle) + public-surface test flip

**Status:** Complete (retro).
**Wave:** 2
**REQ-IDs:** INDEX-02
**Originating commits:** `2110e19` fix(test): update public-surface test for Phase 1 createReceipt re-export; `00fcfac` feat(api): re-export tripwire pipeline + lifecycle events from public surface.

## Goal

Re-export `createHookPipeline` (value), `HookPipeline` (type), and `HookLifecycleEvent` (type) from `packages/lattice/src/index.ts` so the band pipeline is reachable via the bare `lattice` specifier. Flip the stale `public-surface.test.ts` assertion that `createReceipt` is NOT exported (carried forward from v1.1 as a forward-compat marker) to its truth: `createReceipt` IS exported.

## Tasks

1. `packages/lattice/src/index.ts` — add one re-export line for `createHookPipeline` value plus type-only re-exports for `HookPipeline` and `HookLifecycleEvent`.
2. `packages/lattice/test/public-surface.test.ts` — flip the stale assertion. v1.1 closed with this test still asserting the negative (Phase 14 cherry-pick landed the actual re-export). Phase 15 cleanup brings them into agreement.

## Acceptance

- `import { createHookPipeline, type HookPipeline, type HookLifecycleEvent } from "lattice"` resolves.
- `public-surface.test.ts` PASSES (previously the 1 expected FAIL at FSB-side baseline).

## Verification

After this plan lands, Lattice's full vitest suite goes from 311 PASS / 1 expected FAIL to 332 PASS / 0 FAIL (baseline + 20 bands + 1 schema flip).

## Outcome

Land cherry-picks `2110e19` and `00fcfac` on this phase branch.
