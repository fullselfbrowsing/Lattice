# Plan 14-01: Re-export `createReceipt` from public surface

**Status:** Complete (retro from FSB v0.10.0-attempt-2 Phase 1).
**Wave:** 1
**REQ-IDs:** INDEX-01
**Originating commits:** `ab6c1f6` feat(receipts): re-export createReceipt + CreateReceiptInput from package surface; `195e5ae` docs(fsb-integration): add FSB integration gap survey across 6 surfaces.

## Goal

Re-export `createReceipt` (value) and `CreateReceiptInput` (type) from `packages/lattice/src/index.ts` so the receipt-mint API is reachable via the bare `lattice` specifier from any npm consumer (Node, MV3 offscreen, edge runtimes). Land the companion audit doc that drives Phases 14-18 narrative.

## Tasks

1. Add `export { createReceipt, type CreateReceiptInput } from "./receipts/receipt.js";` between the `verifyReceipt` re-export and the `isTerminal` re-export in `packages/lattice/src/index.ts`.
2. Add `docs/fsb-integration-gaps.md` — 6-surface audit doc with rows for Receipts, Tripwires/hooks, Providers, Delegation, MV3-survivability, Observability/step-markers. Initial state: Phase 1 (this plan) has flipped row "createReceipt reachable via public bare specifier" from Needs addition to Covered.

## Acceptance

- `import { createReceipt } from "lattice"` resolves.
- `import type { CreateReceiptInput } from "lattice"` resolves type-only.
- `docs/fsb-integration-gaps.md` exists with the 6-surface audit table.

## Verification

Lattice vitest suite green at 311 PASS / 1 FAIL pre-plan (the FAIL is the stale `public-surface.test.ts` assertion that `createReceipt` is NOT exported; that flips in Plan 15-04 during Phase 15). The fail is expected and forward-compat per FSB v0.10.0-attempt-2 Plan 01-01 SUMMARY.

## Outcome

Land commits `c9c5b9a` (re-export) and `d17fb25` (audit doc) on this phase branch. Both are cherry-picks of FSB v0.10.0-attempt-2 P1 originating SHAs with `git cherry-pick -x` provenance.
