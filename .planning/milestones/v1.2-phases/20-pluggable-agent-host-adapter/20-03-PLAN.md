# Plan 20-03: Public surface re-exports + integration smoke + verification

**Status:** Not started
**Wave:** 3 (closure)
**REQ-IDs:** (closure)

## Goal

Surface AgentHost types + createNoopAgentHost from the public `lattice` specifier. Add an integration smoke that proves end-to-end survivability composition (eviction snapshot → simulated process restart → resume continues at recorded iteration).

## Tasks

### Task 1: `packages/lattice/src/index.ts`

Add to existing Phase 19 agent re-exports:
- `createNoopAgentHost` (value)
- Type-only: `AgentScheduler`, `AgentTransport`, `AgentStorage`, `AgentSnapshot` (note: `AgentHost` already exported in Phase 19 as forward-decl; Phase 20 promotes the shape but the re-export line stays).

### Task 2: Public-surface test

Extend `packages/lattice/test/public-surface.test.ts` Phase 20 describe block asserting the new exports resolve.

### Task 3: Integration smoke

`packages/lattice/src/agent/survivability-integration.test.ts`:
- Create a fake provider, an Ed25519 signer, an in-memory storage host, a real SurvivabilityAdapter (createNoopSurvivabilityAdapter).
- Run agent for 2 iterations, then simulate eviction by capturing the saved snapshot from the storage seam.
- Create a fresh runtime, pre-populate storage with the captured snapshot, call runAgent again.
- Assert recovery.start + recovery.complete fire; assert the second run continues with conversation + iterationIndex from the snapshot.
- Verify all signed receipts from both halves verify cleanly against the ephemeral KeySet.

### Task 4: VERIFICATION.md

Write 20-VERIFICATION.md with goal-backward observable truths for HOST-01..03 + TRACE-EXT-01.

## Acceptance

- All re-exports reachable from `lattice`.
- 3+ integration cases pass.
- VERIFICATION status=passed.
